{"sql_schemas":["table leave_requests | fields: id, employee_id, leave_type, start_date, end_date, reason, status, approved_by, approved_at, created_at, updated_at | pk: id | fks: employee_id -> employees.id; approved_by -> employees.id | indexes: employee_id: frequent lookups of all leave requests for a given employee; status: filtering by status (SUBMITTED, APPROVED, etc.) is the primary access pattern for manager queues; start_date, end_date: date-range queries for calendar views and overlap detection; employee_id, status: combined filter for 'my pending requests' queries; leave_type, status: manager filtering by leave type within a status","table leave_balances | fields: id, employee_id, policy_id, total_entitlement, used_days, remaining_days, fiscal_year, status, created_at, updated_at | pk: id | fks: employee_id -> employees.id; policy_id -> leave_policies.id | indexes: employee_id, fiscal_year: the dominant access pattern — look up an employee's balance for the current fiscal year; employee_id: all balance queries are scoped to an employee; policy_id: policy-driven queries (e.g. recalculate all balances for a policy change)","table employees | fields: id, employee_number, first_name, last_name, email, manager_id, department, hire_date, termination_date, employment_status, created_at, updated_at, deleted_at | pk: id | fks: manager_id -> employees.id | indexes: email: unique lookup for authentication and employee search; employee_number: unique lookup by HR identifier; manager_id: 'my direct reports' queries for manager workflows; employment_status: filtering active/inactive/terminated employees; department: department-scoped queries","table leave_policies | fields: id, policy_name, leave_type, entitlement_days, accrual_rate, max_accumulation, minimum_notice_days, requires_manager_approval, is_active, created_at, updated_at | pk: id | indexes: leave_type: policies are queried by leave type when validating a request; is_active: filtering active policies is the default access pattern; leave_type, is_active: combined filter for 'find the active policy for annual leave'","table audit_logs | fields: id, entity_type, entity_id, action, old_values, new_values, performed_by, performed_at, ip_address, user_agent, created_at | pk: id | indexes: entity_type, entity_id: look up all audit records for a specific entity (e.g. all changes to leave_request #42); performed_by: audit trail queries scoped to a user; performed_at: time-range audit queries; action: filtering by action type (APPROVE, REJECT, etc.)"],"repositories":[{"interface_name":"ILeaveRequestRepository","concrete_name":"PgLeaveRequestRepository","methods":["findById(id: string): Promise<LeaveRequest | null>","findAll(params: LeaveRequestQueryParams): Promise<{ rows: LeaveRequest[]; total: number }>","findByEmployeeId(employeeId: string, params?: { status?: string; limit?: number; offset?: number }): Promise<{ rows: LeaveRequest[]; total: number }>","findOverlapping(employeeId: string, startDate: Date, endDate: Date, excludeId?: string): Promise<LeaveRequest[]>","create(dto: CreateLeaveRequestDto, client?: PoolClient): Promise<LeaveRequest>","updateStatus(id: string, status: string, approvedBy?: string, client?: PoolClient): Promise<LeaveRequest | null>","update(id: string, dto: UpdateLeaveRequestDto, client?: PoolClient): Promise<LeaveRequest | null>","delete(id: string, client?: PoolClient): Promise<boolean>"],"backing":"PostgreSQL via pg Pool (src/shared/db/connection.ts)"},{"interface_name":"ILeaveBalanceRepository","concrete_name":"PgLeaveBalanceRepository","methods":["findByEmployeeAndYear(employeeId: string, fiscalYear: number): Promise<LeaveBalance | null>","findAllByEmployee(employeeId: string): Promise<LeaveBalance[]>","findByEmployeeAndPolicy(employeeId: string, policyId: string, fiscalYear: number): Promise<LeaveBalance | null>","create(dto: CreateBalanceDto, client?: PoolClient): Promise<LeaveBalance>","updateUsedDays(id: string, usedDays: number, remainingDays: number, client?: PoolClient): Promise<LeaveBalance | null>","upsert(dto: UpsertBalanceDto, client?: PoolClient): Promise<LeaveBalance>"],"backing":"PostgreSQL via pg Pool (src/shared/db/connection.ts)"},{"interface_name":"IEmployeeRepository","concrete_name":"PgEmployeeRepository","methods":["findById(id: string): Promise<Employee | null>","findByEmployeeNumber(employeeNumber: string): Promise<Employee | null>","findByEmail(email: string): Promise<Employee | null>","findByManagerId(managerId: string): Promise<Employee[]>","findAll(params?: { status?: string; department?: string; limit?: number; offset?: number }): Promise<{ rows: Employee[]; total: number }>"],"backing":"PostgreSQL via pg Pool (src/shared/db/connection.ts)"},{"interface_name":"ILeavePolicyRepository","concrete_name":"PgLeavePolicyRepository","methods":["findById(id: string): Promise<LeavePolicy | null>","findByLeaveType(leaveType: string): Promise<LeavePolicy | null>","findAllActive(): Promise<LeavePolicy[]>","findAll(params?: { isActive?: boolean; limit?: number; offset?: number }): Promise<{ rows: LeavePolicy[]; total: number }>","create(dto: CreatePolicyDto): Promise<LeavePolicy>","update(id: string, dto: UpdatePolicyDto): Promise<LeavePolicy | null>"],"backing":"PostgreSQL via pg Pool (src/shared/db/connection.ts)"},{"interface_name":"IAuditLogRepository","concrete_name":"PgAuditLogRepository","methods":["create(entry: CreateAuditEntryDto, client?: PoolClient): Promise<AuditLog>","findByEntity(entityType: string, entityId: string, params?: { limit?: number; offset?: number }): Promise<{ rows: AuditLog[]; total: number }>","findByPerformer(performedBy: string, params?: { limit?: number; offset?: number }): Promise<{ rows: AuditLog[]; total: number }>"],"backing":"PostgreSQL via pg Pool (src/shared/db/connection.ts)"}],"data_notes":"All tables use UUID primary keys (id). Timestamps (created_at, updated_at) follow the BaseEntity convention from DOMAIN.md. The leave_requests.leave_type field stores the LeaveType enum value (annual, sick, emergency, etc.) as a string — no separate leave_types lookup table is needed since the enum is stable and small. The leave_requests.status field stores LeaveStatus enum values (DRAFT, SUBMITTED, APPROVED, REJECTED, CANCELLED). The leave_balances table is the single source of truth for remaining leave — it is updated atomically with status changes. The audit_logs table satisfies GP-002: every state-changing operation on leave_requests or leave_balances writes a row here. The employees and leave_policies tables are referenced by FK but owned by their respective modules; their schemas are defined here once and referenced by name elsewhere.","open_questions":[{"question":"How are leave days counted — calendar days or business days (Mon–Fri excluding holidays)?","impact":"Every balance update (used_days increment, remaining_days decrement) depends on this counting rule. If different call sites use different counting methods, balances will drift and become inconsistent. This is a BINDING semantic.","candidate_options":["Calendar days (start_date to end_date inclusive)","Business days (Mon–Fri, excluding weekends)","Business days excluding weekends and company holidays (requires holiday calendar)"]},{"question":"When leave balance remaining_days is fractional (e.g. from accrual), what rounding direction applies when displaying or enforcing the balance?","impact":"Affects whether an employee with 0.4 remaining days can take a half-day or is blocked. Must be consistent across balance queries and request-validation checks.","candidate_options":["Round half-up (>= 0.5 rounds to 1)","Round down / floor (always truncate)","Banker's rounding (round to nearest even)"]}],"transaction_contract":"Repository methods that participate in multi-step writes (create, updateStatus, update, delete on ILeaveRequestRepository; updateUsedDays, upsert on ILeaveBalanceRepository; create on IAuditLogRepository) accept an optional PoolClient parameter. When the client is provided, the method uses it for all queries within that call and does NOT release it. When omitted, the method acquires a client from the shared pool internally and releases it after the single operation. The calling service (e.g. LeaveService) owns the unit of work: it acquires a client from the pool, calls BEGIN, passes the client to every participating repository method, then calls COMMIT on success or ROLLBACK on error, and finally releases the client. This contract applies to: (a) submit — create leave_request + audit_log, (b) approve/reject — update leave_request status + update leave_balance used_days/remaining_days + audit_log, (c) cancel — update leave_request status + revert leave_balance + audit_log."}