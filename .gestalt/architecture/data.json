{"sql_schemas":["table employees | fields: id, employee_number, first_name, last_name, email, manager_id, department, hire_date, termination_date, employment_status, created_at, updated_at, deleted_at | pk: id | fks: manager_id -> employees.id | indexes: employee_number: unique lookup by employee number; email: unique lookup by email; manager_id: team hierarchy queries — list direct reports for manager approval workflows; employment_status: filter active/inactive/terminated employees for leave eligibility checks","table leave_policies | fields: id, policy_name, leave_type, entitlement_days, accrual_rate, max_accumulation, minimum_notice_days, requires_manager_approval, is_active, created_at, updated_at | pk: id | indexes: leave_type: filter policies by leave type (annual, sick, emergency) when resolving applicable policy for a request; is_active: exclude inactive policies from eligibility checks","table leave_requests | fields: id, employee_id, leave_policy_id, start_date, end_date, reason, status, approved_by, approved_at, created_at, updated_at | pk: id | fks: employee_id -> employees.id; leave_policy_id -> leave_policies.id; approved_by -> employees.id | indexes: employee_id: list all leave requests for a given employee (self-service dashboard); status: filter requests by status for manager approval queues and HR reports; leave_policy_id: filter by leave type/policy for reporting; start_date: range queries for calendar views and overlapping-leave detection; end_date: range queries for calendar views and overlapping-leave detection; approved_by: lookup requests approved by a specific manager","table leave_balances | fields: id, employee_id, leave_policy_id, total_entitlement, used_days, remaining_days, fiscal_year, status, created_at, updated_at | pk: id | fks: employee_id -> employees.id; leave_policy_id -> leave_policies.id | indexes: employee_id: lookup balance for a specific employee (self-service dashboard, balance check before approval); leave_policy_id: filter balances by policy type; fiscal_year: filter balances by fiscal year for accrual and reset operations; employee_id, leave_policy_id, fiscal_year: unique constraint to prevent duplicate balance records per employee-policy-year","table audit_logs | fields: id, entity_type, entity_id, action, old_values, new_values, performed_by, performed_at, ip_address, user_agent, created_at | pk: id | indexes: entity_type, entity_id: lookup audit trail for a specific entity (e.g., all changes to a leave request); performed_by: filter audit records by actor for compliance reports; performed_at: time-range queries for audit reports; action: filter by action type (APPROVE, REJECT, CREATE, etc.)","table notifications | fields: id, recipient_id, type, title, message, related_entity_type, related_entity_id, status, created_at, read_at | pk: id | fks: recipient_id -> employees.id | indexes: recipient_id: list notifications for a specific user; status: filter by delivery/read status for notification center; related_entity_type, related_entity_id: lookup notifications tied to a specific leave request or other entity; created_at: chronological ordering for notification feeds"],"repositories":[{"interface_name":"ILeaveRequestRepository","concrete_name":"PgLeaveRequestRepository","methods":["findById(id: string): Promise<LeaveRequest | null>","findAll(params: LeaveRequestQueryParams): Promise<{ data: LeaveRequest[]; total: number }>","findByEmployeeId(employeeId: string, params?: { status?: LeaveStatus; limit?: number; offset?: number }): Promise<{ data: LeaveRequest[]; total: number }>","findOverlapping(employeeId: string, startDate: Date, endDate: Date, excludeId?: string): Promise<LeaveRequest[]>","create(dto: CreateLeaveRequestDto): Promise<LeaveRequest>","update(id: string, dto: UpdateLeaveRequestDto): Promise<LeaveRequest>","updateStatus(id: string, status: LeaveStatus, approvedBy?: string, approvedAt?: Date): Promise<LeaveRequest>","delete(id: string): Promise<void>"],"backing":"PostgreSQL via pg Pool (src/shared/db/connection.ts)"},{"interface_name":"ILeaveBalanceRepository","concrete_name":"PgLeaveBalanceRepository","methods":["findById(id: string): Promise<LeaveBalance | null>","findByEmployeeId(employeeId: string, fiscalYear?: number): Promise<LeaveBalance[]>","findByEmployeeAndPolicy(employeeId: string, leavePolicyId: string, fiscalYear: number): Promise<LeaveBalance | null>","create(dto: CreateLeaveBalanceDto): Promise<LeaveBalance>","updateUsedDays(id: string, usedDays: number, remainingDays: number): Promise<LeaveBalance>","updateEntitlement(id: string, totalEntitlement: number, remainingDays: number): Promise<LeaveBalance>","upsertByEmployeePolicyYear(employeeId: string, leavePolicyId: string, fiscalYear: number, dto: Partial<LeaveBalance>): Promise<LeaveBalance>"],"backing":"PostgreSQL via pg Pool (src/shared/db/connection.ts)"}],"data_notes":"All six tables defined conceptually: employees, leave_policies, leave_requests, leave_balances, audit_logs, notifications. Five repository interfaces with concrete Pg* implementations backed by PostgreSQL via pg Pool. Audit logs table covers GP-002 (all state-changing operations write an audit record). Leave balances use a composite unique constraint on (employee_id, leave_policy_id, fiscal_year) to prevent duplicates. Overlapping leave detection is handled via ILeaveRequestRepository.findOverlapping. The leave_requests table references leave_policies (not a separate leave_types table) since the DOMAIN.md defines leave_type as a string field on the policy entity."}