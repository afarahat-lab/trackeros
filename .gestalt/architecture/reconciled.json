{"domain_entities":[{"name":"LeaveRequest","attributes":["id: string","employeeId: string","leavePolicyId: string","startDate: Date","endDate: Date","reason: string | undefined","status: LeaveRequestStatus","approvedBy: string | null","approvedAt: Date | null","cancelledBy: string | null","cancelledAt: Date | null","createdAt: Date","updatedAt: Date"],"purpose":"Represents an employee's application for leave. Tracks full lifecycle from draft through submission, approval/rejection, and cancellation. Status drives workflow transitions and balance impacts."},{"name":"LeavePolicy","attributes":["id: string","policyName: string","leaveType: LeaveType","entitlementDays: number","accrualRate: number | null","maxAccumulation: number | null","minimumNoticeDays: number | null","requiresManagerApproval: boolean","isActive: boolean","createdAt: Date","updatedAt: Date"],"purpose":"Defines rules and entitlements for a specific leave type. Each LeaveType has exactly one active LeavePolicy at any time."},{"name":"LeaveBalance","attributes":["id: string","employeeId: string","leavePolicyId: string","totalEntitlement: number","usedDays: number","remainingDays: number","fiscalYear: number","status: 'ACTIVE' | 'CLOSED'","createdAt: Date","updatedAt: Date"],"purpose":"Tracks an employee's leave entitlement, usage, and remaining balance for a specific policy and fiscal year. Deductions on approval; restorations on cancellation from approved."},{"name":"Employee","attributes":["id: string","employeeNumber: string","firstName: string","lastName: string","email: string","managerId: string | null","department: string | null","hireDate: Date","terminationDate: Date | null","employmentStatus: 'ACTIVE' | 'INACTIVE' | 'TERMINATED'","createdAt: Date","updatedAt: Date","deletedAt: Date | null"],"purpose":"Represents an employee in the organisation. Owns leave requests and balances. managerId establishes approval hierarchy."},{"name":"LeaveType","attributes":["value: 'annual' | 'sick' | 'emergency' | 'unpaid' | 'maternity' | 'paternity'"],"purpose":"Enumeration of leave categories. Each type has its own LeavePolicy."},{"name":"AuditRecord","attributes":["id: string","entityType: string","entityId: string","action: AuditAction","oldValues: Record<string, unknown> | null","newValues: Record<string, unknown> | null","performedBy: string","performedAt: Date","ipAddress: string | null","userAgent: string | null","createdAt: Date"],"purpose":"Immutable record of a state-changing operation on a domain entity, for compliance and traceability."},{"name":"Notification","attributes":["id: string","recipientId: string","type: NotificationType","title: string","message: string","relatedEntityType: string | null","relatedEntityId: string | null","status: 'UNREAD' | 'READ'","createdAt: Date","readAt: Date | null"],"purpose":"Represents a notification sent to a user (employee/manager) about leave events."}],"modules":[{"name":"shared/types","path":"src/shared/types/","owns":["LeaveStatus enum (DRAFT | SUBMITTED | APPROVED | REJECTED | CANCELLED)","LeaveType enum (annual | sick | emergency | unpaid | maternity | paternity)","LeaveAction enum (CREATE | SUBMIT | APPROVE | REJECT | CANCEL | UPDATE | DELETE)","NotificationType enum (LEAVE_SUBMITTED | LEAVE_APPROVED | LEAVE_REJECTED | LEAVE_CANCELLED | BALANCE_UPDATED)","EmploymentStatus enum (ACTIVE | INACTIVE | TERMINATED)","AuditAction enum (CREATE | UPDATE | DELETE | APPROVE | REJECT)"]},{"name":"employee","path":"src/modules/employee/","owns":["Employee entity","IEmployeeRepository interface","IEmployeeService interface","EmployeeService implementation","EmployeeRepository implementation"]},{"name":"leave-policy","path":"src/modules/leave-policy/","owns":["LeavePolicy entity","ILeavePolicyRepository interface","ILeavePolicyService interface","LeavePolicyService implementation","LeavePolicyRepository implementation"]},{"name":"leave-request","path":"src/modules/leave-request/","owns":["LeaveRequest entity","CreateLeaveRequestDto","UpdateLeaveRequestDto","LeaveRequestQueryParams","ILeaveRequestRepository interface","ILeaveRequestService interface","LeaveRequestService implementation","LeaveRequestRepository implementation","LeaveRequestController","leaveRequestRoutes"]},{"name":"balance","path":"src/modules/balance/","owns":["LeaveBalance entity","ILeaveBalanceRepository interface","ILeaveBalanceService interface","LeaveBalanceService implementation","LeaveBalanceRepository implementation","BalanceController","balanceRoutes"]},{"name":"audit","path":"src/modules/audit/","owns":["AuditRecord entity","IAuditRepository interface","IAuditService interface","AuditService implementation","AuditRepository implementation"]},{"name":"notification","path":"src/modules/notification/","owns":["Notification entity","INotificationRepository interface","INotificationService interface","NotificationService implementation","NotificationRepository implementation"]}],"dependency_map":[{"from_":"employee","to":"shared/types"},{"from_":"leave-policy","to":"shared/types"},{"from_":"leave-request","to":"shared/types"},{"from_":"leave-request","to":"employee"},{"from_":"leave-request","to":"leave-policy"},{"from_":"leave-request","to":"balance"},{"from_":"leave-request","to":"audit"},{"from_":"leave-request","to":"notification"},{"from_":"balance","to":"shared/types"},{"from_":"audit","to":"shared/types"},{"from_":"notification","to":"shared/types"}],"recommended_phases":[{"title":"Phase 1: Foundation & Shared Types","rationale":"Set up project structure, shared enums, database connection, and migrations for all tables. This establishes the data foundation for all modules.","estimated_files":10},{"title":"Phase 2: Employee & LeavePolicy Modules","rationale":"These are foundational reference data modules that other modules depend on. Implement repositories, services, and basic API endpoints for employee and policy management.","estimated_files":12},{"title":"Phase 3: LeaveBalance Module","rationale":"Balance tracking is required before leave requests can be processed. Implement balance initialization, query, and update logic with fiscal year handling.","estimated_files":8},{"title":"Phase 4: LeaveRequest Core Workflow","rationale":"Implement the leave request lifecycle (create, submit, approve, reject, cancel) with all business rules, balance integration, audit logging, and notifications. This is the core feature.","estimated_files":15},{"title":"Phase 5: Integration, Testing & Polish","rationale":"End-to-end testing, performance optimization, error handling refinement, and documentation.","estimated_files":10}],"architecture_md_update":"## Leave Management Module — Reconciled Architecture\n\n### Overview\n\nThe leave management module enables employees to apply for annual, sick, emergency, and other leave types. Managers (or HR admins when no manager exists) approve or reject requests. The system tracks leave balances per employee, policy, and fiscal year, enforcing strict business rules around state transitions, balance sufficiency, overlap prevention, and auditability.\n\n### Domain Entities\n\n- **LeaveRequest** — Core aggregate representing a leave application. Lifecycle: DRAFT → SUBMITTED → {APPROVED | REJECTED}; any non-terminal state → CANCELLED. Balance is deducted on APPROVED and restored on CANCELLED from APPROVED.\n- **LeavePolicy** — Defines entitlement, accrual, notice, and approval rules per leave type. Lifecycle: ACTIVE ↔ INACTIVE.\n- **LeaveBalance** — Tracks entitlement, used, and remaining days per employee per policy per fiscal year. Lifecycle: ACTIVE → CLOSED (on fiscal year rollover).\n- **Employee** — Owns leave requests and balances; managerId establishes approval hierarchy. Lifecycle: ACTIVE, INACTIVE, TERMINATED.\n- **LeaveType** — Enum: annual, sick, emergency, unpaid, maternity, paternity.\n- **AuditRecord** — Immutable log of all state-changing operations.\n- **Notification** — User-facing notification for leave events.\n\n### Conceptual Table Specifications\n\n| Table | Key Fields | Index Rationale |\n|-------|------------|-----------------|\n| `leave_requests` | id, employee_id, leave_policy_id, start_date, end_date, status, approved_by, cancelled_by, … | employee_id, status, (employee_id, status), approved_by, date ranges |\n| `employees` | id, employee_number, email, manager_id, employment_status, … | employee_number (unique), email (unique), manager_id, employment_status |\n| `leave_policies` | id, leave_type, is_active, … | (leave_type, is_active) for active policy lookup |\n| `leave_balances` | id, employee_id, leave_policy_id, fiscal_year, total_entitlement, used_days, remaining_days, status | (employee_id, leave_policy_id, fiscal_year) unique constraint |\n| `audit_logs` | id, entity_type, entity_id, action, performed_by, performed_at, … | (entity_type, entity_id), performed_by, performed_at |\n| `notifications` | id, recipient_id, type, status, created_at, … | recipient_id, status, created_at |\n\n### Module Structure\n\n- `shared/types` — Enums (LeaveStatus, LeaveType, EmploymentStatus, AuditAction, NotificationType, LeaveAction)\n- `employee` — Employee entity, repository, service\n- `leave-policy` — LeavePolicy entity, repository, service\n- `leave-request` — LeaveRequest aggregate, repository, service, controller, routes\n- `balance` — LeaveBalance entity, repository, service, controller, routes\n- `audit` — AuditRecord entity, repository, service\n- `notification` — Notification entity, repository, service\n\n### Dependency Map\n\n```\nshared/types ← employee, leave-policy, leave-request, balance, audit, notification\nleave-request → employee, leave-policy, balance, audit, notification\n```\n\n### Cross-Cutting Contracts\n\n**Auth Contract**  \nJWT bearer token → `request.user: { id: string, role: UserRole }`. Roles: `employee`, `manager`, `hr_admin`. RBAC enforced via `requireRole(...)` middleware. Self-approval prevented in service layer.\n\n**Transaction Contract**  \nRepository methods for multi-step writes accept optional `PoolClient`. Service acquires client, runs `BEGIN`, passes client to repos, then `COMMIT`/`ROLLBACK`. Atomic boundary: status change + balance adjustment + audit log.\n\n**Error Response Contract**  \n`{ error: string, code: string, statusCode: number }`. 400 validation, 401 unauthenticated, 403 forbidden, 404 not found, 409 conflict (overlap), 422 domain rule violation.\n\n### Recommended Implementation Phases\n\n1. Foundation & Shared Types — DB connection, migrations, enums.\n2. Employee & LeavePolicy Modules — Reference data with basic CRUD.\n3. LeaveBalance Module — Balance initialization and queries.\n4. LeaveRequest Core Workflow — Full lifecycle with business rules, balance integration, audit, notifications.\n5. Integration, Testing & Polish — E2E tests, performance, docs.\n\n### Open Questions\n\n- Fiscal year definition (calendar vs custom)\n- Cross-fiscal-year request handling (pro-rate, single-year, reject)\n- Calendar vs business days for leave counting\n- Accrual model (lump sum, monthly, per-pay-period, configurable)\n- Emergency leave bypass of minimum notice\n- Rounding rule for fractional days\n","sql_schemas":["table leave_requests | fields: id, employee_id, leave_policy_id, start_date, end_date, reason, status, approved_by, approved_at, cancelled_by, cancelled_at, created_at, updated_at | pk: id | fks: employee_id -> employees.id; leave_policy_id -> leave_policies.id; approved_by -> employees.id; cancelled_by -> employees.id | indexes: employee_id: frequent lookups for employee leave history; status: filtering by status for approval queues; leave_policy_id: filtering by leave type; start_date, end_date: date-range queries and overlap detection; (employee_id, status): compound lookup for 'my pending/approved requests'; approved_by: manager approval queue lookups","table employees | fields: id, employee_number, first_name, last_name, email, manager_id, department, hire_date, termination_date, employment_status, created_at, updated_at, deleted_at | pk: id | fks: manager_id -> employees.id | indexes: employee_number: unique lookup; email: unique lookup for auth; manager_id: direct reports queries; employment_status: filtering active/inactive/terminated; department: department-level aggregations","table leave_policies | fields: id, policy_name, leave_type, entitlement_days, accrual_rate, max_accumulation, minimum_notice_days, requires_manager_approval, is_active, created_at, updated_at | pk: id | indexes: leave_type: filtering by leave type; is_active: filtering active policies; (leave_type, is_active): dominant lookup for active policy by type","table leave_balances | fields: id, employee_id, leave_policy_id, total_entitlement, used_days, remaining_days, fiscal_year, status, created_at, updated_at | pk: id | fks: employee_id -> employees.id; leave_policy_id -> leave_policies.id | indexes: employee_id: employee balance views; leave_policy_id: filtering by policy; fiscal_year: fiscal-year scoping; (employee_id, leave_policy_id, fiscal_year): unique constraint index for one balance per employee per policy per year","table audit_logs | fields: id, entity_type, entity_id, action, old_values, new_values, performed_by, performed_at, ip_address, user_agent, created_at | pk: id | indexes: (entity_type, entity_id): lookup audit trail for entity; action: filtering by action type; performed_by: filtering by actor; performed_at: time-range queries","table notifications | fields: id, recipient_id, type, title, message, related_entity_type, related_entity_id, status, created_at, read_at | pk: id | fks: recipient_id -> employees.id | indexes: recipient_id: user's notification inbox; status: filtering unread/read; created_at: chronological ordering"],"open_questions":[],"business_rules":["Consolidated decision for all questions: (1) Fiscal year = calendar year (Jan 1 to Dec 31), hardcoded — no configurable fiscal-year start, no per-policy override. (2) Cross-year requests = single-year: the ENTIRE LeaveRequest is charged to the fiscal year of its startDate; a request never touches two LeaveBalance records; do NOT build multi-balance/pro-rate logic. (3 & 7) Day counting = inclusive calendar days: daysRequested = (endDate - startDate) + 1; weekends and public holidays ARE counted; do NOT introduce a holiday calendar. This single formula is BINDING at every call site (balance deduction, sufficiency check, overlap detection, reporting). (4) Accrual = annual lump sum: the full totalEntitlement is granted at fiscal-year start; NO monthly pro-rata, NO per-pay-period, NO accrual scheduler; partial-year employees get the full entitlement. (5) Emergency leave ALWAYS bypasses minimumNoticeDays, regardless of policy setting. (6) Granularity = full days only: leave balances are INTEGERS (total_entitlement, used_days, remaining_days are whole numbers), so no fractional days ever arise and no rounding is needed; remaining_days = total_entitlement - used_days exactly. If a fractional value ever arises, floor it. This integer rule is consistent across deduction on approval, restoration on cancellation, and display. [BINDING RULE — operator decision resolving: What is the fiscal year definition — calendar year (Jan 1 – Dec 31) or a custom period (e.g. Apr 1 – Mar 31)?; How should a LeaveRequest whose date range spans two fiscal years be handled?; Should leave day counting use calendar days or business days?; How does leave entitlement accrue — lump sum at fiscal year start, monthly pro-rata, or per-pay-period?; Does emergency leave bypass the minimumNoticeDays constraint?; When leave_balances.remaining_days is computed from total_entitlement minus used_days, what rounding direction applies if fractional days arise (e.g. half-day leave, pro-rated entitlements)?; How are leave days counted — calendar days or business days (Mon–Fri excluding holidays)?; apply everywhere these apply, not in one place only]"],"auth_contract":"Authentication is via JWT bearer token. The auth middleware (Fastify onRequest hook) verifies the token and populates request.user with { id: string, role: UserRole }. UserRole enum: 'employee' | 'manager' | 'hr_admin'. RBAC is enforced by a requireRole(...) route guard. The leave-request routes use requireRole to restrict approve/reject to managers and HR admins, and cancellation to employee, manager, or HR admin. Self-approval is prevented by business logic in the service layer.","transaction_contract":"Repository methods that participate in multi-step writes (create, update, updateStatus on ILeaveRequestRepository; updateUsedDays, upsertEntitlement on ILeaveBalanceRepository; create on IAuditLogRepository) accept an optional PoolClient parameter. When a PoolClient is passed, the method uses that client for its query instead of the shared pool. The calling service owns the unit of work: it acquires a client from the pool, executes BEGIN, passes the client to each repository call in sequence, then executes COMMIT or ROLLBACK. When no client is passed, the repository method acquires a connection from the pool for a single-query auto-commit. The primary atomic boundary is: leave request status change (APPROVED/REJECTED/CANCELLED) + balance adjustment (used_days/remaining_days) + audit log insertion — all three writes must succeed or roll back together.","error_response_contract":"All API errors return a JSON body with { error: string, code: string, statusCode: number }. Standard HTTP status codes: 400 for validation errors (invalid input, missing fields), 401 for unauthenticated, 403 for unauthorized role, 404 for resource not found, 409 for conflict (e.g., overlapping approved leave), 422 for domain rule violations (invalid state transition, insufficient balance, employee not active). The error code is a machine-readable string (e.g., 'INSUFFICIENT_BALANCE', 'OVERLAPPING_LEAVE')."}