{"domain_entities":[{"name":"LeaveRequest","attributes":["id: string","employeeId: string","leaveTypeId: string","startDate: Date","endDate: Date","reason: string | undefined","status: LeaveRequestStatus","approvedBy: string | null","approvedAt: Date | null","rejectionReason: string | null","createdAt: Date","updatedAt: Date"],"purpose":"Represents an employee's application for leave. Tracks the full lifecycle from draft through submission, manager decision, and potential cancellation."},{"name":"LeaveRequestStatus","attributes":["value: 'DRAFT' | 'SUBMITTED' | 'APPROVED' | 'REJECTED' | 'CANCELLED'"],"purpose":"Enumeration of all possible lifecycle states for a LeaveRequest. Governs which transitions are valid."},{"name":"LeaveType","attributes":["id: string","code: 'annual' | 'sick' | 'emergency' | 'unpaid' | 'maternity' | 'paternity'","label: string","description: string | null","isActive: boolean","createdAt: Date","updatedAt: Date"],"purpose":"Catalog of leave categories. Each LeaveRequest references one LeaveType. The isActive flag controls whether the type is available for new requests."},{"name":"LeavePolicy","attributes":["id: string","policyName: string","leaveTypeId: string","entitlementDays: number","accrualRate: number | null","maxAccumulation: number | null","minimumNoticeDays: number | null","requiresManagerApproval: boolean","isActive: boolean","createdAt: Date","updatedAt: Date"],"purpose":"Defines the rules governing a specific leave type: entitlement, accrual caps, notice periods, and whether manager approval is required. Referenced by LeaveBalance to compute remaining days."},{"name":"LeaveBalance","attributes":["id: string","employeeId: string","leaveTypeId: string","policyId: string","totalEntitlement: number","usedDays: number","pendingDays: number","remainingDays: number","fiscalYear: number","status: 'ACTIVE' | 'EXHAUSTED' | 'FROZEN'","createdAt: Date","updatedAt: Date"],"purpose":"Tracks an employee's leave entitlement, consumption, pending requests, and remaining balance for a specific leave type within a fiscal year. Updated atomically when leave is approved or cancelled."},{"name":"AuditLog","attributes":["id: string","employeeId: string","leaveRequestId: string","action: string","previousState: string | null","newState: string | null","performedBy: string","createdAt: Date"],"purpose":"Immutable record of every state-changing operation on a LeaveRequest, satisfying GP-002 audit requirements."}],"modules":[{"name":"leave-type","path":"src/modules/leave-type/","owns":["LeaveType enum","LeaveType model","index.ts barrel export"]},{"name":"leave-status","path":"src/modules/leave-status/","owns":["LeaveRequestStatus enum","LeaveRequestStatus model","index.ts barrel export"]},{"name":"leave-policy","path":"src/modules/leave-policy/","owns":["LeavePolicy model","ILeavePolicyRepository interface","LeavePolicyRepository","ILeavePolicyService interface","LeavePolicyService","index.ts barrel export"]},{"name":"balance","path":"src/modules/balance/","owns":["LeaveBalance model","IBalanceRepository interface","BalanceRepository","IBalanceService interface","BalanceService","index.ts barrel export"]},{"name":"leave-request","path":"src/modules/leave-request/","owns":["LeaveRequest model","ILeaveRequestRepository interface","LeaveRequestRepository","ILeaveRequestService interface","LeaveRequestService","LeaveRequestController","leaveRequestRoutes","index.ts barrel export"]},{"name":"audit-log","path":"src/modules/audit-log/","owns":["AuditLog model","IAuditRepository interface","AuditRepository","IAuditService interface","AuditService","index.ts barrel export"]},{"name":"notification","path":"src/modules/notification/","owns":["Notification model","INotificationService interface","NotificationService","index.ts barrel export"]}],"dependency_map":[{"from_":"leave-policy","to":"leave-type"},{"from_":"balance","to":"leave-type"},{"from_":"leave-request","to":"leave-type"},{"from_":"leave-request","to":"leave-status"},{"from_":"leave-request","to":"leave-policy"},{"from_":"leave-request","to":"balance"},{"from_":"leave-request","to":"audit-log"},{"from_":"leave-request","to":"notification"}],"recommended_phases":[{"title":"Phase 1: Domain Primitives — leave-type & leave-status","rationale":"Zero-dependency leaf modules. Define the LeaveType enum (annual, sick, emergency, unpaid, maternity, paternity) and LeaveRequestStatus enum (DRAFT, SUBMITTED, APPROVED, REJECTED, CANCELLED). These are referenced by every other module in the slice.","estimated_files":4},{"title":"Phase 2: Audit & Notification infrastructure","rationale":"Leaf modules with no domain dependencies. Audit-log satisfies GP-002 (all state-changing ops write audit records). Notification handles manager/employee alerts. Both are consumed by leave-request in Phase 4.","estimated_files":8},{"title":"Phase 3: Policy & Balance services","rationale":"Both depend only on leave-type (Phase 1). Leave-policy defines entitlement and accrual rules per leave type per fiscal year. Balance tracks per-employee remaining days, with deduct/credit operations. Both are consumed by leave-request.","estimated_files":10},{"title":"Phase 4: Leave-request — orchestration & API surface","rationale":"Depends on all prior phases. Orchestrates apply/approve/reject/cancel workflows: validates against policy, checks/deducts balance, writes audit records, fires notifications. Exposes Fastify routes with RBAC guards. This is the only module with a controller and routes.","estimated_files":8}],"architecture_md_update":"## Leave Management Module\n\n### Overview\n\nThe leave management module enables employees to apply for annual, sick, emergency, and other leave types. Managers approve or reject requests. The system tracks leave balances per employee per leave type per fiscal year, enforcing policies and maintaining a full audit trail.\n\n### Domain Entities\n\n- **LeaveRequest** — core aggregate representing an employee's leave application. Lifecycle: `DRAFT → SUBMITTED → (APPROVED | REJECTED) → CANCELLED` (terminal).\n- **LeaveRequestStatus** — value object enumerating the five lifecycle states.\n- **LeaveType** — catalog of leave categories (`annual`, `sick`, `emergency`, `unpaid`, `maternity`, `paternity`). Each type can be active or inactive.\n- **LeavePolicy** — rules for a leave type: entitlement days, accrual rate, max accumulation, minimum notice, and whether manager approval is required.\n- **LeaveBalance** — per-employee, per-type, per-year tracking of total entitlement, used days, pending days, and remaining days. Status: `ACTIVE`, `EXHAUSTED`, or `FROZEN`.\n- **AuditLog** — immutable record of every state change on a LeaveRequest (GP-002 compliance).\n\n### Module Boundaries\n\n| Module | Path | Responsibility |\n|--------|------|----------------|\n| `leave-type` | `src/modules/leave-type/` | LeaveType enum and model |\n| `leave-status` | `src/modules/leave-status/` | LeaveRequestStatus enum and model |\n| `leave-policy` | `src/modules/leave-policy/` | LeavePolicy model, repository, service |\n| `balance` | `src/modules/balance/` | LeaveBalance model, repository, service |\n| `leave-request` | `src/modules/leave-request/` | LeaveRequest aggregate, repository, service, controller, Fastify routes |\n| `audit-log` | `src/modules/audit-log/` | AuditLog model, repository, service |\n| `notification` | `src/modules/notification/` | Notification service (alerts for applied/decided/low-balance) |\n\n### Dependency Graph\n\n```\nleave-type  ←  leave-policy  ←  leave-request\nleave-type  ←  balance       ←  leave-request\nleave-status ←                ←  leave-request\n                                leave-request → audit-log\n                                leave-request → notification\n```\n\nAll dependencies flow inward; no cycles. Only `leave-request` exposes HTTP endpoints.\n\n### Database Tables (Conceptual)\n\n- **leave_types** — `id`, `code`, `label`, `description`, `is_active`, `created_at`, `updated_at`\n- **leave_policies** — `id`, `policy_name`, `leave_type_id` (FK), `entitlement_days`, `accrual_rate`, `max_accumulation`, `minimum_notice_days`, `requires_manager_approval`, `is_active`, `created_at`, `updated_at`\n- **leave_balances** — `id`, `employee_id` (FK), `leave_type_id` (FK), `policy_id` (FK), `fiscal_year`, `total_entitlement`, `used_days`, `pending_days`, `remaining_days`, `status`, `created_at`, `updated_at`\n- **leave_requests** — `id`, `employee_id` (FK), `leave_type_id` (FK), `start_date`, `end_date`, `reason`, `status`, `approved_by` (FK), `approved_at`, `rejection_reason`, `created_at`, `updated_at`\n- **audit_log** — `id`, `employee_id` (FK), `leave_request_id` (FK), `action`, `previous_state`, `new_state`, `performed_by`, `created_at`\n\n### Key Business Rules\n\n- **State transitions** are strictly enforced: DRAFT → SUBMITTED or CANCELLED; SUBMITTED → APPROVED, REJECTED, or CANCELLED; APPROVED → CANCELLED; REJECTED → DRAFT or CANCELLED; CANCELLED is terminal.\n- **Manager approval**: only the employee's designated manager (via `Employee.managerId`) may approve/reject; self-approval is prohibited.\n- **Balance updates**: on approval, `used_days` increases and `pending_days` decreases; on cancellation of an approved request, `used_days` decreases; on submission, `pending_days` increases.\n- **Audit**: every state change writes an `audit_log` entry with previous and new states, actor, and timestamp.\n- **RBAC**: routes are guarded — `apply` requires employee role, `approve`/`reject` require manager role.\n\n### Implementation Phases\n\n1. **Domain Primitives** — `leave-type` and `leave-status` modules (zero dependencies).\n2. **Infrastructure** — `audit-log` and `notification` modules (cross-cutting, no domain deps).\n3. **Domain Services** — `leave-policy` and `balance` modules (depend only on `leave-type`).\n4. **Orchestration & API** — `leave-request` module (depends on all above, exposes Fastify routes).\n\n### Stack Compliance\n\n- Language: TypeScript (strict)\n- Framework: Fastify (routes in `leave-request`)\n- Database: PostgreSQL (accessed via `pg` Pool from `src/shared/db/connection.ts`)\n- Architecture: modular monolith — each module is a directory under `src/modules/` with its own barrel export\n- Testing: Jest (unit tests for services, integration tests for repositories)\n- Golden Principles: GP-001 (repository pattern), GP-002 (audit), GP-003 (input validation at controller), GP-005 (RBAC), GP-006 (error handling) are all satisfied.","sql_schemas":["table leave_types | fields: id, code, label, description, is_active, created_at, updated_at | pk: id | indexes: code: unique lookup by leave type code for policy enforcement; is_active: filtering active types for dropdowns","table leave_policies | fields: id, policy_name, leave_type_id, entitlement_days, accrual_rate, max_accumulation, minimum_notice_days, requires_manager_approval, is_active, created_at, updated_at | pk: id | fks: leave_type_id -> leave_types.id | indexes: leave_type_id: lookup policies by leave type; is_active: filtering active policies","table leave_balances | fields: id, employee_id, leave_type_id, policy_id, fiscal_year, total_entitlement, used_days, pending_days, remaining_days, status, created_at, updated_at | pk: id | fks: employee_id -> employees.id; leave_type_id -> leave_types.id; policy_id -> leave_policies.id | indexes: employee_id: frequent lookups for employee balance summary; leave_type_id: filtering balances by leave type; (employee_id, leave_type_id): composite lookup for specific balance; (employee_id, fiscal_year): year-scoped balance queries; status: filtering by balance status (active/exhausted/frozen)","table leave_requests | fields: id, employee_id, leave_type_id, start_date, end_date, reason, status, approved_by, approved_at, rejection_reason, created_at, updated_at | pk: id | fks: employee_id -> employees.id; leave_type_id -> leave_types.id; approved_by -> employees.id | indexes: employee_id: frequent lookups for 'my leave requests'; status: filtering by status for approval queues and dashboards; leave_type_id: filtering by leave type for balance calculations; approved_by: lookups for pending approvals assigned to a manager; created_at: ordering and date-range queries for history views","table audit_log | fields: id, employee_id, leave_request_id, action, previous_state, new_state, performed_by, created_at | pk: id | fks: employee_id -> employees.id; leave_request_id -> leave_requests.id | indexes: employee_id: lookups of audit trail by employee; leave_request_id: lookups of audit trail for a specific leave request; action: filtering by action type; created_at: chronological ordering for audit views"]}