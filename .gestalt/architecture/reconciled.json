{"domain_entities":[{"name":"Employee","attributes":["id: string","employeeNumber: string","firstName: string","lastName: string","email: string","role: EmployeeRole","managerId: string | null","department: string","hireDate: Date","terminationDate: Date | null","employmentStatus: EmploymentStatus","passwordHash: string | null"],"purpose":"The authenticated actor and the subject of leave data. Gains a nullable credential (passwordHash) so a caller can authenticate; null means the employee has no login credential yet."},{"name":"AuthToken","attributes":["sub: string (employee id)","role: EmployeeRole","expiresIn: string"],"purpose":"The bearer credential minted at login and verified by the auth hook. Its payload contract (sub = employee id, role = EmployeeRole) is fixed by the existing verifier and must not change."},{"name":"LeaveRequest","attributes":["id: string","employeeId: string","leaveTypeCode: LeaveTypeCode","startDate: Date","endDate: Date","requestedDays: number","reason: string | null","status: LeaveStatus","approverId: string | null","approvalComment: string | null","submittedAt: Date | null","decidedAt: Date | null","cancelledBy: string | null","cancelledAt: Date | null"],"purpose":"The leave application being read. No lifecycle change in this feature; reads are status-unrestricted for visibility."},{"name":"LeaveBalance","attributes":["id: string","employeeId: string","leaveTypeCode: LeaveTypeCode","periodStart: Date","periodEnd: Date","entitledDays: number","usedDays: number","pendingDays: number","available: number (computed)"],"purpose":"The caller's leave balance for one leave type and one accrual period. The read surface adds a computed `available` figure; OPEN vs CLOSED remains inferred from period bounds."},{"name":"LeavePolicy","attributes":["id: string","leaveTypeCode: LeaveTypeCode","policyName: string","annualEntitlementDays: number","accrualPeriodMonths: number","carryForwardDays: number","minNoticeDays: number","maxRequestDays: number","requiresManagerApproval: boolean","effectiveFrom: Date","effectiveTo: Date | null","status: LeavePolicyStatus"],"purpose":"The effective policy governing a leave type's entitlement and accrual period. Balance reads enumerate policies (not the leave-type catalog) to derive which leave types have a current period."},{"name":"LeaveType","attributes":["code: string","name: string","requiresApproval: boolean","maxConsecutiveDays: number","isPaid: boolean"],"purpose":"Catalog of leave types referenced by policies and balances; static reference data with no lifecycle transitions in this feature."}],"modules":[{"name":"shared-date","path":"src/shared/date/","owns":["startOfUtcDay","addMonths","periodContaining","index.ts"]},{"name":"shared-types","path":"src/shared/types/","owns":["EmployeeProfile","LeaveRequestQueryParams.employeeIds","canonical enums","requestedDays"]},{"name":"shared-auth","path":"src/shared/auth/","owns":["registerAuth","signToken","verifyToken","public path set"]},{"name":"shared-errors","path":"src/shared/errors/","owns":["AppError","ValidationError","UnauthorizedError","ForbiddenError","NotFoundError","ConflictError"]},{"name":"shared-db","path":"src/shared/db/","owns":["connection.ts","pg Pool","IUnitOfWork"]},{"name":"validation","path":"src/shared/validation/","owns":["validation helpers"]},{"name":"audit","path":"src/modules/audit/","owns":["audit logging"]},{"name":"notification","path":"src/modules/notification/","owns":["notification dispatch"]},{"name":"leave-type","path":"src/modules/leave-type/","owns":["LeaveType","ILeaveTypeRepository","PgLeaveTypeRepository","LeaveTypeService"]},{"name":"auth","path":"src/modules/auth/","owns":["IAuthService","AuthService","authRoutes (POST /auth/login)","login flow (bcrypt compare + signToken)"]},{"name":"employee","path":"src/modules/employee/","owns":["Employee (incl passwordHash)","IEmployeeRepository (+findByManagerId)","PgEmployeeRepository","IEmployeeService (+getEmployeeByEmail, getEmployeesByManagerId, getEmployeeProfileById)","EmployeeService","employeeRoutes (GET /employees/me)","toEmployeeProfile mapping"]},{"name":"policy","path":"src/modules/policy/","owns":["LeavePolicy","IPolicyRepository","PgLeavePolicyRepository","IPolicyService (+getAllPolicies)","PolicyService"]},{"name":"balance","path":"src/modules/balance/","owns":["LeaveBalance","IBalanceRepository","PgLeaveBalanceRepository","IBalanceService (+getCurrentBalances)","BalanceService","balanceRoutes (GET /balances/me)"]},{"name":"leave","path":"src/modules/leave/","owns":["LeaveRequest","ILeaveRepository","PgLeaveRequestRepository","ILeaveService (+list, getById)","LeaveService","leaveRoutes (+GET /leaves, GET /leaves/:id)"]}],"dependency_map":[{"from_":"auth","to":"employee"},{"from_":"auth","to":"shared-auth"},{"from_":"auth","to":"shared-types"},{"from_":"auth","to":"shared-errors"},{"from_":"employee","to":"shared-types"},{"from_":"employee","to":"shared-errors"},{"from_":"employee","to":"shared-db"},{"from_":"policy","to":"leave-type"},{"from_":"policy","to":"shared-types"},{"from_":"policy","to":"shared-errors"},{"from_":"balance","to":"employee"},{"from_":"balance","to":"policy"},{"from_":"balance","to":"shared-date"},{"from_":"balance","to":"shared-types"},{"from_":"balance","to":"shared-errors"},{"from_":"balance","to":"shared-db"},{"from_":"leave","to":"balance"},{"from_":"leave","to":"validation"},{"from_":"leave","to":"policy"},{"from_":"leave","to":"employee"},{"from_":"leave","to":"audit"},{"from_":"leave","to":"notification"},{"from_":"leave","to":"leave-type"},{"from_":"leave","to":"shared-date"},{"from_":"leave","to":"shared-types"},{"from_":"leave","to":"shared-errors"},{"from_":"leave","to":"shared-db"},{"from_":"shared-date","to":"shared-types"},{"from_":"shared-auth","to":"shared-types"},{"from_":"shared-auth","to":"shared-errors"}],"recommended_phases":[{"title":"Phase 1 — Shared foundations: date helpers + shared types","rationale":"Extract startOfUtcDay/addMonths/periodContaining into src/shared/date/accrual.ts (pure UTC arithmetic, no balance-domain knowledge) and add EmployeeProfile + LeaveRequestQueryParams.employeeIds to shared-types. No module depends on these yet, so this is the dependency root and must land first.","estimated_files":3},{"title":"Phase 2 — Employee: password_hash, findByManagerId, profile mapping","rationale":"Add the nullable password_hash migration, extend IEmployeeRepository with findByManagerId, extend IEmployeeService with getEmployeeByEmail/getEmployeesByManagerId/getEmployeeProfileById, and add toEmployeeProfile (never exposes password_hash). auth and balance/leave read paths depend on these.","estimated_files":6},{"title":"Phase 3 — Policy: getAllPolicies exposure","rationale":"Add IPolicyService.getAllPolicies() calling the existing ILeavePolicyRepository.findAll(). Balance's getCurrentBalances iterates policies, so this must precede the balance work. No new repository method.","estimated_files":2},{"title":"Phase 4 — Auth module: POST /auth/login","rationale":"New auth module with IAuthService.login(email, password) using bcrypt.compare + signToken (sub=id, role=EmployeeRole), indistinguishable wrong-email/wrong-password response, and public-path registration. Depends only on employee + shared-auth.","estimated_files":4},{"title":"Phase 5 — Balance: getCurrentBalances + GET /balances/me","rationale":"Add IBalanceService.getCurrentBalances(employeeId) iterating active effective policies (via policy.getAllPolicies), deriving the current period via shared-date periodContaining, omitting types with no balance row ([] when none). Depends on phases 1-3.","estimated_files":3},{"title":"Phase 6 — Leave reads: list/getById + GET /leaves, GET /leaves/:id","rationale":"Extend findByQuery to honour employeeIds (employee_id = ANY($n)), add ILeaveService.list(actor, query) and getById(actor, requestId) with role scoping (EMPLOYEE self, MANAGER self+direct reports via employee.findByManagerId, ADMIN all) and 404-equivalence for invisible ids. Depends on employee + shared-types.","estimated_files":4},{"title":"Phase 7 — GET /employees/me route + smoke/unit tests","rationale":"Wire the employee profile route, extend scripts/smoke.js with login + authenticated read probes asserting real values, and add unit tests for new routes/services in the existing style. Depends on all prior phases.","estimated_files":5}],"architecture_md_update":"## Authentication and read endpoints (v3) — Reconciled Architecture\n\n### Stack compliance\nTypeScript 20, Fastify, PostgreSQL, modular monolith, Jest. No controller layer is introduced; routes call services directly (open question Q1).\n\n### Domain entities and lifecycle states\n- **Employee**: id, employeeNumber, firstName, lastName, email, role, managerId, department, hireDate, terminationDate, employmentStatus, passwordHash. Lifecycle: ACTIVE, TERMINATED, ON_LEAVE.\n- **AuthToken**: sub, role, expiresIn. Lifecycle: ISSUED, EXPIRED.\n- **LeaveRequest**: id, employeeId, leaveTypeCode, startDate, endDate, requestedDays, reason, status, approverId, approvalComment, submittedAt, decidedAt, cancelledBy, cancelledAt. Lifecycle: DRAFT, SUBMITTED, APPROVED, REJECTED, CANCELLED.\n- **LeaveBalance**: id, employeeId, leaveTypeCode, periodStart, periodEnd, entitledDays, usedDays, pendingDays, available (computed). Lifecycle: OPEN, CLOSED.\n- **LeavePolicy**: id, leaveTypeCode, policyName, annualEntitlementDays, accrualPeriodMonths, carryForwardDays, minNoticeDays, maxRequestDays, requiresManagerApproval, effectiveFrom, effectiveTo, status. Lifecycle: DRAFT, ACTIVE, SUPERSEDED.\n- **LeaveType**: code, name, requiresApproval, maxConsecutiveDays, isPaid. Lifecycle: none (static catalog).\n\n### Binding business rules\n- requestedDays = endDate - startDate + 1 (inclusive calendar days, whole-day UTC, no weekend/holiday exclusion) is the single canonical day-count derivation.\n- Date-range filtering is inclusive on both edges: startDateFrom <= startDate <= startDateTo and endDateFrom <= endDate <= endDateTo.\n- Leave visibility is role-scoped: EMPLOYEE sees only their own requests; MANAGER sees their own plus direct reports (employee.managerId === actor.id), one level only, not transitive; ADMIN sees all. Same rule for list and single-read.\n- Manager visibility is status-unrestricted; visibility and decide authority are separate (decide stays SUBMITTED-only).\n- Unauthorized single-read returns the same 404 as a nonexistent id (no id probing).\n- Login: wrong email vs wrong password indistinguishable; passwordHash never in any response.\n- Accrual-period derivation shared (startOfUtcDay/addMonths/periodContaining, pure UTC, anchored on hireDate) — one implementation reused by leave and balance paths.\n- Effective policy selection: at most one per leaveTypeCode (ACTIVE, effectiveFrom <= asOf, effectiveTo null or >= asOf; tie-break latest effectiveFrom).\n- No balance row for a policy's current period → omit that leave type; no open periods → empty list (200), never an error.\n- available = entitledDays - usedDays - pendingDays.\n- Actor always resolved from the verified token, never a client-supplied id.\n\n### Conceptual tables\n- **employees**: fields id, employee_number, first_name, last_name, email, password_hash, role, manager_id, department, hire_date, termination_date, employment_status. PK id. FK manager_id -> employees.id. Indexes: email unique, employee_number unique, manager_id.\n- **leave_requests**: fields id, employee_id, leave_type_code, start_date, end_date, requested_days, reason, status, approver_id, approval_comment, submitted_at, decided_at, cancelled_by, cancelled_at. PK id. FKs employee_id -> employees.id, leave_type_code -> leave_types.code, approver_id -> employees.id, cancelled_by -> employees.id. Indexes: employee_id, status, (leave_type_code, start_date).\n- **leave_balances**: fields id, employee_id, leave_type_code, period_start, period_end, entitled_days, used_days, pending_days. PK id. FKs employee_id -> employees.id, leave_type_code -> leave_types.code. Indexes: (employee_id, leave_type_code, period_start, period_end) unique, employee_id.\n- **leave_policies**: fields id, leave_type_code, policy_name, annual_entitlement_days, accrual_period_months, carry_forward_days, min_notice_days, max_request_days, requires_manager_approval, effective_from, effective_to, status. PK id. FK leave_type_code -> leave_types.code. Index: leave_type_code.\n- **leave_types**: fields code, name, requires_approval, max_consecutive_days, is_paid. PK code. Index: code natural key.\n\nMigration note: add nullable `password_hash` to `employees` using `t.text(...)` per initial-schema convention; date columns are UTC-parsed by `src/shared/db/connection.ts`.\n\n### Repositories\n- **IEmployeeRepository / PgEmployeeRepository**: create, findById, findByEmployeeNumber, findByEmail, findByManagerId.\n- **ILeaveRepository / PgLeaveRequestRepository**: create, findById, update, findByQuery.\n- **IPolicyRepository / PgLeavePolicyRepository**: create, findById, findByLeaveTypeCode, findAll.\n- **IBalanceRepository / PgLeaveBalanceRepository**: create, findById, findByKey, update.\n- **ILeaveTypeRepository / PgLeaveTypeRepository** (existing, referenced by policy): findById/findByCode.\n\n### Modules and boundaries\n- **shared-date** (`src/shared/date/`): startOfUtcDay, addMonths, periodContaining, index.ts.\n- **shared-types** (`src/shared/types/`): EmployeeProfile, LeaveRequestQueryParams.employeeIds, canonical enums, requestedDays.\n- **shared-auth** (`src/shared/auth/`): registerAuth, signToken, verifyToken, public path set.\n- **shared-errors** (`src/shared/errors/`): AppError, ValidationError, UnauthorizedError, ForbiddenError, NotFoundError, ConflictError.\n- **shared-db** (`src/shared/db/`): connection.ts, pg Pool, IUnitOfWork.\n- **validation** (`src/shared/validation/`): validation helpers.\n- **audit** (`src/modules/audit/`): audit logging.\n- **notification** (`src/modules/notification/`): notification dispatch.\n- **leave-type** (`src/modules/leave-type/`): LeaveType, ILeaveTypeRepository, PgLeaveTypeRepository, LeaveTypeService.\n- **auth** (`src/modules/auth/`): IAuthService, AuthService, authRoutes (POST /auth/login), login flow (bcrypt compare + signToken).\n- **employee** (`src/modules/employee/`): Employee (incl passwordHash), IEmployeeRepository (+findByManagerId), PgEmployeeRepository, IEmployeeService (+getEmployeeByEmail, getEmployeesByManagerId, getEmployeeProfileById), EmployeeService, employeeRoutes (GET /employees/me), toEmployeeProfile mapping.\n- **policy** (`src/modules/policy/`): LeavePolicy, IPolicyRepository, PgLeavePolicyRepository, IPolicyService (+getAllPolicies), PolicyService.\n- **balance** (`src/modules/balance/`): LeaveBalance, IBalanceRepository, PgLeaveBalanceRepository, IBalanceService (+getCurrentBalances), BalanceService, balanceRoutes (GET /balances/me).\n- **leave** (`src/modules/leave/`): LeaveRequest, ILeaveRepository, PgLeaveRequestRepository, ILeaveService (+list, getById), LeaveService, leaveRoutes (+GET /leaves, GET /leaves/:id).\n\n### Dependency map\n- auth -> employee, shared-auth, shared-types, shared-errors\n- employee -> shared-types, shared-errors, shared-db\n- policy -> leave-type, shared-types, shared-errors\n- balance -> employee, policy, shared-date, shared-types, shared-errors, shared-db\n- leave -> balance, validation, policy, employee, audit, notification, leave-type, shared-date, shared-types, shared-errors, shared-db\n- shared-date -> shared-types\n- shared-auth -> shared-types, shared-errors\n\n### Recommended phases\n1. **Shared foundations: date helpers + shared types** — extract accrual helpers and add shared types. (3 files)\n2. **Employee: password_hash, findByManagerId, profile mapping** — migration, repository/service extensions, toEmployeeProfile. (6 files)\n3. **Policy: getAllPolicies exposure** — add IPolicyService.getAllPolicies() calling existing findAll(). (2 files)\n4. **Auth module: POST /auth/login** — bcrypt.compare + signToken, indistinguishable error, public path. (4 files)\n5. **Balance: getCurrentBalances + GET /balances/me** — iterate active policies, derive period, omit missing rows. (3 files)\n6. **Leave reads: list/getById + GET /leaves, GET /leaves/:id** — extend findByQuery with employeeIds, role scoping, 404-equivalence. (4 files)\n7. **GET /employees/me route + smoke/unit tests** — wire profile route, extend smoke.js, add unit tests. (5 files)\n\n### Cross-cutting contracts\n- **Auth**: `request.user: { id: string; role: EmployeeRole }` where `EmployeeRole = 'EMPLOYEE' | 'MANAGER' | 'ADMIN'`. Identity/role obtained from a JWT bearer token verified by the existing `registerAuth` preHandler (`src/shared/auth/index.ts`), which populates `request.user` from `payload.sub` (id) and `payload.role`. RBAC enforced at the API boundary by `resolveActor` and in services (role-scoped visibility), never inline in routes. `POST /auth/login` is added to the public path set; all other new endpoints require a valid token.\n- **Error**: errors return `{ error: string; code: string }`. Validation failure -> HTTP 400 (ValidationError); authentication failure -> 401 (UnauthorizedError); authorization failure -> 403 (ForbiddenError); not found -> 404 (NotFoundError); invalid state transition -> 409 (ConflictError); other -> 500. `POST /auth/login` returns the SAME status and message for wrong email and wrong password (401 UnauthorizedError). `GET /leaves/:id` returns the SAME 404 NotFoundError for a request the caller may not see as for one that does not exist.\n- **Transaction**: none required — this feature is read-only plus a single non-persistent login write; read endpoints must not open a transaction.\n\n### Open questions\n- Q1: Should a controller layer be introduced, or continue with routes calling services directly? (candidates: continue routes-call-services; introduce controllers)\n- Q2: Should LeavePolicyStatus be promoted into src/shared/types as a canonical enum? (candidates: promote to shared/types; keep module-local and import via policy index.ts)\n","sql_schemas":["table employees | fields: id, employee_number, first_name, last_name, email, password_hash, role, manager_id, department, hire_date, termination_date, employment_status | pk: id | fks: manager_id -> employees.id | indexes: email: unique — POST /auth/login resolves the caller via findByEmail; must be a fast, exact lookup; employee_number: unique — duplicate detection on create; manager_id: MANAGER role scoping via findByManagerId (direct reports, one level)","table leave_requests | fields: id, employee_id, leave_type_code, start_date, end_date, requested_days, reason, status, approver_id, approval_comment, submitted_at, decided_at, cancelled_by, cancelled_at | pk: id | fks: employee_id -> employees.id; leave_type_code -> leave_types.code; approver_id -> employees.id; cancelled_by -> employees.id | indexes: employee_id: role scoping (employeeIds = ANY(...)) and per-employee listing; status: status filter in findByQuery; (leave_type_code, start_date): leave-type + date-range filtering in findByQuery","table leave_balances | fields: id, employee_id, leave_type_code, period_start, period_end, entitled_days, used_days, pending_days | pk: id | fks: employee_id -> employees.id; leave_type_code -> leave_types.code | indexes: (employee_id, leave_type_code, period_start, period_end): unique — findByKey enforces at-most-one-row-per-period; employee_id: GET /balances/me lists all balances for the caller","table leave_policies | fields: id, leave_type_code, policy_name, annual_entitlement_days, accrual_period_months, carry_forward_days, min_notice_days, max_request_days, requires_manager_approval, effective_from, effective_to, status | pk: id | fks: leave_type_code -> leave_types.code | indexes: leave_type_code: findAll() is unfiltered and GET /balances/me filters by leaveTypeCode + effective window in service; an index supports the per-code selection","table leave_types | fields: code, name, requires_approval, max_consecutive_days, is_paid | pk: code | indexes: code: natural key — referenced by leave_policies.leave_type_code and leave_balances.leave_type_code"],"open_questions":[],"business_rules":["1. NO CONTROLLER LAYER — continue routes calling services directly.\n\n   This is not a preference, it is the codebase's universal existing shape: `src/modules/leave/` contains model, repository, routes, service and index, with no controller, and there is no controller file anywhere in `src/`. `leave.routes.ts` resolves its actor, parses the body, calls the service and maps errors, and every new route should do exactly that.\n\n   Introducing controllers for the new endpoints ONLY would be the worst of the three options: the codebase would then have two layering conventions, and a reader could not tell from a module's name which one applies. A controller layer is a defensible design — but adopting it is a deliberate, whole-codebase refactor with its own review, not something a read-endpoints feature introduces as a side effect.\n\n   Concretely: `auth.routes.ts`, `employee.routes.ts`, `balance.routes.ts` and the new leave read routes each call their service directly, resolve the actor from `request.user`, and reuse the existing `resolveActor` / `sendError` helper shape from `leave.routes.ts` so error mapping stays identical across modules.\n\n2. KEEP `LeavePolicyStatus` MODULE-LOCAL for this feature; import it through the policy module's public entry point (`../policy`), exactly as you already import `createPolicyService`.\n\n   It is a real enum in `policy.model.ts` and it is already re-exported by policy's `index.ts`, so balance consuming it is legal under the committed dependency map (`balance -> policy`) and needs no new edge. Use the enum, never a bare `'ACTIVE'` string literal, so the filter stays type-checked.\n\n   You are right that this is inconsistent — `LeaveStatus`, `LeaveTypeCode`, `AuditAction`, `NotificationStatus`, `EmploymentStatus` and `EmployeeRole` all live in `src/shared/types`, and `LeavePolicyStatus` is the outlier. Promoting it is the correct end state and worth doing. It is not worth doing HERE: it touches `policy.model.ts`, `policy.service.ts` and `policy.repository.interface.ts`, none of which this feature otherwise changes, and a cross-cutting type move buried inside a feature diff is hard to review and easy to get subtly wrong. Do it as its own small mechanical change where the diff is obviously a move and nothing else.\n\n   So: no drive-by refactor, and no string literals either.\n\nEverything in the original brief and in the numbered decisions stands unchanged. [BINDING RULE — operator decision resolving: Should a controller layer be introduced, or continue with routes calling services directly?; Should LeavePolicyStatus be promoted into src/shared/types as a canonical enum?; apply everywhere these apply, not in one place only]"],"auth_contract":"request.user: { id: string; role: EmployeeRole } where EmployeeRole = 'EMPLOYEE' | 'MANAGER' | 'ADMIN'. Identity/role obtained from a JWT bearer token verified by the existing registerAuth preHandler (src/shared/auth/index.ts), which populates request.user from payload.sub (id) and payload.role. RBAC is enforced at the API boundary by resolveActor (validates presence + role membership) and in services (role-scoped visibility), never inline in routes. POST /auth/login is added to the public path set so it is reachable without a token; all other new endpoints require a valid token.","transaction_contract":"","error_response_contract":"Errors return { error: string; code: string }. Validation failure -> HTTP 400 (ValidationError); authentication failure -> 401 (UnauthorizedError); authorization failure -> 403 (ForbiddenError); not found -> 404 (NotFoundError); invalid state transition -> 409 (ConflictError); other -> 500. POST /auth/login returns the SAME status and message for wrong email and wrong password (401 UnauthorizedError) to prevent account enumeration. GET /leaves/:id returns the SAME 404 NotFoundError for a request the caller may not see as for one that does not exist."}