{"domain_entities":[{"name":"LeaveRequest","attributes":["id: string","employeeId: string","leaveTypeId: string","startDate: Date","endDate: Date","reason: string | undefined","status: LeaveRequestStatus","approvedBy: string | null","approvedAt: Date | null","rejectedBy: string | null","rejectedAt: Date | null","rejectionReason: string | null","cancelledBy: string | null","cancelledAt: Date | null","cancellationReason: string | null","durationDays: number","isEmergency: boolean","createdAt: Date","updatedAt: Date"],"purpose":"Core aggregate representing an employee's leave application. Owns the full lifecycle from draft through approval/rejection to cancellation. Tracks who acted at each transition and why."},{"name":"LeaveBalance","attributes":["id: string","employeeId: string","leaveTypeId: string","policyId: string","totalEntitlement: number","usedDays: number","remainingDays: number","pendingDays: number","fiscalYear: number","status: LeaveBalanceStatus","createdAt: Date","updatedAt: Date"],"purpose":"Tracks an employee's leave entitlement, usage, and remaining balance for a specific leave type within a fiscal year. Deducted upon approval; restored upon cancellation of approved leave. pendingDays tracks submitted-but-not-yet-decided requests for informational purposes."},{"name":"LeavePolicy","attributes":["id: string","policyName: string","leaveTypeId: string","entitlementDays: number","accrualRate: number | null","maxAccumulation: number | null","minimumNoticeDays: number","requiresManagerApproval: boolean","allowNegativeBalance: boolean","maxConsecutiveDays: number | null","isActive: boolean","createdAt: Date","updatedAt: Date"],"purpose":"Defines the rules and entitlements for a specific leave type. Acts as the template from which employee LeaveBalances are derived. Controls approval requirements, notice periods, and balance constraints."},{"name":"Employee","attributes":["id: string","employeeNumber: string","firstName: string","lastName: string","email: string","managerId: string | null","department: string | null","hireDate: Date","terminationDate: Date | null","employmentStatus: EmploymentStatus","createdAt: Date","updatedAt: Date","deletedAt: Date | null"],"purpose":"Represents an employee in the organization. The managerId field establishes the approval hierarchy: a leave request is routed to the employee's manager for approval. Employment status gates eligibility for leave requests."},{"name":"LeaveType","attributes":["id: string","code: string","label: string","description: string | null","isActive: boolean","createdAt: Date","updatedAt: Date"],"purpose":"Enumeration of leave categories (ANNUAL, SICK, EMERGENCY, UNPAID, MATERNITY, PATERNITY). Each type maps to a LeavePolicy that defines its rules. The code determines whether minimumNoticeDays is enforced and whether the leave is balance-tracked."}],"modules":[{"name":"leave-type","path":"src/modules/leave-type/","owns":["LeaveType model (id, code, label, description, isActive, createdAt, updatedAt)","leave-type.model.ts","leave-type.repository.ts (ILeaveTypeRepository + PgLeaveTypeRepository)","index.ts barrel export"]},{"name":"leave-policy","path":"src/modules/leave-policy/","owns":["LeavePolicy model (id, policyName, leaveTypeId, entitlementDays, accrualRate, maxAccumulation, minimumNoticeDays, requiresManagerApproval, allowNegativeBalance, maxConsecutiveDays, isActive, createdAt, updatedAt)","leave-policy.model.ts","leave-policy.repository.ts (ILeavePolicyRepository + PgLeavePolicyRepository)","ILeavePolicyService interface","leave-policy.service.ts","index.ts barrel export"]},{"name":"leave-balance","path":"src/modules/leave-balance/","owns":["LeaveBalance model (id, employeeId, leaveTypeId, policyId, totalEntitlement, usedDays, remainingDays, pendingDays, fiscalYear, status, createdAt, updatedAt)","leave-balance.model.ts","leave-balance.repository.ts (ILeaveBalanceRepository + PgLeaveBalanceRepository)","ILeaveBalanceService interface","leave-balance.service.ts","index.ts barrel export"]},{"name":"leave-request","path":"src/modules/leave-request/","owns":["LeaveRequest model (id, employeeId, leaveTypeId, startDate, endDate, reason, status, durationDays, isEmergency, approvedBy, approvedAt, rejectedBy, rejectedAt, rejectionReason, cancelledBy, cancelledAt, cancellationReason, createdAt, updatedAt)","LeaveRequestStatus enum (DRAFT, SUBMITTED, APPROVED, REJECTED, CANCELLED)","leave-request.model.ts","leave-request.repository.ts (ILeaveRequestRepository + PgLeaveRequestRepository)","ILeaveRequestService interface","leave-request.service.ts","index.ts barrel export"]},{"name":"audit-log","path":"src/modules/audit-log/","owns":["AuditLog model (id, entityType, entityId, action, oldValues, newValues, performedBy, performedAt, ipAddress, userAgent, createdAt)","audit-log.model.ts","audit-log.repository.ts (IAuditLogRepository + PgAuditLogRepository)","IAuditService interface","audit-log.service.ts","index.ts barrel export"]}],"dependency_map":[{"from_":"leave-request","to":"leave-balance"},{"from_":"leave-request","to":"leave-policy"},{"from_":"leave-request","to":"leave-type"},{"from_":"leave-request","to":"audit-log"},{"from_":"leave-balance","to":"leave-type"},{"from_":"leave-balance","to":"audit-log"},{"from_":"leave-policy","to":"leave-type"}],"recommended_phases":[{"title":"Phase 1: Foundation — leave-type enum + audit-log module","rationale":"These two modules have zero internal dependencies. leave-type defines the shared LeaveType entity (ANNUAL, SICK, EMERGENCY, UNPAID, MATERNITY, PATERNITY) used by every other module. audit-log provides the cross-cutting IAuditService interface and repository required by GP-002. Both must exist before any dependent module is built.","estimated_files":5},{"title":"Phase 3: Balance tracker — leave-balance module","rationale":"Depends on leave-type and audit-log. Manages the balance ledger: deduct on approval, restore on cancellation, accrue periodically. The ILeaveBalanceService is called by leave-request during the approval/rejection workflow. Building it before leave-request avoids forward references.","estimated_files":4},{"title":"Phase 2: Policy engine — leave-policy module","rationale":"Depends only on leave-type. Defines policy rules (entitlement days, notice hours, approval requirements, max consecutive days, negative balance allowance) that gate every leave application. The ILeavePolicyService.validateRequest method is the single entry point for policy enforcement. Must exist before leave-request can validate applications.","estimated_files":4},{"title":"Phase 4: Request workflow — leave-request module (service + repository)","rationale":"Depends on leave-type, leave-policy, leave-balance, and audit-log. This is the orchestrator: applyLeave validates against policy, checks balance, creates the request, and writes audit. approveLeave/rejectLeave delegate to balance and audit. This is the largest phase — keep it to the service interface, service implementation, and repository.","estimated_files":4},{"title":"Phase 5: API surface — controllers + routes for all modules","rationale":"With all services built, expose REST endpoints via Fastify. Each module gets its own controller (input validation per GP-003) and route registration. RBAC enforcement (GP-005) is applied at the route level: employees can apply/cancel their own leave; managers can approve/reject for their team; HR admins can configure policies. Fastify plugin registration per module.","estimated_files":5}],"architecture_md_update":"## Leave Management Module — Feature Architecture\n\n### Reconciliation Summary\n\nThree specialist designs (domain, data, application) were reconciled into a single coherent architecture. The following conflicts were resolved:\n\n| Conflict | Domain Design | Data Design | App Design | **Resolved** |\n|----------|--------------|-------------|------------|---------------|\n| Request status enum | `LeaveRequestStatus`: DRAFT, SUBMITTED, APPROVED, REJECTED, CANCELLED | `status` column (no enum specified) | `LeaveStatus`: pending, approved, rejected, cancelled | **`LeaveRequestStatus`: DRAFT, SUBMITTED, APPROVED, REJECTED, CANCELLED** — domain design is richer and captures the full lifecycle |\n| LeaveType representation | Entity with `code` field (ANNUAL, SICK, EMERGENCY, UNPAID, MATERNITY, PATERNITY) | Table `leave_types` with `code` column | Enum (annual, sick, emergency) | **Entity-backed enum**: `leave_types` table with `code` column; the six domain values are canonical |\n| LeaveBalance fields | `leaveTypeId`, `policyId`, `totalEntitlement`, `usedDays`, `remainingDays`, `pendingDays` | `policy_id`, `total_entitlement`, `used_days`, `remaining_days` | `leaveType`, `totalDays`, `accruedDays`, `lastAccrualDate` | **Domain design**: `leaveTypeId` + `policyId` + `totalEntitlement` + `usedDays` + `remainingDays` + `pendingDays` |\n| LeaveRequest approver fields | `approvedBy`, `approvedAt`, `rejectedBy`, `rejectedAt`, `rejectionReason`, `cancelledBy`, `cancelledAt`, `cancellationReason` | `approved_by`, `approved_at` only | `managerId`, `reviewedAt`, `reviewedBy` | **Domain design**: full audit trail with separate fields per transition |\n| LeavePolicy fields | `leaveType` (enum), `entitlementDays`, `accrualRate`, `maxAccumulation`, `minimumNoticeDays`, `requiresManagerApproval`, `allowNegativeBalance`, `maxConsecutiveDays` | `leave_type_id` (FK), `entitlement_days`, `accrual_rate`, `max_accumulation`, `minimum_notice_days`, `requires_manager_approval` | `leaveType`, `maxDaysPerYear`, `maxConsecutiveDays`, `requiresApproval`, `minNoticeHours`, `allowNegativeBalance`, `fiscalYearStart`, `fiscalYearEnd` | **Domain + Data**: `leaveTypeId` FK, `entitlementDays`, `accrualRate`, `maxAccumulation`, `minimumNoticeDays`, `requiresManagerApproval`, `allowNegativeBalance`, `maxConsecutiveDays` |\n\n### Module Boundaries\n\n```\nsrc/modules/\n├── leave-type/           # Zero-dependency leaf — shared LeaveType entity\n│   ├── leave-type.model.ts\n│   ├── leave-type.repository.ts   (ILeaveTypeRepository + PgLeaveTypeRepository)\n│   └── index.ts\n├── audit-log/            # Zero-dependency leaf — cross-cutting audit (GP-002)\n│   ├── audit-log.model.ts\n│   ├── audit-log.repository.ts    (IAuditLogRepository + PgAuditLogRepository)\n│   ├── audit-log.service.ts       (IAuditService)\n│   └── index.ts\n├── leave-policy/         # Depends on leave-type\n│   ├── leave-policy.model.ts\n│   ├── leave-policy.repository.ts (ILeavePolicyRepository + PgLeavePolicyRepository)\n│   ├── leave-policy.service.ts    (ILeavePolicyService)\n│   └── index.ts\n├── leave-balance/        # Depends on leave-type, audit-log\n│   ├── leave-balance.model.ts\n│   ├── leave-balance.repository.ts (ILeaveBalanceRepository + PgLeaveBalanceRepository)\n│   ├── leave-balance.service.ts    (ILeaveBalanceService)\n│   └── index.ts\n└── leave-request/        # Orchestrator — depends on all four above\n    ├── leave-request.model.ts\n    ├── leave-request.repository.ts (ILeaveRequestRepository + PgLeaveRequestRepository)\n    ├── leave-request.service.ts    (ILeaveRequestService)\n    └── index.ts\n```\n\n### Dependency Graph (all inward)\n\n```\nleave-type ◄── leave-policy ◄── leave-request\n    ▲                               ▲\n    └────── leave-balance ──────────┘\n                ▲\n           audit-log\n```\n\n- `leave-type` and `audit-log` are zero-dependency leaves\n- `leave-policy` depends only on `leave-type`\n- `leave-balance` depends on `leave-type` + `audit-log`\n- `leave-request` is the orchestrator, depending on all four\n\n### Conceptual Tables (no DDL)\n\n| Table | Primary Key | Key Foreign Keys | Purpose |\n|-------|------------|------------------|---------|\n| `employees` | `id` | `manager_id → employees.id` | Employee directory with self-referencing manager hierarchy |\n| `leave_types` | `id` | — | Lookup: ANNUAL, SICK, EMERGENCY, UNPAID, MATERNITY, PATERNITY |\n| `leave_policies` | `id` | `leave_type_id → leave_types.id` | Rules per leave type: entitlements, notice periods, approval flags |\n| `leave_requests` | `id` | `employee_id → employees.id`, `leave_type_id → leave_types.id`, `approved_by → employees.id` | Core aggregate: full lifecycle from DRAFT to CANCELLED |\n| `leave_balances` | `id` | `employee_id → employees.id`, `policy_id → leave_policies.id` | Per-employee, per-policy, per-fiscal-year balance ledger |\n| `audit_logs` | `id` | `performed_by → employees.id` | GP-002 compliance: all state-changing operations |\n\n### Repository Interfaces → Concrete Implementations\n\nAll backed by PostgreSQL via `src/shared/db/connection.ts` (pg Pool):\n\n| Interface | Concrete | Module |\n|-----------|----------|--------|\n| `ILeaveTypeRepository` | `PgLeaveTypeRepository` | `leave-type` |\n| `ILeavePolicyRepository` | `PgLeavePolicyRepository` | `leave-policy` |\n| `ILeaveBalanceRepository` | `PgLeaveBalanceRepository` | `leave-balance` |\n| `ILeaveRequestRepository` | `PgLeaveRequestRepository` | `leave-request` |\n| `IAuditLogRepository` | `PgAuditLogRepository` | `audit-log` |\n| `IEmployeeRepository` | `PgEmployeeRepository` | shared (pre-existing) |\n\n### Service Interfaces\n\n| Interface | Module | Key Methods |\n|-----------|--------|-------------|\n| `ILeaveRequestService` | `leave-request` | `applyLeave`, `approveLeave`, `rejectLeave`, `cancelLeave`, `getRequestById`, `getRequestsByEmployee`, `getPendingRequestsForManager`, `getTeamCalendar` |\n| `ILeaveBalanceService` | `leave-balance` | `getBalance`, `getAllBalances`, `deductBalance`, `restoreBalance`, `accrueBalance`, `initializeBalancesForEmployee` |\n| `ILeavePolicyService` | `leave-policy` | `getPolicy`, `getAllPolicies`, `validateRequest`, `getMaxDays`, `getMinNoticeDays`, `requiresManagerApproval` |\n| `IAuditService` | `audit-log` | `log` (generic: entityType, entityId, action, oldValues, newValues, performedBy) |\n\n### Lifecycle States (all reflected in architecture)\n\n- **LeaveRequest**: DRAFT → SUBMITTED → APPROVED / REJECTED; CANCELLED reachable from DRAFT, SUBMITTED, or APPROVED\n- **LeaveBalance**: ACTIVE → EXHAUSTED / FROZEN → CLOSED\n- **LeavePolicy**: ACTIVE → INACTIVE → ARCHIVED\n- **Employee (employmentStatus)**: ACTIVE → INACTIVE → TERMINATED\n\n### Business Rules (canonical set)\n\n- **BR-001**: Only ACTIVE employees may submit leave; INACTIVE requires HR; TERMINATED blocked\n- **BR-002**: Manager approval chain via `Employee.managerId`; null manager escalates to department head/HR; self-approval prohibited\n- **BR-003**: Balance sufficiency check at DRAFT→SUBMITTED: `remainingDays - pendingDays >= durationDays`; waived if `allowNegativeBalance=true`\n- **BR-004**: Atomic balance deduction on APPROVED: `usedDays += durationDays`, `remainingDays -= durationDays`, `pendingDays -= durationDays`\n- **BR-005**: Balance restoration on CANCELLATION of APPROVED leave: only unelapsed portion restored\n- **BR-006**: Minimum notice enforced for ANNUAL type; SICK and EMERGENCY bypass\n- **BR-007**: Date validity: startDate ≤ endDate, no backdating, no overlapping APPROVED/SUBMITTED requests\n- **BR-008**: `maxConsecutiveDays` enforced at submission\n- **BR-010**: Fiscal year scoping with `maxAccumulation` carry-forward\n\n### Golden Principles Coverage\n\n| Principle | Implementation |\n|-----------|---------------|\n| GP-001 (Repository) | Every module has its own repository interface + Pg* concrete; services call repositories only |\n| GP-002 (Audit) | `IAuditService` injected into `leave-request` and `leave-balance` services; every state transition writes audit |\n| GP-003 (Validation) | Controllers validate at API boundary (Phase 5); policy service validates business rules |\n| GP-005 (RBAC) | Route-level: employee (own requests), manager (team approvals), HR admin (policy config) |\n| GP-006 (Errors) | All async service methods use try/catch; Fastify error handler for unhandled rejections |\n\n### Phase Build Order\n\n1. **Foundation**: `leave-type` + `audit-log` (zero deps, 5 files)\n2. **Policy engine**: `leave-policy` (depends on leave-type, 4 files)\n3. **Balance tracker**: `leave-balance` (depends on leave-type + audit-log, 4 files)\n4. **Request workflow**: `leave-request` (orchestrator, depends on all, 4 files)\n5. **API surface**: controllers + routes for all modules with Fastify plugin registration, RBAC, and input validation (5 files)\n\n### Stack Compliance\n\n- **Language**: TypeScript ✓\n- **Node**: 20 ✓\n- **Package manager**: npm ✓\n- **Test framework**: Jest ✓\n- **Framework**: Fastify ✓ (controllers/routes in Phase 5)\n- **Frontend**: React Native ✓ (consumes REST API)\n- **Database**: PostgreSQL ✓ (via pg Pool at `src/shared/db/connection.ts`)\n- **Architecture style**: modular-monolith ✓ (5 modules under `src/modules/`)","sql_schemas":["table employees | fields: id, employee_number, first_name, last_name, email, manager_id, department, hire_date, termination_date, employment_status, created_at, updated_at, deleted_at | pk: id | fks: manager_id -> employees.id | indexes: employee_number: unique lookup by employee number for HR and payroll integrations; email: unique lookup for login and identity resolution; manager_id: team-level queries — find all direct reports of a manager; employment_status: filtering active/inactive/terminated employees for leave eligibility checks; department: department-scoped leave calendars and reports","table leave_types | fields: id, code, label, description, is_active, created_at, updated_at | pk: id | indexes: code: unique lookup by leave type code (ANNUAL, SICK, EMERGENCY, etc.) for policy assignment and request validation; is_active: filtering active leave types when presenting options to employees","table leave_policies | fields: id, policy_name, leave_type_id, entitlement_days, accrual_rate, max_accumulation, minimum_notice_days, requires_manager_approval, allow_negative_balance, max_consecutive_days, is_active, created_at, updated_at | pk: id | fks: leave_type_id -> leave_types.id | indexes: leave_type_id: join with leave_types when resolving policy for a given leave type; is_active: filtering active policies during leave request validation and balance calculation","table leave_requests | fields: id, employee_id, leave_type_id, start_date, end_date, reason, status, duration_days, is_emergency, approved_by, approved_at, rejected_by, rejected_at, rejection_reason, cancelled_by, cancelled_at, cancellation_reason, created_at, updated_at | pk: id | fks: employee_id -> employees.id; leave_type_id -> leave_types.id; approved_by -> employees.id | indexes: employee_id: list all leave requests for a given employee (self-service dashboard); status: filter by status (pending approvals queue for managers, approved/rejected lists); leave_type_id: filter requests by leave type for reporting and balance reconciliation; start_date, end_date: date-range queries for calendar views and overlap detection; approved_by: find all requests approved by a specific manager for audit and manager dashboards; employee_id + status: composite access pattern — employee's requests filtered by status","table leave_balances | fields: id, employee_id, policy_id, leave_type_id, total_entitlement, used_days, remaining_days, pending_days, fiscal_year, status, created_at, updated_at | pk: id | fks: employee_id -> employees.id; policy_id -> leave_policies.id | indexes: employee_id: look up balances for a specific employee (self-service dashboard); policy_id: join with leave_policies for entitlement rules and recalculation; fiscal_year: filter balances by fiscal year for annual reset and accrual jobs; employee_id + fiscal_year: composite access pattern — employee's balances scoped to a fiscal year; employee_id + policy_id + fiscal_year: unique constraint enforcement — one balance row per employee per policy per fiscal year","table audit_logs | fields: id, entity_type, entity_id, action, old_values, new_values, performed_by, performed_at, ip_address, user_agent, created_at | pk: id | fks: performed_by -> employees.id | indexes: entity_type + entity_id: look up all audit records for a specific entity (e.g., all changes to a leave request); performed_by: find all actions performed by a specific user for compliance reports; action: filter by action type (APPROVE, REJECT, CREATE, etc.) for audit reports; performed_at: time-range queries for audit log browsing and retention policies"]}