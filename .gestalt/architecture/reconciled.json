{"domain_entities":[{"name":"LeaveRequest","attributes":["id: string","employeeId: string","leaveType: string","startDate: Date","endDate: Date","reason: string | undefined","status: LeaveRequestStatus","approvedBy: string | null","approvedAt: Date | null","rejectionReason: string | undefined","createdAt: Date","updatedAt: Date"],"purpose":"Represents an employee's application for leave. Tracks the full lifecycle from draft through approval/rejection to cancellation. The status field drives all state transitions and guards downstream operations like balance deduction."},{"name":"LeavePolicy","attributes":["id: string","policyName: string","leaveType: string","entitlementDays: number","accrualRate: number | undefined","maxAccumulation: number | undefined","minimumNoticeDays: number","requiresManagerApproval: boolean","isActive: boolean","createdAt: Date","updatedAt: Date"],"purpose":"Defines the rules and entitlements for a specific leave type. Each policy governs how many days an employee is entitled to, whether manager approval is required, minimum notice periods, and accrual behaviour. Policies are referenced by Balance records and LeaveRequests to enforce business rules."},{"name":"Employee","attributes":["id: string","employeeNumber: string","firstName: string","lastName: string","email: string","managerId: string | null","department: string | null","hireDate: Date","terminationDate: Date | null","employmentStatus: string","createdAt: Date","updatedAt: Date","deletedAt: Date | null"],"purpose":"Represents an employee in the organisation. Holds employment status, reporting line (managerId), and department. Employment status gates whether the employee can submit leave requests. The managerId defines the approval chain."},{"name":"Balance","attributes":["id: string","employeeId: string","leaveType: string","totalEntitlement: number","usedDays: number","remainingDays: number","fiscalYear: number","status: string","createdAt: Date","updatedAt: Date"],"purpose":"Tracks an employee's leave balance for a specific leave type and fiscal year. usedDays is incremented when a leave request is approved; remainingDays = totalEntitlement - usedDays. The status reflects whether the balance has available days."},{"name":"Notification","attributes":["id: string","recipientId: string","type: string","title: string","message: string","relatedEntityType: string | null","relatedEntityId: string | null","status: string","createdAt: Date","readAt: Date | null"],"purpose":"Represents a notification sent to an employee or manager about leave-related events (request submitted, approved, rejected, cancelled). Used to fulfil the notification requirement of the leave management module."}],"modules":[{"name":"shared-types","path":"src/shared/types/","owns":["LeaveType enum (annual | sick | emergency)","LeaveStatus enum (draft | submitted | approved | rejected | cancelled)","EmploymentStatus enum (active | inactive | terminated)","BalanceStatus enum (active | exhausted)","NotificationType enum (leave_submitted | leave_approved | leave_rejected | leave_cancelled)","NotificationStatus enum (pending | sent | read | archived)"]},{"name":"employee","path":"src/modules/employee/","owns":["Employee entity","IEmployeeRepository interface","PgEmployeeRepository implementation"]},{"name":"leave-policy","path":"src/modules/leave-policy/","owns":["LeavePolicy entity","ILeavePolicyRepository interface","PgLeavePolicyRepository implementation","ILeavePolicyService interface","LeavePolicyService implementation"]},{"name":"balance","path":"src/modules/balance/","owns":["Balance entity","IBalanceRepository interface","PgBalanceRepository implementation","IBalanceService interface","BalanceService implementation","BalanceController","Balance routes"]},{"name":"leave-request","path":"src/modules/leave-request/","owns":["LeaveRequest entity","ILeaveRequestRepository interface","PgLeaveRequestRepository implementation","ILeaveRequestService interface","LeaveRequestService implementation","LeaveRequestController","LeaveRequest routes","LeaveRequest validation schemas"]},{"name":"notification","path":"src/modules/notification/","owns":["Notification entity","INotificationRepository interface","PgNotificationRepository implementation","INotificationService interface","NotificationService implementation"]},{"name":"audit-log","path":"src/modules/audit-log/","owns":["AuditLog entity","IAuditLogRepository interface","PgAuditLogRepository implementation"]}],"dependency_map":[{"from_":"leave-request","to":"shared-types"},{"from_":"leave-request","to":"balance"},{"from_":"leave-request","to":"leave-policy"},{"from_":"leave-request","to":"employee"},{"from_":"leave-request","to":"notification"},{"from_":"leave-request","to":"audit-log"},{"from_":"balance","to":"shared-types"},{"from_":"balance","to":"leave-policy"},{"from_":"balance","to":"employee"},{"from_":"leave-policy","to":"shared-types"},{"from_":"employee","to":"shared-types"},{"from_":"notification","to":"shared-types"},{"from_":"notification","to":"employee"},{"from_":"audit-log","to":"shared-types"},{"from_":"audit-log","to":"employee"}],"recommended_phases":[{"title":"Phase 1: Foundation – Shared types, Employee, LeavePolicy","rationale":"Establish the shared enums and the two zero-dependency modules (Employee and LeavePolicy). Employee provides the employee data needed by all other modules. LeavePolicy defines the rules that balance and leave-request depend on. This phase can be built and tested independently.","estimated_files":12},{"title":"Phase 2: Balance & Audit Log","rationale":"Balance module depends on shared-types, leave-policy, and employee. Audit-log module depends on shared-types and employee. Both are needed before leave-request can be fully implemented. Balance service provides the deduction/restoration logic that leave-request orchestrates.","estimated_files":10},{"title":"Phase 3: Leave Request & Notification","rationale":"Leave-request is the core orchestrator, depending on all previous modules. Notification module is introduced to handle event-driven notifications. This phase delivers the complete leave application workflow.","estimated_files":14}],"architecture_md_update":"# Leave Management Module Architecture\n\n## Overview\nModular monolith built with TypeScript, Fastify, PostgreSQL. The leave management module enables employees to apply for annual, sick, and emergency leave, managers to approve/reject, and the system to track leave balances.\n\n## Domain Entities\n- **LeaveRequest** – core leave application with lifecycle: DRAFT → SUBMITTED → APPROVED/REJECTED/CANCELLED.\n- **LeavePolicy** – rules per leave type (entitlement, accrual, notice, approval requirement).\n- **Employee** – employee record with employment status and reporting line.\n- **Balance** – per-employee, per-leave-type, per-fiscal-year entitlement tracking.\n- **Notification** – event-driven notifications for leave lifecycle events.\n\n## Modules\n| Module | Path | Responsibility |\n|--------|------|----------------|\n| shared-types | src/shared/types/ | Enums: LeaveType, LeaveStatus, EmploymentStatus, BalanceStatus, NotificationType, NotificationStatus |\n| employee | src/modules/employee/ | Employee entity, repository |\n| leave-policy | src/modules/leave-policy/ | LeavePolicy entity, repository, service |\n| balance | src/modules/balance/ | Balance entity, repository, service, controller, routes |\n| leave-request | src/modules/leave-request/ | LeaveRequest entity, repository, service, controller, routes, validation |\n| notification | src/modules/notification/ | Notification entity, repository, service |\n| audit-log | src/modules/audit-log/ | AuditLog entity, repository |\n\n## Dependency Map\n- leave-request → shared-types, balance, leave-policy, employee, notification, audit-log\n- balance → shared-types, leave-policy, employee\n- leave-policy → shared-types\n- employee → shared-types\n- notification → shared-types, employee\n- audit-log → shared-types, employee\n\n## Database Tables (Conceptual)\n- **leave_requests** (id, employee_id, leave_type, start_date, end_date, reason, status, approved_by, approved_at, rejection_reason, created_at, updated_at)\n- **leave_balances** (id, employee_id, leave_type, fiscal_year, total_entitlement, used_days, remaining_days, status, created_at, updated_at)\n- **leave_policies** (id, policy_name, leave_type, entitlement_days, accrual_rate, max_accumulation, minimum_notice_days, requires_manager_approval, is_active, created_at, updated_at)\n- **employees** (id, employee_number, first_name, last_name, email, manager_id, department, hire_date, termination_date, employment_status, created_at, updated_at, deleted_at)\n- **audit_logs** (id, entity_type, entity_id, action, old_values, new_values, performed_by, performed_at, created_at)\n- **notifications** (id, recipient_id, type, title, message, related_entity_type, related_entity_id, status, created_at, read_at)\n\n## Cross-Cutting Contracts\n- **Auth**: JWT bearer token → `request.user: { id: string; role: UserRole }` where `UserRole = 'employee' | 'manager' | 'hr_admin'`. RBAC enforced via `requireRole(...)` middleware.\n- **Error Response**: `{ error: string; code: string }`. HTTP 400 (VALIDATION_ERROR), 401 (UNAUTHORIZED), 403 (FORBIDDEN), 404 (NOT_FOUND), 422 (INSUFFICIENT_BALANCE), 409 (OVERLAPPING_REQUEST).\n- **Transaction**: Multi-step writes (approve/cancel) use a caller-owned transaction. Repository methods accept an optional `pg.PoolClient`; the service acquires a client, runs `BEGIN`, passes it to repositories, then `COMMIT`/`ROLLBACK`.\n\n## Phased Implementation\n1. **Foundation**: shared-types, employee, leave-policy\n2. **Balance & Audit**: balance module, audit-log module\n3. **Core Workflow**: leave-request module, notification module\n\n## Open Question\n- Day-counting semantics (inclusive calendar days vs business days) – see open questions list.","sql_schemas":["table leave_requests | fields: id, employee_id, leave_type, start_date, end_date, reason, status, approved_by, approved_at, rejection_reason, created_at, updated_at | pk: id | fks: employee_id -> employees.id; approved_by -> employees.id | indexes: employee_id: frequent lookups of all leave requests for a given employee; status: filtering by status (SUBMITTED for manager approval queues, APPROVED for calendar views); start_date, end_date: range queries for calendar views and overlapping-leave detection; employee_id, status: combined filter for 'my pending requests' queries; approved_by: manager approval queue lookups","table leave_balances | fields: id, employee_id, leave_type, fiscal_year, total_entitlement, used_days, remaining_days, status, created_at, updated_at | pk: id | fks: employee_id -> employees.id | indexes: employee_id, leave_type, fiscal_year: unique-lookup for a specific employee's balance for a given leave type and year — the dominant access pattern when checking available days before applying; must be unique to prevent duplicate balance rows; employee_id: all-balances-for-employee queries (dashboard views)","table leave_policies | fields: id, policy_name, leave_type, entitlement_days, accrual_rate, max_accumulation, minimum_notice_days, requires_manager_approval, is_active, created_at, updated_at | pk: id | indexes: leave_type, is_active: dominant lookup — find the active policy for a given leave type when validating a request; is_active: filtering active policies in admin views","table employees | fields: id, employee_number, first_name, last_name, email, manager_id, department, hire_date, termination_date, employment_status, created_at, updated_at, deleted_at | pk: id | fks: manager_id -> employees.id | indexes: employee_number: unique lookup by employee number; email: unique lookup for auth and notifications; manager_id: team-listing queries for manager dashboards; employment_status: filtering active employees for leave eligibility","table audit_logs | fields: id, entity_type, entity_id, action, old_values, new_values, performed_by, performed_at, created_at | pk: id | fks: performed_by -> employees.id | indexes: entity_type, entity_id: audit-trail lookups for a specific entity; performed_by: audit queries by actor; performed_at: time-range audit queries","table notifications | fields: id, recipient_id, type, title, message, related_entity_type, related_entity_id, status, created_at, read_at | pk: id | fks: recipient_id -> employees.id | indexes: recipient_id, status: fetching unread notifications for a user; recipient_id: all notifications for a user; created_at: time-based queries"],"open_questions":[],"business_rules":["Calendar days inclusive: start_date through end_date, all days count toward used_days (weekends and public holidays included). end_date is inclusive. [BINDING RULE — operator decision resolving: How are leave days counted — are start_date and end_date inclusive, and do weekends/public holidays count toward the used-days total?; apply everywhere these apply, not in one place only]"],"auth_contract":"request.user: { id: string; role: UserRole } where UserRole = 'employee' | 'manager' | 'hr_admin'. JWT bearer token verified by an auth middleware that populates request.user. RBAC enforced by a requireRole(...) route-level guard. Employees can submit/cancel their own requests and view their own balances. Managers can approve/reject requests for their direct reports and view team leave requests. HR admins can configure policies and view all balances/requests.","transaction_contract":"Multi-step writes that must be atomic: (1) approving a leave request updates leave_requests.status + leave_balances.used_days/remaining_days; (2) cancelling an already-approved request reverts the status + restores balance days. Repository methods that must join a caller's transaction (updateStatus on ILeaveRequestRepository, updateUsedDays on IBalanceRepository) accept an optional pg PoolClient parameter. When omitted, each method uses the shared pool and runs in its own implicit transaction. The calling service (LeaveService) owns the unit of work: it acquires a client from the pool, executes BEGIN, passes the client to each repository method, then COMMIT or ROLLBACK.","error_response_contract":"Standard error shape: { error: string; code: string }. Validation failure → HTTP 400 (code: VALIDATION_ERROR). Authentication failure → 401 (code: UNAUTHORIZED). Authorization failure → 403 (code: FORBIDDEN). Not found → 404 (code: NOT_FOUND). Insufficient balance → 422 (code: INSUFFICIENT_BALANCE). Overlapping leave request → 409 (code: OVERLAPPING_REQUEST)."}