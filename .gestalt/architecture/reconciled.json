{"domain_entities":[{"name":"DemoSeed","attributes":["environment: 'development' (only environment the seed targets)","appliedAt: Date | null (when the seed last ran successfully)","naturalKeys: string[] (identity keys used to detect already-seeded rows — employeeNumber/email, leaveTypeCode, policy leaveTypeCode+effectiveFrom, balance (employeeId,leaveTypeCode,periodStart,periodEnd), request identity)"],"purpose":"Represents the development-only, idempotent demo dataset (one manager, one employee, leave types, policies, a current-period balance per employee, and 2–3 leave requests in different states). It is a seed concept, not a persisted runtime entity; its lifecycle captures whether the dataset has been applied."},{"name":"Employee","attributes":["id: string","employeeNumber: string","firstName: string","lastName: string","email: string","role: EmployeeRole (EMPLOYEE | MANAGER | ADMIN)","managerId: string | null","department: string","hireDate: Date","terminationDate: Date | null","employmentStatus: EmploymentStatus (ACTIVE | TERMINATED | ON_LEAVE)","passwordHash: string | null"],"purpose":"A person who can authenticate and request leave. The seed mints one MANAGER and one EMPLOYEE whose managerId links the employee to the manager, with bcrypt password hashes so POST /auth/login succeeds."},{"name":"LeaveType","attributes":["code: LeaveTypeCode (annual | sick | emergency | unpaid | maternity | paternity)","name: string","requiresApproval: boolean","maxConsecutiveDays: number","isPaid: boolean"],"purpose":"Static catalog of leave categories. The seed inserts the leave types the balance logic needs (at minimum annual, plus any others referenced by seeded policies)."},{"name":"LeavePolicy","attributes":["id: string","leaveTypeCode: LeaveTypeCode","policyName: string","annualEntitlementDays: number","accrualPeriodMonths: number","carryForwardDays: number","minNoticeDays: number","maxRequestDays: number","requiresManagerApproval: boolean","effectiveFrom: Date","effectiveTo: Date | null","status: LeavePolicyStatus (DRAFT | ACTIVE | SUPERSEDED)"],"purpose":"Defines entitlement and accrual rules per leave type. The seed inserts ACTIVE policies so the balance logic (openPeriod/getCurrentBalances) can resolve entitlements and accrual periods."},{"name":"LeaveBalance","attributes":["id: string","employeeId: string","leaveTypeCode: LeaveTypeCode","periodStart: Date","periodEnd: Date","entitledDays: number","usedDays: number","pendingDays: number"],"purpose":"One employee's entitlement counters for one leave type and one accrual period. The seed creates a balance for the CURRENT accrual period for each seeded employee so GET /balances/me returns data."},{"name":"LeaveRequest","attributes":["id: string","employeeId: string","leaveTypeCode: LeaveTypeCode","startDate: Date","endDate: Date","requestedDays: number","reason: string | null","status: LeaveStatus (DRAFT | SUBMITTED | APPROVED | REJECTED | CANCELLED)","approverId: string | null","approvalComment: string | null","submittedAt: Date | null","decidedAt: Date | null","cancelledBy: string | null","cancelledAt: Date | null"],"purpose":"An employee's leave application moving through the approval lifecycle. The seed inserts 2–3 requests in different states so the list and detail pages have data to render."}],"modules":[{"name":"seed","path":"src/modules/seed/","owns":["ISeedService (seed.service.interface.ts)","SeedService (seed.service.ts) — idempotent demo-data seeding: employees (manager + employee with bcrypt hashes), leave types, leave policies, current-period leave balances, leave requests in multiple states","index.ts (public entry point re-exporting ISeedService + SeedService)"]}],"dependency_map":[{"from_":"seed","to":"shared-date"},{"from_":"seed","to":"shared-types"}],"recommended_phases":[{"title":"Phase 1 — Seed service module","rationale":"Foundation: ISeedService + SeedService + index.ts under src/modules/seed/. Depends only on shared-date (periodContaining/startOfUtcDay/addMonths for the current accrual period) and shared-types (LeaveTypeCode/EmployeeRole/EmploymentStatus/LeaveStatus enums), plus bcrypt for hashing. No module depends on it, so it is the dependency root.","estimated_files":3},{"title":"Phase 2 — Knex seed entry + knexfile wiring + seed script","rationale":"Add seeds/001_demo_data.ts (thin entry calling SeedService.seedDemoData), configure the seeds directory + ts-node registration in knexfile.js for the development environment, and add the 'npm run seed' script. Depends on Phase 1.","estimated_files":3},{"title":"Phase 3 — Dev tooling + docs","rationale":"Root 'npm run dev' (run API + web together), fix web/package.json test script to 'vitest run', and add the README run/seed/credentials section. Independent of the seed code but lands last so the documented commands are verified against the finished seed.","estimated_files":3}],"architecture_md_update":"## Feature: Runnable end-to-end demo seed and dev run\n\n### Stack compliance\n- Language: TypeScript, Node 20, npm.\n- API: Fastify; frontend: React (Vite SPA); database: PostgreSQL; architecture: modular monolith.\n- Backend tests: Jest. Web tests: Vitest (existing web test runner; this feature fixes the script to `vitest run` so it terminates).\n- Seed uses knex and bcrypt (bcrypt.hashSync, cost 10, matching the auth service's bcrypt.compare).\n\n### Canonical naming\n- Domain entities: PascalCase (`Employee`, `LeaveType`, `LeavePolicy`, `LeaveBalance`, `LeaveRequest`, `DemoSeed`).\n- Database tables: snake_case (`employees`, `leave_types`, `leave_policies`, `leave_balances`, `leave_requests`).\n- Service: `ISeedService` / `SeedService` under `src/modules/seed/`.\n- Knex seed entry: `seeds/001_demo_data.ts` (root `seeds/` directory, wired into `knexfile.js` development environment only).\n\n### Domain entities and lifecycle states\n- `DemoSeed` (seed-only concept, no table): `UNAPPLIED` → `APPLIED`.\n- `Employee`: `ACTIVE` | `TERMINATED` | `ON_LEAVE`.\n- `LeaveType`: static catalog, no lifecycle states.\n- `LeavePolicy`: `DRAFT` | `ACTIVE` | `SUPERSEDED`.\n- `LeaveBalance`: `OPEN` | `CLOSED` (inferred from periodStart/periodEnd, not stored).\n- `LeaveRequest`: `DRAFT` | `SUBMITTED` | `APPROVED` | `REJECTED` | `CANCELLED`.\n\n### Conceptual tables (no DDL)\n- `employees`: id PK; unique email, unique employee_number; manager_id FK to employees.id; index on manager_id.\n- `leave_types`: code PK; static catalog.\n- `leave_policies`: id PK; leave_type_code FK to leave_types.code; index on leave_type_code.\n- `leave_balances`: id PK; employee_id FK, leave_type_code FK; unique (employee_id, leave_type_code, period_start, period_end); index on employee_id.\n- `leave_requests`: id PK; employee_id, leave_type_code, approver_id, cancelled_by FKs; indexes (employee_id, status) and (leave_type_code, start_date).\n\n### Modules and dependencies\n- New module `seed` at `src/modules/seed/` owns `ISeedService`, `SeedService`, `index.ts`.\n- Dependencies: `seed` → `shared-date` (periodContaining, startOfUtcDay, addMonths), `seed` → `shared-types` (LeaveTypeCode, EmployeeRole, EmploymentStatus, LeaveStatus).\n- No new repository interfaces or implementations; the seed writes directly via the knex client.\n\n### Seed requirements\n- Idempotent: natural keys for employees (employee_number/email), leave types (code), policies (leave_type_code + effective_from), balances (employee_id + leave_type_code + period_start + period_end). Leave request idempotency identity is an open question.\n- Password hashes minted with the same bcrypt call the auth service verifies (bcrypt.hashSync, cost 10); never plaintext.\n- All seeded dates are UTC calendar dates (whole-day UTC midnight).\n- Current-period balance derived via shared `periodContaining(hireDate, accrualPeriodMonths, now)`; never hand-written dates.\n- Seeded requests consistent with balance counters: SUBMITTED → pending_days, APPROVED → used_days; requested_days via shared `requestedDays(startDate, endDate)` helper.\n- Development-only; never runs automatically as part of migrate; wired into `knexfile.js` development seeds config.\n\n### Scripts and docs\n- `npm run seed` runs the knex seed for development.\n- Root `npm run dev` runs API and web dev server together (concurrency mechanism is an open question).\n- `web/package.json` test script fixed to `vitest run`.\n- README section documents: migrate, seed, start API, start web, and demo credentials.\n\n### Cross-cutting contracts\n- `authContract`: empty — no API surface or role-gated access introduced.\n- `errorResponseContract`: empty — no API endpoints introduced.\n- `transactionContract`: empty — seed is a one-shot dev script with no caller-controlled unit-of-work boundary; knex's built-in per-seed transaction may be used if atomicity is desired.\n\n### Open questions carried forward\n1. LeaveRequest idempotency identity (no natural key).\n2. Whether the MANAGER also gets a current-period balance.\n3. Whether seeded balance counters must be consistent with seeded request states.\n4. How root `npm run dev` runs API and web concurrently.","sql_schemas":["table employees | fields: id, employee_number, first_name, last_name, email, password_hash, role, manager_id, department, hire_date, termination_date, employment_status | pk: id | fks: manager_id -> employees.id | indexes: email: unique — login lookup by email (auth service); employee_number: unique — natural key; manager_id: findByManagerId direct-report scoping","table leave_types | fields: code, name, requires_approval, max_consecutive_days, is_paid | pk: code | indexes: code: natural key / PK — static catalog lookup by LeaveTypeCode","table leave_policies | fields: id, leave_type_code, policy_name, annual_entitlement_days, accrual_period_months, carry_forward_days, min_notice_days, max_request_days, requires_manager_approval, effective_from, effective_to, status | pk: id | fks: leave_type_code -> leave_types.code | indexes: leave_type_code: findByLeaveTypeCode / effective-policy selection","table leave_balances | fields: id, employee_id, leave_type_code, period_start, period_end, entitled_days, used_days, pending_days | pk: id | fks: employee_id -> employees.id; leave_type_code -> leave_types.code | indexes: (employee_id, leave_type_code, period_start, period_end): unique — findByKey at-most-one-row invariant; employee_id: per-employee balance reads","table leave_requests | fields: id, employee_id, leave_type_code, start_date, end_date, requested_days, reason, status, approver_id, approval_comment, submitted_at, decided_at, cancelled_by, cancelled_at | pk: id | fks: employee_id -> employees.id; leave_type_code -> leave_types.code; approver_id -> employees.id; cancelled_by -> employees.id | indexes: (employee_id, status): list scoping by owner/status; (leave_type_code, start_date): date-range + type filtering"],"open_questions":[],"business_rules":["Decisions, in order:\n\n1. Idempotency key for seeded leave requests: use a DETERMINISTIC COMPOSITE KEY of (employee_id, leave_type_code, start_date). Look that up before inserting and skip if present. Do not invent magic id strings - the id column is a generated UUID and a sentinel like 'seed-leave-1' is not a valid one. Do not delete-and-reinsert: a demo database may have been edited by hand and the seed must not destroy that.\n\n2. Create a current-period leave balance for BOTH the manager and the employee. The manager must be able to log in and see a working dashboard, otherwise half the demo flow is unusable.\n\n3. Keep the balances CONSISTENT with the seeded requests: seed requests in multiple states AND replicate the reservation arithmetic into pending_days/used_days so that submitted days appear as pending and approved days as used. An internally inconsistent demo - list and detail pages showing reserved days while GET /balances/me reports full entitlement - is worse than a smaller demo, because the first thing anyone does with a demo account is check whether the numbers agree.\n\n4. Root 'npm run dev': write a small scripts/dev.js that spawns both child processes and forwards their output and exit codes. No new devDependency. The repo already ships scripts/smoke.js, so a node script is the established pattern here and it avoids adding a dependency for a convenience script. [BINDING RULE — operator decision resolving: LeaveRequest has no natural key (id is a generated UUID), so how does the idempotent seed detect an already-seeded leave request to skip it on re-run?; Should the seed create a current-period leave balance for the MANAGER as well as the EMPLOYEE, or only for the EMPLOYEE?; Should the seed keep leave_balances counters (pending_days/used_days) consistent with the seeded leave_requests' states, or seed requests purely for list/detail display with zeroed counters?; How should the root 'npm run dev' script run the API and the web dev server concurrently?; apply everywhere these apply, not in one place only]"],"auth_contract":"","transaction_contract":"","error_response_contract":""}