{"domain_entities":[{"name":"LeaveRequest","attributes":["id: string","employeeId: string","policyId: string","startDate: Date","endDate: Date","reason: string | undefined","status: LeaveRequestStatus","approvedBy: string | null","approvedAt: Date | null","createdAt: Date","updatedAt: Date"],"purpose":"Represents an employee's application for leave. Tracks the request from draft through submission, manager decision, and optional cancellation. The day count (endDate - startDate + 1, inclusive) drives balance deduction and sufficiency checks."},{"name":"LeaveBalance","attributes":["id: string","employeeId: string","policyId: string","totalEntitlement: number","usedDays: number","remainingDays: number","fiscalYear: number","status: BalanceStatus","createdAt: Date","updatedAt: Date"],"purpose":"Tracks an employee's leave entitlement, consumption, and remaining balance for a specific LeavePolicy within a fiscal year. Every APPROVED LeaveRequest deducts from this balance; every CANCELLED (from APPROVED) restores to it."},{"name":"LeavePolicy","attributes":["id: string","policyName: string","leaveType: LeaveType","entitlementDays: number","accrualRate: number | null","maxAccumulation: number | null","minimumNoticeDays: number | null","requiresManagerApproval: boolean","isActive: boolean","createdAt: Date","updatedAt: Date"],"purpose":"Defines the rules and entitlements for a category of leave. Each LeaveBalance is linked to one LeavePolicy. The policy determines entitlement, accrual behavior, notice requirements, and whether manager approval is mandatory."},{"name":"LeaveType","attributes":["value: 'annual' | 'sick' | 'emergency' | 'unpaid' | 'maternity' | 'paternity'"],"purpose":"Enumeration of leave categories. Each LeavePolicy is scoped to one LeaveType. The type influences business rules: emergency leave may bypass minimum notice; sick leave typically has no notice requirement."},{"name":"Employee","attributes":["id: string","employeeNumber: string","firstName: string","lastName: string","email: string","managerId: string | null","department: string | null","hireDate: Date","terminationDate: Date | null","employmentStatus: 'ACTIVE' | 'INACTIVE' | 'TERMINATED'","createdAt: Date","updatedAt: Date","deletedAt: Date | null"],"purpose":"Represents an employee in the organization. Employment status gates leave eligibility. The managerId establishes the approval chain for leave requests."},{"name":"LeaveRequestStatus","attributes":["value: 'DRAFT' | 'SUBMITTED' | 'APPROVED' | 'REJECTED' | 'CANCELLED'"],"purpose":"Enumeration of states a LeaveRequest progresses through. Governs which transitions are legal and which side effects (balance reservation, deduction, restoration) apply."},{"name":"BalanceStatus","attributes":["value: 'ACTIVE' | 'EXHAUSTED' | 'FROZEN' | 'CLOSED'"],"purpose":"Enumeration of LeaveBalance lifecycle states. Determines whether the balance can be drawn upon for new leave requests."},{"name":"AuditLog","attributes":["id: string","entityType: string","entityId: string","action: AuditAction","oldValues: Record<string, unknown> | null","newValues: Record<string, unknown> | null","performedBy: string","performedAt: Date","ipAddress: string | null","userAgent: string | null","createdAt: Date"],"purpose":"Immutable record of every state-changing operation on leave_requests or leave_balances. Satisfies audit trail requirements (GP-002)."},{"name":"Notification","attributes":["id: string","recipientId: string","type: string","title: string","message: string","relatedEntityType: string | null","relatedEntityId: string | null","isRead: boolean","createdAt: Date"],"purpose":"Stores notifications sent to users (e.g., leave request submitted, approved, rejected). Supports read/unread tracking."}],"modules":[{"name":"shared-types","path":"src/shared/types/","owns":["LeaveType enum","LeaveRequestStatus enum","BalanceStatus enum","AuditAction enum","EmploymentStatus enum"]},{"name":"leave-request","path":"src/modules/leave-request/","owns":["LeaveRequest model","ILeaveRequestRepository interface","ILeaveRequestService interface","LeaveRequestService implementation","LeaveRequestController","leaveRequestRoutes","CreateLeaveRequestDto","UpdateLeaveRequestDto","LeaveRequestQueryParams"]},{"name":"leave-balance","path":"src/modules/leave-balance/","owns":["LeaveBalance model","ILeaveBalanceRepository interface","ILeaveBalanceService interface","LeaveBalanceService implementation"]},{"name":"leave-policy","path":"src/modules/leave-policy/","owns":["LeavePolicy model","ILeavePolicyRepository interface","ILeavePolicyService interface","LeavePolicyService implementation"]},{"name":"employee","path":"src/modules/employee/","owns":["Employee model","IEmployeeRepository interface","IEmployeeService interface","EmployeeService implementation"]},{"name":"audit","path":"src/modules/audit/","owns":["AuditLog model","IAuditRepository interface","IAuditService interface","AuditService implementation"]},{"name":"notification","path":"src/modules/notification/","owns":["Notification model","INotificationRepository interface","INotificationService interface","NotificationService implementation"]}],"dependency_map":[{"from_":"leave-request","to":"shared-types"},{"from_":"leave-request","to":"leave-balance"},{"from_":"leave-request","to":"leave-policy"},{"from_":"leave-request","to":"employee"},{"from_":"leave-request","to":"audit"},{"from_":"leave-request","to":"notification"},{"from_":"leave-balance","to":"shared-types"},{"from_":"leave-balance","to":"leave-policy"},{"from_":"leave-policy","to":"shared-types"},{"from_":"employee","to":"shared-types"},{"from_":"audit","to":"shared-types"},{"from_":"notification","to":"shared-types"}],"recommended_phases":[{"title":"Phase 1 — Shared types & foundation","rationale":"All modules depend on shared-types. Establish the enums (LeaveType, LeaveRequestStatus, BalanceStatus, AuditAction, EmploymentStatus) and base repository pattern first. No downstream modules can compile without these.","estimated_files":4},{"title":"Phase 2 — Employee & LeavePolicy modules","rationale":"These are leaf modules with no inter-module dependencies beyond shared-types. Employee provides identity/manager lookups; LeavePolicy provides entitlement rules. Both are prerequisites for leave-request and leave-balance.","estimated_files":12},{"title":"Phase 3 — Audit & Notification modules","rationale":"Cross-cutting infrastructure modules. Audit records all state changes (GP-002); Notification sends alerts on status transitions. Both depend only on shared-types and are consumed by leave-request.","estimated_files":10},{"title":"Phase 4 — LeaveBalance module","rationale":"Depends on shared-types and leave-policy (for entitlement rules). Must exist before leave-request can deduct/restore balances during approval/rejection workflows.","estimated_files":6},{"title":"Phase 5 — LeaveRequest module (core orchestration)","rationale":"The orchestrating module. Depends on all other modules: validates against policy, checks balances, resolves employee/manager, writes audit records, sends notifications. Contains the controller and routes — the API surface.","estimated_files":8}],"architecture_md_update":"## Leave Management Module — Reconciled Architecture\n\n### Domain Entities\n- **LeaveRequest** — employee leave application; lifecycle: DRAFT → SUBMITTED → APPROVED/REJECTED/CANCELLED; day count = (endDate - startDate + 1) inclusive.\n- **LeaveBalance** — tracks entitlement, used, remaining per policy per fiscal year; lifecycle: ACTIVE, EXHAUSTED, FROZEN, CLOSED.\n- **LeavePolicy** — rules for a leave type (entitlement, accrual, notice, approval requirement); lifecycle: ACTIVE, INACTIVE, ARCHIVED.\n- **Employee** — employment status gates eligibility; managerId defines approval chain.\n- **AuditLog** — immutable record of state changes (GP-002).\n- **Notification** — user notifications (submitted, approved, rejected); lifecycle: UNREAD, READ.\n- **Enums**: LeaveType, LeaveRequestStatus, BalanceStatus, AuditAction, EmploymentStatus (all in `src/shared/types/`).\n\n### Conceptual Tables (no DDL)\n- `leave_requests` (id, employee_id, policy_id, start_date, end_date, reason, status, approved_by, approved_at, created_at, updated_at) — PK id; FKs employee_id→employees, policy_id→leave_policies, approved_by→employees.\n- `leave_balances` (id, employee_id, policy_id, total_entitlement, used_days, remaining_days, fiscal_year, status, created_at, updated_at) — PK id; FKs employee_id→employees, policy_id→leave_policies.\n- `employees` (id, employee_number, first_name, last_name, email, manager_id, department, hire_date, termination_date, employment_status, created_at, updated_at, deleted_at) — PK id; FK manager_id→employees.\n- `leave_policies` (id, policy_name, leave_type, entitlement_days, accrual_rate, max_accumulation, minimum_notice_days, requires_manager_approval, is_active, created_at, updated_at) — PK id.\n- `audit_logs` (id, entity_type, entity_id, action, old_values, new_values, performed_by, performed_at, ip_address, user_agent, created_at) — PK id.\n- `notifications` (id, recipient_id, type, title, message, related_entity_type, related_entity_id, is_read, created_at) — PK id; FK recipient_id→employees.\n\n### Module Boundaries\n- **shared-types** (`src/shared/types/`) — enums used by all modules.\n- **employee** (`src/modules/employee/`) — Employee model, repository, service.\n- **leave-policy** (`src/modules/leave-policy/`) — LeavePolicy model, repository, service.\n- **leave-balance** (`src/modules/leave-balance/`) — LeaveBalance model, repository, service; depends on leave-policy.\n- **audit** (`src/modules/audit/`) — AuditLog model, repository, service.\n- **notification** (`src/modules/notification/`) — Notification model, repository, service.\n- **leave-request** (`src/modules/leave-request/`) — orchestrator; depends on all other modules; contains controller, routes, DTOs.\n\n### Dependency Map (DAG)\n- leave-request → shared-types, leave-balance, leave-policy, employee, audit, notification\n- leave-balance → shared-types, leave-policy\n- leave-policy → shared-types\n- employee → shared-types\n- audit → shared-types\n- notification → shared-types\n\n### Recommended Phases\n1. Shared types & foundation (enums, base patterns)\n2. Employee & LeavePolicy modules (leaf modules)\n3. Audit & Notification modules (cross-cutting infrastructure)\n4. LeaveBalance module (depends on leave-policy)\n5. LeaveRequest module (orchestrator, API surface)\n\n### Cross-Cutting Contracts\n- **Auth**: JWT bearer token → `request.user = { id: string; role: UserRole }`; UserRole = 'employee' | 'manager' | 'hr_admin'; RBAC via `requireRole` guard. Endpoints: POST /leave-requests (employee), PATCH /leave-requests/:id/submit (employee, own), PATCH /leave-requests/:id/approve (manager, team), PATCH /leave-requests/:id/reject (manager, team), GET /leave-requests (employee sees own; manager sees team; hr_admin sees all).\n- **Error Response**: `{ error: string; code: string; details?: unknown }`. 400 validation, 401 unauthorized, 403 forbidden, 404 not found, 409 conflict.\n- **Transaction**: Repository methods that participate in multi-step writes accept an optional `PoolClient`. The service owns the unit of work: acquires client, BEGIN, passes client to all participating repositories, COMMIT/ROLLBACK, releases client. Applies to submit, approve/reject, cancel operations.\n\n### Open Questions\nSee the open questions list for unresolved decisions on fiscal year definition, cross-year split, sick leave notice, day counting (calendar vs business), fractional balance rounding, deduction timing, and overlap prevention.","sql_schemas":["table leave_requests | fields: id, employee_id, policy_id, start_date, end_date, reason, status, approved_by, approved_at, created_at, updated_at | pk: id | fks: employee_id -> employees.id; policy_id -> leave_policies.id; approved_by -> employees.id | indexes: employee_id: frequent lookups of all leave requests for a given employee; status: filtering by status (SUBMITTED, APPROVED, etc.) is the primary access pattern for manager queues; start_date, end_date: date-range queries for calendar views and overlap detection; employee_id, status: combined filter for 'my pending requests' queries; policy_id, status: manager filtering by leave type within a status (via join with leave_policies.leave_type)","table leave_balances | fields: id, employee_id, policy_id, total_entitlement, used_days, remaining_days, fiscal_year, status, created_at, updated_at | pk: id | fks: employee_id -> employees.id; policy_id -> leave_policies.id | indexes: employee_id, fiscal_year: the dominant access pattern — look up an employee's balance for the current fiscal year; employee_id: all balance queries are scoped to an employee; policy_id: policy-driven queries (e.g. recalculate all balances for a policy change)","table employees | fields: id, employee_number, first_name, last_name, email, manager_id, department, hire_date, termination_date, employment_status, created_at, updated_at, deleted_at | pk: id | fks: manager_id -> employees.id | indexes: email: unique lookup for authentication and employee search; employee_number: unique lookup by HR identifier; manager_id: 'my direct reports' queries for manager workflows; employment_status: filtering active/inactive/terminated employees; department: department-scoped queries","table leave_policies | fields: id, policy_name, leave_type, entitlement_days, accrual_rate, max_accumulation, minimum_notice_days, requires_manager_approval, is_active, created_at, updated_at | pk: id | indexes: leave_type: policies are queried by leave type when validating a request; is_active: filtering active policies is the default access pattern; leave_type, is_active: combined filter for 'find the active policy for annual leave'","table audit_logs | fields: id, entity_type, entity_id, action, old_values, new_values, performed_by, performed_at, ip_address, user_agent, created_at | pk: id | indexes: entity_type, entity_id: look up all audit records for a specific entity (e.g. all changes to leave_request #42); performed_by: audit trail queries scoped to a user; performed_at: time-range audit queries; action: filtering by action type (APPROVE, REJECT, etc.)","table notifications | fields: id, recipient_id, type, title, message, related_entity_type, related_entity_id, is_read, created_at | pk: id | fks: recipient_id -> employees.id | indexes: recipient_id, is_read: fetch unread notifications for a user; related_entity_type, related_entity_id: look up notifications for a specific entity (e.g., a leave request)"],"open_questions":[],"business_rules":["Business policy answers (scoped to what the codebase provisions — do NOT introduce new unplanned data sources):\n\n1. Fiscal (leave) year: CALENDAR YEAR (Jan 1 to Dec 31), organisation-wide, keyed off leave startDate.\n\n2. Cross-fiscal-year request: deduct the WHOLE request entirely from the fiscal year of startDate. Do NOT split across two LeaveBalance records.\n\n3. Sick leave notice: NO minimum notice period for sick leave (like emergency — it is unplanned). Do not enforce any advance-notice rule for sick or emergency leave. If a LeavePolicy defines minimumNoticeDays, apply it ONLY to annual leave; sick and emergency are exempt.\n\n4. Day counting: WEEKDAYS ONLY — count Monday–Friday, exclude Saturday and Sunday. Do NOT exclude public holidays: there is NO holiday table, repository, or provider in scope, and you must NOT introduce one. Both startDate and endDate inclusive. Keep the counting as a self-contained pure date helper with no external dependency.\n\n5. Rounding / fractional days: WHOLE DAYS ONLY — there is no fractional accrual. Annual entitlement is a whole-number lump sum granted at fiscal-year start, and day counts are whole business days, so remaining_days is always an integer and NO rounding is needed. (If a fractional value ever arises, floor it for enforcement — but the design should not produce fractions.)\n\n6. Deduction timing: deduct on APPROVAL. On submission the days are held as PENDING (a reservation); on approval move pending to used; on reject or cancel release the reservation. available = entitled - (used + pending). Not on the leave start date.\n\n7. Overlap: YES — prevent overlapping leave requests. Reject a new SUBMITTED/APPROVED request whose date range overlaps (inclusive) an existing SUBMITTED/APPROVED request for the same employee.\n\nEmployee/manager data: the LeaveRequest service obtains employee data (managerId, employmentStatus, hireDate) via an injected IEmployeeRepository backed by an employees table (same repository-interface pattern as other modules). The JWT provides ONLY the caller identity (employeeId) and role for RBAC; manager relationship, hire date, and employment status come from IEmployeeRepository. Approvals route to the target employee managerId; if null, escalate to HR (role hr_admin). Managers act only on direct reports. Every endpoint enforces RBAC + input validation. Balances auto-created for all leave types on employee creation. Emergency leave is a separate pool. Only ACTIVE employees may submit. [BINDING RULE — operator decision resolving: What is the fiscal year definition for leave balances? Calendar year (Jan 1 – Dec 31) or a custom period (e.g., Apr 1 – Mar 31)?; When a LeaveRequest spans two fiscal years (e.g., Dec 28 – Jan 3), should the day count be split across two LeaveBalance records or deducted entirely from the startDate's fiscal year?; Should sick leave require a minimum notice period? Currently the rule says emergency bypasses notice, but sick leave is ambiguous.; How are leave days counted — calendar days or business days (Mon–Fri excluding holidays)?; When leave balance remaining_days is fractional (e.g. from accrual), what rounding direction applies when displaying or enforcing the balance?; When should leave balance be deducted — at approval time or on the leave start date?; Should the system prevent overlapping leave requests for the same employee?; apply everywhere these apply, not in one place only]"],"auth_contract":"Authenticated request shape: request.user = { id: string; role: UserRole }. UserRole enum: 'employee' | 'manager' | 'hr_admin'. Identity obtained via JWT bearer token verified by an auth middleware (verifyJwt) that populates request.user. RBAC enforced by a requireRole(...) route-level guard, never inline. Endpoints: POST /leave-requests (employee), PATCH /leave-requests/:id/submit (employee, own requests only), PATCH /leave-requests/:id/approve (manager, team members only), PATCH /leave-requests/:id/reject (manager, team members only), GET /leave-requests (employee sees own; manager sees team; hr_admin sees all).","transaction_contract":"Repository methods that participate in multi-step writes (create, updateStatus, update, delete on ILeaveRequestRepository; updateUsedDays, upsert on ILeaveBalanceRepository; create on IAuditLogRepository) accept an optional PoolClient parameter. When the client is provided, the method uses it for all queries within that call and does NOT release it. When omitted, the method acquires a client from the shared pool internally and releases it after the single operation. The calling service (e.g. LeaveService) owns the unit of work: it acquires a client from the pool, calls BEGIN, passes the client to every participating repository method, then calls COMMIT on success or ROLLBACK on error, and finally releases the client. This contract applies to: (a) submit — create leave_request + audit_log, (b) approve/reject — update leave_request status + update leave_balance used_days/remaining_days + audit_log, (c) cancel — update leave_request status + revert leave_balance + audit_log.","error_response_contract":"Standard error shape: { error: string; code: string; details?: unknown }. Validation failure → HTTP 400 { error: \"Validation failed\", code: \"VALIDATION_ERROR\", details: [...] }. Authentication failure → HTTP 401 { error: \"Authentication required\", code: \"UNAUTHORIZED\" }. Authorization failure → HTTP 403 { error: \"Insufficient permissions\", code: \"FORBIDDEN\" }. Not found → HTTP 404 { error: \"Resource not found\", code: \"NOT_FOUND\" }. Conflict (e.g. insufficient balance, overlapping dates) → HTTP 409 { error: \"...\", code: \"CONFLICT\" }."}