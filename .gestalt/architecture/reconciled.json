{"domain_entities":[{"name":"Employee","attributes":["id: string","employeeNumber: string","firstName: string","lastName: string","email: string","managerId: string | null","department: string | null","hireDate: Date","terminationDate: Date | null","employmentStatus: 'ACTIVE' | 'INACTIVE' | 'TERMINATED'","createdAt: Date","updatedAt: Date","deletedAt: Date | null"],"purpose":"Represents an employee in the organization. The managerId establishes the reporting relationship used for leave approval routing. employmentStatus gates eligibility for leave requests."},{"name":"LeaveType","attributes":["id: string","code: 'annual' | 'sick' | 'emergency' | 'unpaid' | 'maternity' | 'paternity'","name: string","description: string | null","isActive: boolean","createdAt: Date","updatedAt: Date"],"purpose":"Catalog of leave categories available in the system. Each LeaveType has an associated LeavePolicy that governs its rules. The code field is the canonical identifier used in business logic."},{"name":"LeavePolicy","attributes":["id: string","policyName: string","leaveTypeId: string","entitlementDays: number","accrualRate: number | null","maxAccumulation: number | null","minimumNoticeDays: number | null","requiresManagerApproval: boolean","isActive: boolean","createdAt: Date","updatedAt: Date"],"purpose":"Defines the rules and entitlements for a specific leave type. Each policy governs how many days are granted, whether accrual applies, notice requirements, and whether manager approval is mandatory. Referenced by LeaveBalance to compute remaining days."},{"name":"LeaveRequest","attributes":["id: string","employeeId: string","leavePolicyId: string","startDate: Date","endDate: Date","reason: string | undefined","status: LeaveStatus","approvedBy: string | null","approvedAt: Date | null","rejectionReason: string | null","createdAt: Date","updatedAt: Date"],"purpose":"Represents an employee's application for leave. Tracks the full lifecycle from draft through submission, manager decision, and potential cancellation. The status field drives all workflow transitions."},{"name":"LeaveBalance","attributes":["id: string","employeeId: string","policyId: string","totalEntitlement: number","usedDays: number","remainingDays: number","fiscalYear: number","status: 'ACTIVE' | 'EXHAUSTED' | 'CLOSED'","createdAt: Date","updatedAt: Date"],"purpose":"Tracks an employee's leave entitlement, usage, and remaining days for a specific policy within a fiscal year. Updated atomically when leave is approved or cancelled. One balance record per employee-policy-fiscalYear combination."},{"name":"AuditLog","attributes":["id: string","entityType: string","entityId: string","action: string","oldValues: Record<string, unknown> | null","newValues: Record<string, unknown> | null","performedBy: string","performedAt: Date","ipAddress: string | null","userAgent: string | null","createdAt: Date"],"purpose":"Immutable record of all state-changing operations across the leave management module. Satisfies GP-002 (audit trail) by capturing who did what, when, and the before/after state."}],"modules":[{"name":"BaseEntity","path":"src/modules/base-entity/","owns":["base-entity.model.ts","index.ts"]},{"name":"Employee","path":"src/modules/employee/","owns":["employee.model.ts","employee.repository.interface.ts","index.ts"]},{"name":"LeaveType","path":"src/modules/leave-type/","owns":["leave-type.model.ts","leave-type.repository.interface.ts","index.ts"]},{"name":"LeaveStatus","path":"src/modules/leave-status/","owns":["leave-status.model.ts","index.ts"]},{"name":"LeavePolicy","path":"src/modules/leave-policy/","owns":["leave-policy.model.ts","leave-policy.repository.interface.ts","index.ts"]},{"name":"LeaveRequest","path":"src/modules/leave-request/","owns":["leave-request.model.ts","leave-request.repository.interface.ts","index.ts"]},{"name":"LeaveBalance","path":"src/modules/leave-balance/","owns":["leave-balance.model.ts","leave-balance.repository.interface.ts","index.ts"]},{"name":"AuditLog","path":"src/modules/audit-log/","owns":["audit-record.model.ts","audit-log.repository.interface.ts","audit.service.interface.ts","index.ts"]},{"name":"Balance","path":"src/modules/balance/","owns":["balance.service.interface.ts","balance.service.ts","index.ts"]},{"name":"Notification","path":"src/modules/notification/","owns":["notification.service.interface.ts","notification.service.ts","index.ts"]},{"name":"Approval","path":"src/modules/approval/","owns":["approval.service.interface.ts","approval.service.ts","index.ts"]},{"name":"Leave","path":"src/modules/leave/","owns":["leave.service.interface.ts","leave.service.ts","leave.controller.ts","leave.routes.ts","index.ts"]}],"dependency_map":[{"from_":"LeaveRequest","to":"BaseEntity"},{"from_":"LeaveRequest","to":"Employee"},{"from_":"LeaveRequest","to":"LeavePolicy"},{"from_":"LeaveRequest","to":"LeaveStatus"},{"from_":"LeaveBalance","to":"BaseEntity"},{"from_":"LeaveBalance","to":"Employee"},{"from_":"LeaveBalance","to":"LeavePolicy"},{"from_":"LeavePolicy","to":"BaseEntity"},{"from_":"LeavePolicy","to":"LeaveType"},{"from_":"AuditLog","to":"BaseEntity"},{"from_":"Balance","to":"LeaveBalance"},{"from_":"Balance","to":"LeavePolicy"},{"from_":"Balance","to":"AuditLog"},{"from_":"Notification","to":"AuditLog"},{"from_":"Approval","to":"LeaveRequest"},{"from_":"Approval","to":"Employee"},{"from_":"Approval","to":"Balance"},{"from_":"Approval","to":"Notification"},{"from_":"Approval","to":"AuditLog"},{"from_":"Leave","to":"LeaveRequest"},{"from_":"Leave","to":"Employee"},{"from_":"Leave","to":"Balance"},{"from_":"Leave","to":"Approval"},{"from_":"Leave","to":"Notification"}],"recommended_phases":[{"title":"Phase 1: BaseEntity foundation","rationale":"Zero-dependency base model shared by all domain entities. Must exist first.","estimated_files":2},{"title":"Phase 2: Employee model and repository","rationale":"Core organizational entity referenced by leave requests, balances, and approval routing. Independent of leave-specific concepts.","estimated_files":3},{"title":"Phase 3: LeaveType model, repository, and LeaveStatus enum","rationale":"Standalone catalog and value object. LeaveType requires a persistence table; LeaveStatus is a pure enum. Both are needed by downstream policies and requests.","estimated_files":5},{"title":"Phase 4: LeavePolicy model and repository","rationale":"Policy entity depending on LeaveType. Defines accrual rules and entitlement caps per leave type.","estimated_files":3},{"title":"Phase 5: LeaveRequest model and repository","rationale":"Core domain entity depending on Employee, LeavePolicy, and LeaveStatus. Repository interface defines persistence contract including overlap detection.","estimated_files":3},{"title":"Phase 6: LeaveBalance model and repository","rationale":"Balance entity depending on Employee and LeavePolicy. Repository interface for balance persistence with atomic deduction/accrual.","estimated_files":3},{"title":"Phase 7: AuditLog model, repository, and audit service interface","rationale":"Cross-cutting audit concern. Depends only on BaseEntity. Required by all service layers for GP-002 compliance.","estimated_files":4},{"title":"Phase 8: Balance service","rationale":"Domain service orchestrating balance queries, deductions, and accruals. Depends on LeaveBalance, LeavePolicy, and AuditLog.","estimated_files":3},{"title":"Phase 9: Notification service","rationale":"Domain service for leave-related notifications. Depends on AuditLog. Independent of other services.","estimated_files":3},{"title":"Phase 10: Approval service","rationale":"Manager approval/rejection logic. Depends on LeaveRequest, Employee, Balance, Notification, and AuditLog. Orchestrates balance deduction on approval.","estimated_files":3},{"title":"Phase 11: Leave service, controller, and routes","rationale":"Top-level application service and HTTP layer. Depends on LeaveRequest, Employee, Balance, Approval, and Notification. Exposes REST endpoints with RBAC enforcement.","estimated_files":5}],"architecture_md_update":"## Leave Management Module\n\n### Overview\n\nThe Leave Management module enables employees to apply for annual, sick, emergency, and other leave types. Managers approve or reject requests, and the system tracks leave balances per policy and fiscal year. The module is built as a modular monolith using TypeScript, Fastify, PostgreSQL, and React Native (frontend). It adheres to the golden principles: every state change is audited (GP-002), RBAC is enforced at the route level (GP-005), input validation occurs at controller boundaries (GP-003), and all persistence goes through repository interfaces (GP-001).\n\n### Domain Entities\n\n- **Employee** — organizational actor with employment status (ACTIVE, INACTIVE, TERMINATED). The `managerId` field establishes the approval hierarchy.\n- **LeaveType** — catalog of leave categories (annual, sick, emergency, unpaid, maternity, paternity). Each type has a code and active flag.\n- **LeavePolicy** — rules and entitlements for a leave type: entitlement days, accrual rate, max accumulation, minimum notice, and whether manager approval is required.\n- **LeaveRequest** — an employee's leave application. Lifecycle: DRAFT → SUBMITTED → APPROVED / REJECTED; can be CANCELLED. References the employee, the applicable policy, and the approving manager.\n- **LeaveBalance** — tracks used and remaining days for an employee-policy-fiscalYear combination. Statuses: ACTIVE, EXHAUSTED, CLOSED.\n- **AuditLog** — immutable record of all state-changing operations (who, what, when, before/after).\n\n### Business Rules\n\n- **BR-001** — Only ACTIVE employees may submit leave requests.\n- **BR-002** — `startDate` ≥ today; `endDate` ≥ `startDate`; requested days > 0.\n- **BR-003** — Before approval, the employee's LeaveBalance for the policy and fiscal year must have `remainingDays` ≥ requested days (excluding weekends/holidays per policy).\n- **BR-004** — Submitted requests are routed to the employee's manager (`employee.managerId`). Only that manager (or a role subsuming manager authority) may approve/reject. Self-approval is forbidden.\n- **BR-005** — Rejection requires a non-empty `rejectionReason`.\n- **BR-006** — On approval, `LeaveBalance.usedDays` is incremented and `remainingDays` recalculated atomically within the same transaction.\n\n### Module Structure\n\n```\nsrc/modules/\n├── base-entity/          # BaseEntity model (id, createdAt, updatedAt)\n├── employee/             # Employee model + repository interface\n├── leave-type/           # LeaveType model + repository interface\n├── leave-status/         # LeaveStatus enum (DRAFT, SUBMITTED, APPROVED, REJECTED, CANCELLED)\n├── leave-policy/         # LeavePolicy model + repository interface\n├── leave-request/        # LeaveRequest model + repository interface\n├── leave-balance/        # LeaveBalance model + repository interface\n├── audit-log/            # AuditRecord model, repository interface, audit service interface\n├── balance/              # BalanceService (domain service)\n├── notification/         # NotificationService (domain service)\n├── approval/             # ApprovalService (domain service)\n└── leave/                # LeaveService (application service), controller, routes\n```\n\n### Persistence (Conceptual Tables)\n\n- **employees** — `id`, `employee_number`, `first_name`, `last_name`, `email`, `manager_id` (FK→employees), `department`, `hire_date`, `termination_date`, `employment_status`, `created_at`, `updated_at`, `deleted_at`\n- **leave_types** — `id`, `code`, `name`, `description`, `is_active`, `created_at`, `updated_at`\n- **leave_policies** — `id`, `policy_name`, `leave_type_id` (FK→leave_types), `entitlement_days`, `accrual_rate`, `max_accumulation`, `minimum_notice_days`, `requires_manager_approval`, `is_active`, `created_at`, `updated_at`\n- **leave_requests** — `id`, `employee_id` (FK→employees), `leave_policy_id` (FK→leave_policies), `start_date`, `end_date`, `reason`, `status`, `approved_by` (FK→employees), `approved_at`, `rejection_reason`, `created_at`, `updated_at`\n- **leave_balances** — `id`, `employee_id` (FK→employees), `leave_policy_id` (FK→leave_policies), `total_entitlement`, `used_days`, `remaining_days`, `fiscal_year`, `status`, `created_at`, `updated_at`\n- **audit_logs** — `id`, `entity_type`, `entity_id`, `action`, `old_values`, `new_values`, `performed_by`, `performed_at`, `ip_address`, `user_agent`, `created_at`\n\nAll tables use UUID primary keys. Indexes are defined for frequent query patterns: employee lookups, status filtering, date-range queries, and unique compound keys for balances.\n\n### Repository Interfaces (with Concrete Implementations)\n\n| Interface | Concrete Implementation |\n|-----------|-------------------------|\n| `IEmployeeRepository` | `PgEmployeeRepository` |\n| `ILeaveTypeRepository` | `PgLeaveTypeRepository` |\n| `ILeavePolicyRepository` | `PgLeavePolicyRepository` |\n| `ILeaveRequestRepository` | `PgLeaveRequestRepository` |\n| `ILeaveBalanceRepository` | `PgLeaveBalanceRepository` |\n| `IAuditLogRepository` | `PgAuditLogRepository` |\n\nAll implementations use the shared PostgreSQL connection pool (`src/shared/db/connection.ts`).\n\n### Services\n\n- **ILeaveService** — employee-facing operations: apply, view, cancel leave requests.\n- **IApprovalService** — manager operations: approve, reject, list pending approvals.\n- **IBalanceService** — balance queries, deduction, accrual, fiscal-year management.\n- **INotificationService** — sends notifications on leave events (applied, approved, rejected, balance updated).\n- **IAuditService** — records all state changes; used by every service.\n\n### Dependency Flow\n\nDependencies flow strictly inward:\n\n```\nLeave (app) → Approval / Balance / Notification (domain services) → domain entities → value objects\n```\n\n- `Leave` depends on `LeaveRequest`, `Employee`, `Balance`, `Approval`, `Notification`.\n- `Approval` depends on `LeaveRequest`, `Employee`, `Balance`, `Notification`, `AuditLog`.\n- `Balance` depends on `LeaveBalance`, `LeavePolicy`, `AuditLog`.\n- `Notification` depends on `AuditLog`.\n- Domain entities depend on `BaseEntity` and, where appropriate, on each other (e.g., `LeaveRequest` → `Employee`, `LeavePolicy`).\n\nNo circular dependencies exist.\n\n### Implementation Phases\n\n1. **BaseEntity foundation** — shared base model.\n2. **Employee model + repository** — core organizational entity.\n3. **LeaveType model + repository, LeaveStatus enum** — catalog and status value object.\n4. **LeavePolicy model + repository** — rules per leave type.\n5. **LeaveRequest model + repository** — core leave application entity.\n6. **LeaveBalance model + repository** — entitlement tracking.\n7. **AuditLog model, repository, audit service interface** — cross-cutting audit.\n8. **Balance service** — balance orchestration.\n9. **Notification service** — event notifications.\n10. **Approval service** — manager workflow.\n11. **Leave service, controller, routes** — application layer and HTTP endpoints.\n\n### Stack Compliance\n\n- Language: TypeScript (strict mode)\n- Runtime: Node.js 20\n- Framework: Fastify (routes, validation, serialization)\n- Frontend: React Native (consumes REST API)\n- Database: PostgreSQL (via `pg` Pool)\n- Architecture: modular monolith with clear layer boundaries\n- Testing: Jest (unit + integration)\n\n### Golden Principles Adherence\n\n- **GP-001 (Repository Pattern)**: All data access goes through repository interfaces; concrete implementations are injected.\n- **GP-002 (Audit Trail)**: Every state-changing operation is recorded in `audit_logs` via `IAuditService`.\n- **GP-003 (Input Validation)**: Fastify schema validation at route level; additional business rule validation in services.\n- **GP-005 (RBAC)**: Role-based access control enforced in `leave.routes.ts`; only managers (or admin role) can access approval endpoints.\n- **GP-006 (Transaction Semantics)**: Balance deduction on approval runs in a database transaction to ensure atomicity.","sql_schemas":["table employees | fields: id, employee_number, first_name, last_name, email, manager_id, department, hire_date, termination_date, employment_status, created_at, updated_at, deleted_at | pk: id | fks: manager_id -> employees.id | indexes: employee_number: unique lookup by employee number; email: unique lookup by email; manager_id: team hierarchy queries — list direct reports; employment_status: filter active/inactive/terminated employees; deleted_at: soft-delete filtering","table leave_types | fields: id, code, name, description, is_active, created_at, updated_at | pk: id | indexes: code: unique lookup by leave type code (annual, sick, etc.); is_active: filter only active types for policy assignment and request validation","table leave_policies | fields: id, policy_name, leave_type_id, entitlement_days, accrual_rate, max_accumulation, minimum_notice_days, requires_manager_approval, is_active, created_at, updated_at | pk: id | fks: leave_type_id -> leave_types.id | indexes: leave_type_id: filter policies by leave type; is_active: filter only active policies for balance calculations and request validation","table leave_requests | fields: id, employee_id, leave_policy_id, start_date, end_date, reason, status, approved_by, approved_at, rejection_reason, created_at, updated_at | pk: id | fks: employee_id -> employees.id; leave_policy_id -> leave_policies.id; approved_by -> employees.id | indexes: employee_id: list all leave requests for an employee; status: filter by status — pending approvals queue; approved_by: manager approval queue lookup; start_date: calendar-range queries and conflict detection; end_date: calendar-range queries and conflict detection; (employee_id, status): compound filter for employee-specific status views","table leave_balances | fields: id, employee_id, leave_policy_id, total_entitlement, used_days, remaining_days, fiscal_year, status, created_at, updated_at | pk: id | fks: employee_id -> employees.id; leave_policy_id -> leave_policies.id | indexes: employee_id: look up balances for an employee; leave_policy_id: filter balances by policy; fiscal_year: filter balances by fiscal year; (employee_id, leave_policy_id, fiscal_year): unique compound lookup — one balance row per employee per policy per year","table audit_logs | fields: id, entity_type, entity_id, action, old_values, new_values, performed_by, performed_at, ip_address, user_agent, created_at | pk: id | indexes: (entity_type, entity_id): look up audit trail for a specific entity; performed_by: look up actions by a specific user; performed_at: time-range queries for audit reports; action: filter by action type (CREATE, UPDATE, DELETE, APPROVE, REJECT)"]}