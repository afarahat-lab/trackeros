{"domain_entities":[{"name":"AuthSession","attributes":["token: string","profile: EmployeeProfile","status: AuthSessionStatus"],"purpose":"Frontend-held authentication state: the bearer token and the signed-in employee's profile, plus the session's lifecycle status. Created on successful login, cleared on logout or on any 401 response."},{"name":"EmployeeProfile","attributes":["id: string","employeeNumber: string","firstName: string","lastName: string","email: string","role: EmployeeRole","managerId: string | null","department: string","hireDate: Date","employmentStatus: EmploymentStatus"],"purpose":"Read-only view of the signed-in employee shown on the dashboard. Never carries passwordHash or terminationDate."},{"name":"LeaveBalanceView","attributes":["id: string","employeeId: string","leaveTypeCode: LeaveTypeCode","periodStart: Date","periodEnd: Date","entitledDays: number","usedDays: number","pendingDays: number","available: number"],"purpose":"Read-only leave balance for the signed-in employee, with available computed as entitledDays - usedDays - pendingDays (never persisted)."},{"name":"LeaveRequestView","attributes":["id: string","employeeId: string","leaveTypeCode: LeaveTypeCode","startDate: Date","endDate: Date","requestedDays: number","reason: string | null","status: LeaveStatus","approverId: string | null","approvalComment: string | null","submittedAt: Date | null","decidedAt: Date | null","cancelledBy: string | null","cancelledAt: Date | null"],"purpose":"Read-only view of one of the signed-in employee's leave requests, shown in the list and detail views. This feature is read-only: no state transitions are performed by the frontend."}],"modules":[{"name":"shared-types","path":"web/src/shared/types/","owns":["EmployeeProfile","LeaveBalanceView","LeaveRequestView","LoginResponse","EmployeeRole","LeaveTypeCode","LeaveStatus","EmploymentStatus","AuthSessionStatus"]},{"name":"shared-date","path":"web/src/shared/date/","owns":["formatUtcDate","parseUtcDate"]},{"name":"api-client","path":"web/src/infrastructure/api/","owns":["IApiClient","ApiClient","ITokenStorage","TokenStorage","ApiError"]},{"name":"auth","path":"web/src/modules/auth/","owns":["IAuthService","AuthService","AuthProvider","useAuth","AuthSession"]},{"name":"employee","path":"web/src/modules/employee/","owns":["IEmployeeService","EmployeeService"]},{"name":"leave","path":"web/src/modules/leave/","owns":["ILeaveService","LeaveService","IBalanceService","BalanceService"]},{"name":"presentation","path":"web/src/presentation/","owns":["App router","LoginPage","DashboardPage","LeaveListPage","LeaveDetailPage","shared UI components"]}],"dependency_map":[{"from_":"presentation","to":"auth"},{"from_":"presentation","to":"employee"},{"from_":"presentation","to":"leave"},{"from_":"presentation","to":"shared-types"},{"from_":"presentation","to":"shared-date"},{"from_":"auth","to":"api-client"},{"from_":"auth","to":"shared-types"},{"from_":"employee","to":"api-client"},{"from_":"employee","to":"shared-types"},{"from_":"leave","to":"api-client"},{"from_":"leave","to":"shared-types"},{"from_":"api-client","to":"shared-types"},{"from_":"shared-date","to":"shared-types"}],"recommended_phases":[{"title":"Phase 1 — web/ scaffold + shared foundations","rationale":"Stand up the separate Vite build (package.json, tsconfig, vite config, index.html, main.tsx) plus shared-types and shared-date. No dependencies on other modules; everything else imports these.","estimated_files":8},{"title":"Phase 2 — infrastructure API client + token storage","rationale":"IApiClient/ApiClient (bearer header, 401 handling) and ITokenStorage/TokenStorage. Depends only on shared-types; unblocks all service modules.","estimated_files":4},{"title":"Phase 3 — auth module","rationale":"IAuthService/AuthService (login/logout/token/profile) plus AuthProvider/useAuth. Depends on api-client and shared-types.","estimated_files":4},{"title":"Phase 4 — employee + leave + balance services","rationale":"Read-only service modules (getMe, list, getById, getMyBalances) over the API client. Depends on api-client and shared-types.","estimated_files":6},{"title":"Phase 5 — presentation: router, pages, guards","rationale":"LoginPage, DashboardPage, LeaveListPage, LeaveDetailPage, protected-route guard, UTC date rendering. Depends on all service modules and shared foundations.","estimated_files":8},{"title":"Phase 6 — component tests + README","rationale":"Vitest + Testing Library coverage for login success/failure, 401 redirect, and bearer-token sending; README run instructions. Depends on all prior phases.","estimated_files":6}],"architecture_md_update":"## Web Frontend (React + Vite) — Login and Read-Only Employee Leave View\n\n### Scope\nNew top-level `web/` directory, separate from the `src/` Fastify backend. React + TypeScript + Vite SPA. Dependencies: React, React DOM, React Router, Vite, TypeScript, Vitest, Testing Library. No UI component library, no state-management library.\n\n### Domain entities (client-side)\n- **AuthSession** — token, profile, status. Lifecycle: `LOGGED_OUT` → `LOGGED_IN` → `EXPIRED` → `LOGGED_OUT`. Created on login, cleared on logout or any 401.\n- **EmployeeProfile** — read-only signed-in employee; never carries `passwordHash` or `terminationDate`.\n- **LeaveBalanceView** — read-only balance; `available = entitledDays - usedDays - pendingDays` (display only, never persisted). Lifecycle: `OPEN`, `CLOSED`.\n- **LeaveRequestView** — read-only leave request. Lifecycle displayed only: `DRAFT`, `SUBMITTED`, `APPROVED`, `REJECTED`, `CANCELLED`.\n\n### Module boundaries\n- `shared-types` — `web/src/shared/types/` — `EmployeeProfile`, `LeaveBalanceView`, `LeaveRequestView`, `LoginResponse`, enums (`EmployeeRole`, `LeaveTypeCode`, `LeaveStatus`, `EmploymentStatus`, `AuthSessionStatus`).\n- `shared-date` — `web/src/shared/date/` — `formatUtcDate`, `parseUtcDate`.\n- `api-client` — `web/src/infrastructure/api/` — `IApiClient`/`ApiClient`, `ITokenStorage`/`TokenStorage`, `ApiError`.\n- `auth` — `web/src/modules/auth/` — `IAuthService`/`AuthService`, `AuthProvider`/`useAuth`, `AuthSession`.\n- `employee` — `web/src/modules/employee/` — `IEmployeeService`/`EmployeeService`.\n- `leave` — `web/src/modules/leave/` — `ILeaveService`/`LeaveService`, `IBalanceService`/`BalanceService`.\n- `presentation` — `web/src/presentation/` — App router, `LoginPage`, `DashboardPage`, `LeaveListPage`, `LeaveDetailPage`, shared UI components.\n\n### Dependency map\n`presentation` → `auth`, `employee`, `leave`, `shared-types`, `shared-date`; `auth` → `api-client`, `shared-types`; `employee` → `api-client`, `shared-types`; `leave` → `api-client`, `shared-types`; `api-client` → `shared-types`; `shared-date` → `shared-types`. No circular edges.\n\n### Cross-cutting contracts\n- **Auth**: consumes backend contract; `request.user = { id: string; role: EmployeeRole }`, `EmployeeRole = 'EMPLOYEE' | 'MANAGER' | 'ADMIN'`; JWT bearer verified server-side; frontend sends `Authorization: Bearer <token>`, stores token, redirects to login on any 401.\n- **Error**: `{ error: string; code: string }`; 400 validation, 401 auth, 403 forbidden, 404 not found, 409 conflict, 500 other. Login failure shows server message verbatim; no email enumeration.\n- **Transaction**: none — read-only; login is a single non-persistent write.\n\n### Data\nNo new SQL schemas, tables, or repositories. Existing backend tables (`employees`, `leave_requests`, `leave_balances`, `leave_types`, `leave_policies`) are consumed via HTTP and remain unchanged.\n\n### Binding rules\n- All API dates render in UTC; never shift by browser local timezone.\n- Every authenticated call sends bearer token; any 401 clears token and returns to login.\n- Login failure must not reveal whether email exists.\n- `available` is display-only, never persisted or mutated.\n- Frontend is read-only for leave requests: no create/submit/approve/reject/cancel.\n\n### Phases\n1. `web/` scaffold + shared foundations\n2. Infrastructure API client + token storage\n3. Auth module\n4. Employee + leave + balance services\n5. Presentation: router, pages, guards\n6. Component tests + README\n\n### Open questions\n- Token storage mechanism (localStorage/sessionStorage/in-memory)\n- Proactive JWT expiry detection vs server 401 only\n- UTC date rendering implementation (Date.UTC/getUTC*, Intl timeZone UTC, raw YYYY-MM-DD substring)\n\n### Done when\n- `npm run build` in `web/` produces a production bundle with zero TypeScript errors.\n- Component tests cover login success/failure, 401 redirect, and bearer-token sending.\n- Existing backend build and its 181 tests still pass, untouched.\n- README documents how to run the frontend against a local API.","sql_schemas":null,"open_questions":[],"business_rules":["Answering all four questions; note that questions 1 and 4 are the same question (token storage) and share one answer.\n\n1 and 4 — TOKEN STORAGE: sessionStorage. It is cleared when the tab closes, which bounds the XSS exposure window, while still surviving a page reload so the user is not thrown back to the login screen by a refresh. Do not use localStorage (the token would outlive the browser session) and do not use in-memory only (every reload would force a re-login, which makes the app unusable on a refresh).\n\n2 — TOKEN EXPIRY: rely solely on the server's 401 response. Do NOT decode the JWT exp client-side. The server is the only authority on whether a session is valid; a client-side clock can drift, and a token can be revoked server-side before its exp. One source of truth also means one code path to test: every authenticated request already has to handle 401 by clearing the token and routing to login, so proactive expiry adds a second way to reach the same state without removing the need for the first.\n\n3 — DATE RENDERING: use Intl.DateTimeFormat with timeZone: 'UTC'. Apply it through a single shared formatter module that every view imports — the list and the detail view must not each implement their own formatting, because that is how the two drift apart. Do not use bare new Date(...) with local getters (that is exactly the UTC+3 off-by-one defect the backend already suffered), and do not string-slice the ISO date, which produces an unformatted YYYY-MM-DD and silently breaks if the API ever returns a full timestamp.\n\nEverything else in the submitted scope stands unchanged: read-only views, no create/submit/approve/reject/cancel, no manager or role-gated screens, and the frontend lives in web/ rather than under src/. [BINDING RULE — operator decision resolving: Where should the bearer token be persisted on the client (localStorage vs sessionStorage vs in-memory)?; Should the frontend proactively detect token expiry (JWT exp) or rely solely on the server's 401 response?; How should the frontend render UTC date strings so the displayed day never shifts with the browser timezone?; Where should the JWT token be persisted in the browser (localStorage vs sessionStorage vs in-memory)?; apply everywhere these apply, not in one place only]"],"auth_contract":"Frontend consumes the backend's existing auth contract; it does not define a new one. Identity shape: request.user = { id: string; role: EmployeeRole } where EmployeeRole = 'EMPLOYEE' | 'MANAGER' | 'ADMIN'. The backend's registerAuth preHandler verifies the JWT bearer token and populates request.user; RBAC is enforced server-side. The frontend's only auth responsibilities are: send 'Authorization: Bearer <token>' on every authenticated call, store the token returned by POST /auth/login, and redirect to the login screen on any 401 (clearing the token). No role-gated screens are built in this run.","transaction_contract":"","error_response_contract":"Frontend consumes the backend's existing error contract; it does not define a new one. Errors return { error: string; code: string }. Validation failure -> HTTP 400; authentication failure -> 401 (UnauthorizedError); authorization failure -> 403 (ForbiddenError); not found -> 404 (NotFoundError); invalid state transition -> 409 (ConflictError); other -> 500. The frontend must surface the server's error message on login failure without distinguishing wrong-email from wrong-password (the API returns identical 401 bodies), and must treat any 401 on an authenticated call as a signal to clear the token and return to login."}