{"domain_entities":[{"name":"LeaveRequest","attributes":["id: string","employeeId: string","leavePolicyId: string","startDate: Date","endDate: Date","reason: string | undefined","status: LeaveRequestStatus","approvedBy: string | null","approvedAt: Date | null","cancelledBy: string | null","cancelledAt: Date | null","cancellationReason: string | undefined","createdAt: Date","updatedAt: Date"],"purpose":"Represents an employee's leave application. Central aggregate of the leave management module. Tracks the full lifecycle from draft through approval/rejection to cancellation, and carries the audit trail of who acted and when."},{"name":"LeavePolicy","attributes":["id: string","policyName: string","leaveType: LeaveType","entitlementDays: number","accrualRate: number | undefined","maxAccumulation: number | undefined","minimumNoticeDays: number | undefined","requiresManagerApproval: boolean","isActive: boolean","allowsNegativeBalance: boolean","maxConsecutiveDays: number | undefined","createdAt: Date","updatedAt: Date"],"purpose":"Defines the rules and entitlements for a specific leave type. Acts as the template from which employee balances are derived. Controls approval requirements, notice periods, accrual behaviour, and accumulation caps."},{"name":"LeaveBalance","attributes":["id: string","employeeId: string","policyId: string","totalEntitlement: number","usedDays: number","remainingDays: number","pendingDays: number","fiscalYear: number","status: BalanceStatus","createdAt: Date","updatedAt: Date"],"purpose":"Tracks an employee's leave entitlement, consumption, and remaining balance for a specific leave type and fiscal year. Updated atomically when leave is approved or cancelled. The pendingDays field tracks submitted-but-not-yet-decided leave to prevent over-application."},{"name":"LeaveType","attributes":["ANNUAL","SICK","EMERGENCY","UNPAID","MATERNITY","PATERNITY"],"purpose":"Enumeration of leave categories. Each type maps to a LeavePolicy that defines its rules. ANNUAL, SICK, and EMERGENCY are the primary types for this feature; UNPAID, MATERNITY, and PATERNITY are defined for future extension."},{"name":"Employee","attributes":["id: string","employeeNumber: string","firstName: string","lastName: string","email: string","managerId: string | null","department: string | null","hireDate: Date","terminationDate: Date | null","employmentStatus: EmploymentStatus","createdAt: Date","updatedAt: Date","deletedAt: Date | null"],"purpose":"Represents an employee in the organisation. Acts as the actor who submits leave and the subject whose balances are tracked. The managerId establishes the approval chain. Employment status gates leave eligibility."},{"name":"LeaveRequestStatus","attributes":["DRAFT","SUBMITTED","APPROVED","REJECTED","CANCELLED"],"purpose":"Enumeration of states a LeaveRequest passes through during its lifecycle. Drives all state-transition validation and determines which actions are available at each stage."},{"name":"BalanceStatus","attributes":["ACTIVE","EXHAUSTED","FROZEN","ROLLED_OVER"],"purpose":"Enumeration of states a LeaveBalance can occupy. ACTIVE means the balance is usable. EXHAUSTED means remainingDays is 0. FROZEN means the employee is terminated/inactive and the balance is preserved but unusable. ROLLED_OVER means the balance has been carried into a new fiscal year and is no longer the active record."}],"modules":[{"name":"leave-request","path":"src/modules/leave-request/","owns":["LeaveRequest model","ILeaveRequestRepository","PgLeaveRequestRepository","LeaveRequestService interface + implementation","LeaveRequestController","LeaveRequestRoutes","CreateLeaveRequestDto","UpdateLeaveRequestDto","LeaveRequestQueryParams"]},{"name":"leave-balance","path":"src/modules/leave-balance/","owns":["LeaveBalance model","ILeaveBalanceRepository","PgLeaveBalanceRepository","LeaveBalanceService interface + implementation"]},{"name":"leave-policy","path":"src/modules/leave-policy/","owns":["LeavePolicy model","ILeavePolicyRepository","PgLeavePolicyRepository","LeavePolicyService interface + implementation","LeaveType enum"]},{"name":"employee","path":"src/modules/employee/","owns":["Employee model","IEmployeeRepository","PgEmployeeRepository","EmployeeService interface + implementation"]},{"name":"audit","path":"src/modules/audit/","owns":["AuditLog model","IAuditLogRepository","PgAuditLogRepository","AuditService interface + implementation"]},{"name":"notification","path":"src/modules/notification/","owns":["Notification model","INotificationRepository","PgNotificationRepository","NotificationService interface + implementation"]}],"dependency_map":[{"from_":"leave-request","to":"leave-balance"},{"from_":"leave-request","to":"leave-policy"},{"from_":"leave-request","to":"employee"},{"from_":"leave-request","to":"audit"},{"from_":"leave-request","to":"notification"},{"from_":"leave-balance","to":"leave-policy"},{"from_":"leave-balance","to":"employee"},{"from_":"leave-balance","to":"audit"},{"from_":"notification","to":"employee"},{"from_":"audit","to":"employee"}],"recommended_phases":[{"title":"Phase 1 — Foundation: Employee + LeavePolicy + Audit","rationale":"These three modules have zero dependencies on other leave modules. Employee provides identity/manager lookups. LeavePolicy defines leave types and entitlement rules. Audit is the cross-cutting audit record sink required by GP-002. Build them first so every subsequent phase can depend on them.","estimated_files":12},{"title":"Phase 2 — LeaveBalance","rationale":"Depends only on Phase 1 (Employee + LeavePolicy + Audit). Provides balance tracking, deduction, restoration, and initialization. Must exist before LeaveRequest can deduct/restore balances during approval/rejection/cancellation flows.","estimated_files":6},{"title":"Phase 3 — Notification","rationale":"Standalone module with no dependencies on other leave modules. Can be built in parallel with Phase 2, but placed here so LeaveRequest can wire notifications for status changes. Sends leave-status-change notifications to employees and managers.","estimated_files":5},{"title":"Phase 4 — LeaveRequest (core orchestration)","rationale":"Depends on all prior phases. Orchestrates the full leave-request lifecycle: submit (validates policy, checks balance, creates request, audits), approve/reject (updates status, deducts/restores balance, audits, notifies), cancel (restores balance, audits, notifies). This is the largest module and the integration point.","estimated_files":8}],"architecture_md_update":"## Leave Management Module — Reconciled Architecture\n\n### Overview\n\nThe leave management module enables employees to apply for annual, sick, and emergency leave, managers to approve or reject requests, and the system to track leave balances. The architecture follows a modular-monolith style with Fastify (TypeScript), PostgreSQL, and React Native frontend.\n\n### Domain Entities\n\n- **LeaveRequest** — central aggregate; lifecycle: DRAFT → SUBMITTED → APPROVED/REJECTED/CANCELLED. References `leavePolicyId` (not `leaveTypeId`) to link to the governing policy.\n- **LeavePolicy** — defines rules per leave type (entitlement, accrual, notice, approval requirements). Lifecycle: ACTIVE ↔ INACTIVE → ARCHIVED.\n- **LeaveBalance** — per-employee, per-policy, per-fiscal-year balance with `pendingDays` to prevent over-application. Lifecycle: ACTIVE → EXHAUSTED/FROZEN/ROLLED_OVER.\n- **Employee** — actor and subject; employment status gates eligibility. Lifecycle: ACTIVE ↔ INACTIVE → TERMINATED.\n- **LeaveType** enum — ANNUAL, SICK, EMERGENCY, UNPAID, MATERNITY, PATERNITY.\n- **LeaveRequestStatus** enum — DRAFT, SUBMITTED, APPROVED, REJECTED, CANCELLED.\n- **BalanceStatus** enum — ACTIVE, EXHAUSTED, FROZEN, ROLLED_OVER.\n\n### Database Tables (Conceptual)\n\n| Table | Key Fields | Notes |\n|-------|------------|-------|\n| `leave_request` | id, employee_id, leave_policy_id, start_date, end_date, status, approved_by, cancelled_by, cancellation_reason | FK to employee, leave_policy. Indexes on employee_id, status, date range, leave_policy_id. |\n| `leave_balance` | id, employee_id, leave_policy_id, total_entitlement, used_days, remaining_days, pending_days, fiscal_year, status | Unique constraint on (employee_id, leave_policy_id, fiscal_year). |\n| `leave_policy` | id, policy_name, leave_type, entitlement_days, requires_manager_approval, is_active, allows_negative_balance, max_consecutive_days | Indexes on leave_type, is_active. |\n| `audit_log` | id, entity_type, entity_id, action, old_values, new_values, performed_by, performed_at | FK to employee. Indexes for entity lookups and time-range queries. |\n| `employee` | id, employee_number, email, manager_id, employment_status | FK self-reference for manager. Unique indexes on employee_number, email. |\n| `notification` | id, recipient_id, type, title, message, related_entity_type, related_entity_id, is_read, created_at | FK to employee. Indexes for recipient and read status. |\n\n### Module Boundaries\n\n- **leave-request** (`src/modules/leave-request/`) — orchestrates the full lifecycle; depends on all other modules.\n- **leave-balance** (`src/modules/leave-balance/`) — balance arithmetic and state transitions.\n- **leave-policy** (`src/modules/leave-policy/`) — policy definitions and validation.\n- **employee** (`src/modules/employee/`) — employee identity, manager chain, status checks.\n- **audit** (`src/modules/audit/`) — cross-cutting audit trail (GP-002).\n- **notification** (`src/modules/notification/`) — fire-and-forget status notifications.\n\n### Dependency Map\n\n```\nleave-request ──► leave-balance ──► leave-policy\n     │                │\n     │                ├──► employee\n     │                │\n     ├──► leave-policy │\n     ├──► employee     │\n     ├──► audit ◄──────┘\n     └──► notification\n\naudit ──► employee\nnotification ──► employee\n```\n\nNo cycles. All dependencies point inward.\n\n### Recommended Build Phases\n\n1. **Foundation** — Employee, LeavePolicy, Audit (zero leave-module dependencies).\n2. **LeaveBalance** — depends on Phase 1.\n3. **Notification** — standalone, can parallel Phase 2.\n4. **LeaveRequest** — integrates all prior phases.\n\n### Key Reconciliation Decisions\n\n- **leaveTypeId → leavePolicyId**: The domain entity `LeaveRequest` originally had `leaveTypeId`, but the data design correctly uses `leave_policy_id` to reference the policy that governs the leave type. The reconciled entity uses `leavePolicyId`. The application service `submitLeaveRequest` accepts a `leaveType` enum and internally resolves the active policy.\n- **Missing fields added**: `cancellation_reason`, `cancelled_by`, `cancelled_at` added to `leave_request`; `pending_days` added to `leave_balance`; `allows_negative_balance` and `max_consecutive_days` added to `leave_policy`.\n- **Notification table supplied**: The data architect omitted a notification table; a minimal `notification` table is included to support the notification module.\n- **Employee table included**: Although owned by the employee module, the employee table is specified here because it is referenced by foreign keys in all leave tables.\n- **Stack compliance**: All designs use TypeScript, Fastify, PostgreSQL, and the modular-monolith style. No corrections needed.\n- **Audit and RBAC**: Every state-changing operation writes an audit record (GP-002). RBAC is enforced at the route layer (GP-005).\n\n### Business Rules Summary\n\nAll business rules (BR-001 through BR-015) from the domain design are preserved. Key rules include balance sufficiency with pending days, overlap prevention, self-approval prohibition, auto-approval for non-manager-approved leave types, fiscal-year splitting, and cancellation windows.","sql_schemas":["table leave_request | fields: id, employee_id, leave_policy_id, start_date, end_date, reason, status, approved_by, approved_at, cancelled_by, cancelled_at, cancellation_reason, created_at, updated_at | pk: id | fks: employee_id -> employee.id; leave_policy_id -> leave_policy.id; approved_by -> employee.id; cancelled_by -> employee.id | indexes: employee_id: frequent lookups to retrieve all leave requests for a given employee; status: filtering and counting requests by status (SUBMITTED, APPROVED, REJECTED, CANCELLED); start_date, end_date: range queries for calendar views and overlap detection when validating new requests; leave_policy_id: filtering requests by leave type (annual, sick, emergency)","table leave_balance | fields: id, employee_id, leave_policy_id, total_entitlement, used_days, remaining_days, pending_days, fiscal_year, status, created_at, updated_at | pk: id | fks: employee_id -> employee.id; leave_policy_id -> leave_policy.id | indexes: employee_id: lookups to retrieve all balances for a given employee; leave_policy_id: filtering balances by policy/leave type; fiscal_year: filtering balances by fiscal year for accrual and reset operations; employee_id, leave_policy_id, fiscal_year: unique constraint enforcement — one balance row per employee per policy per fiscal year","table leave_policy | fields: id, policy_name, leave_type, entitlement_days, accrual_rate, max_accumulation, minimum_notice_days, requires_manager_approval, is_active, allows_negative_balance, max_consecutive_days, created_at, updated_at | pk: id | indexes: leave_type: filtering policies by leave type (annual, sick, emergency); is_active: filtering only active policies for assignment and validation","table audit_log | fields: id, entity_type, entity_id, action, old_values, new_values, performed_by, performed_at, created_at, updated_at | pk: id | fks: performed_by -> employee.id | indexes: entity_type, entity_id: lookups to retrieve all audit records for a specific entity; performed_by: lookups to retrieve all actions performed by a specific user; performed_at: time-range queries for audit reports; action: filtering audit records by action type (CREATE, UPDATE, DELETE, APPROVE, REJECT)","table employee | fields: id, employee_number, first_name, last_name, email, manager_id, department, hire_date, termination_date, employment_status, created_at, updated_at, deleted_at | pk: id | fks: manager_id -> employee.id | indexes: employee_number: unique lookup by employee number; email: unique lookup by email; manager_id: team lookups for manager approval chain; employment_status: filtering active/inactive/terminated employees","table notification | fields: id, recipient_id, type, title, message, related_entity_type, related_entity_id, is_read, created_at | pk: id | fks: recipient_id -> employee.id | indexes: recipient_id: retrieve notifications for a specific user; is_read: filter unread notifications; created_at: chronological ordering"]}