{"domain_entities":[{"name":"LeaveRequest","attributes":["id: string","employeeId: string","leaveTypeId: string","startDate: Date","endDate: Date","reason: string | undefined","status: LeaveStatus","approvedBy: string | null","approvedAt: Date | null","createdAt: Date","updatedAt: Date"],"purpose":"Represents an employee's application for leave. Tracks the full lifecycle from draft through submission, manager decision, and potential cancellation."},{"name":"LeavePolicy","attributes":["id: string","policyName: string","leaveType: LeaveType","entitlementDays: number","accrualRate: number | null","maxAccumulation: number | null","minimumNoticeDays: number | null","requiresManagerApproval: boolean","isActive: boolean","createdAt: Date","updatedAt: Date"],"purpose":"Defines the rules and entitlements for a specific leave type. Governs how many days an employee is entitled to, accrual behaviour, notice requirements, and whether manager approval is needed."},{"name":"LeaveBalance","attributes":["id: string","employeeId: string","policyId: string","totalEntitlement: number","usedDays: number","remainingDays: number","fiscalYear: number","status: BalanceStatus","createdAt: Date","updatedAt: Date"],"purpose":"Tracks an employee's leave entitlement, usage, and remaining days for a specific leave policy within a fiscal year. Updated atomically when leave is approved or cancelled."},{"name":"Employee","attributes":["id: string","employeeNumber: string","firstName: string","lastName: string","email: string","managerId: string | null","department: string | null","hireDate: Date","terminationDate: Date | null","employmentStatus: EmploymentStatus","createdAt: Date","updatedAt: Date","deletedAt: Date | null"],"purpose":"Represents an employee in the organisation. Holds reporting-line information (managerId) used for leave approval routing, and employment status which gates leave eligibility."}],"modules":[{"name":"shared-types","path":"src/shared/types/","owns":["LeaveType enum (annual, sick, emergency, unpaid, maternity, paternity)","LeaveStatus enum (DRAFT, SUBMITTED, APPROVED, REJECTED, CANCELLED)","EmploymentStatus enum (ACTIVE, INACTIVE, TERMINATED)","AuditAction enum (CREATE, UPDATE, DELETE, APPROVE, REJECT)","BaseEntity interface (id, created_at, updated_at)"]},{"name":"employee","path":"src/modules/employee/","owns":["Employee entity","IEmployeeRepository interface","EmployeeRepository class","IEmployeeService interface","EmployeeService class","EmployeeController class","employeeRoutes function","employee.validation.ts (Zod schemas)","index.ts barrel"]},{"name":"policy","path":"src/modules/policy/","owns":["LeavePolicy entity","IPolicyRepository interface","PolicyRepository class","IPolicyService interface","PolicyService class","PolicyController class","policyRoutes function","policy.validation.ts (Zod schemas)","index.ts barrel"]},{"name":"balance","path":"src/modules/balance/","owns":["LeaveBalance entity","IBalanceRepository interface","BalanceRepository class","IBalanceService interface","BalanceService class","BalanceController class","balanceRoutes function","balance.validation.ts (Zod schemas)","index.ts barrel"]},{"name":"notification","path":"src/modules/notification/","owns":["Notification entity","INotificationRepository interface","NotificationRepository class","INotificationService interface","NotificationService class","index.ts barrel"]},{"name":"audit","path":"src/modules/audit/","owns":["AuditRecord entity","IAuditRepository interface","AuditRepository class","IAuditService interface","AuditService class","index.ts barrel"]},{"name":"leave","path":"src/modules/leave/","owns":["LeaveRequest entity","ILeaveRepository interface","LeaveRepository class","ILeaveService interface","LeaveService class","LeaveController class","leaveRoutes function","leave.validation.ts (Zod schemas)","index.ts barrel"]}],"dependency_map":[{"from_":"src/modules/employee/","to":"src/shared/types/"},{"from_":"src/modules/employee/","to":"src/shared/db/"},{"from_":"src/modules/policy/","to":"src/shared/types/"},{"from_":"src/modules/policy/","to":"src/shared/db/"},{"from_":"src/modules/balance/","to":"src/shared/types/"},{"from_":"src/modules/balance/","to":"src/shared/db/"},{"from_":"src/modules/balance/","to":"src/modules/employee/"},{"from_":"src/modules/balance/","to":"src/modules/policy/"},{"from_":"src/modules/notification/","to":"src/shared/types/"},{"from_":"src/modules/notification/","to":"src/shared/db/"},{"from_":"src/modules/audit/","to":"src/shared/types/"},{"from_":"src/modules/audit/","to":"src/shared/db/"},{"from_":"src/modules/leave/","to":"src/shared/types/"},{"from_":"src/modules/leave/","to":"src/shared/db/"},{"from_":"src/modules/leave/","to":"src/modules/employee/"},{"from_":"src/modules/leave/","to":"src/modules/policy/"},{"from_":"src/modules/leave/","to":"src/modules/balance/"},{"from_":"src/modules/leave/","to":"src/modules/notification/"},{"from_":"src/modules/leave/","to":"src/modules/audit/"}],"recommended_phases":[{"title":"Phase 1: Shared types + Employee module (foundation)","rationale":"Shared enums (LeaveType, LeaveStatus, EmploymentStatus, AuditAction) and BaseEntity must exist before any domain module. Employee is the root foundation module with zero domain dependencies — it only depends on shared/types and shared/db. Build the full vertical slice: model, repository interface, repository, service interface, service, barrel export.","estimated_files":7},{"title":"Phase 2: Policy module (foundation)","rationale":"Policy is the second foundation module. It depends only on shared/types and shared/db. No dependency on employee. Build full vertical slice: model, repository interface, repository, service interface, service, barrel export.","estimated_files":6},{"title":"Phase 3: Balance module (mid-layer)","rationale":"Balance depends on employee (to verify employee exists/is active) and policy (to look up entitlement). Both foundation modules are complete from Phases 1-2. Build full vertical slice: model, repository interface, repository, service interface, service, barrel export.","estimated_files":6},{"title":"Phase 4: Notification + Audit modules (mid-layer, independent)","rationale":"Notification and Audit have zero domain-module dependencies — they only depend on shared/types and shared/db. They can be built together since they are independent of each other and of employee/policy/balance. Build both full vertical slices: model, repository interface, repository, service interface, service, barrel export for each.","estimated_files":10},{"title":"Phase 5: Leave module (orchestrator)","rationale":"Leave orchestrates all five downstream modules via constructor injection. All dependencies (employee, policy, balance, notification, audit) are complete. Build full vertical slice: model, repository interface, repository, service interface, service, barrel export. The LeaveService constructor receives IEmployeeService, IPolicyService, IBalanceService, INotificationService, IAuditService.","estimated_files":5},{"title":"Phase 6: Validation schemas (all modules)","rationale":"Create Zod validation schemas for all six modules. Each module gets a <module>.validation.ts file. Schemas cover: leave request create/approve/reject/cancel/query, balance lookup, employee lookup, policy lookup. Depends on shared types (enums) and domain models from Phases 1-5.","estimated_files":6},{"title":"Phase 7: Controllers (all modules)","rationale":"Create controller classes for all six modules. Each controller receives its corresponding service interface via constructor. Controllers translate HTTP concerns (request parsing, status codes) to service calls. Depends on service interfaces and validation schemas from Phases 1-6.","estimated_files":6},{"title":"Phase 8: Routes + app registration","rationale":"Create route files for all six modules, register them in app.ts. Each route file instantiates its controller and service (no DI container), wires Zod validation, and applies RBAC middleware stubs. Update app.ts to register all route plugins. Depends on controllers from Phase 7.","estimated_files":7}],"architecture_md_update":"## Leave Management Module — Reconciled Architecture\n\n### Overview\n\nThe leave management module enables employees to apply for annual, sick, and emergency leave, managers to approve or reject requests, and the system to track leave balances. The architecture follows a modular-monolith style with Fastify (backend), React Native (frontend), PostgreSQL, and TypeScript. All business rules are enforced in the domain layer; persistence is abstracted behind repository interfaces with concrete PostgreSQL implementations.\n\n### Domain Entities\n\n| Entity | Purpose | Lifecycle States |\n|--------|---------|------------------|\n| **LeaveRequest** | Employee leave application. Tracks from draft to terminal states. | DRAFT → SUBMITTED → APPROVED / REJECTED; APPROVED → CANCELLED; SUBMITTED → CANCELLED |\n| **LeavePolicy** | Rules and entitlements for a leave type. | ACTIVE, INACTIVE |\n| **LeaveBalance** | Employee's entitlement, usage, and remaining days per policy per fiscal year. | ACTIVE → EXHAUSTED (when remainingDays=0); ACTIVE/EXHAUSTED → CLOSED (fiscal year end) |\n| **Employee** | Organisation member with reporting line and employment status. | ACTIVE, INACTIVE, TERMINATED |\n\n**Key Business Rules (binding across all layers):**\n- Day count = `endDate - startDate + 1` (inclusive calendar days).\n- Only ACTIVE employees can submit leave.\n- Manager approval is verified via `Employee.managerId`.\n- Balance sufficiency checked before approval; atomic deduction on approval, restoration on cancellation.\n- Overlap detection: no two APPROVED/SUBMITTED requests with overlapping date ranges.\n- Policy `minimumNoticeDays` enforced on submission; `requiresManagerApproval=false` enables auto-approval.\n\n### Database Tables (Conceptual)\n\n| Table | Key Fields | Primary Key | Foreign Keys |\n|-------|------------|-------------|--------------|\n| `leave_requests` | id, employee_id, leave_type_id, start_date, end_date, reason, status, approved_by, approved_at, created_at, updated_at | id | employee_id → employees.id, leave_type_id → leave_policies.id, approved_by → employees.id |\n| `leave_balances` | id, employee_id, policy_id, total_entitlement, used_days, remaining_days, fiscal_year, status, created_at, updated_at | id | employee_id → employees.id, policy_id → leave_policies.id |\n| `employees` | id, employee_number, first_name, last_name, email, manager_id, department, hire_date, termination_date, employment_status, created_at, updated_at, deleted_at | id | manager_id → employees.id |\n| `leave_policies` | id, policy_name, leave_type, entitlement_days, accrual_rate, max_accumulation, minimum_notice_days, requires_manager_approval, is_active, created_at, updated_at | id | — |\n| `audit_logs` | id, entity_type, entity_id, action, old_values, new_values, performed_by, performed_at, ip_address, user_agent, created_at | id | performed_by → employees.id |\n| `notifications` | id, recipient_id, type, title, message, related_entity_type, related_entity_id, status, created_at, read_at | id | recipient_id → employees.id |\n\nIndexes are defined for frequent access patterns: employee lookups, status filtering, date-range scans, compound unique constraints, and audit trails.\n\n### Module Boundaries\n\n```\nsrc/\n├── shared/\n│   ├── types/          # Enums (LeaveType, LeaveStatus, EmploymentStatus, AuditAction), BaseEntity\n│   └── db/             # PostgreSQL connection pool (pg Pool)\n├── modules/\n│   ├── employee/       # Employee CRUD, status checks\n│   ├── policy/         # LeavePolicy CRUD, active policy lookups\n│   ├── balance/        # LeaveBalance management, deduction/restoration\n│   ├── notification/   # Notification creation and retrieval\n│   ├── audit/          # Audit trail recording and querying\n│   └── leave/          # Orchestrator: submit, approve, reject, cancel\n```\n\n**Dependency direction (acyclic):**\n- `employee` and `policy` are foundation modules (only depend on shared).\n- `balance` depends on `employee` and `policy`.\n- `notification` and `audit` are independent mid-layer modules.\n- `leave` orchestrates all five downstream modules via constructor injection.\n\n### Recommended Build Phases\n\n1. **Shared types + Employee module** — foundation enums and employee vertical slice.\n2. **Policy module** — policy vertical slice.\n3. **Balance module** — balance vertical slice (depends on employee & policy).\n4. **Notification + Audit modules** — independent mid-layer slices.\n5. **Leave module** — orchestrator (depends on all above).\n6. **Validation schemas** — Zod schemas for all modules.\n7. **Controllers** — HTTP translation layer.\n8. **Routes + app registration** — Fastify route plugins, RBAC stubs, app.ts wiring.\n\n### Open Questions (Require Stakeholder Decision)\n\n1. **Fiscal year definition** — calendar, configurable, or fixed? Affects balance lifecycle and cross-year leave handling.\n2. **Mid-year hire entitlement** — pro-rata, full, or accrual-based?\n3. **Cancellation authorization** — employee only, employee/manager, or employee/manager/HR?\n4. **Emergency leave special-casing** — standard policy, bypass notice, or auto-approve?\n5. **Partial-day rounding** — nearest half-day, ceiling, floor, or exact decimal?\n\nThese decisions materially impact balance arithmetic, authorization, and policy enforcement. They must be resolved before implementation reaches the balance and leave modules.","sql_schemas":["table leave_requests | fields: id, employee_id, leave_type_id, start_date, end_date, reason, status, approved_by, approved_at, created_at, updated_at | pk: id | fks: employee_id -> employees.id; leave_type_id -> leave_policies.id; approved_by -> employees.id | indexes: employee_id: frequent lookups for 'my requests' per employee; status: filtering pending/approved/rejected queues; manager approval inbox; leave_type_id: filtering by leave type (annual, sick, emergency); start_date, end_date: date-range scans for calendar views and overlap/conflict detection; approved_by: manager's approval queue lookups; (employee_id, status): compound filter for employee-specific status queries; (status, start_date): sorted approval queues by date","table leave_balances | fields: id, employee_id, policy_id, total_entitlement, used_days, remaining_days, fiscal_year, status, created_at, updated_at | pk: id | fks: employee_id -> employees.id; policy_id -> leave_policies.id | indexes: employee_id: balance lookups per employee (most frequent access pattern); policy_id: policy-driven balance queries; fiscal_year: scoping balances to a fiscal year; (employee_id, policy_id, fiscal_year): unique-lookup compound — one balance row per employee per policy per year; also used for accrual/consumption updates; (employee_id, fiscal_year): employee's annual balance summary across all leave types","table employees | fields: id, employee_number, first_name, last_name, email, manager_id, department, hire_date, termination_date, employment_status, created_at, updated_at, deleted_at | pk: id | fks: manager_id -> employees.id | indexes: employee_number: unique lookup by employee number; email: unique lookup for auth and notifications; manager_id: team listing — find all direct reports of a manager; employment_status: filtering active/inactive/terminated employees; department: department-scoped queries","table leave_policies | fields: id, policy_name, leave_type, entitlement_days, accrual_rate, max_accumulation, minimum_notice_days, requires_manager_approval, is_active, created_at, updated_at | pk: id | indexes: leave_type: filtering active policies by leave type (annual, sick, emergency); is_active: filtering only active policies for entitlement lookups; (leave_type, is_active): common compound filter for policy resolution","table audit_logs | fields: id, entity_type, entity_id, action, old_values, new_values, performed_by, performed_at, ip_address, user_agent, created_at | pk: id | fks: performed_by -> employees.id | indexes: entity_type, entity_id: audit trail lookup for a specific entity; performed_by: audit trail by actor; performed_at: time-range audit queries; action: filtering by action type (CREATE, UPDATE, DELETE, APPROVE, REJECT); (entity_type, entity_id, performed_at): chronological audit trail for an entity","table notifications | fields: id, recipient_id, type, title, message, related_entity_type, related_entity_id, status, created_at, read_at | pk: id | fks: recipient_id -> employees.id | indexes: recipient_id: fetching notifications for a specific user; status: filtering unread/pending notifications; related_entity_type, related_entity_id: finding notifications tied to a specific leave request or entity; (recipient_id, status): inbox queries — unread notifications for a user"],"open_questions":[],"business_rules":["Consolidated decision (apply consistently across annual, sick, and emergency leave):\n\n1. Fiscal/leave year = CALENDAR YEAR (Jan 1 – Dec 31). fiscalYear = the calendar year of the request start_date. Not tenant-configurable.\n\n2. Day counting = BUSINESS DAYS ONLY, excluding weekends AND public holidays, uniform across ALL leave types. One shared countBusinessDays function + a `holidays` table used by every call site (balance check, deduction, restoration). Whole days only. Compare dates by CALENDAR-DATE equality in UTC (normalize to UTC midnight; compare YYYY-MM-DD), never by raw timestamp.\n\n3. Employee with no manager (managerId null): ESCALATE to the HR admin role for approval. Do NOT auto-approve and do NOT block submission.\n\n4. leave_balances.used_days = DENORMALIZED COUNTER, source of truth (O(1) reads). Deduct-on-submission: increment used_days atomically in the same transaction when a LeaveRequest is SUBMITTED. Restore-on-reject/cancel: decrement when REJECTED or CANCELLED. Approval does NOT change used_days again. Submission fails if it would drive remaining below zero.\n\n5. remainingDays = COMPUTED/DERIVED, never stored: totalEntitlement - usedDays, at query time. No code writes remainingDays directly.\n\n6. RBAC (GP-005): every endpoint enforces role-based access control (employee acts only on own requests; managers/HR admins on those they oversee). The authenticated identity/role comes from `request.user` = { id, role }, role ∈ 'employee'|'manager'|'hr_admin', populated by the application's EXISTING auth middleware — do NOT build/mock auth in this feature; the controller only CONSUMES request.user (401 if absent). Declare a concrete `AuthenticatedUser { id: string; role: 'employee'|'manager'|'hr_admin' }` TYPE (no runtime middleware). Validate all inputs at the API boundary (GP-003) before calling the service (400 on invalid). Do NOT add a role field to the Employee entity.\n\n7. Service authorization: thread the actor's role INTO the service — approve(leaveRequestId, approverId, approverRole), reject(..., approverRole). The controller reads request.user, passes id + role; the service enforces (approver must be the employee's manager, or hr_admin when no manager, else throw ApproverNotAuthorizedError). Role is an explicit parameter, never ambient state inside the service.\n\n8. Test files use the project's Jest convention — `*.test.ts` under `tests/` (matching the configured testMatch), NOT `.spec.ts`. Do not change the Jest config. [BINDING RULE — operator decision resolving: How is the fiscal year defined for leave balances — calendar year (Jan 1 – Dec 31), a configurable start month, or company-specific fiscal calendar?; How is totalEntitlement determined for an employee hired mid-year — full annual entitlement or pro-rated?; Who is authorised to cancel a LeaveRequest? Can a manager or HR admin cancel an approved leave on behalf of the employee?; Does emergency leave have special domain behaviour (e.g. bypassing minimum notice, auto-approval) or does it follow the same rules as other leave types governed by their LeavePolicy?; How should partial-day leave deductions be rounded?; How are leave days counted — calendar days or business/working days?; What is the fiscal-year boundary for leave balances?; How are leave requests spanning two fiscal years handled for balance deduction?; What are the valid values for LeaveBalance.status?; apply everywhere these apply, not in one place only]"]}