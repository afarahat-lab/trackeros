{"domain_entities":[{"name":"Employee","attributes":["id: string","employeeNumber: string","firstName: string","lastName: string","email: string","managerId: string | null","department: string","hireDate: Date","terminationDate: Date | null","employmentStatus: 'ACTIVE' | 'INACTIVE' | 'TERMINATED'","createdAt: Date","updatedAt: Date"],"purpose":"Represents an employee in the organization. Tracks employment status and reporting hierarchy."},{"name":"LeaveType","attributes":["id: string","code: string","name: string","description: string | undefined","isActive: boolean","createdAt: Date","updatedAt: Date"],"purpose":"Catalog of leave categories (annual, sick, emergency, unpaid, maternity, paternity). Each type governs which policy rules apply and how balances are tracked."},{"name":"LeavePolicy","attributes":["id: string","policyName: string","leaveTypeId: string","entitlementDays: number","accrualRate: number | undefined","maxAccumulation: number | undefined","minimumNoticeDays: number | undefined","requiresManagerApproval: boolean","isActive: boolean","createdAt: Date","updatedAt: Date"],"purpose":"Defines the rules governing a specific leave type: how many days are entitled, accrual mechanics, notice requirements, and whether manager approval is needed."},{"name":"LeaveBalance","attributes":["id: string","employeeId: string","leavePolicyId: string","totalEntitlement: number","usedDays: number","remainingDays: number","fiscalYear: number","status: 'ACTIVE' | 'EXHAUSTED' | 'CLOSED'","createdAt: Date","updatedAt: Date"],"purpose":"Tracks an employee's entitlement, consumption, and remaining leave days for a specific leave policy within a fiscal year. Deducted on approval; restored on cancellation of an approved request."},{"name":"LeaveRequest","attributes":["id: string","employeeId: string","leavePolicyId: string","startDate: Date","endDate: Date","reason: string | undefined","status: LeaveRequestStatus","approvedBy: string | null","approvedAt: Date | null","createdAt: Date","updatedAt: Date"],"purpose":"Represents an employee's application for leave. Tracks the full lifecycle from draft through submission, manager decision, and potential cancellation."},{"name":"LeaveRequestStatus","attributes":["DRAFT","SUBMITTED","APPROVED","REJECTED","CANCELLED"],"purpose":"Enumeration of all possible states a LeaveRequest can occupy during its lifecycle."},{"name":"AuditLog","attributes":["id: string","entityType: string","entityId: string","action: string","oldValues: Record<string, unknown> | null","newValues: Record<string, unknown> | null","performedBy: string","performedAt: Date","createdAt: Date"],"purpose":"Immutable record of state-changing operations across the leave module for compliance (GP-002)."},{"name":"Notification","attributes":["id: string","recipientId: string","type: 'LEAVE_APPLIED' | 'LEAVE_DECISION' | 'BALANCE_LOW'","message: string","isRead: boolean","createdAt: Date"],"purpose":"Stores notifications dispatched to employees/managers during leave lifecycle events."}],"modules":[{"name":"shared-types","path":"src/shared/types/","owns":["LeaveTypeCode enum (ANNUAL, SICK, EMERGENCY, UNPAID, MATERNITY, PATERNITY)","LeaveRequestStatus enum (DRAFT, SUBMITTED, APPROVED, REJECTED, CANCELLED)"]},{"name":"employee","path":"src/modules/employee/","owns":["Employee model","IEmployeeRepository interface","EmployeeRepository implementation"]},{"name":"leave-type","path":"src/modules/leave-type/","owns":["LeaveType model","ILeaveTypeRepository interface","LeaveTypeRepository implementation"]},{"name":"leave-policy","path":"src/modules/leave-policy/","owns":["LeavePolicy model","ILeavePolicyRepository interface","LeavePolicyRepository implementation","ILeavePolicyService interface","LeavePolicyService implementation"]},{"name":"leave-balance","path":"src/modules/leave-balance/","owns":["LeaveBalance model","ILeaveBalanceRepository interface","LeaveBalanceRepository implementation","ILeaveBalanceService interface","LeaveBalanceService implementation"]},{"name":"leave-request","path":"src/modules/leave-request/","owns":["LeaveRequest model","ILeaveRequestRepository interface","LeaveRequestRepository implementation","ILeaveRequestService interface","LeaveRequestService implementation"]},{"name":"audit-log","path":"src/modules/audit-log/","owns":["AuditLog model","IAuditLogRepository interface","AuditLogRepository implementation","IAuditService interface","AuditService implementation"]},{"name":"notification","path":"src/modules/notification/","owns":["Notification model","INotificationRepository interface","NotificationRepository implementation","INotificationService interface","NotificationService implementation"]}],"dependency_map":[{"from_":"leave-request","to":"leave-policy"},{"from_":"leave-request","to":"leave-balance"},{"from_":"leave-request","to":"audit-log"},{"from_":"leave-request","to":"notification"},{"from_":"leave-request","to":"shared-types"},{"from_":"leave-request","to":"employee"},{"from_":"leave-balance","to":"leave-policy"},{"from_":"leave-balance","to":"audit-log"},{"from_":"leave-balance","to":"shared-types"},{"from_":"leave-policy","to":"leave-type"},{"from_":"leave-policy","to":"audit-log"},{"from_":"leave-policy","to":"shared-types"},{"from_":"leave-type","to":"shared-types"},{"from_":"leave-type","to":"audit-log"},{"from_":"employee","to":"shared-types"},{"from_":"employee","to":"audit-log"},{"from_":"notification","to":"audit-log"},{"from_":"notification","to":"shared-types"},{"from_":"notification","to":"employee"}],"recommended_phases":[{"title":"Phase 1: Foundation — shared-types, audit-log, employee, leave-type","rationale":"Zero-dependency modules first. shared-types owns enums used everywhere. audit-log provides GP-002 audit trail. employee and leave-type are core catalogs needed by policies and requests.","estimated_files":12},{"title":"Phase 2: leave-policy","rationale":"Depends on Phase 1 modules. Defines leave policies governing entitlements and rules.","estimated_files":5},{"title":"Phase 3: leave-balance and notification","rationale":"leave-balance depends on leave-policy and Phase 1; notification depends on employee and audit-log. Both can be built in parallel.","estimated_files":10},{"title":"Phase 4: leave-request (orchestration)","rationale":"Depends on all prior phases. Orchestrates the full leave lifecycle: validates policy, checks/deducts balances, records audits, dispatches notifications.","estimated_files":5}],"architecture_md_update":"## Leave Management Module Architecture\n\n### Domain Entities\n- **Employee**: Core employee record with status and manager hierarchy.\n- **LeaveType**: Catalog of leave categories (annual, sick, emergency, etc.) with active/inactive lifecycle.\n- **LeavePolicy**: Rules per leave type (entitlement, accrual, notice, approval flag).\n- **LeaveBalance**: Per-employee, per-policy, per-fiscal-year tracking of entitlement, used, and remaining days. Lifecycle: ACTIVE → EXHAUSTED → CLOSED.\n- **LeaveRequest**: Employee leave application with full lifecycle: DRAFT → SUBMITTED → (APPROVED | REJECTED) → CANCELLED.\n- **LeaveRequestStatus**: Enum: DRAFT, SUBMITTED, APPROVED, REJECTED, CANCELLED.\n- **AuditLog**: Immutable record of all state-changing operations (GP-002).\n- **Notification**: Messages dispatched on leave events (applied, decided, low balance).\n\n### Business Rules\n- **BR-DAY-COUNT**: Leave days consumed = (endDate - startDate + 1) — inclusive. Applied everywhere.\n- **BR-BALANCE-SUFFICIENCY**: A request can only be submitted or approved if remainingDays >= requested days. Checked at submission and again at approval.\n\n### Modules (src/modules/)\n| Module | Path | Responsibilities |\n|--------|------|------------------|\n| shared-types | src/shared/types/ | LeaveTypeCode and LeaveRequestStatus enums |\n| employee | src/modules/employee/ | Employee model and repository |\n| leave-type | src/modules/leave-type/ | LeaveType catalog model and repository |\n| leave-policy | src/modules/leave-policy/ | LeavePolicy model, repository, service |\n| leave-balance | src/modules/leave-balance/ | LeaveBalance model, repository, service |\n| leave-request | src/modules/leave-request/ | LeaveRequest model, repository, service (orchestrator) |\n| audit-log | src/modules/audit-log/ | AuditLog model, repository, service |\n| notification | src/modules/notification/ | Notification model, repository, service |\n\n### Dependency Flow\nAll dependencies flow inward. leave-request orchestrates the full lifecycle, depending on leave-policy, leave-balance, audit-log, notification, employee, and shared-types. No circular dependencies.\n\n### Conceptual Data Model (PostgreSQL)\n- **employees**: id, employee_number (unique), email (unique), manager_id (FK self), department, employment_status, etc.\n- **leave_types**: id, code (unique), name, is_active.\n- **leave_policies**: id, leave_type_id (FK), entitlement_days, accrual_rate, max_accumulation, minimum_notice_days, requires_manager_approval, is_active.\n- **leave_requests**: id, employee_id (FK), leave_policy_id (FK), start_date, end_date, status, approved_by (FK), etc.\n- **leave_balances**: id, employee_id (FK), leave_policy_id (FK), total_entitlement, used_days, remaining_days, fiscal_year, status; unique (employee_id, leave_policy_id, fiscal_year).\n- **audit_logs**: id, entity_type, entity_id, action, old_values, new_values, performed_by, performed_at.\n- **notifications**: id, recipient_id (FK), type, message, is_read.\n\n### Implementation Phases\n1. **Foundation**: shared-types, audit-log, employee, leave-type (zero dependencies).\n2. **leave-policy**: depends on Phase 1.\n3. **leave-balance & notification**: parallel; balance depends on policy; notification depends on employee.\n4. **leave-request**: orchestration, depends on all prior phases.\n\n### Open Questions (require stakeholder decision)\n1. **used_days computation**: derived vs stored counter vs hybrid.\n2. **Day granularity**: whole days, half-day, or hourly.\n3. **Balance deduction timing**: at application, at approval, or reservation model.\n\n### Stack Compliance\n- Language: TypeScript (Node 20)\n- Framework: Fastify (HTTP routes, plugins)\n- Database: PostgreSQL (via pg Pool)\n- Testing: Jest\n- Frontend: React Native (out of scope for this backend module)\n- Architecture: Modular monolith — each module is a self-contained directory with model, repository, service; shared kernel in src/shared/.","sql_schemas":["table employees | fields: id, employee_number, first_name, last_name, email, manager_id, department, hire_date, termination_date, employment_status, created_at, updated_at | pk: id | fks: manager_id -> employees.id | indexes: employee_number: unique lookup by employee number; email: unique lookup by email; manager_id: team queries — find direct reports; department: filtering by department; employment_status: filtering active/inactive/terminated employees","table leave_types | fields: id, code, name, description, is_active, created_at, updated_at | pk: id | indexes: code: unique lookup by leave type code; is_active: filtering active types only","table leave_policies | fields: id, policy_name, leave_type_id, entitlement_days, accrual_rate, max_accumulation, minimum_notice_days, requires_manager_approval, is_active, created_at, updated_at | pk: id | fks: leave_type_id -> leave_types.id | indexes: leave_type_id: filtering policies by leave type; is_active: filtering active policies only; leave_type_id, is_active: combined filter for active policies of a given type","table leave_requests | fields: id, employee_id, leave_policy_id, start_date, end_date, reason, status, approved_by, approved_at, created_at, updated_at | pk: id | fks: employee_id -> employees.id; leave_policy_id -> leave_policies.id; approved_by -> employees.id | indexes: employee_id: lookups by employee — my requests; status: filtering by status (pending approvals, etc.); leave_policy_id: filtering by leave type; start_date, end_date: date-range queries for calendar views and overlap detection; approved_by: manager approval history; employee_id, status: combined filter — employee's requests by status","table leave_balances | fields: id, employee_id, leave_policy_id, total_entitlement, used_days, remaining_days, fiscal_year, status, created_at, updated_at | pk: id | fks: employee_id -> employees.id; leave_policy_id -> leave_policies.id | indexes: employee_id: lookups by employee — my balances; leave_policy_id: filtering by policy; fiscal_year: filtering by fiscal year; employee_id, leave_policy_id, fiscal_year: unique composite for one balance row per employee/policy/year; employee_id, fiscal_year: employee's balances for a given year","table audit_logs | fields: id, entity_type, entity_id, action, old_values, new_values, performed_by, performed_at, created_at | pk: id | indexes: entity_type, entity_id: lookup audit trail for a specific entity; performed_by: audit trail by actor; performed_at: time-range audit queries; action: filtering by action type","table notifications | fields: id, recipient_id, type, message, is_read, created_at | pk: id | fks: recipient_id -> employees.id | indexes: recipient_id: lookups by recipient; type: filtering by notification type; is_read: filtering unread notifications"],"open_questions":[],"business_rules":["Consolidated decision (apply consistently across annual, sick, and emergency leave):\n\n1. Fiscal/leave year = CALENDAR YEAR (Jan 1 – Dec 31). Derive fiscalYear = the calendar year of the request start_date. Not tenant-configurable for now.\n\n2. Day counting = BUSINESS DAYS ONLY, excluding weekends AND public holidays, applied uniformly to ALL leave types. A single shared countBusinessDays function + a `holidays` table (public-holiday calendar) is used by every call site (balance sufficiency check, deduction, restoration). Whole days only — no half-day/partial-day leave. Compare dates by CALENDAR-DATE equality in UTC (normalize each day/holiday/weekend check to UTC midnight; compare the YYYY-MM-DD triple), never by raw timestamp — so a holiday matches regardless of time-of-day/timezone.\n\n3. Employee with no manager (managerId is null): ESCALATE to the HR admin role for approval. Do NOT auto-approve and do NOT block submission.\n\n4. leave_balances.used_days = DENORMALIZED COUNTER and the source of truth (O(1) reads). Deduct-on-submission: increment used_days atomically, in the same transaction, when a LeaveRequest is SUBMITTED (reserving the balance so an employee cannot over-book). Restore-on-reject/cancel: decrement used_days when that request is REJECTED or CANCELLED. Approval does NOT change used_days again. A submission must fail if it would drive remaining below zero.\n\n5. remainingDays = COMPUTED/DERIVED, never stored: remainingDays = totalEntitlement - usedDays, at query time. No code path writes remainingDays directly.\n\n6. RBAC (GP-005): every endpoint enforces role-based access control — an employee may act only on their own requests; managers/HR admins on those they oversee. The authenticated identity/role comes from the request context (`request.user` = { id, role }, role ∈ 'employee'|'manager'|'hr_admin'), populated by the app's existing auth middleware — do NOT build auth here; the controller only CONSUMES request.user (401 if absent). Validate all inputs at the API boundary (GP-003) before calling the service (400 on invalid: startDate not in the past, startDate <= endDate, minimum notice). Do NOT add a role field to the Employee entity.\n\n7. Service authorization: thread the actor's role INTO the service — approve(leaveRequestId, approverId, approverRole) and reject(..., approverRole). The controller reads request.user and passes id + role; the service enforces the business rule (approver must be the employee's manager, or hr_admin when no manager, else throw ApproverNotAuthorizedError). Role is an explicit parameter, never ambient state read inside the service.\n\n8. Test files use the project's Jest convention — `*.test.ts` under `tests/` (matching the configured testMatch), NOT `.spec.ts`; do not change the Jest config. [BINDING RULE — operator decision resolving: How is used_days on leave_balances computed — derived live from approved leave_requests or stored counter incremented/decremented on state changes?; Are leave days always whole days, or does the system need to support half-day or hourly leave requests?; When should leave balance be deducted — at application time or at approval time?; How is used_days on leave_balances computed — is it derived live from approved leave_requests (SUM of day counts where status=APPROVED) or is it a stored counter incremented/decremented on approval/rejection/cancellation?; apply everywhere these apply, not in one place only]"]}