{"domain_entities":[{"name":"LeaveBalance","attributes":["id: string (required)","employeeId: string (required)","policyId: string (required)","totalEntitlement: number (required)","usedDays: number (required)","remainingDays: number (required, derived)","fiscalYear: number (required)","status: LeaveBalanceStatus (required)","createdAt: Date (required)","updatedAt: Date (required)"],"purpose":"Tracks an employee's leave balance for a specific leave policy within a fiscal year. Uses the incrementing usedDays counter model — remainingDays is always derived as totalEntitlement - usedDays. This is the authoritative source for leave balance queries and reporting."},{"name":"BalanceAdjustment","attributes":["id: string (required)","leaveBalanceId: string (required)","leaveRequestId: string | null (optional — set when the adjustment is tied to a specific leave request)","adjustmentType: 'DEBIT' | 'CREDIT' (required)","amountDays: number (required, positive)","reason: string (required)","performedBy: string | null (optional — userId who triggered the adjustment)","status: BalanceAdjustmentStatus (required)","appliedAt: Date | null (optional)","createdAt: Date (required)","updatedAt: Date (required)"],"purpose":"Records every individual debit or credit against a LeaveBalance. Each approved leave request produces a DEBIT adjustment. Reversals, accruals, and manual corrections produce CREDIT adjustments. Provides the audit trail for all balance changes."}],"modules":[{"name":"balance","path":"src/modules/balance/","owns":["LeaveBalance model","BalanceAdjustment model","LeaveBalance repository interface","BalanceAdjustment repository interface","IBalanceService interface","BalanceService implementation","BalanceController","Balance routes","Balance validation schemas"]},{"name":"leave","path":"src/modules/leave/","owns":["LeaveRequest model","LeaveStatus enum","Leave repository interface","ILeaveService interface","LeaveService implementation","LeaveController","Leave routes","Leave validation schemas","CreateLeaveRequestDto","UpdateLeaveRequestDto","LeaveRequestQueryParams"]},{"name":"policy","path":"src/modules/policy/","owns":["LeavePolicy model","LeaveType enum","Policy repository interface","IPolicyService interface","PolicyService implementation"]},{"name":"employee","path":"src/modules/employee/","owns":["Employee model","Employee repository interface","IEmployeeService interface","EmployeeService implementation"]},{"name":"audit","path":"src/modules/audit/","owns":["AuditLog model","AuditRecord model","Audit repository interface","IAuditService interface","AuditService implementation"]}],"dependency_map":[{"from_":"leave","to":"balance"},{"from_":"leave","to":"policy"},{"from_":"leave","to":"employee"},{"from_":"leave","to":"audit"},{"from_":"balance","to":"policy"},{"from_":"balance","to":"employee"},{"from_":"balance","to":"audit"}],"recommended_phases":[{"title":"Phase 1 — Foundation: employee, policy, audit","rationale":"These are leaf modules with no inter-module dependencies. Employee and policy provide reference data consumed by balance and leave. Audit is a cross-cutting concern required by GP-002. Build them first so downstream modules can depend on stable interfaces.","estimated_files":18},{"title":"Phase 2 — Balance module","rationale":"Depends on policy (for entitlementDays), employee (for employee existence), and audit (GP-002). Implements the hybrid usedDays/remainingDays model already defined in DOMAIN.md. Must be built before leave because leave approval triggers balance deduction.","estimated_files":7},{"title":"Phase 3 — Leave module","rationale":"Depends on all other modules: balance (deduct on approve, restore on cancel/reject), policy (validate entitlement), employee (validate employee/manager), audit (GP-002). The approveLeaveRequest method is the critical integration point that calls IBalanceService.deductBalance.","estimated_files":7}],"architecture_md_update":"## Leave Balance Feature — Reconciled Architecture\n\n### Design Fork Resolution\n\nThe existing DOMAIN.md already defines `Balance`/`LeaveBalance` with `totalEntitlement`, `usedDays`, and `remainingDays` — the **incrementing usedDays counter** model. This design follows that established pattern: `remainingDays` is always derived as `totalEntitlement - usedDays`. The alternative decrementing-remainingDays model is rejected as incompatible with the existing architecture.\n\n### Domain Entities\n\n- **LeaveBalance** — Tracks an employee's leave balance for a specific leave policy within a fiscal year. Lifecycle: ACTIVE → EXHAUSTED (auto when remainingDays=0), FROZEN (manual), CLOSED (year-end), CANCELLED (void).\n- **BalanceAdjustment** — Records every debit/credit against a LeaveBalance. Lifecycle: PENDING → APPLIED (success), REVERSED (undo), FAILED (insufficient balance/frozen).\n\n### Business Rules (Key)\n\n- BR-001: Uniqueness on (employeeId, policyId, fiscalYear) for active balances.\n- BR-002: remainingDays = totalEntitlement - usedDays; never stored independently.\n- BR-003: Debit only if ACTIVE and amountDays ≤ remainingDays.\n- BR-004: Leave approval creates a DEBIT BalanceAdjustment; if debit fails, approval is rolled back.\n- BR-005: Leave cancellation/rejection creates a CREDIT reversal.\n- BR-006: Auto-transition to EXHAUSTED when remainingDays=0; back to ACTIVE on credit.\n- BR-007: Frozen balances reject all adjustments.\n- BR-008: CLOSED/CANCELLED balances are immutable.\n- BR-009: APPLIED/REVERSED/FAILED adjustments are immutable.\n- BR-010: Every state change on LeaveBalance and every BalanceAdjustment application produces an AuditRecord (GP-002).\n- BR-011: Fiscal year scoping; cross-year carryover governed by LeavePolicy.\n- BR-012: amountDays must be positive.\n\n### Conceptual Tables\n\n- **leave_balance** — Core balance entity. Composite uniqueness on (employee_id, policy_id, fiscal_year). Indexed for employee+fiscalYear lookup, leave-approval deduction path, policy-level reporting, and bulk accrual/rollover jobs.\n- **balance_adjustment** — Immutable audit trail of every balance mutation. Linked to leave_balance and optionally to leave_request. Indexed for balance-scoped queries, leave request linkage, status filtering, and time-range reporting.\n- **audit_log** — Required by GP-002 for all state-changing operations. Indexed for entity-trail lookups, performer accountability, time-range reports, and action-type filtering.\n\n### Module Boundaries\n\n- **balance** (`src/modules/balance/`) — Owns LeaveBalance and BalanceAdjustment models, repositories, IBalanceService, and controller. Exposes methods: getBalance, getBalances, initializeBalance, deductBalance, restoreBalance, recalculateBalance, getBalanceReport. All methods use `policyId` (not leaveTypeId).\n- **leave** (`src/modules/leave/`) — Owns LeaveRequest, ILeaveService, and controller. `approveLeaveRequest` calls `IBalanceService.deductBalance` within a transaction; `cancelLeaveRequest`/`rejectLeaveRequest` call `restoreBalance` when previously approved.\n- **policy** (`src/modules/policy/`) — Owns LeavePolicy, provides entitlement and accrual rules.\n- **employee** (`src/modules/employee/`) — Owns Employee, provides employee existence and department info.\n- **audit** (`src/modules/audit/`) — Cross-cutting audit logging per GP-002.\n\n### Dependency Flow\n\n```\nleave → balance → {policy, employee, audit}\n```\nNo module depends on leave. Balance depends only on leaf modules. Leaf modules have zero inter-module dependencies.\n\n### Implementation Phases\n\n1. **Phase 1 — Foundation**: employee, policy, audit (leaf modules, no dependencies).\n2. **Phase 2 — Balance module**: depends on policy, employee, audit; implements the usedDays/remainingDays model.\n3. **Phase 3 — Leave module**: depends on all others; integrates balance deduction on approval.\n\n### Stack Compliance\n\n- Language: TypeScript\n- Framework: Fastify (controllers as route handlers)\n- Database: PostgreSQL (accessed via pg Pool behind repository interfaces)\n- Architecture: modular-monolith with clear layer boundaries (presentation, application, domain, infrastructure)\n- RBAC: Enforced at controller level per GP-005 (managers/HR approve/reject; employees create/submit/cancel own requests; HR views reports).\n- Audit: Every state transition on LeaveBalance and every BalanceAdjustment application produces an AuditRecord per GP-002.\n","sql_schemas":["table leave_balance | fields: id, employee_id, policy_id, total_entitlement, used_days, remaining_days, fiscal_year, status, created_at, updated_at | pk: id | fks: employee_id -> employee.id; policy_id -> leave_policy.id | indexes: employee_id, fiscal_year: primary access pattern — look up an employee's balance for the current fiscal year; also used by reporting queries aggregating balances per employee per year; employee_id, policy_id, fiscal_year: uniqueness constraint — at most one balance row per employee per policy per fiscal year; also supports the leave-approval path that checks available balance before deducting; policy_id: reporting and admin queries that aggregate balances across employees for a given policy; fiscal_year: bulk accrual jobs and year-end rollover processes that operate on all balances for a given fiscal year","table balance_adjustment | fields: id, leave_balance_id, leave_request_id, adjustment_type, amount_days, reason, performed_by, status, applied_at, created_at, updated_at | pk: id | fks: leave_balance_id -> leave_balance.id; leave_request_id -> leave_request.id | indexes: leave_balance_id: retrieve all adjustments for a given balance (audit trail, balance recalculation); leave_request_id: link adjustment to the originating leave request; status: filter pending adjustments for processing; created_at: time-range queries for reporting and reconciliation","table audit_log | fields: id, entity_type, entity_id, action, old_values, new_values, performed_by, performed_at, ip_address, user_agent, created_at | pk: id | indexes: entity_type, entity_id: primary audit lookup — retrieve all audit records for a given entity (e.g. all balance changes for an employee); performed_by: accountability queries — find all actions performed by a specific user; performed_at: time-range audit reports and compliance queries; action: filter audit trail by action type (CREATE, UPDATE, DELETE, APPROVE, REJECT)"]}