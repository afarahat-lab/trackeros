{"domain_entities":[{"name":"LeaveRequest","attributes":["id: string","employeeId: string","leavePolicyId: string","startDate: Date","endDate: Date","reason: string | undefined","status: LeaveRequestStatus","approvedBy: string | null","approvedAt: Date | null","cancelledAt: Date | null","createdAt: Date","updatedAt: Date"],"purpose":"Represents an employee's application for leave. Tracks the full lifecycle from draft through approval/rejection to cancellation, carrying the dates, type, and reason for the absence."},{"name":"LeavePolicy","attributes":["id: string","policyName: string","leaveType: LeaveType","entitlementDays: number","accrualRate: number | null","maxAccumulation: number | null","minimumNoticeDays: number | null","requiresManagerApproval: boolean","isActive: boolean","createdAt: Date","updatedAt: Date"],"purpose":"Defines the rules and entitlements for a specific leave type. Governs how many days an employee is entitled to, whether manager approval is required, minimum notice periods, and accrual behaviour."},{"name":"LeaveBalance","attributes":["id: string","employeeId: string","leavePolicyId: string","totalEntitlement: number","usedDays: number","remainingDays: number","fiscalYear: number","status: 'ACTIVE' | 'EXHAUSTED' | 'CLOSED'","createdAt: Date","updatedAt: Date"],"purpose":"Tracks an employee's leave entitlement, usage, and remaining balance for a specific leave policy within a fiscal year. Deducted on approval, restored on cancellation of approved leave."},{"name":"LeaveNotification","attributes":["id: string","recipientId: string","type: 'SUBMITTED' | 'APPROVED' | 'REJECTED' | 'CANCELLED' | 'BALANCE_LOW' | 'BALANCE_EXHAUSTED'","title: string","message: string","leaveRequestId: string","status: 'PENDING' | 'SENT' | 'READ' | 'ARCHIVED'","createdAt: Date","readAt: Date | null"],"purpose":"Domain event notification triggered by leave lifecycle transitions. Sent to employees (status updates on their requests) and managers (new requests to approve). Also triggered when balance crosses thresholds."},{"name":"LeaveType","attributes":["value: 'annual' | 'sick' | 'emergency' | 'unpaid' | 'maternity' | 'paternity'"],"purpose":"Enumeration of leave categories. Each LeavePolicy is associated with exactly one LeaveType. The type determines the policy's behaviour (e.g. sick leave may not require advance notice, annual leave may accrue)."},{"name":"Employee","attributes":["id: string","employeeNumber: string","firstName: string","lastName: string","email: string","managerId: string | null","department: string","hireDate: Date","terminationDate: Date | null","employmentStatus: 'ACTIVE' | 'INACTIVE' | 'TERMINATED'","createdAt: Date","updatedAt: Date"],"purpose":"Represents an employee in the organization. Used for ownership of leave requests, manager approval chains, and employment status checks."},{"name":"AuditLog","attributes":["id: string","entityType: string","entityId: string","action: string","oldValues: Record<string, any> | null","newValues: Record<string, any> | null","performedBy: string | null","performedAt: Date","ipAddress: string | null","userAgent: string | null","createdAt: Date"],"purpose":"Immutable record of all state-changing operations on leave requests, balances, and policies. Supports compliance, debugging, and audit trails."}],"modules":[{"name":"shared-types","path":"src/shared/types/","owns":["LeaveStatus enum (DRAFT | SUBMITTED | APPROVED | REJECTED | CANCELLED)","LeaveType enum (annual | sick | emergency | unpaid | maternity | paternity)","AuditAction enum (CREATE | UPDATE | DELETE | APPROVE | REJECT)","NotificationStatus enum (PENDING | SENT | READ | ARCHIVED)","EmploymentStatus enum (ACTIVE | INACTIVE | TERMINATED)","CreateLeaveRequestDto","UpdateLeaveRequestDto","LeaveRequestQueryParams","ValidationResult"]},{"name":"leave-request","path":"src/modules/leave-request/","owns":["LeaveRequest entity","LeaveRequestRepository interface","LeaveRequestService interface"]},{"name":"leave-balance","path":"src/modules/leave-balance/","owns":["LeaveBalance entity","LeaveBalanceRepository interface","BalanceService interface"]},{"name":"leave-policy","path":"src/modules/leave-policy/","owns":["LeavePolicy entity","LeavePolicyRepository interface","LeavePolicyService interface"]},{"name":"employee","path":"src/modules/employee/","owns":["Employee entity","EmployeeRepository interface","EmployeeService interface"]},{"name":"notification","path":"src/modules/notification/","owns":["LeaveNotification entity","NotificationRepository interface","NotificationService interface"]},{"name":"audit","path":"src/modules/audit/","owns":["AuditLog entity","AuditLogRepository interface","AuditService interface"]}],"dependency_map":[{"from_":"leave-request","to":"shared-types"},{"from_":"leave-request","to":"leave-balance"},{"from_":"leave-request","to":"leave-policy"},{"from_":"leave-request","to":"employee"},{"from_":"leave-request","to":"notification"},{"from_":"leave-request","to":"audit"},{"from_":"leave-balance","to":"shared-types"},{"from_":"leave-balance","to":"leave-policy"},{"from_":"leave-policy","to":"shared-types"},{"from_":"employee","to":"shared-types"},{"from_":"notification","to":"shared-types"},{"from_":"audit","to":"shared-types"}],"recommended_phases":[{"title":"Phase 1: Shared types foundation","rationale":"All cross-module enums and DTOs must exist before any module references them. No dependencies.","estimated_files":3},{"title":"Phase 2: Employee module","rationale":"Employee is a foundational domain entity referenced by leave-request (submitter, approver) and leave-balance. Depends only on shared-types.","estimated_files":5},{"title":"Phase 3: Leave policy module","rationale":"LeavePolicy defines entitlement rules and approval requirements consumed by both balance and leave-request. Depends only on shared-types.","estimated_files":5},{"title":"Phase 4: Leave balance module","rationale":"LeaveBalance tracks entitlements and usage; needed by leave-request for balance checks and mutations. Depends on shared-types and leave-policy.","estimated_files":5},{"title":"Phase 5: Notification module","rationale":"Notifications are triggered by leave-request lifecycle events. Depends on shared-types.","estimated_files":5},{"title":"Phase 6: Audit module","rationale":"Audit logging is required for all state-changing operations in leave-request. Depends on shared-types.","estimated_files":5},{"title":"Phase 7: Leave request module","rationale":"Core module orchestrating the leave lifecycle. Depends on all other modules. Implements the main business rules and API endpoints.","estimated_files":8}],"architecture_md_update":"## Leave Management Module — Reconciled Architecture\n\n### Domain Entities\n\n- **LeaveRequest** — Core entity representing an employee's leave application. Lifecycle: DRAFT → SUBMITTED → APPROVED | REJECTED | CANCELLED. Key attributes: employeeId, leavePolicyId, startDate, endDate, status, approvedBy, cancelledAt.\n- **LeavePolicy** — Defines rules for a leave type (annual, sick, emergency, etc.). Lifecycle: ACTIVE, INACTIVE. Attributes: leaveType, entitlementDays, minimumNoticeDays, requiresManagerApproval.\n- **LeaveBalance** — Tracks an employee's entitlement, used, and remaining days per policy per fiscal year. Lifecycle: ACTIVE, EXHAUSTED, CLOSED.\n- **LeaveNotification** — Domain event notifications for leave lifecycle transitions. Lifecycle: PENDING → SENT → READ → ARCHIVED.\n- **Employee** — Represents an employee; used for ownership, manager hierarchy, and employment status checks. Lifecycle: ACTIVE, INACTIVE, TERMINATED.\n- **AuditLog** — Immutable record of all state-changing operations.\n- **LeaveType** — Enum: annual, sick, emergency, unpaid, maternity, paternity.\n\n### Conceptual Tables\n\n- **employees** — id, employee_number, first_name, last_name, email, manager_id, department, hire_date, termination_date, employment_status, created_at, updated_at, deleted_at. PK: id. FK: manager_id → employees.id. Indexes: employee_number (unique), email (unique), manager_id, employment_status.\n- **leave_policies** — id, policy_name, leave_type, entitlement_days, accrual_rate, max_accumulation, minimum_notice_days, requires_manager_approval, is_active, created_at, updated_at. PK: id. Indexes: leave_type, is_active.\n- **leave_requests** — id, employee_id, leave_policy_id, start_date, end_date, reason, status, approved_by, approved_at, cancelled_at, created_at, updated_at. PK: id. FKs: employee_id → employees.id, leave_policy_id → leave_policies.id, approved_by → employees.id. Indexes: employee_id, status, approved_by, start_date/end_date, leave_policy_id.\n- **leave_balances** — id, employee_id, leave_policy_id, total_entitlement, used_days, remaining_days, fiscal_year, status, created_at, updated_at. PK: id. FKs: employee_id → employees.id, leave_policy_id → leave_policies.id. Unique composite index on (employee_id, leave_policy_id, fiscal_year).\n- **leave_notifications** — id, recipient_id, type, title, message, leave_request_id, status, created_at, read_at. PK: id. FKs: recipient_id → employees.id, leave_request_id → leave_requests.id. Indexes: recipient_id, leave_request_id, status, created_at.\n- **audit_logs** — id, entity_type, entity_id, action, old_values, new_values, performed_by, performed_at, ip_address, user_agent, created_at. PK: id. FK: performed_by → employees.id. Indexes: (entity_type, entity_id), performed_by, action, performed_at.\n\n### Module Boundaries\n\n- **shared-types** (`src/shared/types/`) — Enums and DTOs used across all modules.\n- **employee** (`src/modules/employee/`) — Employee entity, repository, and service.\n- **leave-policy** (`src/modules/leave-policy/`) — LeavePolicy entity, repository, and service.\n- **leave-balance** (`src/modules/leave-balance/`) — LeaveBalance entity, repository, and BalanceService.\n- **notification** (`src/modules/notification/`) — LeaveNotification entity, repository, and NotificationService.\n- **audit** (`src/modules/audit/`) — AuditLog entity, repository, and AuditService.\n- **leave-request** (`src/modules/leave-request/`) — LeaveRequest entity, repository, and LeaveRequestService. Orchestrates the leave lifecycle, depending on all other modules.\n\n### Dependency Map\n\n- leave-request → shared-types, leave-balance, leave-policy, employee, notification, audit\n- leave-balance → shared-types, leave-policy\n- leave-policy → shared-types\n- employee → shared-types\n- notification → shared-types\n- audit → shared-types\n\n### Recommended Implementation Phases\n\n1. **Shared types foundation** — Enums and DTOs.\n2. **Employee module** — Foundational entity.\n3. **Leave policy module** — Policy rules.\n4. **Leave balance module** — Balance tracking.\n5. **Notification module** — Event notifications.\n6. **Audit module** — Audit logging.\n7. **Leave request module** — Core orchestration and API endpoints.\n\n### Cross-Cutting Contracts\n\n- **Auth**: JWT bearer token → `request.user: { id: string; role: UserRole }`. Roles: `employee`, `manager`, `hr_admin`. RBAC enforced via `requireRole(...)` guard. Employee-only actions: submit, withdraw, cancelApproved. Manager/HR admin actions: approve, reject.\n- **Transaction**: Repository methods that participate in multi-step writes accept an optional `PoolClient`. The service owns the unit of work: acquires client, BEGIN, passes client to repositories, COMMIT/ROLLBACK. Applies to submission, approval/rejection, and cancellation flows.\n- **Error Response**: Standard shape `{ error: string; code: string }`. HTTP 400 for validation, 401 for unauthenticated, 403 for unauthorized, 404 for not found, 409 for business rule conflicts (overlap, insufficient balance), 500 for internal errors.\n\n### Open Questions\n\n1. Fiscal year boundary definition (calendar vs configurable).\n2. Leave day counting granularity (calendar days vs business days vs business days minus holidays).\n3. Minimum leave granularity (full days, half days, or hours).\n\nThese decisions affect balance calculations, overlap detection, and policy configuration. They must be resolved before implementation of the balance and leave-request modules.\n","sql_schemas":["table employees | fields: id, employee_number, first_name, last_name, email, manager_id, department, hire_date, termination_date, employment_status, created_at, updated_at, deleted_at | pk: id | fks: manager_id -> employees.id | indexes: employee_number: unique lookup by employee number; email: unique lookup by email; manager_id: team-level queries (manager views their direct reports); employment_status: filtering active/inactive/terminated employees","table leave_policies | fields: id, policy_name, leave_type, entitlement_days, accrual_rate, max_accumulation, minimum_notice_days, requires_manager_approval, is_active, created_at, updated_at | pk: id | indexes: leave_type: filtering policies by leave type (annual, sick, emergency); is_active: filtering active policies for assignment and balance calculation","table leave_requests | fields: id, employee_id, leave_policy_id, start_date, end_date, reason, status, approved_by, approved_at, cancelled_at, created_at, updated_at | pk: id | fks: employee_id -> employees.id; leave_policy_id -> leave_policies.id; approved_by -> employees.id | indexes: employee_id: frequent lookups of leave requests by employee; status: filtering by status (SUBMITTED for manager approval queues); approved_by: manager queries for requests they approved; start_date, end_date: date-range queries for calendar views and overlap detection; leave_policy_id: filtering by leave type","table leave_balances | fields: id, employee_id, leave_policy_id, total_entitlement, used_days, remaining_days, fiscal_year, status, created_at, updated_at | pk: id | fks: employee_id -> employees.id; leave_policy_id -> leave_policies.id | indexes: employee_id: lookups of all balances for an employee; employee_id, leave_policy_id, fiscal_year: unique composite lookup for a specific balance row (one balance per employee per policy per fiscal year); leave_policy_id: aggregate queries across employees for a policy; fiscal_year: filtering balances by fiscal year","table leave_notifications | fields: id, recipient_id, type, title, message, leave_request_id, status, created_at, read_at | pk: id | fks: recipient_id -> employees.id; leave_request_id -> leave_requests.id | indexes: recipient_id: inbox queries for a specific employee; leave_request_id: notification history for a specific leave request; status: filtering unread notifications; created_at: chronological ordering","table audit_logs | fields: id, entity_type, entity_id, action, old_values, new_values, performed_by, performed_at, ip_address, user_agent, created_at | pk: id | fks: performed_by -> employees.id | indexes: entity_type, entity_id: lookups of audit trail for a specific entity; performed_by: audit queries by actor; action: filtering by action type (APPROVE, REJECT, CREATE); performed_at: time-range audit queries"],"open_questions":[],"business_rules":["Consolidated decision for all questions: (1) Fiscal year = calendar year (Jan 1 to Dec 31), hardcoded — no configurable fiscalYearStart field. (2) Leave duration counts calendar days inclusive — weekends and public holidays ARE counted as leave days; do NOT introduce a holiday calendar entity. (3) Minimum granularity = full-day increments only — leave balances are integers, no half-days, no hours, no time-of-day on startDate/endDate. (4) Day counting from start_date to end_date is calendar days inclusive of both ends: daysRequested = (end_date - start_date) + 1. This single formula is BINDING at every call site (used_days deduction, overlap detection, remaining_days) — no weekend or holiday exclusion anywhere. [BINDING RULE — operator decision resolving: How is the fiscal year boundary defined — calendar year (Jan 1 – Dec 31) or a configurable start month/day? This determines when LeaveBalance transitions from ACTIVE/EXHAUSTED to CLOSED and when new balance records are created.; Should leave duration count weekends and public holidays as leave days, or only business days? The current rule uses calendar days inclusive, but this may not match all organisational policies.; What is the minimum granularity of a leave request — full days, half days, or hours? This affects the daysRequested calculation and balance precision.; How are leave days counted from start_date to end_date — calendar days (inclusive of both ends, e.g. Mon–Fri = 5 days) or business/working days only? This is BINDING across all balance-deduction and overlap-detection call sites.; apply everywhere these apply, not in one place only]"],"auth_contract":"Authenticated requests carry a JWT bearer token verified by an auth middleware that populates request.user with { id: string; role: UserRole }. UserRole enum: 'employee' | 'manager' | 'hr_admin'. RBAC enforced by a requireRole(...) route guard. Employee-only actions: submit, withdraw, cancelApproved. Manager/HR admin actions: approve, reject. The employee's managerId is obtained from the Employee entity; HR admins have global approval rights.","transaction_contract":"Repository methods that participate in multi-step writes accept an optional PoolClient parameter (defaulting to the shared pool when omitted). The calling service owns the unit of work: it acquires a client from the pool, executes BEGIN, passes the client to each repository call, and then executes COMMIT or ROLLBACK. This contract applies to: (1) leave request submission — create leave_request + write audit_log; (2) leave request approval/rejection — update leave_request status + increment/decrement leave_balance used_days/remaining_days + write audit_log; (3) leave request cancellation — update leave_request status + decrement leave_balance used_days + write audit_log.","error_response_contract":"Standard error response shape: { error: string; code: string }. HTTP status codes: 400 for validation errors (e.g., invalid dates, missing fields), 401 for unauthenticated, 403 for unauthorized role, 404 for resource not found, 409 for business rule conflicts (e.g., overlapping leave, insufficient balance), 500 for internal server errors."}