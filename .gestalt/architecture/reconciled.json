{"domain_entities":[{"name":"LeaveRequest","attributes":["id: string","employeeId: string","leaveTypeId: string","startDate: Date","endDate: Date","reason: string | undefined","status: LeaveRequestStatus","approvedBy: string | null","approvedAt: Date | null","rejectedBy: string | null","rejectedAt: Date | null","rejectionReason: string | null","cancelledBy: string | null","cancelledAt: Date | null","createdAt: Date","updatedAt: Date"],"purpose":"Represents an employee's request for leave. Central aggregate of the leave management bounded context. Owns the full lifecycle from draft through final resolution."},{"name":"LeaveType","attributes":["value: 'annual' | 'sick' | 'emergency' | 'unpaid' | 'maternity' | 'paternity'"],"purpose":"Enumeration of leave categories. Each type maps to a LeavePolicy that governs entitlement, accrual, and approval rules. The feature scope covers annual, sick, and emergency; unpaid, maternity, and paternity are reserved for future expansion."},{"name":"LeaveRequestStatus","attributes":["value: 'DRAFT' | 'SUBMITTED' | 'APPROVED' | 'REJECTED' | 'CANCELLED'"],"purpose":"Discriminated union representing the current lifecycle state of a LeaveRequest. Drives all state-transition guards."},{"name":"LeavePolicy","attributes":["id: string","policyName: string","leaveTypeId: string","entitlementDays: number","accrualRate: number | null","maxAccumulation: number | null","minimumNoticeDays: number | null","requiresManagerApproval: boolean","isActive: boolean","createdAt: Date","updatedAt: Date"],"purpose":"Defines the rules and entitlements for a specific leave type. Each LeaveType has exactly one active LeavePolicy at any time. Governs how many days an employee is entitled to, whether manager approval is required, and any notice-period constraints."},{"name":"LeaveBalance","attributes":["id: string","employeeId: string","leaveTypeId: string","policyId: string","totalEntitlement: number","usedDays: number","remainingDays: number","fiscalYear: number","status: 'ACTIVE' | 'EXHAUSTED' | 'FROZEN'","createdAt: Date","updatedAt: Date"],"purpose":"Tracks an employee's leave entitlement, consumption, and remaining balance for a specific LeavePolicy within a fiscal year. Deducted on approval, restored on cancellation of an approved request."},{"name":"Employee","attributes":["id: string","employeeNumber: string","firstName: string","lastName: string","email: string","managerId: string | null","department: string | null","hireDate: Date","terminationDate: Date | null","employmentStatus: 'ACTIVE' | 'INACTIVE' | 'TERMINATED'","createdAt: Date","updatedAt: Date","deletedAt: Date | null"],"purpose":"Represents an employee in the organization. Owns the managerId used for approval routing. Employment status gates eligibility for leave requests."}],"modules":[{"name":"leave-type","path":"src/modules/leave-type/","owns":["LeaveType enum","LeaveType model","ILeaveTypeRepository","LeaveTypeRepository"]},{"name":"leave-policy","path":"src/modules/leave-policy/","owns":["LeavePolicy model","ILeavePolicyRepository","LeavePolicyRepository","ILeavePolicyService","LeavePolicyService","LeavePolicyController","leavePolicyRoutes"]},{"name":"employee","path":"src/modules/employee/","owns":["Employee model","IEmployeeRepository","EmployeeRepository","IEmployeeService","EmployeeService"]},{"name":"audit","path":"src/modules/audit/","owns":["AuditRecord model","IAuditRepository","AuditRepository","IAuditService","AuditService"]},{"name":"leave-balance","path":"src/modules/leave-balance/","owns":["LeaveBalance model","ILeaveBalanceRepository","LeaveBalanceRepository","ILeaveBalanceService","LeaveBalanceService","LeaveBalanceController","leaveBalanceRoutes"]},{"name":"leave-validation","path":"src/modules/leave-validation/","owns":["ILeaveValidationService","LeaveValidationService","ValidationResult model"]},{"name":"leave-request","path":"src/modules/leave-request/","owns":["LeaveRequest model","LeaveStatus enum","ILeaveRequestRepository","LeaveRequestRepository","ILeaveRequestService","LeaveRequestService","LeaveRequestController","leaveRequestRoutes"]},{"name":"notification","path":"src/modules/notification/","owns":["Notification model","INotificationRepository","NotificationRepository","INotificationService","NotificationService"]}],"dependency_map":[{"from_":"leave-request","to":"leave-validation"},{"from_":"leave-request","to":"leave-balance"},{"from_":"leave-request","to":"leave-policy"},{"from_":"leave-request","to":"employee"},{"from_":"leave-request","to":"audit"},{"from_":"leave-request","to":"notification"},{"from_":"leave-request","to":"leave-type"},{"from_":"leave-validation","to":"leave-balance"},{"from_":"leave-validation","to":"leave-policy"},{"from_":"leave-validation","to":"leave-type"},{"from_":"leave-balance","to":"leave-policy"},{"from_":"leave-balance","to":"employee"},{"from_":"leave-balance","to":"leave-type"},{"from_":"leave-balance","to":"audit"},{"from_":"leave-policy","to":"leave-type"},{"from_":"leave-policy","to":"audit"},{"from_":"employee","to":"audit"},{"from_":"notification","to":"employee"}],"recommended_phases":[{"title":"Phase 1: Foundation — leave-type, audit, employee","rationale":"Zero-dependency leaf modules. leave-type is a pure enum with no dependencies. audit is a standalone infrastructure concern (GP-002). employee provides identity and manager lookup needed by all other modules. These must exist before anything else can be built.","estimated_files":12},{"title":"Phase 2: Policy layer — leave-policy","rationale":"Depends only on leave-type and audit (both in Phase 1). Defines entitlement rules, accrual rates, and approval requirements that leave-balance and leave-validation consume.","estimated_files":7},{"title":"Phase 3: Balance engine — leave-balance","rationale":"Depends on leave-policy, employee, leave-type, and audit (all in Phases 1-2). Tracks entitlements, used days, remaining days per employee per leave type per fiscal year. Provides deductBalance and restoreBalance used by leave-request workflow.","estimated_files":7},{"title":"Phase 4: Validation & notification — leave-validation, notification","rationale":"leave-validation depends on leave-balance, leave-policy, leave-type (Phases 1-3). notification depends on employee (Phase 1). Both are consumed by leave-request in Phase 5. Can be built in parallel.","estimated_files":8},{"title":"Phase 5: Orchestration — leave-request","rationale":"Depends on all prior phases. Orchestrates the full leave workflow: submit (validate → deduct → audit → notify), approve/reject (update status → audit → notify), cancel (restore balance → audit → notify). Exposes the public REST API via controller and routes.","estimated_files":7}],"architecture_md_update":"## Leave Management Module\n\n### Overview\n\nThe leave management module enables employees to apply for annual, sick, and emergency leave, managers to approve or reject requests, and the system to track leave balances. It is implemented as a modular monolith using TypeScript, Fastify, PostgreSQL, and React Native (frontend).\n\n### Domain Entities\n\n- **LeaveRequest** — central aggregate representing an employee's leave application. Lifecycle: DRAFT → SUBMITTED → APPROVED/REJECTED/CANCELLED. Tracks approver, rejector, canceller, and timestamps.\n- **LeaveType** — enum of leave categories (`annual`, `sick`, `emergency`, `unpaid`, `maternity`, `paternity`). Feature scope covers annual, sick, emergency; others reserved for future.\n- **LeaveRequestStatus** — enum of lifecycle states (`DRAFT`, `SUBMITTED`, `APPROVED`, `REJECTED`, `CANCELLED`).\n- **LeavePolicy** — defines entitlement rules (days, accrual, notice period, approval requirement) per leave type. Lifecycle: ACTIVE, INACTIVE.\n- **LeaveBalance** — tracks an employee's entitlement, used, and remaining days per leave type per fiscal year. Lifecycle: ACTIVE, EXHAUSTED, FROZEN.\n- **Employee** — organizational actor with manager relationship and employment status (ACTIVE, INACTIVE, TERMINATED).\n\n### Module Boundaries\n\n| Module | Path | Responsibilities |\n|--------|------|------------------|\n| `leave-type` | `src/modules/leave-type/` | LeaveType enum, model, repository |\n| `leave-policy` | `src/modules/leave-policy/` | LeavePolicy CRUD, service, controller, routes |\n| `employee` | `src/modules/employee/` | Employee model, repository, service (identity, manager lookup) |\n| `audit` | `src/modules/audit/` | AuditRecord model, repository, service (cross-cutting GP-002) |\n| `leave-balance` | `src/modules/leave-balance/` | LeaveBalance tracking, service, controller, routes |\n| `leave-validation` | `src/modules/leave-validation/` | Validation rules (balance, policy, overlap) — reusable service |\n| `leave-request` | `src/modules/leave-request/` | LeaveRequest aggregate, workflow orchestration, REST API |\n| `notification` | `src/modules/notification/` | Notification model, repository, service (fire-and-forget) |\n\n### Dependency Map\n\n- `leave-request` depends on `leave-validation`, `leave-balance`, `leave-policy`, `employee`, `audit`, `notification`, `leave-type`\n- `leave-validation` depends on `leave-balance`, `leave-policy`, `leave-type`\n- `leave-balance` depends on `leave-policy`, `employee`, `leave-type`, `audit`\n- `leave-policy` depends on `leave-type`, `audit`\n- `employee` depends on `audit`\n- `notification` depends on `employee`\n\n### Conceptual Data Model\n\nAll tables are conceptual; the code agent generates migrations.\n\n- **leave_types** (id, name, description, is_active, created_at, updated_at) — PK id; unique index on name; index on is_active.\n- **leave_policies** (id, policy_name, leave_type_id, entitlement_days, accrual_rate, max_accumulation, minimum_notice_days, requires_manager_approval, is_active, created_at, updated_at) — PK id; FK leave_type_id → leave_types.id; indexes on leave_type_id, is_active.\n- **leave_requests** (id, employee_id, leave_type_id, start_date, end_date, reason, status, approved_by, approved_at, rejected_by, rejected_at, rejection_reason, cancelled_by, cancelled_at, created_at, updated_at) — PK id; FKs employee_id → employees.id, leave_type_id → leave_types.id, approved_by → employees.id, rejected_by → employees.id, cancelled_by → employees.id; indexes on employee_id, status, leave_type_id, (start_date, end_date), approved_by.\n- **leave_balances** (id, employee_id, leave_type_id, policy_id, fiscal_year, total_entitlement, used_days, remaining_days, status, created_at, updated_at) — PK id; FKs employee_id → employees.id, leave_type_id → leave_types.id, policy_id → leave_policies.id; indexes on employee_id, (employee_id, fiscal_year), leave_type_id, policy_id.\n- **employees** (id, employee_number, first_name, last_name, email, manager_id, department, hire_date, termination_date, employment_status, created_at, updated_at, deleted_at) — PK id; FK manager_id → employees.id; unique indexes on employee_number, email; indexes on manager_id, employment_status, department.\n- **audit_logs** (id, entity_type, entity_id, action, old_values, new_values, performed_by, performed_at, ip_address, user_agent, created_at) — PK id; FK performed_by → employees.id; indexes on (entity_type, entity_id), performed_by, action, performed_at.\n- **notifications** (id, recipient_id, type, title, message, related_entity_type, related_entity_id, status, created_at, read_at) — PK id; FK recipient_id → employees.id; indexes on recipient_id, status, (related_entity_type, related_entity_id).\n\n### REST API Surface\n\n**Leave Requests** (exposed by `leave-request` controller)\n- `POST /api/leave-requests` — submit (employee)\n- `GET /api/leave-requests/:id` — get by ID (employee/manager)\n- `GET /api/leave-requests/employee/:employeeId` — list by employee (employee/manager)\n- `GET /api/leave-requests/manager/:managerId` — list for manager (manager)\n- `PATCH /api/leave-requests/:id/approve` — approve (manager)\n- `PATCH /api/leave-requests/:id/reject` — reject (manager)\n- `PATCH /api/leave-requests/:id/cancel` — cancel (employee)\n\n**Leave Balances** (exposed by `leave-balance` controller)\n- `GET /api/leave-balances/:employeeId` — list balances (employee/manager)\n- `GET /api/leave-balances/:employeeId/:leaveTypeId/:fiscalYear` — single balance (employee/manager)\n\n**Leave Policies** (exposed by `leave-policy` controller)\n- `GET /api/leave-policies` — list active policies (authenticated)\n- `GET /api/leave-policies/:id` — get policy (authenticated)\n- `POST /api/leave-policies` — create (HR admin)\n- `PUT /api/leave-policies/:id` — update (HR admin)\n\n### Key Business Rules\n\n- Only ACTIVE employees can submit leave requests.\n- State transitions enforce strict guards (e.g., only manager can approve/reject).\n- Approval decrements leave balance; cancellation of an approved request restores it.\n- Balance status becomes EXHAUSTED when remaining days reach zero.\n- Every state-changing operation writes an audit log (GP-002).\n- Notifications are sent on submission, approval, rejection, and cancellation (fire-and-forget).\n\n### Implementation Phases\n\n1. **Foundation** — `leave-type`, `audit`, `employee` (zero-dependency leaf modules).\n2. **Policy Layer** — `leave-policy` (depends on Phase 1).\n3. **Balance Engine** — `leave-balance` (depends on Phases 1–2).\n4. **Validation & Notification** — `leave-validation`, `notification` (depends on Phases 1–3; can be parallel).\n5. **Orchestration** — `leave-request` (depends on all prior phases; exposes public API).\n\n### Stack Compliance\n\n- Language: TypeScript (Node 20)\n- Framework: Fastify (REST API)\n- Frontend: React Native (mobile client)\n- Database: PostgreSQL (via `pg` Pool at `src/shared/db/connection.ts`)\n- Architecture: modular-monolith with clear layer separation (domain, application, infrastructure, presentation)\n- Cross-cutting: audit (GP-002), RBAC (GP-005), input validation (GP-003)","sql_schemas":["table leave_types | fields: id, name, description, is_active, created_at, updated_at | pk: id | indexes: name: unique lookup by leave type name (annual, sick, emergency, etc.); is_active: filter active leave types in dropdowns and policy assignment","table leave_policies | fields: id, policy_name, leave_type_id, entitlement_days, accrual_rate, max_accumulation, minimum_notice_days, requires_manager_approval, is_active, created_at, updated_at | pk: id | fks: leave_type_id -> leave_types.id | indexes: leave_type_id: join with leave_types for policy lookups by type; is_active: filter active policies during leave application validation","table leave_requests | fields: id, employee_id, leave_type_id, start_date, end_date, reason, status, approved_by, approved_at, rejected_by, rejected_at, rejection_reason, cancelled_by, cancelled_at, created_at, updated_at | pk: id | fks: employee_id -> employees.id; leave_type_id -> leave_types.id; approved_by -> employees.id; rejected_by -> employees.id; cancelled_by -> employees.id | indexes: employee_id: list own leave requests; join to employee; status: filter by status for approval queues and dashboards; leave_type_id: filter requests by leave type; start_date, end_date: date-range overlap detection and calendar views; approved_by: manager approval queue lookups","table leave_balances | fields: id, employee_id, leave_type_id, policy_id, fiscal_year, total_entitlement, used_days, remaining_days, status, created_at, updated_at | pk: id | fks: employee_id -> employees.id; leave_type_id -> leave_types.id; policy_id -> leave_policies.id | indexes: employee_id: look up balance for a specific employee; employee_id, fiscal_year: unique balance per employee per fiscal year; frequent composite lookup; leave_type_id: filter balances by leave type; policy_id: join to policy for entitlement rules","table employees | fields: id, employee_number, first_name, last_name, email, manager_id, department, hire_date, termination_date, employment_status, created_at, updated_at, deleted_at | pk: id | fks: manager_id -> employees.id | indexes: employee_number: unique lookup by employee number; email: unique lookup by email; manager_id: team hierarchy queries for approval chains; employment_status: filter active employees; department: group employees by department","table audit_logs | fields: id, entity_type, entity_id, action, old_values, new_values, performed_by, performed_at, ip_address, user_agent, created_at | pk: id | fks: performed_by -> employees.id | indexes: entity_type, entity_id: look up audit trail for a specific entity; performed_by: filter audit records by actor; action: filter by action type (CREATE, UPDATE, APPROVE, REJECT); performed_at: time-range queries for audit reports","table notifications | fields: id, recipient_id, type, title, message, related_entity_type, related_entity_id, status, created_at, read_at | pk: id | fks: recipient_id -> employees.id | indexes: recipient_id: list notifications for a user; status: filter unread/pending notifications; related_entity_type, related_entity_id: find notifications linked to a specific leave request"]}