{"domain_entities":[{"name":"DemoAccount","attributes":["manager: Employee","employee: Employee","leaveTypes: LeaveType[]","leavePolicies: LeavePolicy[]","leaveBalances: LeaveBalance[]","leaveRequests: LeaveRequest[]","demoCredentials: { email: string; password: string }"],"purpose":"The idempotent development seed dataset: one manager, one employee, the leave types and policies the balance logic needs, a current-period balance for each, and two or three leave requests in different lifecycle states. Exists only in the development environment; never part of migrate."},{"name":"Employee","attributes":["id: string","employeeNumber: string","firstName: string","lastName: string","email: string","role: EmployeeRole","managerId: string | null","department: string","hireDate: Date","terminationDate: Date | null","employmentStatus: EmploymentStatus","passwordHash: string | null"],"purpose":"A person who can authenticate and request leave. The seed mints passwordHash via the same bcrypt call the auth service verifies with, so the demo account can log in."},{"name":"LeaveRequest","attributes":["id: string","employeeId: string","leaveTypeCode: LeaveTypeCode","startDate: Date","endDate: Date","requestedDays: number","reason: string | null","status: LeaveStatus","approverId: string | null","approvalComment: string | null","submittedAt: Date | null","decidedAt: Date | null","cancelledBy: string | null","cancelledAt: Date | null"],"purpose":"A single leave application. The seed creates requests in different states (e.g. SUBMITTED and APPROVED) so list and detail pages have data."},{"name":"LeaveBalance","attributes":["id: string","employeeId: string","leaveTypeCode: LeaveTypeCode","periodStart: Date","periodEnd: Date","entitledDays: number","usedDays: number","pendingDays: number"],"purpose":"One balance row per (employee, leave type, accrual period). The seed creates a balance for the current accrual period for each seeded employee, with counters consistent with the seeded requests."},{"name":"LeavePolicy","attributes":["id: string","leaveTypeCode: LeaveTypeCode","policyName: string","annualEntitlementDays: number","accrualPeriodMonths: number","carryForwardDays: number","minNoticeDays: number","maxRequestDays: number","requiresManagerApproval: boolean","effectiveFrom: Date","effectiveTo: Date | null","status: LeavePolicyStatus"],"purpose":"The entitlement and accrual rules the balance logic needs. The seed creates an ACTIVE policy per seeded leave type."},{"name":"LeaveType","attributes":["code: LeaveTypeCode","name: string","requiresApproval: boolean","maxConsecutiveDays: number","isPaid: boolean"],"purpose":"Static catalog of leave types referenced by policies, balances, and requests. The seed creates the leave types the balance logic needs."}],"modules":[{"name":"seed","path":"seeds/","owns":["Demo dataset: one manager (role MANAGER) and one employee (role EMPLOYEE, managerId -> manager) with bcrypt password hashes minted via bcrypt.hashSync(plaintext, 10) — the same call AuthService verifies with","Leave types (annual, sick) and their ACTIVE leave policies (annualEntitlementDays, accrualPeriodMonths, carryForwardDays, minNoticeDays, maxRequestDays)","One leave_balances row per employee per leave type for the CURRENT accrual period, derived via shared/date periodContaining anchored on hireDate (never a hand-written date)","Two or three leave_requests in different states (SUBMITTED, APPROVED, REJECTED) with requestedDays computed via shared/types requestedDays","Balance-counter consistency: SUBMITTED request days appear in pending_days, APPROVED days in used_days (derived from the seeded requests, never hardcoded independently)","Idempotency: detect an already-seeded row by composite key (employee, leave_type, start_date for requests; email/employee_number for employees; code for leave types; stable seed id for policies; employee+leave_type+period_start+period_end for balances) and skip rather than duplicate or delete","knex seed entry (001_demo_data.js) that registers ts-node and delegates to the seed service"]},{"name":"dev-runner","path":"scripts/dev.js","owns":["Concurrent process orchestration: spawn the API (npm run start) and the web dev server (npm run dev in web/) together","Forward both children's stdout/stderr to the parent terminal with a per-process prefix","Forward exit codes: when either child exits, terminate the sibling and exit with the child's code","Signal handling so Ctrl-C tears down both children"]},{"name":"shared-types","path":"src/shared/types","owns":["LeaveTypeCode, LeaveStatus, EmployeeRole, LeavePolicyStatus enums","requestedDays helper (inclusive whole-day UTC day count)"]},{"name":"shared-date","path":"src/shared/date","owns":["startOfUtcDay","periodContaining"]}],"dependency_map":[{"from_":"seed","to":"shared-types"},{"from_":"seed","to":"shared-date"}],"recommended_phases":[{"title":"Phase 1 — Seed module + knex wiring + npm run seed","rationale":"Foundational: the demo dataset is the dependency every later step documents. Deliver the seed service (idempotent, bcrypt hashes, current-period balances, balance-counter consistency), the knex seed entry, the knexfile.js seeds config for the development environment, and the root `npm run seed` script.","estimated_files":4},{"title":"Phase 2 — Dev runner + npm run dev","rationale":"Independent of the seed; delivers scripts/dev.js and the root `npm run dev` script so the documented command exists and works.","estimated_files":2},{"title":"Phase 3 — web test script fix","rationale":"One-line change to web/package.json (`vitest` -> `vitest run`) so the test command exits instead of entering watch mode.","estimated_files":1},{"title":"Phase 4 — README documentation","rationale":"Last, because it documents the exact commands (migrate, seed, start API, start web, npm run dev) and demo credentials that the prior phases make real.","estimated_files":1}],"architecture_md_update":"## Runnable End-to-End: Seeded Demo Account and Dev Run\n\n### Context\nFeature adds development-only seed data, a dev runner, npm scripts, and documentation. No API contract or route changes.\n\n### Domain Entities\n- **DemoAccount** (aggregate; no table): manager, employee, leaveTypes, leavePolicies, leaveBalances, leaveRequests, demoCredentials. Lifecycle: none.\n- **Employee**: id, employeeNumber, firstName, lastName, email, role, managerId, department, hireDate, terminationDate, employmentStatus, passwordHash. Lifecycle: ACTIVE | TERMINATED | ON_LEAVE.\n- **LeaveRequest**: id, employeeId, leaveTypeCode, startDate, endDate, requestedDays, reason, status, approverId, approvalComment, submittedAt, decidedAt, cancelledBy, cancelledAt. Lifecycle: DRAFT | SUBMITTED | APPROVED | REJECTED | CANCELLED.\n- **LeaveBalance**: id, employeeId, leaveTypeCode, periodStart, periodEnd, entitledDays, usedDays, pendingDays. Lifecycle: OPEN | CLOSED.\n- **LeavePolicy**: id, leaveTypeCode, policyName, annualEntitlementDays, accrualPeriodMonths, carryForwardDays, minNoticeDays, maxRequestDays, requiresManagerApproval, effectiveFrom, effectiveTo, status. Lifecycle: DRAFT | ACTIVE | SUPERSEDED.\n- **LeaveType**: code, name, requiresApproval, maxConsecutiveDays, isPaid. Static catalog; no lifecycle.\n\n### Conceptual Tables\n- **employees**: fields id, employee_number, first_name, last_name, email, password_hash, role, manager_id, department, hire_date, termination_date, employment_status. PK id. FK manager_id -> employees.id. Indexes: email unique (login lookup, seed idempotency), employee_number unique (natural key, seed idempotency), manager_id (direct-report lookups).\n- **leave_types**: fields code, name, requires_approval, max_consecutive_days, is_paid. PK code. Index: code (natural key, seed idempotency, policy FK lookups).\n- **leave_policies**: fields id, leave_type_code, policy_name, annual_entitlement_days, accrual_period_months, carry_forward_days, min_notice_days, max_request_days, requires_manager_approval, effective_from, effective_to, status. PK id. FK leave_type_code -> leave_types.code. Indexes: leave_type_code (policy lookup by type), id (seed idempotency by stable seed id).\n- **leave_balances**: fields id, employee_id, leave_type_code, period_start, period_end, entitled_days, used_days, pending_days. PK id. FKs employee_id -> employees.id, leave_type_code -> leave_types.code. Unique index (employee_id, leave_type_code, period_start, period_end) for findByKey and seed idempotency. Index employee_id for balance reads.\n- **leave_requests**: fields id, employee_id, leave_type_code, start_date, end_date, requested_days, reason, status, approver_id, approval_comment, submitted_at, decided_at, cancelled_by, cancelled_at. PK id. FKs employee_id -> employees.id, leave_type_code -> leave_types.code, approver_id -> employees.id, cancelled_by -> employees.id. Indexes: employee_id (owner scoping), status (filtering), (leave_type_code, start_date) (date-range/type queries), (employee_id, leave_type_code, start_date) (seed idempotency composite key).\n\n### Module Boundaries and Dependencies\n- **seed** (`seeds/`): owns demo dataset, idempotency, balance-counter consistency, knex entry. Depends on shared-types and shared-date.\n- **dev-runner** (`scripts/dev.js`): spawns API and web dev server, forwards output/exit codes, signal handling.\n- **shared-types** (`src/shared/types`): existing enums and requestedDays helper.\n- **shared-date** (`src/shared/date`): existing startOfUtcDay and periodContaining helpers.\n\n### Repository Interfaces\nNo new repository interfaces are introduced. Existing concrete implementations remain: PgEmployeeRepository, PgLeaveTypeRepository, PgLeavePolicyRepository, PgLeaveBalanceRepository, PgLeaveRequestRepository (PostgreSQL via shared pg Pool). The seed writes directly through the knex instance (standard knex seed mechanism), not through application repositories.\n\n### Contracts\n- **authContract**: none — no API endpoints or role-gated access.\n- **errorResponseContract**: none — no API endpoints.\n- **transactionContract**: none — seed is idempotent and re-runnable; no runtime multi-step atomic write.\n\n### Business Rules\n- Seed idempotency: skip already-seeded rows by composite key; never delete or truncate. Keys: employees by email/employee_number; leave_types by code; leave_policies by stable seed id; leave_balances by (employee_id, leave_type_code, period_start, period_end); leave_requests by (employee_id, leave_type_code, start_date).\n- Balance counters: SUBMITTED requestedDays -> pending_days; APPROVED requestedDays -> used_days; DRAFT/REJECTED/CANCELLED reserve nothing.\n- Seed is development-only; never runs as part of migrate; wired only into knexfile.js development seeds config.\n- requestedDays = endDate - startDate + 1 inclusive whole-day UTC, derived only via shared requestedDays helper.\n- All seeded dates use UTC discipline via startOfUtcDay/periodContaining anchored on hireDate.\n- Demo passwordHash minted with bcrypt.hashSync(plaintext, 10), the same call AuthService verifies.\n\n### Phases\n1. Seed module + knex wiring + npm run seed (4 files)\n2. Dev runner + npm run dev (2 files)\n3. web test script fix: vitest -> vitest run (1 file)\n4. README documentation (1 file)\n\n### Open Questions\n- Demo credentials: hardcoded vs env-driven with defaults.\n- Test framework: declared stack lists Jest, but web uses Vitest and feature requires vitest run; confirm whether Vitest is accepted for the Vite SPA or web should migrate to Jest.\n\n### Stack Compliance Note\nAPI/backend uses Jest per declared stack; web subproject retains Vitest (existing) with `vitest run` per feature requirement. This deviation is surfaced as an open question.","sql_schemas":["table employees | fields: id, employee_number, first_name, last_name, email, password_hash, role, manager_id, department, hire_date, termination_date, employment_status | pk: id | fks: manager_id -> employees.id | indexes: email: unique — login lookup by email (auth service) and seed idempotency check; employee_number: unique — natural key, seed idempotency check; manager_id: direct-report lookups (manager visibility)","table leave_types | fields: code, name, requires_approval, max_consecutive_days, is_paid | pk: code | indexes: code: natural key — seed idempotency check and policy FK lookups","table leave_policies | fields: id, leave_type_code, policy_name, annual_entitlement_days, accrual_period_months, carry_forward_days, min_notice_days, max_request_days, requires_manager_approval, effective_from, effective_to, status | pk: id | fks: leave_type_code -> leave_types.code | indexes: leave_type_code: policy lookup by leave type (balance entitlement/carry-forward resolution); id: seed idempotency check by stable seed id","table leave_balances | fields: id, employee_id, leave_type_code, period_start, period_end, entitled_days, used_days, pending_days | pk: id | fks: employee_id -> employees.id; leave_type_code -> leave_types.code | indexes: (employee_id, leave_type_code, period_start, period_end): unique — findByKey lookup and seed idempotency check; employee_id: balance reads by employee","table leave_requests | fields: id, employee_id, leave_type_code, start_date, end_date, requested_days, reason, status, approver_id, approval_comment, submitted_at, decided_at, cancelled_by, cancelled_at | pk: id | fks: employee_id -> employees.id; leave_type_code -> leave_types.code; approver_id -> employees.id; cancelled_by -> employees.id | indexes: employee_id: list/visibility scoping by owner; status: list filtering by state; (leave_type_code, start_date): date-range/type queries; (employee_id, leave_type_code, start_date): seed idempotency composite key — detect already-seeded request and skip"],"open_questions":[],"business_rules":["Decisions. Note that questions 1 and 4 are the same question; one answer covers both.\n\n1 and 4. HARDCODE a clearly-demo password literal in the seed and document that same literal in the README. Do not read it from an environment variable. This is dev-only seed data in a seeds/ file that never runs in production, so it is not a secret. More importantly, an env var with a default makes the documented command conditionally correct: it works on the machine that set the var and fails on the one that did not. The previous run of this feature was blocked precisely because the README documented a command that did not work, so do not introduce a second way for that to happen. Name the constant something unmistakably non-production, for example DEMO_PASSWORD_LITERAL = 'demo-password', and add a one-line comment saying it is demo data.\n\n2. ACCEPT Vitest for the web subproject and update the declared stack to record the split: Vitest for the React/Vite frontend, Jest for the API. Vitest is what web/ already uses, its 27 tests pass, and it is the idiomatic runner for a Vite SPA. Migrating them to Jest would be churn with no benefit and is explicitly outside this feature. The stack declaration is what is wrong here, not the code.\n\n3. Use manager@example.com and employee@example.com with the SHARED password 'demo-password'. Shared because the README must be short enough that someone actually follows it, and two passwords is one more thing to get wrong. Do not reuse the smoke.js convention: those credentials belong to the smoke check's throwaway schema and reusing them would blur which dataset a reader is looking at. [BINDING RULE — operator decision resolving: Should the demo account credentials (email/password) be hardcoded in the seed or sourced from environment variables with documented defaults?; The declared stack lists Jest as the test framework, but web/package.json uses Vitest and the feature explicitly requires changing the test script to `vitest run`. Should the web subproject be migrated to Jest, or is Vitest accepted for the Vite SPA?; What exact demo credentials (email/password) should the seed mint and the README document?; How should the demo account password be supplied to the seed and documented?; apply everywhere these apply, not in one place only]"],"auth_contract":"","transaction_contract":"","error_response_contract":""}