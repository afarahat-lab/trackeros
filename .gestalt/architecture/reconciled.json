{"domain_entities":[{"name":"Employee","attributes":["id: string","employeeNumber: string","firstName: string","lastName: string","email: string","role: EmployeeRole","managerId: string | null","department: string","hireDate: Date","terminationDate: Date | null","employmentStatus: EmploymentStatus","passwordHash: string | null"],"purpose":"The authenticated actor and subject of leave data. Gains a nullable credential (passwordHash) to support login; it is never serialized into any response body."},{"name":"AuthToken","attributes":["sub: string (employee id)","role: EmployeeRole","issuedAt: Date","expiresAt: Date"],"purpose":"A stateless JWT bearer credential minted at login. Its payload contract is fixed by the existing auth verifier: sub = employee id, role = EmployeeRole."},{"name":"LeaveRequest","attributes":["id: string","employeeId: string","leaveTypeCode: LeaveTypeCode","startDate: Date","endDate: Date","requestedDays: number","reason: string","status: LeaveRequestStatus","approverId: string | null","approvalComment: string | null","submittedAt: Date | null","decidedAt: Date | null","cancelledBy: string | null","cancelledAt: Date | null"],"purpose":"A leave request read by GET /leaves and GET /leaves/:id; visibility is scoped by role (EMPLOYEE self, MANAGER self + direct reports, ADMIN all)."},{"name":"LeaveBalance","attributes":["id: string","employeeId: string","leaveTypeCode: LeaveTypeCode","periodStart: Date","periodEnd: Date","entitledDays: number","usedDays: number","pendingDays: number","available: number (computed = entitledDays - usedDays - pendingDays)"],"purpose":"The caller's current-period leave balance per effective policy. `available` is derived, never stored; OPEN vs CLOSED is inferred from periodStart/periodEnd relative to now."},{"name":"LeavePolicy","attributes":["id: string","leaveTypeCode: LeaveTypeCode","policyName: string","annualEntitlementDays: number","accrualPeriodMonths: number","carryForwardDays: number","minNoticeDays: number","maxRequestDays: number","requiresManagerApproval: boolean","effectiveFrom: Date","effectiveTo: Date | null","status: LeavePolicyStatus"],"purpose":"The effective entitlement rule for a leave type over a time window. A leave type may have several policies over time; exactly one ACTIVE policy applies per leaveTypeCode as of a given date."}],"modules":[{"name":"shared-date","path":"src/shared/date/accrual.ts","owns":["startOfUtcDay(date)","addMonths(date, months)","periodContaining(anchor, accrualMonths, date) — pure UTC date arithmetic, no balance-domain knowledge"]},{"name":"shared-types","path":"src/shared/types/index.ts","owns":["LeaveRequestQueryParams gains employeeIds?: string[] (role scoping filter)"]},{"name":"shared-auth","path":"src/shared/auth/index.ts","owns":["registerAuth preHandler hook","signToken","verifyToken","PUBLIC_PATHS"]},{"name":"shared-errors","path":"src/shared/errors/index.ts","owns":["ValidationError","UnauthorizedError","ForbiddenError","NotFoundError","ConflictError","error response shape"]},{"name":"shared-db","path":"src/shared/db/connection.ts","owns":["defaultPool","DATE parsing pinned to UTC"]},{"name":"auth","path":"src/modules/auth/","owns":["IAuthService + AuthService (login, bcrypt.compare, signToken)","auth.routes.ts (POST /auth/login)","login body parsing/validation","index.ts"]},{"name":"employee","path":"src/modules/employee/","owns":["Employee model gains passwordHash: string | null","IEmployeeRepository gains findByManagerId","IEmployeeService gains getEmployeeByEmail, getEmployeesByManagerId","employee.routes.ts (GET /employees/me)"]},{"name":"policy","path":"src/modules/policy/","owns":["IPolicyService gains getActivePolicies(asOf) — at most one ACTIVE policy per leaveTypeCode, latest effective_from","PolicyService concrete","createPolicyService factory"]},{"name":"balance","path":"src/modules/balance/","owns":["IBalanceService gains getBalancesForEmployee(employeeId)","BalanceSummary value type (leaveTypeCode, periodStart, periodEnd, entitledDays, usedDays, pendingDays, available)","balance.routes.ts (GET /balances/me)"]},{"name":"leave","path":"src/modules/leave/","owns":["ILeaveService gains list(actor, query), getById(actor, requestId)","ILeaveRepository.findByQuery honors employeeIds (employee_id = ANY($n))","leave.routes.ts gains GET /leaves, GET /leaves/:id","role-scoping + visibility rule (EMPLOYEE self, MANAGER self+direct reports, ADMIN all)"]},{"name":"leave-type","path":"src/modules/leave-type/","owns":["LeaveType catalog (existing, unchanged)"]},{"name":"audit","path":"src/modules/audit/","owns":["audit logging (existing, unchanged)"]},{"name":"notification","path":"src/modules/notification/","owns":["notifications (existing, unchanged)"]},{"name":"validation","path":"src/modules/validation/","owns":["request validation helpers (existing, unchanged)"]}],"dependency_map":[{"from_":"auth","to":"employee"},{"from_":"auth","to":"shared-auth"},{"from_":"auth","to":"shared-errors"},{"from_":"auth","to":"shared-types"},{"from_":"employee","to":"shared-types"},{"from_":"employee","to":"shared-errors"},{"from_":"employee","to":"shared-db"},{"from_":"policy","to":"leave-type"},{"from_":"policy","to":"shared-types"},{"from_":"policy","to":"shared-errors"},{"from_":"policy","to":"shared-db"},{"from_":"balance","to":"employee"},{"from_":"balance","to":"policy"},{"from_":"balance","to":"shared-date"},{"from_":"balance","to":"shared-types"},{"from_":"balance","to":"shared-errors"},{"from_":"balance","to":"shared-db"},{"from_":"leave","to":"balance"},{"from_":"leave","to":"employee"},{"from_":"leave","to":"policy"},{"from_":"leave","to":"audit"},{"from_":"leave","to":"notification"},{"from_":"leave","to":"validation"},{"from_":"leave","to":"shared-date"},{"from_":"leave","to":"shared-types"},{"from_":"leave","to":"shared-errors"},{"from_":"leave","to":"shared-db"},{"from_":"shared-date","to":"shared-types"}],"recommended_phases":[{"title":"Phase 1 — Shared foundations: accrual date helpers + query param extension","rationale":"Extract startOfUtcDay/addMonths/periodContaining into src/shared/date/accrual.ts and add employeeIds to LeaveRequestQueryParams. No module depends on these yet; both are leaf changes. Add direct unit tests incl. TZ=Asia/Riyadh.","estimated_files":3},{"title":"Phase 2 — Employee: password_hash migration, findByManagerId, service methods","rationale":"Add nullable password_hash migration, extend Employee model/repository (findByManagerId) and service (getEmployeeByEmail, getEmployeesByManagerId). Auth and leave list both depend on these.","estimated_files":5},{"title":"Phase 3 — Policy: getActivePolicies service exposure","rationale":"Add IPolicyService.getActivePolicies(asOf) calling existing findAll, filtering ACTIVE + effective window, latest effective_from. Balance depends on it; no new repository method.","estimated_files":2},{"title":"Phase 4 — Auth module: login service + POST /auth/login + public path","rationale":"New auth module with IAuthService.login (bcrypt.compare + signToken), route, and add /auth/login to PUBLIC_PATHS. Depends on employee (Phase 2) and shared-auth.","estimated_files":4},{"title":"Phase 5 — Balance: getBalancesForEmployee + GET /balances/me","rationale":"Add IBalanceService.getBalancesForEmployee using getActivePolicies + shared-date periodContaining + findByKey; add route. Depends on Phases 1, 3.","estimated_files":3},{"title":"Phase 6 — Leave reads: findByQuery employeeIds, list/getById, GET /leaves + /leaves/:id","rationale":"Extend findByQuery to honor employeeIds (ANY), add ILeaveService.list/getById with role scoping + visibility, add routes. Depends on Phases 1, 2.","estimated_files":4},{"title":"Phase 7 — Employee /me route + smoke check extension + unit tests","rationale":"Add GET /employees/me route, extend scripts/smoke.js with login+read stages asserting real values, and add unit tests for new routes/services. Depends on all prior phases.","estimated_files":5}],"architecture_md_update":"# Authentication and read endpoints (v2) — Reconciled Architecture\n\n## Stack compliance\nTypeScript, Node 20, npm, Jest, Fastify, PostgreSQL via pg Pool, modular monolith. React Native frontend unaffected. No framework deviations.\n\n## Domain entities and lifecycle states\n- **Employee**: id, employeeNumber, firstName, lastName, email, role, managerId, department, hireDate, terminationDate, employmentStatus, passwordHash (nullable, never serialized). Lifecycle: ACTIVE, TERMINATED, ON_LEAVE.\n- **AuthToken**: stateless JWT bearer credential. Payload contract fixed: sub = employee id, role = EmployeeRole. Lifecycle: ISSUED, EXPIRED.\n- **LeaveRequest**: id, employeeId, leaveTypeCode, startDate, endDate, requestedDays, reason, status, approverId, approvalComment, submittedAt, decidedAt, cancelledBy, cancelledAt. Lifecycle: DRAFT, SUBMITTED, APPROVED, REJECTED, CANCELLED.\n- **LeaveBalance**: id, employeeId, leaveTypeCode, periodStart, periodEnd, entitledDays, usedDays, pendingDays, available (computed = entitledDays - usedDays - pendingDays). Lifecycle: OPEN, CLOSED.\n- **LeavePolicy**: id, leaveTypeCode, policyName, annualEntitlementDays, accrualPeriodMonths, carryForwardDays, minNoticeDays, maxRequestDays, requiresManagerApproval, effectiveFrom, effectiveTo, status. Lifecycle: DRAFT, ACTIVE, SUPERSEDED.\n\n## Conceptual tables (no DDL)\n- **employees**: id, employee_number, first_name, last_name, email, password_hash, role, manager_id, department, hire_date, termination_date, employment_status. PK id. FK manager_id -> employees.id. Indexes: email unique, employee_number unique, manager_id (new).\n- **leave_requests**: id, employee_id, leave_type_code, start_date, end_date, requested_days, reason, status, approver_id, approval_comment, submitted_at, decided_at, cancelled_by, cancelled_at. PK id. FKs employee_id -> employees.id, leave_type_code -> leave_types.code, approver_id -> employees.id, cancelled_by -> employees.id. Indexes: employee_id, status, (employee_id, status), (leave_type_code, start_date).\n- **leave_balances**: id, employee_id, leave_type_code, period_start, period_end, entitled_days, used_days, pending_days. PK id. FKs employee_id -> employees.id, leave_type_code -> leave_types.code. Indexes: unique (employee_id, leave_type_code, period_start, period_end), employee_id.\n- **leave_policies**: id, leave_type_code, policy_name, annual_entitlement_days, accrual_period_months, carry_forward_days, min_notice_days, max_request_days, requires_manager_approval, effective_from, effective_to, status. PK id. FK leave_type_code -> leave_types.code. Index: leave_type_code.\n- **leave_types**: code, name. PK code. Referenced by leave_requests and leave_balances.\n\nOnly schema change: nullable `password_hash` on `employees` (text contract, `t.text(...)`).\n\n## Repository interfaces and concrete implementations\n- **IEmployeeRepository / PgEmployeeRepository**: create, findById, findByEmployeeNumber, findByEmail, findByManagerId (new).\n- **ILeaveRepository / PgLeaveRequestRepository**: create, findById, update, findByQuery (extended to honour `employeeIds?: string[]` via `employee_id = ANY($n)`).\n- **IPolicyRepository / PgLeavePolicyRepository**: create, findById, findByLeaveTypeCode, findAll.\n- **IBalanceRepository / PgLeaveBalanceRepository**: create, findById, findByKey, update.\n- **IPolicyService / PolicyService**: createLeavePolicy, getLeavePolicyById, getPolicyByLeaveTypeCode, getActivePolicies(asOf) (new).\n- **IAuthService / AuthService**: login(email, password) (new).\n- **IEmployeeService**: createEmployee, getEmployeeById, getEmployeeByEmail (new), getEmployeesByManagerId (new).\n- **IBalanceService**: openPeriod, carryForward, getBalance, getBalanceById, getBalancesForEmployee (new).\n- **ILeaveService**: create, submit, approve, reject, cancel, list(actor, query) (new), getById(actor, requestId) (new).\n\n## Module boundaries\n- **shared-date** (`src/shared/date/accrual.ts`): startOfUtcDay, addMonths, periodContaining.\n- **shared-types** (`src/shared/types/index.ts`): LeaveRequestQueryParams gains employeeIds.\n- **shared-auth** (`src/shared/auth/index.ts`): registerAuth hook, signToken, verifyToken, PUBLIC_PATHS.\n- **shared-errors**: error classes and response shape.\n- **shared-db** (`src/shared/db/connection.ts`): defaultPool, UTC DATE parsing.\n- **auth** (`src/modules/auth/`): login service, POST /auth/login, body parsing.\n- **employee** (`src/modules/employee/`): passwordHash, findByManagerId, getEmployeeByEmail, getEmployeesByManagerId, GET /employees/me.\n- **policy** (`src/modules/policy/`): getActivePolicies, createPolicyService factory.\n- **balance** (`src/modules/balance/`): getBalancesForEmployee, BalanceSummary, GET /balances/me.\n- **leave** (`src/modules/leave/`): list, getById, findByQuery employeeIds, GET /leaves, GET /leaves/:id.\n- Existing unchanged modules: leave-type, audit, notification, validation.\n\n## Dependency map\n- auth -> employee, shared-auth, shared-errors, shared-types\n- employee -> shared-types, shared-errors, shared-db\n- policy -> leave-type, shared-types, shared-errors, shared-db\n- balance -> employee, policy, shared-date, shared-types, shared-errors, shared-db\n- leave -> balance, employee, policy, audit, notification, validation, shared-date, shared-types, shared-errors, shared-db\n- shared-date -> shared-types\n\n## Cross-cutting contracts\n- **Auth contract**: `request.user: { id: string; role: EmployeeRole }` where `EmployeeRole = 'EMPLOYEE' | 'MANAGER' | 'ADMIN'`. Identity/role obtained from a JWT bearer token verified by the existing `registerAuth` preHandler hook (`src/shared/auth/index.ts`), which decodes `sub` -> id and `role` and populates `request.user`; missing/invalid token -> 401 UnauthorizedError. RBAC enforced at the API boundary via `resolveActor` and in services (leave visibility scoping). `POST /auth/login` is the sole public endpoint added to `PUBLIC_PATHS`.\n- **Error contract**: errors return `{ error: string; code: string }`. Validation failure -> 400 ValidationError; authentication failure -> 401 UnauthorizedError; authorization failure -> 403 ForbiddenError; not found -> 404 NotFoundError (GET /leaves/:id returns the SAME 404 for invisible and nonexistent ids); invalid state -> 409 ConflictError; other -> 500 `{ error: 'Internal Server Error' }`. Login failure (wrong email OR password) returns a single indistinguishable 401.\n- **Transaction contract**: EMPTY — this feature is read-only plus a single-read login (bcrypt compare + token mint). No multi-step writes exist, so no unit-of-work contract is introduced. Existing write endpoints and their PgUnitOfWork semantics are unchanged.\n\n## Recommended phases\n1. Shared foundations: accrual date helpers + query param extension (3 files).\n2. Employee: password_hash migration, findByManagerId, service methods (5 files).\n3. Policy: getActivePolicies service exposure (2 files).\n4. Auth module: login service + POST /auth/login + public path (4 files).\n5. Balance: getBalancesForEmployee + GET /balances/me (3 files).\n6. Leave reads: findByQuery employeeIds, list/getById, GET /leaves + /leaves/:id (4 files).\n7. Employee /me route + smoke check extension + unit tests (5 files).\n\n## Open questions\n1. GET /balances/me available computation when no balance row exists for a policy's current period — synthesize entry vs omit leave type.\n2. GET /leaves/:id visibility for a MANAGER whose direct report's request is APPROVED/REJECTED/CANCELLED vs DRAFT/SUBMITTED — status-unrestricted vs SUBMITTED-only.\n","sql_schemas":["table employees | fields: id, employee_number, first_name, last_name, email, password_hash, role, manager_id, department, hire_date, termination_date, employment_status | pk: id | fks: manager_id -> employees.id | indexes: email: unique — POST /auth/login resolves caller via findByEmail; fast unique lookup; employee_number: unique — existing findByEmployeeNumber lookup; manager_id: MANAGER role scoping for GET /leaves uses findByManagerId (direct reports); currently unindexed and must be added","table leave_requests | fields: id, employee_id, leave_type_code, start_date, end_date, requested_days, reason, status, approver_id, approval_comment, submitted_at, decided_at, cancelled_by, cancelled_at | pk: id | fks: employee_id -> employees.id; leave_type_code -> leave_types.code; approver_id -> employees.id; cancelled_by -> employees.id | indexes: employee_id: role scoping for GET /leaves and GET /leaves/:id filters by employee_id; status: status filter on GET /leaves; (employee_id, status): composite — dominant list query scopes by employee then filters by status; (leave_type_code, start_date): date-range + type filtering on GET /leaves","table leave_balances | fields: id, employee_id, leave_type_code, period_start, period_end, entitled_days, used_days, pending_days | pk: id | fks: employee_id -> employees.id; leave_type_code -> leave_types.code | indexes: (employee_id, leave_type_code, period_start, period_end): unique — findByKey lookup for GET /balances/me and balance resolution; employee_id: balance lookups by employee","table leave_policies | fields: id, leave_type_code, policy_name, annual_entitlement_days, accrual_period_months, carry_forward_days, min_notice_days, max_request_days, requires_manager_approval, effective_from, effective_to, status | pk: id | fks: leave_type_code -> leave_types.code | indexes: leave_type_code: findAll() filtered per leaveTypeCode for GET /balances/me (ACTIVE + effective window, latest effective_from)","table leave_types | fields: code, name | pk: code | indexes: code: primary key; referenced by leave_requests.leave_type_code and leave_balances.leave_type_code"],"open_questions":[],"business_rules":["1. NO BALANCE ROW FOR A POLICY'S CURRENT PERIOD — OMIT the leave type from the list. Do not synthesize an entry.\n\n   The deciding constraint is that this endpoint must never advertise a number the rest of the system will not honour. A balance row is opened deliberately (BalanceService.openPeriod); until it exists, LeaveService.resolveBalance throws NotFoundError, so a create request for that leave type FAILS. Synthesizing `available = policy.annualEntitlementDays` would render \"Annual: 25 days available\" on a screen where submitting a request immediately errors — a UI that lies, and the worst of the two outcomes because the user only discovers it by being rejected.\n\n   Omitting is merely incomplete, which is recoverable and honest: the type is absent because nothing has been opened for it.\n\n   Consequences to apply consistently:\n   - An employee with no open periods at all gets `[]` — a valid, successful 200 response, NOT a 404 and NOT an error.\n   - Never throw because one policy lacks a row. One missing period must not fail the whole request; that is the behaviour I asked for in the previous clarification and it still holds.\n   - The smoke check must pin this: seed TWO leave types with effective policies but a balance row for only ONE, and assert the response contains exactly the seeded one. That makes the omission a tested contract rather than an accident of iteration order.\n\n   If the product later wants every entitled type visible, the correct fix is to OPEN the period (write the balance row), not to have the read endpoint invent one.\n\n2. MANAGER VISIBILITY — status-UNRESTRICTED. A manager sees their direct reports' requests in every status, plus their own.\n\n   Visibility and authority to act are separate concerns and must not be conflated. \"Which of my team are off next month\", \"what did I approve last quarter\", \"why was this rejected\" are the core of a manager view; restricting reads to SUBMITTED would reduce it to an approval inbox and make the endpoint nearly useless for its actual purpose.\n\n   The existing decide authorization is CORRECT and does not change: a manager may still only approve/reject a request that is SUBMITTED. Do not widen it, and do not narrow reads to match it. They are different rules because they answer different questions — one is \"may I see this\", the other is \"may I change this\".\n\n   So there is no divergence from the stated rule: the scope rule (direct reports plus self, one level, not transitive) governs VISIBILITY, and it is status-independent. Apply it identically to GET /leaves and GET /leaves/:id, and keep the existing rule that a request the caller may not see returns the SAME response as one that does not exist.\n\n   Pin both halves in tests: a manager CAN read a direct report's APPROVED and REJECTED requests, and a manager still CANNOT approve a request that is not SUBMITTED. [BINDING RULE — operator decision resolving: GET /balances/me available computation when no balance row exists for a policy's current period; GET /leaves/:id visibility for a MANAGER whose direct report's request is APPROVED/REJECTED/CANCELLED vs DRAFT/SUBMITTED; apply everywhere these apply, not in one place only]"],"auth_contract":"request.user: { id: string; role: EmployeeRole } where EmployeeRole = 'EMPLOYEE' | 'MANAGER' | 'ADMIN'. Identity/role obtained from a JWT bearer token verified by the existing registerAuth preHandler hook (src/shared/auth/index.ts), which decodes sub->id and role and populates request.user; missing/invalid token -> UnauthorizedError (401). RBAC enforced at the API boundary via resolveActor (role membership check) and in services (leave visibility scoping: EMPLOYEE self, MANAGER self+direct reports, ADMIN all). POST /auth/login is the sole public (unauthenticated) endpoint added to PUBLIC_PATHS.","transaction_contract":"EMPTY — this feature is read-only plus a single-read login (bcrypt compare + token mint). No multi-step writes exist, so no unit-of-work contract is introduced. Existing write endpoints and their PgUnitOfWork semantics are unchanged.","error_response_contract":"Errors return { error: string; code: string }. Validation failure -> 400 ValidationError; authentication failure -> 401 UnauthorizedError; authorization failure -> 403 ForbiddenError; not found -> 404 NotFoundError (GET /leaves/:id returns the SAME 404 for invisible and nonexistent ids); invalid state -> 409 ConflictError; other -> 500 { error: 'Internal Server Error' }. Login failure (wrong email OR password) returns a single indistinguishable 401."}