{"domain_entities":[{"name":"LeaveRequest","attributes":["id: string","employeeId: string","leaveTypeId: string","startDate: Date","endDate: Date","reason: string | undefined","status: LeaveRequestStatus","approvedBy: string | null","approvedAt: Date | null","rejectedBy: string | null","rejectedAt: Date | null","rejectionReason: string | undefined","createdAt: Date","updatedAt: Date"],"purpose":"Central aggregate of the leave management module. Tracks the full lifecycle from draft through submission, manager decision, and potential cancellation."},{"name":"LeaveRequestStatus","attributes":["value: 'DRAFT' | 'SUBMITTED' | 'APPROVED' | 'REJECTED' | 'CANCELLED'"],"purpose":"Enumeration of all lifecycle states a LeaveRequest can occupy. Governs which transitions are legal."},{"name":"LeaveType","attributes":["id: string","code: 'annual' | 'sick' | 'emergency' | 'unpaid' | 'maternity' | 'paternity'","label: string","description: string | undefined","isActive: boolean","createdAt: Date","updatedAt: Date"],"purpose":"Catalog of leave categories. Each LeaveRequest references one LeaveType. The code drives policy lookup and balance tracking."},{"name":"LeavePolicy","attributes":["id: string","policyName: string","leaveTypeId: string","entitlementDays: number","accrualRate: number | undefined","maxAccumulation: number | undefined","minimumNoticeDays: number | undefined","requiresManagerApproval: boolean","isActive: boolean","createdAt: Date","updatedAt: Date"],"purpose":"Defines the rules governing a specific leave type: entitlement, approval requirements, notice periods, and accrual behaviour. Referenced by LeaveBalance to compute remaining entitlement."},{"name":"LeaveBalance","attributes":["id: string","employeeId: string","policyId: string","totalEntitlement: number","usedDays: number","pendingDays: number","remainingDays: number","fiscalYear: number","status: 'ACTIVE' | 'CLOSED'","createdAt: Date","updatedAt: Date"],"purpose":"Tracks an employee's leave entitlement, consumption, and remaining balance for a specific policy and fiscal year. pendingDays captures days tied to SUBMITTED (not yet decided) requests; usedDays captures APPROVED days; remainingDays = totalEntitlement - usedDays - pendingDays."},{"name":"Employee","attributes":["id: string","employeeNumber: string","firstName: string","lastName: string","email: string","managerId: string | null","department: string | null","hireDate: Date","terminationDate: Date | null","employmentStatus: 'ACTIVE' | 'INACTIVE' | 'TERMINATED'","createdAt: Date","updatedAt: Date","deletedAt: Date | null"],"purpose":"Represents an employee in the organisation. The managerId establishes the approval hierarchy. Employment status gates eligibility for new leave requests."},{"name":"Notification","attributes":["id: string","recipientId: string","type: 'LEAVE_SUBMITTED' | 'LEAVE_APPROVED' | 'LEAVE_REJECTED' | 'LEAVE_CANCELLED'","title: string","message: string","relatedEntityType: 'LeaveRequest'","relatedEntityId: string","status: 'PENDING' | 'SENT' | 'READ' | 'ARCHIVED'","createdAt: Date","readAt: Date | null"],"purpose":"Domain event notification triggered by LeaveRequest lifecycle transitions. Notifies managers of new submissions and employees of decisions on their requests."}],"modules":[{"name":"shared-types","path":"src/shared/types/","owns":["LeaveStatus enum (DRAFT | SUBMITTED | APPROVED | REJECTED | CANCELLED)","LeaveTypeCode enum (annual | sick | emergency | unpaid | maternity | paternity)"]},{"name":"audit","path":"src/modules/audit/","owns":["AuditRecord model","IAuditRepository interface","AuditRepository implementation","IAuditService interface","AuditService implementation","index.ts barrel export"]},{"name":"leave-policy","path":"src/modules/leave-policy/","owns":["LeaveType model","ILeaveTypeRepository interface","LeaveTypeRepository implementation","LeavePolicy model","ILeavePolicyRepository interface","LeavePolicyRepository implementation","ILeavePolicyService interface","LeavePolicyService implementation","leave-policy routes","index.ts barrel export"]},{"name":"leave-balance","path":"src/modules/leave-balance/","owns":["LeaveBalance model","ILeaveBalanceRepository interface","LeaveBalanceRepository implementation","ILeaveBalanceService interface","LeaveBalanceService implementation","leave-balance routes","index.ts barrel export"]},{"name":"notification","path":"src/modules/notification/","owns":["Notification model","INotificationRepository interface","NotificationRepository implementation","INotificationService interface","NotificationService implementation","index.ts barrel export"]},{"name":"leave-request","path":"src/modules/leave-request/","owns":["LeaveRequest model","ILeaveRequestRepository interface","LeaveRequestRepository implementation","ILeaveRequestService interface","LeaveRequestService implementation","leave-request routes","index.ts barrel export"]}],"dependency_map":[{"from_":"leave-policy","to":"shared-types"},{"from_":"leave-policy","to":"audit"},{"from_":"leave-balance","to":"shared-types"},{"from_":"leave-balance","to":"leave-policy"},{"from_":"leave-balance","to":"audit"},{"from_":"notification","to":"shared-types"},{"from_":"leave-request","to":"shared-types"},{"from_":"leave-request","to":"leave-policy"},{"from_":"leave-request","to":"leave-balance"},{"from_":"leave-request","to":"audit"},{"from_":"leave-request","to":"notification"}],"recommended_phases":[{"title":"Phase 1: Shared types + Audit foundation","rationale":"Zero-dependency leaf modules. LeaveStatus and LeaveTypeCode enums in shared-types are consumed by every other module. Audit module is required by GP-002 for all state-changing operations. Both have no internal dependencies.","estimated_files":6},{"title":"Phase 2: Leave Policy module (includes LeaveType catalog)","rationale":"Depends only on shared-types and audit. Defines the leave type catalog and policy rules (entitlement, approval, notice) that balance and request modules consume. Must exist before balance initialization or request validation can work.","estimated_files":8},{"title":"Phase 3: Leave Balance module","rationale":"Depends on shared-types, leave-policy, and audit. Tracks per-employee balances per policy per fiscal year, including pending days. Required by leave-request for validation and deduction/restore operations.","estimated_files":6},{"title":"Phase 4: Notification module","rationale":"Leaf module with no internal dependencies. Can be built in parallel with Phase 3. Required by leave-request for notifying employees and managers on submission/decision events.","estimated_files":5},{"title":"Phase 5: Leave Request module","rationale":"Depends on all prior modules. Orchestrates the full leave request lifecycle: validates against policy, checks/deducts/restores balances (including pending days), audits every state change, and triggers notifications. Supports draft creation and submission.","estimated_files":6}],"architecture_md_update":"## Leave Management Module — Reconciled Architecture\n\n### Domain Entities\n\n- **LeaveRequest** — Central aggregate. Lifecycle: DRAFT → SUBMITTED → (APPROVED | REJECTED); any non-terminal state → CANCELLED. REJECTED is terminal (no resubmission — create new request).\n- **LeaveRequestStatus** — Enum: DRAFT, SUBMITTED, APPROVED, REJECTED, CANCELLED.\n- **LeaveType** — Catalog of leave categories (annual, sick, emergency, unpaid, maternity, paternity). Lifecycle: ACTIVE, INACTIVE.\n- **LeavePolicy** — Rules per leave type: entitlement, accrual, notice, approval flag. Lifecycle: ACTIVE, INACTIVE.\n- **LeaveBalance** — Per-employee, per-policy, per-fiscal-year balance. Tracks totalEntitlement, usedDays, pendingDays, remainingDays. Lifecycle: ACTIVE, CLOSED.\n- **Employee** — Organisation member with manager hierarchy and employment status (ACTIVE, INACTIVE, TERMINATED).\n- **Notification** — Domain event for leave lifecycle transitions. Lifecycle: PENDING, SENT, READ, ARCHIVED.\n\n### Key Business Rules (Binding)\n\n1. **Inclusive day counting**: `requestedDays = (endDate - startDate) + 1` (calendar days).\n2. **State transitions**: Only DRAFT→SUBMITTED, DRAFT→CANCELLED, SUBMITTED→APPROVED, SUBMITTED→REJECTED, SUBMITTED→CANCELLED, APPROVED→CANCELLED. REJECTED is terminal.\n3. **Balance impact**: SUBMITTED increments pendingDays; APPROVED decrements pendingDays and increments usedDays; REJECTED decrements pendingDays; CANCELLED (from APPROVED) decrements usedDays (restores balance). DRAFT has no impact.\n4. **Sufficiency check**: Before DRAFT→SUBMITTED, `remainingDays - pendingDays >= requestedDays`.\n5. **Manager routing**: Request routed to employee.managerId; if null, auto-escalated to ADMIN. If LeavePolicy.requiresManagerApproval is false, auto-approve on submission.\n6. **Minimum notice**: If LeavePolicy.minimumNoticeDays > 0, `startDate - submissionDate >= minimumNoticeDays`.\n7. **Employee eligibility**: Only ACTIVE employees may create/submit requests.\n8. **Active leave type/policy**: Only active LeaveType and LeavePolicy may be used.\n9. **Overlap prevention**: No two SUBMITTED or APPROVED requests for same employee may have overlapping date ranges.\n10. **Rejection reason**: Required when transitioning to REJECTED.\n11. **Date validity**: `endDate >= startDate`.\n\n### Module Boundaries\n\n| Module | Path | Responsibilities |\n|--------|------|------------------|\n| shared-types | src/shared/types/ | LeaveStatus and LeaveTypeCode enums |\n| audit | src/modules/audit/ | AuditRecord model, repository, service |\n| leave-policy | src/modules/leave-policy/ | LeaveType and LeavePolicy models, repositories, service, routes |\n| leave-balance | src/modules/leave-balance/ | LeaveBalance model, repository, service, routes |\n| notification | src/modules/notification/ | Notification model, repository, service |\n| leave-request | src/modules/leave-request/ | LeaveRequest model, repository, service, routes |\n\n### Dependency Map\n\n- leave-policy → shared-types, audit\n- leave-balance → shared-types, leave-policy, audit\n- notification → shared-types\n- leave-request → shared-types, leave-policy, leave-balance, audit, notification\n\n### Conceptual Data Model (Tables)\n\n- **leave_types**: id, code, label, description, is_active, created_at, updated_at\n- **leave_policies**: id, policy_name, leave_type_id (FK), entitlement_days, accrual_rate, max_accumulation, minimum_notice_days, requires_manager_approval, is_active, created_at, updated_at\n- **employees**: id, employee_number, first_name, last_name, email, manager_id (FK self), department, hire_date, termination_date, employment_status, created_at, updated_at, deleted_at\n- **leave_requests**: id, employee_id (FK), leave_type_id (FK), start_date, end_date, reason, status, approved_by (FK), approved_at, rejected_by (FK), rejected_at, rejection_reason, created_at, updated_at\n- **leave_balances**: id, employee_id (FK), policy_id (FK), total_entitlement, used_days, pending_days, remaining_days, fiscal_year, status, created_at, updated_at\n- **audit_logs**: id, entity_type, entity_id, action, old_values, new_values, performed_by, performed_at, ip_address, user_agent, created_at\n- **notifications**: id, recipient_id (FK), type, title, message, related_entity_type, related_entity_id, status, created_at, read_at\n\n### Cross-Cutting Contracts\n\n**Auth Contract**\n- `request.user: { id: string; role: UserRole }`\n- `UserRole = 'employee' | 'manager' | 'hr_admin'`\n- JWT bearer verified by auth middleware; RBAC enforced by `requireRole(...)` route guard.\n\n**Transaction Contract**\n- Repository methods accept optional `PoolClient` parameter (defaults to shared pool).\n- Service owns unit of work: acquires client, `BEGIN`, passes client to all participating repositories, `COMMIT` or `ROLLBACK`.\n- Required for leave-approval flow: updating leave_requests.status, leave_balances.used_days/pending_days/remaining_days, and inserting audit_logs row must be atomic.\n\n**Error Response Contract**\n- Standard shape: `{ error: string; code: string }`\n- Validation failure → 400 (VALIDATION_ERROR)\n- Authentication failure → 401 (UNAUTHORIZED)\n- Authorization failure → 403 (FORBIDDEN)\n- Not found → 404 (NOT_FOUND)\n- Insufficient balance → 400 (INSUFFICIENT_BALANCE)\n- Policy violation → 400 (POLICY_VIOLATION)\n- Invalid state transition → 409 (INVALID_STATE)\n\n### Recommended Build Phases\n\n1. **Shared types + Audit foundation** — Zero-dependency leaf modules.\n2. **Leave Policy module** — Includes LeaveType catalog; defines rules consumed by balance and request modules.\n3. **Leave Balance module** — Tracks balances with pending days; required for validation and deduction.\n4. **Notification module** — Leaf module for leave lifecycle notifications.\n5. **Leave Request module** — Orchestrator; supports draft creation and submission, full lifecycle, balance integration, audit, and notifications.\n\n### Open Questions\n\n- Fiscal year boundary (calendar, anniversary, configurable)\n- Accrual mechanics (lump-sum, monthly, per-pay-period, tenure-based)\n- Emergency leave special rules (bypass notice/approval, separate pool)\n- Balance deduction timing (immediate on approval vs. start of leave period)\n","sql_schemas":["table leave_types | fields: id, code, label, description, is_active, created_at, updated_at | pk: id | indexes: code: unique lookup for validation and policy association","table leave_policies | fields: id, policy_name, leave_type_id, entitlement_days, accrual_rate, max_accumulation, minimum_notice_days, requires_manager_approval, is_active, created_at, updated_at | pk: id | fks: leave_type_id -> leave_types.id | indexes: leave_type_id: look up policies by leave type; is_active: filter only active policies during request validation","table employees | fields: id, employee_number, first_name, last_name, email, manager_id, department, hire_date, termination_date, employment_status, created_at, updated_at, deleted_at | pk: id | fks: manager_id -> employees.id | indexes: email: unique lookup for login and identity resolution; manager_id: find direct reports for manager approval workflows; employment_status: filter active employees for leave eligibility; department: HR reporting and departmental leave calendars","table leave_requests | fields: id, employee_id, leave_type_id, start_date, end_date, reason, status, approved_by, approved_at, rejected_by, rejected_at, rejection_reason, created_at, updated_at | pk: id | fks: employee_id -> employees.id; leave_type_id -> leave_types.id; approved_by -> employees.id; rejected_by -> employees.id | indexes: employee_id: look up all leave requests for a given employee; status: filter by status for manager approval queues and HR reports; start_date: date-range queries for calendar views and conflict detection; end_date: date-range queries for calendar views and conflict detection; (employee_id, status): combined filter for employee-specific status views; (status, start_date): manager approval queue ordered by upcoming start dates","table leave_balances | fields: id, employee_id, policy_id, total_entitlement, used_days, pending_days, remaining_days, fiscal_year, status, created_at, updated_at | pk: id | fks: employee_id -> employees.id; policy_id -> leave_policies.id | indexes: (employee_id, fiscal_year): primary access pattern — look up an employee's balances for the current fiscal year; policy_id: aggregate usage across a policy for reporting; fiscal_year: fiscal-year-scoped queries for accrual jobs and year-end rollover; (employee_id, policy_id, fiscal_year): unique constraint candidate — one balance row per employee per policy per fiscal year","table audit_logs | fields: id, entity_type, entity_id, action, old_values, new_values, performed_by, performed_at, ip_address, user_agent, created_at | pk: id | indexes: (entity_type, entity_id): look up audit trail for a specific entity; performed_by: audit queries scoped to a specific actor; performed_at: time-range audit queries for compliance reports; action: filter audit trail by action type","table notifications | fields: id, recipient_id, type, title, message, related_entity_type, related_entity_id, status, created_at, read_at | pk: id | fks: recipient_id -> employees.id | indexes: recipient_id: look up notifications for a specific user; status: filter unread/active notifications; (recipient_id, status): active inbox query"],"open_questions":[],"business_rules":["Business policy answers for the leave management module:\n\n1/5/8/10. Fiscal (leave) year boundary: CALENDAR YEAR (Jan 1 to Dec 31), organisation-wide, keyed off the leave start_date. Not hire-date anniversary, not configurable.\n\n2/9. Accrual: ANNUAL LUMP-SUM allocation at the start of the fiscal year (Jan 1) — each employee is granted the full entitlement for that leave type up front (not monthly pro-rata). Mid-year hires: pro-rate the first year by the number of whole months remaining in the year from the hire date (rounded down). maxAccumulation caps the balance; accrualRate is the annual entitlement. Carryover: USE-IT-OR-LOSE-IT — unused balance does NOT carry across fiscal years.\n\n3/6. Emergency leave: it is a SEPARATE pool with its own entitlement, distinct from annual and sick. Emergency leave bypasses the normal advance-notice requirement (it can be applied for same-day / retroactively), but still goes through manager approval and still deducts from its own balance. It does not draw from annual or sick.\n\n4. Deduction timing: deduct on APPROVAL (finalize). On submission the requested days are held as PENDING (a reservation); on approval the pending days move to used; on reject or cancel the reservation is released. Available balance = entitled - (used + pending). Deduct at approval time, not at the start of the leave period.\n\n7. Day counting: BUSINESS/WORKING DAYS only — exclude weekends (Saturday, Sunday) and public holidays. Both start_date and end_date are inclusive. Whole days only (no half-days).\n\nCross-cutting rules that apply throughout:\n- Overlapping leave requests are prevented (reject a new SUBMITTED/APPROVED request overlapping an existing SUBMITTED/APPROVED one for the same employee).\n- A request spanning two fiscal years deducts wholly from the fiscal year of start_date (no split).\n- Balances are auto-created for all leave types on employee creation.\n- Every endpoint enforces RBAC (employees act on their own records; managers approve/reject their direct reports) plus input validation.\n- When an employee has no manager, approval escalates to HR. [BINDING RULE — operator decision resolving: How is the fiscal year boundary determined for LeaveBalance?; How does leave accrual work? LeavePolicy defines accrualRate and maxAccumulation, but the accrual mechanics (frequency, proration for mid-year hires, carryover rules) are not specified.; Does emergency leave have special rules that distinguish it from annual and sick leave?; When a leave request is approved, should the balance be deducted immediately at approval time or at the start of the leave period?; How is the fiscal year boundary determined for LeaveBalance? Is it calendar year (Jan 1 – Dec 31), the employee's hire-date anniversary, or a configurable organisation-wide fiscal year start?; Does emergency leave have special rules that distinguish it from annual and sick leave? The feature description lists all three but does not specify whether emergency leave bypasses notice periods, approval requirements, or balance checks.; How are leave days counted — calendar days or business/working days?; What are the fiscal year boundaries for balance scoping?; How does leave balance accrual work — annual lump-sum allocation at fiscal-year start vs. monthly pro-rata accrual?; What is the fiscal year boundary — calendar year (Jan 1 – Dec 31) or a configurable company fiscal year?; apply everywhere these apply, not in one place only]"],"auth_contract":"request.user: { id: string; role: UserRole }; UserRole = 'employee' | 'manager' | 'hr_admin'; JWT bearer verified by auth middleware that populates request.user; RBAC enforced by requireRole(...) route-level guard, never inline. leave-policy routes: upsertPolicy requires hr_admin; getPolicyForType/getAllPolicies require authenticated user. leave-balance routes: getBalance/getAllBalances require employee (own) or manager (team). leave-request routes: createDraftRequest/submitDraftRequest/cancelRequest require employee; approveRequest/rejectRequest require manager; getPendingRequestsForManager requires manager.","transaction_contract":"Repository methods that must join a caller's transaction accept an optional PoolClient parameter (defaulting to the shared pool when omitted). The calling service owns the unit of work: it acquires a client from the pool, executes BEGIN, passes the client to every repository call participating in the transaction, then executes COMMIT or ROLLBACK. This contract is required for the leave-approval flow: updating leave_requests.status to APPROVED, updating leave_balances.used_days, leave_balances.pending_days, and leave_balances.remaining_days, and inserting an audit_logs row must all commit atomically or roll back together.","error_response_contract":"Standard error shape: { error: string; code: string }. Validation failure → HTTP 400 (code: VALIDATION_ERROR). Authentication failure → 401 (code: UNAUTHORIZED). Authorization failure → 403 (code: FORBIDDEN). Not found → 404 (code: NOT_FOUND). Insufficient balance → 400 (code: INSUFFICIENT_BALANCE). Policy violation (e.g., exceeds max days) → 400 (code: POLICY_VIOLATION). Invalid state transition (e.g., approving an already-rejected request) → 409 (code: INVALID_STATE)."}