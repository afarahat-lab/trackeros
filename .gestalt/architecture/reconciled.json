{"domain_entities":[{"name":"LeaveType","attributes":["value: 'annual' | 'sick' | 'emergency' | 'unpaid' | 'maternity' | 'paternity'"],"purpose":"Enumeration of leave categories. Feature scope targets annual, sick, emergency; others defined for completeness."},{"name":"LeaveStatus","attributes":["value: 'DRAFT' | 'SUBMITTED' | 'APPROVED' | 'REJECTED' | 'CANCELLED'"],"purpose":"Lifecycle states for a LeaveRequest. Governs valid transitions and operations."},{"name":"LeaveRequest","attributes":["id: string","employeeId: string","leaveTypeId: string","startDate: Date","endDate: Date","reason: string | undefined","status: LeaveStatus","approvedBy: string | null","approvedAt: Date | null","rejectedBy: string | null","rejectedAt: Date | null","cancelledBy: string | null","cancelledAt: Date | null","createdAt: Date","updatedAt: Date"],"purpose":"Employee leave application. Tracks full lifecycle with actor and timestamp at each transition."},{"name":"LeavePolicy","attributes":["id: string","policyName: string","leaveType: LeaveType","entitlementDays: number","accrualRate: number | null","maxAccumulation: number | null","minimumNoticeDays: number","requiresManagerApproval: boolean","isActive: boolean","isPaid: boolean","createdAt: Date","updatedAt: Date"],"purpose":"Rules governing a leave type: entitlement, notice, approval requirement, accrual caps. One active policy per leave type per organisational scope."},{"name":"LeaveBalance","attributes":["id: string","employeeId: string","policyId: string","fiscalYear: number","totalEntitlement: number","usedDays: number","remainingDays: number","status: 'ACTIVE' | 'EXHAUSTED' | 'CLOSED'","createdAt: Date","updatedAt: Date"],"purpose":"Tracks an employee's leave entitlement, consumption, and remaining balance for a specific policy within a fiscal year. One record per employee-policy-fiscalYear."},{"name":"Employee","attributes":["id: string","employeeNumber: string","firstName: string","lastName: string","email: string","role: 'employee' | 'manager' | 'hr_admin'","managerId: string | null","department: string | null","hireDate: Date","terminationDate: Date | null","employmentStatus: 'ACTIVE' | 'INACTIVE' | 'TERMINATED'","createdAt: Date","updatedAt: Date","deletedAt: Date | null"],"purpose":"Organisational employee. In leave domain, employees are requesters and managers are approvers via managerId self-reference."},{"name":"AuditLog","attributes":["id: string","entityType: string","entityId: string","action: string","changedBy: string","changes: Record<string, unknown>","createdAt: Date"],"purpose":"Immutable record of state-changing operations for compliance (GP-002). Captures before/after snapshots in JSON changes field."}],"modules":[{"name":"shared-types","path":"src/shared/types/","owns":["LeaveType enum","LeaveStatus enum"]},{"name":"employee","path":"src/modules/employee/","owns":["Employee model","IEmployeeRepository","EmployeeRepository","IEmployeeService","EmployeeService"]},{"name":"policy","path":"src/modules/policy/","owns":["LeavePolicy model","ILeavePolicyRepository","LeavePolicyRepository","ILeavePolicyService","LeavePolicyService"]},{"name":"balance","path":"src/modules/balance/","owns":["LeaveBalance model","ILeaveBalanceRepository","LeaveBalanceRepository","IBalanceService","BalanceService"]},{"name":"leave","path":"src/modules/leave/","owns":["LeaveRequest model","ILeaveRequestRepository","LeaveRequestRepository","ILeaveService","LeaveService","LeaveController","leaveRoutes"]},{"name":"notification","path":"src/modules/notification/","owns":["INotificationService","NotificationService"]},{"name":"audit","path":"src/modules/audit/","owns":["AuditLog model","IAuditLogRepository","AuditLogRepository","IAuditService","AuditService"]}],"dependency_map":[{"from_":"leave","to":"balance"},{"from_":"leave","to":"policy"},{"from_":"leave","to":"employee"},{"from_":"leave","to":"notification"},{"from_":"leave","to":"audit"},{"from_":"leave","to":"shared-types"},{"from_":"balance","to":"policy"},{"from_":"balance","to":"audit"},{"from_":"balance","to":"shared-types"},{"from_":"policy","to":"shared-types"},{"from_":"employee","to":"shared-types"}],"recommended_phases":[{"title":"Phase 1: Shared types — LeaveType and LeaveStatus enums","rationale":"Zero-dependency foundation. Every downstream module needs these enums. Single file, no logic.","estimated_files":1},{"title":"Phase 2: Audit module","rationale":"GP-002 requires every state-changing operation to write an audit record. Audit is a cross-cutting dependency for balance and leave. Depends only on shared-types.","estimated_files":5},{"title":"Phase 3: Employee module","rationale":"Leave service needs employee lookup and manager-subordinate validation. Depends only on shared-types.","estimated_files":5},{"title":"Phase 4: Policy module","rationale":"LeavePolicy defines allowances per leave type and fiscal year. Balance initialization and leave validation both depend on policy. Depends only on shared-types.","estimated_files":5},{"title":"Phase 5: Balance module","rationale":"LeaveBalance tracks per-employee, per-type, per-fiscal-year balances. Depends on policy (for default allowances), audit (GP-002), and shared-types.","estimated_files":5},{"title":"Phase 6: Notification module","rationale":"Lightweight module for notifying employees and managers on leave events. Depends only on shared-types.","estimated_files":2},{"title":"Phase 7: Leave model and repository","rationale":"LeaveRequest model and its repository layer. Depends on shared-types for LeaveType and LeaveStatus enums.","estimated_files":4},{"title":"Phase 8: Leave service","rationale":"Core business logic: submit, approve, reject, cancel. Orchestrates balance, policy, employee, notification, and audit. Depends on all prior modules.","estimated_files":2},{"title":"Phase 9: Leave controller and routes","rationale":"Fastify route handlers with RBAC guards and input validation. Exposes the REST API surface for the leave module.","estimated_files":2}],"architecture_md_update":"## Leave Management Module — Reconciled Architecture\n\n### Domain Entities\n\n- **LeaveType** — enum: `annual`, `sick`, `emergency`, `unpaid`, `maternity`, `paternity` (feature scope: annual, sick, emergency).\n- **LeaveStatus** — lifecycle enum: `DRAFT`, `SUBMITTED`, `APPROVED`, `REJECTED`, `CANCELLED`.\n- **LeaveRequest** — employee application; tracks status transitions with actor/timestamp fields (`approvedBy`, `rejectedBy`, `cancelledBy`).\n- **LeavePolicy** — rules per leave type: entitlement days, notice period, manager approval flag, accrual caps, active flag.\n- **LeaveBalance** — per-employee, per-policy, per-fiscal-year balance: `totalEntitlement`, `usedDays`, `remainingDays`, status (`ACTIVE`, `EXHAUSTED`, `CLOSED`).\n- **Employee** — organisational employee with `managerId` self-reference; `employmentStatus` (`ACTIVE`, `INACTIVE`, `TERMINATED`) controls eligibility.\n- **AuditLog** — immutable record of state changes (GP-002).\n\n### Business Rules (Binding)\n\n1. Day counting is inclusive calendar days: `(endDate - startDate + 1)`. No exclusion of weekends/holidays.\n2. Only ACTIVE employees can submit leave.\n3. Submission validates `minimumNoticeDays` from policy.\n4. Submission checks `remainingDays >= requested days` for the fiscal year (calendar year of `startDate`).\n5. On APPROVED, balance is deducted atomically (`usedDays += days`, `remainingDays -= days`).\n6. On CANCELLED (from APPROVED), balance is restored atomically.\n7. Only the employee's direct manager (`Employee.managerId`) may approve/reject; self-approval forbidden.\n8. When `requiresManagerApproval` is false, request auto-approves (DRAFT → APPROVED, skipping SUBMITTED).\n9. `endDate >= startDate` enforced.\n10. Fiscal year = calendar year of `startDate`.\n\n### Conceptual Tables\n\n- **employees** — `id`, `employee_number`, `first_name`, `last_name`, `email` (unique), `role`, `manager_id` (FK self), `department`, `hire_date`, `termination_date`, `employment_status`, timestamps, soft-delete.\n- **leave_policies** — `id`, `policy_name`, `leave_type`, `entitlement_days`, `accrual_rate`, `max_accumulation`, `minimum_notice_days`, `requires_manager_approval`, `is_active`, `is_paid`, timestamps.\n- **leave_requests** — `id`, `employee_id` (FK), `leave_type_id` (FK), `start_date`, `end_date`, `status`, `reason`, `approved_by` (FK), `approved_at`, `rejected_by` (FK), `rejected_at`, `cancelled_by` (FK), `cancelled_at`, timestamps.\n- **leave_balances** — `id`, `employee_id` (FK), `policy_id` (FK), `fiscal_year`, `total_entitlement`, `used_days`, `remaining_days`, `status`, timestamps. Unique composite `(employee_id, policy_id, fiscal_year)`.\n- **audit_logs** — `id`, `entity_type`, `entity_id`, `action`, `changed_by` (FK), `changes` (JSON), `created_at`.\n\n### Modules & Dependencies\n\n| Module | Path | Key Contents |\n|--------|------|-------------|\n| shared-types | `src/shared/types/` | `LeaveType`, `LeaveStatus` enums |\n| employee | `src/modules/employee/` | Employee model, repo, service |\n| policy | `src/modules/policy/` | LeavePolicy model, repo, service |\n| balance | `src/modules/balance/` | LeaveBalance model, repo, service |\n| leave | `src/modules/leave/` | LeaveRequest model, repo, service, controller, routes |\n| notification | `src/modules/notification/` | Notification service |\n| audit | `src/modules/audit/` | AuditLog model, repo, service |\n\nDependencies flow inward: `leave` → `balance`, `policy`, `employee`, `notification`, `audit`, `shared-types`; `balance` → `policy`, `audit`, `shared-types`; `policy` → `shared-types`; `employee` → `shared-types`.\n\n### Cross-Cutting Contracts\n\n- **Auth**: `request.user: { id: string; role: UserRole }` where `UserRole = 'employee' | 'manager' | 'hr_admin'`. JWT verified by Fastify hook; RBAC via `requireRole(...)` guard.\n- **Transaction**: Multi-step writes (approve/reject/cancel) use an optional `client: PoolClient` parameter on repository methods (`updateStatus`, `deductDays`, `upsert`, `create`). Service acquires client, `BEGIN`, passes to repos, `COMMIT`/`ROLLBACK`.\n- **Error Response**: `{ error: string; code: string }`. 400 validation, 401 auth, 403 forbidden, 404 not found, 500 internal.\n\n### Recommended Build Phases\n\n1. Shared types (enums)\n2. Audit module\n3. Employee module\n4. Policy module\n5. Balance module\n6. Notification module\n7. Leave model & repository\n8. Leave service\n9. Leave controller & routes\n\n### Open Questions\n\n1. Fiscal year configurability (calendar vs custom).\n2. Overlap detection policy (none, SUBMITTED+APPROVED, APPROVED only).\n3. Cross-year balance splitting (single year vs split).\n4. Balance initialisation strategy (eager vs lazy).\n5. Balance deduction timing (on approval per domain rule, but two-phase alternatives exist).\n","sql_schemas":["table employees | fields: id, employee_number, first_name, last_name, email, role, manager_id, department, hire_date, termination_date, employment_status, created_at, updated_at, deleted_at | pk: id | fks: manager_id -> employees.id | indexes: email: unique lookup for authentication and employee search; role: filter employees by role for RBAC and manager assignment; manager_id: find direct reports for a given manager","table leave_policies | fields: id, policy_name, leave_type, entitlement_days, accrual_rate, max_accumulation, minimum_notice_days, requires_manager_approval, is_active, is_paid, created_at, updated_at | pk: id | indexes: leave_type: unique active policy lookup per leave type (application-enforced unique where is_active = true)","table leave_requests | fields: id, employee_id, leave_type_id, start_date, end_date, status, reason, approved_by, approved_at, rejected_by, rejected_at, cancelled_by, cancelled_at, created_at, updated_at | pk: id | fks: employee_id -> employees.id; leave_type_id -> leave_policies.id; approved_by -> employees.id; rejected_by -> employees.id; cancelled_by -> employees.id | indexes: employee_id: look up all leave requests for an employee; status: filter by status for pending-approval queues; approved_by: find requests pending review by a specific manager (combined with status); leave_type_id: filter requests by leave type; (employee_id, status): common composite lookup for an employee's pending/approved requests; (approved_by, status): manager's pending review queue","table leave_balances | fields: id, employee_id, policy_id, fiscal_year, total_entitlement, used_days, remaining_days, status, created_at, updated_at | pk: id | fks: employee_id -> employees.id; policy_id -> leave_policies.id | indexes: (employee_id, policy_id, fiscal_year): unique composite for balance lookup per employee/policy/year; employee_id: all balances for an employee; policy_id: aggregate balance queries by policy","table audit_logs | fields: id, entity_type, entity_id, action, changed_by, changes, created_at | pk: id | fks: changed_by -> employees.id | indexes: (entity_type, entity_id): look up audit trail for a specific entity; changed_by: audit trail by user; created_at: time-range audit queries"],"open_questions":[],"business_rules":["Business policy answers:\n1. Fiscal/leave year: calendar year (Jan 1 to Dec 31), keyed off the leave startDate.\n2. Overlapping leave requests: PREVENTED. Reject a new SUBMITTED or APPROVED request whose date range overlaps (inclusive) any existing SUBMITTED or APPROVED request for the same employee. Adjacent non-overlapping ranges are allowed.\n3. Cross-fiscal-year request: deduct the whole request from a single fiscal year (the fiscal year of startDate). Do not split across years.\n4. Balance initialization: auto-create balances for ALL leave types on employee creation (not lazily).\n5 & 6. Balance deduction timing: deduct on APPROVAL (finalize). On submission the days are counted as PENDING (a reservation, not a deduction); on approval move pending to used; on reject or cancel release the reservation. Available balance = entitled - (used + pending).\n\nStandard rules that apply throughout:\n- Count business days only (exclude weekends); whole days only.\n- Separate emergency leave pool from annual/sick.\n- Every endpoint enforces RBAC (employees act on their own records; managers approve/reject their reports) plus input validation.\n- When an employee has no manager, approval escalates to HR.\n- Use-it-or-lose-it: no carryover of unused balance across leave years. [BINDING RULE — operator decision resolving: Should the fiscal year be calendar year (Jan 1 – Dec 31) or a configurable period (e.g., Apr 1 – Mar 31)? The current domain rule uses calendar year of startDate.; Should overlapping leave requests be prevented? E.g., an employee with an existing APPROVED or SUBMITTED request for Jan 5–10 should not be allowed to submit another for Jan 8–12.; When a leave request spans two fiscal years (e.g., Dec 28 – Jan 3), should the balance be deducted from a single fiscal year (the year of startDate) or split across both years?; How are leave balances initialized for new employees — auto-created for all leave types on employee creation, or created lazily on first leave application?; When should leave balance be deducted — on submission (reserve/hold) or on approval (finalize)? The domain rule already specifies deduction on approval, but the application architect raised this as an open question.; When should leave balance be deducted — on submission (reserve/hold) or on approval (finalize)?; apply everywhere these apply, not in one place only]"],"auth_contract":"request.user: { id: string; role: UserRole } where UserRole = 'employee' | 'manager' | 'hr_admin'. JWT bearer token verified by an auth middleware (fastify hook) that populates request.user. RBAC enforced by a requireRole(...roles) route-level guard, never inline in handlers.","transaction_contract":"Multi-step writes (approve/reject/cancel a leave request) require atomicity across leave_requests, leave_balances, and audit_logs. Repository methods that must join a caller's transaction accept an optional `client: PoolClient` parameter. When `client` is provided, the method uses it for all queries; when omitted, the method uses the shared pool directly. The calling service owns the unit of work: it acquires a client from the pool, executes `BEGIN`, passes the client to each participating repository call, then executes `COMMIT` or `ROLLBACK` on success/failure. This contract applies to `ILeaveRequestRepository.updateStatus`, `ILeaveBalanceRepository.deductDays`, `ILeaveBalanceRepository.upsert`, and `IAuditLogRepository.create`.","error_response_contract":"Standard error shape: { error: string; code: string }. Validation failure → HTTP 400. Authentication failure → 401. Authorization failure → 403. Not found → 404. Internal server error → 500."}