{"domain_entities":[{"name":"LeaveRequest","attributes":["id: string","employeeId: string","leaveTypeCode: LeaveTypeCode","startDate: string (ISO 8601)","endDate: string (ISO 8601)","requestedDays: number","reason: string | null","status: LeaveStatus","approverId: string | null","approvalComment: string | null","submittedAt: string | null","decidedAt: string | null","cancelledBy: string | null","cancelledAt: string | null"],"purpose":"The leave request an employee creates and acts on; its status and the signed-in user's role jointly determine which actions are available in the web app."},{"name":"Employee","attributes":["id: string","role: EmployeeRole (EMPLOYEE | MANAGER | ADMIN)","managerId: string | null"],"purpose":"The signed-in user's identity and role, sourced from GET /employees/me, used to decide which leave actions to render. Never derived from decoding the JWT in the browser."},{"name":"LeaveType","attributes":["code: LeaveTypeCode","name: string"],"purpose":"The catalog of leave types selectable in the request-leave form; the chosen code is submitted with the request."}],"modules":[{"name":"shared-types","path":"web/src/shared/types/","owns":["CreateLeaveRequestInput DTO (leaveTypeCode, startDate, endDate as ISO strings)","LeaveTypeCode enum (existing)","LeaveStatus enum (existing)","EmployeeRole enum (existing)","LeaveRequestView (existing)","EmployeeProfile (existing)"]},{"name":"api-client","path":"web/src/infrastructure/api/","owns":["IApiClient interface (extended with 5 write methods)","ApiClient implementation (request<T> bearer-token + ApiError mapping)","ApiError","ITokenStorage / TokenStorage"]},{"name":"leave","path":"web/src/modules/leave/","owns":["ILeaveService (extended with create/submit/approve/reject/cancel)","LeaveService implementation","getAvailableLeaveActions pure helper (status+role action matrix)","validateLeaveRequestInput pure helper (client-side pre-send validation)","IBalanceService / BalanceService (existing)"]},{"name":"employee","path":"web/src/modules/employee/","owns":["IEmployeeService (getMe)","EmployeeService implementation"]},{"name":"presentation-pages","path":"web/src/presentation/pages/","owns":["RequestLeavePage (new form)","LeaveDetailPage (modified: actions + role awareness)","LeaveListPage (modified: link to request form)","DashboardPage (existing)"]},{"name":"presentation-guards","path":"web/src/presentation/guards/","owns":["RequireAuth (existing)"]}],"dependency_map":[{"from_":"leave","to":"api-client"},{"from_":"leave","to":"shared-types"},{"from_":"employee","to":"api-client"},{"from_":"employee","to":"shared-types"},{"from_":"api-client","to":"shared-types"},{"from_":"presentation-pages","to":"leave"},{"from_":"presentation-pages","to":"employee"},{"from_":"presentation-pages","to":"shared-types"},{"from_":"presentation-pages","to":"presentation-guards"}],"recommended_phases":[{"title":"Phase 1 — Extend IApiClient + ApiClient with write endpoints","rationale":"Foundation for everything else. Add createLeave/submitLeave/approveLeave/rejectLeave/cancelLeave to IApiClient and ApiClient using the existing request<T> pattern (bearer token, ApiError mapping, ISO-string dates). No other module can proceed until the transport supports writes.","estimated_files":2},{"title":"Phase 2 — Extend ILeaveService + LeaveService with write methods","rationale":"Thin delegation layer over the API client. Add create/submit/approve/reject/cancel plus the pure getAvailableLeaveActions and validateLeaveRequestInput helpers. Depends only on Phase 1 and shared-types.","estimated_files":2},{"title":"Phase 3 — RequestLeavePage form + route wiring","rationale":"New form page capturing leave type, start/end dates; client-side validation (both dates required, end >= start, type chosen); surfaces API error message on rejection; navigates to detail on success. Depends on Phase 2.","estimated_files":3},{"title":"Phase 4 — LeaveDetailPage actions + role awareness","rationale":"Render submit/cancel (owner) and approve/reject (manager) only when available per status+role; refresh status after action without reload; forbidden actions never rendered. Depends on Phase 2 and employee profile.","estimated_files":2},{"title":"Phase 5 — LeaveListPage link + composition root wiring","rationale":"Add 'Request leave' link from list; wire new page into App routes and main.tsx. Depends on Phases 3-4.","estimated_files":3}],"architecture_md_update":"## Feature: Leave workflow in web app (request, submit, approve, reject, cancel)\n\n### Scope\nClient-side only under `web/`. No changes to backend or `src/`. Consumes existing backend endpoints: POST /leaves, POST /leaves/:id/submit, POST /leaves/:id/approve, POST /leaves/:id/reject, POST /leaves/:id/cancel.\n\n### Domain entities\n- **LeaveRequest** — lifecycle: DRAFT → SUBMITTED → APPROVED | REJECTED, plus CANCELLED. Attributes: id, employeeId, leaveTypeCode, startDate (ISO string), endDate (ISO string), requestedDays, reason, status, approverId, approvalComment, submittedAt, decidedAt, cancelledBy, cancelledAt.\n- **Employee** — role awareness only; lifecycle: ACTIVE, TERMINATED, ON_LEAVE. Attributes: id, role (EMPLOYEE | MANAGER | ADMIN), managerId.\n- **LeaveType** — static catalog; no lifecycle. Attributes: code, name.\n\n### Conceptual table specifications\nNone. No new persistence in `web/`; existing backend tables (`leave_requests`, `leave_balances`, `audit_logs`, `notifications`, `employees`, `leave_types`, `leave_policies`) are unchanged and out of scope.\n\n### Repository interfaces and concrete implementations\nNone in `web/`. Transport is `IApiClient`/`ApiClient`; no database repositories are introduced.\n\n### Module boundaries\n- `shared-types` — `web/src/shared/types/` — CreateLeaveRequestInput DTO, LeaveTypeCode, LeaveStatus, EmployeeRole, LeaveRequestView, EmployeeProfile.\n- `api-client` — `web/src/infrastructure/api/` — IApiClient (extended with createLeave/submitLeave/approveLeave/rejectLeave/cancelLeave), ApiClient, ApiError, ITokenStorage/TokenStorage.\n- `leave` — `web/src/modules/leave/` — ILeaveService (extended with create/submit/approve/reject/cancel), LeaveService, getAvailableLeaveActions, validateLeaveRequestInput, IBalanceService/BalanceService.\n- `employee` — `web/src/modules/employee/` — IEmployeeService.getMe(), EmployeeService.\n- `presentation-pages` — `web/src/presentation/pages/` — RequestLeavePage, LeaveDetailPage, LeaveListPage, DashboardPage.\n- `presentation-guards` — `web/src/presentation/guards/` — RequireAuth.\n\n### Dependency map\n- leave → api-client, shared-types\n- employee → api-client, shared-types\n- api-client → shared-types\n- presentation-pages → leave, employee, shared-types, presentation-guards\n\n### Cross-cutting contracts\n- **Auth**: Existing session contract reused; no new API surface. AuthSession { token, profile: EmployeeProfile, status } held in AuthContext. Identity/role come from profile.role (EmployeeRole = 'EMPLOYEE' | 'MANAGER' | 'ADMIN') fetched via GET /employees/me; JWT is never decoded in the browser. ApiClient attaches bearer token from ITokenStorage and clears it on 401. Route-level access enforced by RequireAuth; action-level visibility enforced by getAvailableLeaveActions(status, role, isOwner) in pages — forbidden actions are never rendered.\n- **Error/response**: ApiError { error: string; code: string } mapped from non-2xx bodies. Validation failure → 400; authentication → 401; authorization → 403; not found → 404; conflict → 409. RequestLeavePage and LeaveDetailPage display ApiError.message (the backend's own error text, e.g. insufficient balance) rather than a generic failure. Client-side validation failures (missing dates, end < start, no type) are caught before any network call and shown inline.\n- **Transaction**: None. Each write action is a single HTTP call to an existing backend endpoint; atomicity is owned by the backend's IUnitOfWork/PgUnitOfWork contract, out of scope for this web/ change.\n\n### Lifecycle states\n- LeaveRequest: DRAFT, SUBMITTED, APPROVED, REJECTED, CANCELLED\n- Employee: ACTIVE, TERMINATED, ON_LEAVE\n- LeaveType: static (no lifecycle)\n\n### Business rules\n- Dates cross the wire as ISO strings, never Date objects; the web client serializes startDate/endDate as ISO strings and parses server responses back to strings for display.\n- requestedDays = endDate - startDate + 1 inclusive calendar days, whole-day UTC, no weekend/holiday exclusion; never re-derived client-side.\n- A leave request may be created only when endDate is not before startDate, both dates are present, and a leave type is chosen; validated client-side before any network call.\n- Action availability is a pure function of (request.status, actor.role, actor.id vs request.employeeId):\n  - Owner (request.employeeId === profile.id): submit available when status DRAFT; cancel available when status DRAFT or SUBMITTED.\n  - Manager/Admin (profile.role === 'MANAGER' || 'ADMIN'): approve/reject available when status SUBMITTED. The backend enforces the direct-manager rule; the client renders based on role+status and surfaces any 403 as an error.\n  - Forbidden actions are never rendered.\n- Role awareness comes exclusively from GET /employees/me; the JWT is never decoded in the browser.\n- When the backend rejects a create/action, the API's own error message is surfaced verbatim (e.g. insufficient balance), never replaced by a generic failure.\n- After a successful action, the leave detail page re-fetches the request and reflects the new status without a manual reload.\n\n### Recommended phases\n1. **Phase 1 — Extend IApiClient + ApiClient with write endpoints** (2 files): Add createLeave/submitLeave/approveLeave/rejectLeave/cancelLeave using existing request<T> pattern (bearer token, ApiError mapping, ISO-string dates).\n2. **Phase 2 — Extend ILeaveService + LeaveService with write methods** (2 files): Add create/submit/approve/reject/cancel plus pure getAvailableLeaveActions and validateLeaveRequestInput helpers.\n3. **Phase 3 — RequestLeavePage form + route wiring** (3 files): New form capturing leave type, start/end dates; client-side validation; surfaces API error on rejection; navigates to detail on success.\n4. **Phase 4 — LeaveDetailPage actions + role awareness** (2 files): Render submit/cancel (owner) and approve/reject (manager) only when available; refresh status after action without reload; forbidden actions never rendered.\n5. **Phase 5 — LeaveListPage link + composition root wiring** (3 files): Add 'Request leave' link; wire new page into App routes and main.tsx.\n\n### Stack compliance note\nWeb tests use Vitest (existing web test runner; required by feature done criteria `vitest run`). The declared project stack lists Jest for backend tests; this web-only feature does not alter backend test tooling.\n\n### Open questions\nSee openQuestions field.","sql_schemas":null,"open_questions":[],"business_rules":["1. Client validates ONLY the three rules in the spec: both dates required, end >= start compared as calendar dates, and a leave type chosen. It must NOT mirror min-notice or max-duration. Those are backend policy, they are configurable per leave type, and a second copy in the browser is a copy that will drift the first time the policy changes — the failure would be a form that silently blocks a request the backend would have allowed, which is worse than a round trip. When the backend rejects for notice or duration, the form surfaces the API's own error message, which is exactly the path the spec already asks for. So validateLeaveRequestInput stays to the three rules, takes no policy dependency, and fetches nothing.\n\n2. No comment input. approve and reject are comment-less, matching the current backend contract, which takes no body — the web-only scope of this feature is a hard boundary and adding the field would require a backend change. A captured-but-unsent input is worse than none: it tells the manager their words were recorded when they were discarded.\n\n   One consequence to handle rather than ignore: since approvalComment is therefore always null, the leave detail page must NOT render an approval-comment row showing a dash or an empty value. Omit the row entirely when there is no comment. A field that is always blank reads as a defect to anyone using the app, and it would be the only visible trace of a decision we made deliberately.\n\nNothing else in the spec changes. [BINDING RULE — operator decision resolving: Should the client-side 'end date not before start date' validation compare calendar dates only, or also enforce the backend's min-notice / max-duration rules before sending?; For the approve/reject action, should the manager be able to enter an approval comment, and if so is it sent to the backend?; Should the client-side \"end date not before start date\" validation compare calendar dates only, or also enforce the backend's min-notice / max-duration rules before sending?; apply everywhere these apply, not in one place only]"],"auth_contract":"Existing session contract reused; no new API surface. AuthSession { token: string; profile: EmployeeProfile; status: 'authenticated' | 'unauthenticated' } held in AuthContext. Identity/role come from profile.role: EmployeeRole = 'EMPLOYEE' | 'MANAGER' | 'ADMIN', fetched via GET /employees/me; JWT is never decoded in the browser. ApiClient attaches bearer token from ITokenStorage and clears it on 401. Route-level access enforced by RequireAuth; action-level visibility enforced by getAvailableLeaveActions(status, role, isOwner) in pages — forbidden actions are never rendered.","transaction_contract":"","error_response_contract":"ApiError { error: string; code: string } mapped from non-2xx bodies. Validation failure → 400; authentication → 401; authorization → 403; not found → 404; conflict → 409. RequestLeavePage and LeaveDetailPage display ApiError.message (the backend's own error text, e.g. insufficient balance) rather than a generic failure. Client-side validation failures (missing dates, end < start, no type) are caught before any network call and shown inline."}