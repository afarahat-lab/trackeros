{"domain_entities":[{"name":"LeaveRequest","attributes":["id: string","employeeId: string","policyId: string","startDate: Date","endDate: Date","reason: string | undefined","status: LeaveRequestStatus","approvedBy: string | null","approvedAt: Date | null","createdAt: Date","updatedAt: Date"],"purpose":"Represents an employee's application for leave. Tracks the full lifecycle from draft through submission, manager decision, and potential cancellation. The status field drives all workflow gating."},{"name":"LeaveRequestTransitions","attributes":[],"purpose":"Documents every valid state transition for LeaveRequest. Each transition is a named operation that the domain service must enforce. No transition outside this set is valid."},{"name":"LeaveBalance","attributes":["id: string","employeeId: string","policyId: string","totalEntitlement: number","usedDays: number","remainingDays: number","fiscalYear: number","status: 'ACTIVE' | 'EXHAUSTED' | 'CLOSED'","createdAt: Date","updatedAt: Date"],"purpose":"Tracks an employee's leave entitlement, consumption, and remaining balance for a specific leave policy within a fiscal year. Drives sufficiency checks during leave application and is mutated on approval and cancellation."},{"name":"LeavePolicy","attributes":["id: string","policyName: string","leaveType: 'annual' | 'sick' | 'emergency' | 'unpaid' | 'maternity' | 'paternity'","entitlementDays: number","accrualRate: number | undefined","maxAccumulation: number | undefined","minimumNoticeDays: number | undefined","requiresManagerApproval: boolean","isActive: boolean","createdAt: Date","updatedAt: Date"],"purpose":"Defines the rules and entitlements for a category of leave. Drives balance initialisation, sufficiency checks, notice-period validation, and whether manager approval is required. One policy per leave type per organisation context."},{"name":"Employee","attributes":["id: string","employeeNumber: string","firstName: string","lastName: string","email: string","managerId: string | null","department: string | null","hireDate: Date","terminationDate: Date | null","employmentStatus: 'ACTIVE' | 'INACTIVE' | 'TERMINATED'","createdAt: Date","updatedAt: Date","deletedAt: Date | null"],"purpose":"Represents an employee in the organisation. The employmentStatus gates eligibility for leave applications. The managerId establishes the approval chain for submitted leave requests."}],"modules":[{"name":"shared-types","path":"src/shared/types/","owns":["LeaveType enum (annual | sick | emergency | unpaid | maternity | paternity)","LeaveStatus enum (DRAFT | SUBMITTED | APPROVED | REJECTED | CANCELLED)","BaseEntity interface (id, createdAt, updatedAt)","UserRole enum (employee | manager | hr_admin)"]},{"name":"employee","path":"src/modules/employee/","owns":["Employee entity","EmployeeRepository interface","EmployeeService interface + implementation"]},{"name":"policy","path":"src/modules/policy/","owns":["LeavePolicy entity","PolicyRepository interface","PolicyService interface + implementation","Policy validation rules (entitlement, notice period, accrual caps)"]},{"name":"balance","path":"src/modules/balance/","owns":["LeaveBalance entity","BalanceRepository interface","BalanceService interface + implementation","Balance calculation rules (entitlement, accrual, deduction, carry-over)"]},{"name":"audit","path":"src/modules/audit/","owns":["AuditRecord entity (id, entityType, entityId, action, oldValues, newValues, performedBy, performedAt, createdAt)","AuditRepository interface","AuditService interface + implementation","Audit action enum (CREATE | UPDATE | DELETE | APPROVE | REJECT)"]},{"name":"leave","path":"src/modules/leave/","owns":["LeaveRequest entity","CreateLeaveRequestDto","UpdateLeaveRequestDto","LeaveRequestQueryParams","LeaveRequestRepository interface","LeaveService interface + implementation","LeaveRequest validation rules (date ordering, overlap detection, minimum notice, balance sufficiency)","LeaveRequest controller + routes"]}],"dependency_map":[{"from_":"src/modules/leave/","to":"src/shared/types/"},{"from_":"src/modules/leave/","to":"src/modules/employee/"},{"from_":"src/modules/leave/","to":"src/modules/policy/"},{"from_":"src/modules/leave/","to":"src/modules/balance/"},{"from_":"src/modules/leave/","to":"src/modules/audit/"},{"from_":"src/modules/balance/","to":"src/shared/types/"},{"from_":"src/modules/balance/","to":"src/modules/policy/"},{"from_":"src/modules/policy/","to":"src/shared/types/"},{"from_":"src/modules/employee/","to":"src/shared/types/"},{"from_":"src/modules/audit/","to":"src/shared/types/"}],"recommended_phases":[{"title":"Phase 1: Shared types + Employee module","rationale":"Foundation layer. LeaveType and LeaveStatus enums in shared/types are needed by every other module. Employee module has zero dependencies on other domain modules and is needed by leave for manager lookups and active-status checks.","estimated_files":5},{"title":"Phase 2: Policy module","rationale":"Depends only on shared/types. Defines leave policy rules (entitlement, accrual, notice periods). Balance module needs policy to initialize balances; leave-request needs policy for validation (minimum notice, requiresManagerApproval).","estimated_files":5},{"title":"Phase 3: Balance module","rationale":"Depends on shared/types and policy. Owns balance lifecycle: initialization from policy, deduction on approval, restoration on cancellation/rejection. Must exist before leave-request can enforce balance-sufficiency checks.","estimated_files":5},{"title":"Phase 4: Audit module","rationale":"Depends only on shared/types. Records all state-changing operations per GP-002. Must exist before leave-request so approval/rejection/submission actions can write audit records.","estimated_files":4},{"title":"Phase 5: Leave module (core)","rationale":"Depends on all prior modules: shared/types, employee, policy, balance, audit. Implements the core leave-request workflow: submit, approve, reject, cancel, list, calendar. This is the largest phase — the leave service orchestrates policy validation, balance checks, employee lookups, and audit recording.","estimated_files":5}],"architecture_md_update":"# Leave Management Module — Reconciled Architecture\n\n## Overview\nModular monolith built with TypeScript, Fastify, React Native, PostgreSQL. The leave management module enables employees to apply for annual, sick, and emergency leave; managers to approve or reject; and the system to track leave balances.\n\n## Domain Entities\n\n### LeaveRequest\nRepresents an employee's leave application. Lifecycle: DRAFT → SUBMITTED → {APPROVED | REJECTED}; SUBMITTED → DRAFT (withdraw); APPROVED → CANCELLED; DRAFT → CANCELLED. Terminal states: REJECTED, CANCELLED.\n\n### LeaveRequestTransitions\nValid transitions: DRAFT→SUBMITTED (submit), SUBMITTED→APPROVED (approve), SUBMITTED→REJECTED (reject), SUBMITTED→DRAFT (withdraw), APPROVED→CANCELLED (cancel), DRAFT→CANCELLED (cancelDraft).\n\n### LeaveBalance\nTracks entitlement per policy per fiscal year. Lifecycle: ACTIVE → EXHAUSTED (remainingDays=0); EXHAUSTED → ACTIVE (restored); ACTIVE/EXHAUSTED → CLOSED (fiscal year end).\n\n### LeavePolicy\nDefines rules for a leave type. Lifecycle: ACTIVE → INACTIVE → ARCHIVED; INACTIVE → ACTIVE.\n\n### Employee\nOrganisation member. Lifecycle: ACTIVE → INACTIVE → TERMINATED; INACTIVE → ACTIVE.\n\n## Business Rules (binding)\n- Day count: calendar days inclusive, `(endDate - startDate) + 1`.\n- Only ACTIVE employees may submit; only ACTIVE policies may be used.\n- startDate ≥ today; endDate ≥ startDate.\n- Minimum notice: `(startDate - today) >= policy.minimumNoticeDays` (if defined).\n- Approval: balance sufficiency check (`remainingDays >= daysRequested`), fiscal year derived from startDate (default January).\n- On approval: `usedDays += daysRequested`, `remainingDays -= daysRequested`; if remainingDays=0 → EXHAUSTED.\n- On cancel from APPROVED: reverse deduction; if EXHAUSTED → ACTIVE.\n- Manager authorisation: only employee's manager (or HR admin if no manager).\n- Auto-approval when `policy.requiresManagerApproval === false`.\n- One balance record per employee per policy per fiscal year; cross-fiscal-year requests rejected.\n- Employee termination cascades: cancel all SUBMITTED and future APPROVED requests, restore balances.\n\n## Conceptual Data Model\n\n### employees\nFields: id, employee_number, first_name, last_name, email, manager_id, department, hire_date, termination_date, employment_status, created_at, updated_at, deleted_at. PK: id. FK: manager_id → employees.id. Indexes: employee_number (unique), manager_id, employment_status.\n\n### leave_policies\nFields: id, policy_name, leave_type, entitlement_days, accrual_rate, max_accumulation, minimum_notice_days, requires_manager_approval, is_active, created_at, updated_at. PK: id. Indexes: leave_type, is_active.\n\n### leave_balances\nFields: id, employee_id, policy_id, fiscal_year, total_entitlement, used_days, remaining_days, status, created_at, updated_at. PK: id. FK: employee_id → employees.id, policy_id → leave_policies.id. Unique: (employee_id, policy_id, fiscal_year). Indexes: employee_id, policy_id.\n\n### leave_requests\nFields: id, employee_id, policy_id, start_date, end_date, reason, status, approved_by, approved_at, created_at, updated_at. PK: id. FK: employee_id → employees.id, policy_id → leave_policies.id, approved_by → employees.id. Indexes: employee_id, status, policy_id, start_date/end_date, (employee_id, status).\n\n### audit_logs\nFields: id, entity_type, entity_id, action, old_values, new_values, performed_by, performed_at, created_at. PK: id. FK: performed_by → employees.id. Indexes: performed_by, action, created_at, (entity_type, entity_id).\n\n## Modules & Dependencies\n- **shared-types** (`src/shared/types/`): enums, base interface, UserRole.\n- **employee** (`src/modules/employee/`): Employee entity, repository, service.\n- **policy** (`src/modules/policy/`): LeavePolicy entity, repository, service.\n- **balance** (`src/modules/balance/`): LeaveBalance entity, repository, service.\n- **audit** (`src/modules/audit/`): AuditRecord entity, repository, service.\n- **leave** (`src/modules/leave/`): LeaveRequest entity, repository, service, controller, routes.\n\nDependencies flow inward: leave → {employee, policy, balance, audit} → shared-types. No circular dependencies.\n\n## Phased Build Order\n1. Shared types + Employee module\n2. Policy module\n3. Balance module\n4. Audit module\n5. Leave module (core)\n\n## Cross-Cutting Contracts\n\n### Auth\n- Identity: `request.user: { id: string; role: UserRole }` (JWT bearer token).\n- Roles: `employee`, `manager`, `hr_admin`.\n- RBAC enforced via `requireRole(...)` Fastify route guard.\n- Endpoint access: submit/cancel → employee; approve/reject/listTeam → manager; policy CRUD → hr_admin; listOwn → employee.\n\n### Error Response\n- Shape: `{ error: string; code: string; details?: unknown }`.\n- 400 VALIDATION_ERROR, 401 UNAUTHORIZED, 403 FORBIDDEN, 404 NOT_FOUND, 409 CONFLICT.\n\n### Transaction\n- Repository methods that participate in multi-step writes accept optional `client?: PoolClient`.\n- Service owns unit of work: acquires client, BEGIN, passes client to repositories, COMMIT/ROLLBACK.\n\n## Open Questions\n1. Fiscal year start month (January vs configurable).\n2. Overlapping leave request policy (reject vs allow).\n3. Half-day leave support (full-day only vs flags vs DateTime).\n4. RemainingDays computation (derived vs stored).\n5. Fiscal-year carry-over and accrual rules.\n","sql_schemas":["table employees | fields: id, employee_number, first_name, last_name, email, manager_id, department, hire_date, termination_date, employment_status, created_at, updated_at, deleted_at | pk: id | fks: manager_id -> employees.id | indexes: employee_number: unique lookup; manager_id: hierarchy queries; employment_status: filtering active/inactive","table leave_policies | fields: id, policy_name, leave_type, entitlement_days, accrual_rate, max_accumulation, minimum_notice_days, requires_manager_approval, is_active, created_at, updated_at | pk: id | indexes: leave_type: filtering by type; is_active: filtering active policies","table leave_balances | fields: id, employee_id, policy_id, fiscal_year, total_entitlement, used_days, remaining_days, status, created_at, updated_at | pk: id | fks: employee_id -> employees.id; policy_id -> leave_policies.id | indexes: (employee_id, policy_id, fiscal_year): unique constraint and primary access pattern; employee_id: lookups by employee; policy_id: aggregating by policy","table leave_requests | fields: id, employee_id, policy_id, start_date, end_date, reason, status, approved_by, approved_at, created_at, updated_at | pk: id | fks: employee_id -> employees.id; policy_id -> leave_policies.id; approved_by -> employees.id | indexes: employee_id: lookups by employee; status: filtering by status; policy_id: filtering by leave type; start_date, end_date: range queries for overlapping detection; (employee_id, status): compound for 'my pending requests'","table audit_logs | fields: id, entity_type, entity_id, action, old_values, new_values, performed_by, performed_at, created_at | pk: id | fks: performed_by -> employees.id | indexes: performed_by: lookups by user; action: filtering by action type; created_at: time-range queries; (entity_type, entity_id): lookups for a specific entity's audit trail"],"open_questions":[],"business_rules":["CONSOLIDATED DECISION — binding for every phase of this feature.\n\n1 — THERE IS NO FISCAL YEAR. The leave year IS the CALENDAR year: 1 January to 31 December.\nBalances reset on 1 January. Do NOT implement a configurable fiscal-year start month and do\nNOT implement employee-anniversary years (the latter would additionally require hire dates\nthis feature does not own). Store the year as a plain integer (e.g. 2026) on the balance\nrecord and derive it from the leave request's startDate. A request whose range crosses\n31 December is charged IN FULL to the year of its startDate — never split across two years.\n\n2 — NO overlapping APPROVED leave for the same employee. Enforce at APPROVAL time, not at\nsubmission: a submission-time check cannot account for other PENDING requests approved later,\nso approval is the only authoritative gate. Run the overlap check and the balance-sufficiency\ncheck at the SAME point in the approval path so both invariants hold together. Overlap = any\nintersection of the [startDate, endDate] range with an existing APPROVED request for that\nemployee, regardless of leave type.\n\n3 — NO half-day leave. Leave is counted in WHOLE days only; every day counter is an integer.\nDo not add a fraction, a half-day flag, or a start/end period (AM/PM) field. This is a\ndeliberate scope decision: half-days would make every counter fractional and would interact\nbadly with the inclusive day-count rule in (6).\n\n4 — remainingDays IS DERIVED, NEVER STORED. The balance record holds three non-negative\ninteger counters — entitlementDays, usedDays, pendingDays — and availability is always\ncomputed:\n    availableDays = entitlementDays - usedDays - pendingDays\nDo not add a remainingDays column. A stored counter updated on every deduction and\nrestoration is the classic drift defect: two code paths disagree and the balance silently\ngoes wrong. Expose it as a computed property/selector so every caller derives it identically.\n\nThe ONLY permitted writes to the counters:\n- SUBMIT a request for n days:  pendingDays += n   (reserve; usedDays unchanged)\n- APPROVE it:                   pendingDays -= n, usedDays += n   (availableDays unchanged —\n                                the days were already reserved at submit)\n- REJECT a PENDING request:     pendingDays -= n   (release)\n- CANCEL a PENDING request:     pendingDays -= n   (release)\n- CANCEL an APPROVED request:   usedDays -= n      (restore)\nNo counter may go negative; a transition that would take one below zero is an ERROR, not a\nclamp. The sufficiency check before approval is n <= availableDays.\n\n5 — NO CARRY-OVER AND NO ACCRUAL. The full annual entitlement is available from 1 January.\nUnused days simply EXPIRE at 31 December — they do not roll into the next year, and there is\nno monthly/pro-rata accrual, no maximum-carry-over cap, and no expiry-deadline logic. On\n1 January a fresh balance row is created for the new year with usedDays = 0 and\npendingDays = 0. Mid-year joiners receive the full entitlement (no proration).\n\n6 — LEAVE DAY COUNT = CALENDAR DAYS, INCLUSIVE OF BOTH ENDS:\ndays = endDate - startDate + 1. Do NOT exclude weekends. Do NOT exclude public holidays.\nThere is no holiday calendar in scope.\nImplement this EXACTLY ONCE as a single shared exported helper (e.g.\ncountLeaveDays(startDate, endDate)) in the leave domain module, and call that helper from\nEVERY site needing a day count: balance deduction on APPROVED, restoration on CANCELLED, the\nsufficiency check before approval, entitlement comparison, and minimumNoticeDays enforcement.\nNo call site may re-derive the count inline — inline re-derivation is the anti-pattern this\ndecision exists to prevent.\n\nSTANDING DECISIONS carried forward from earlier runs of this feature (unchanged):\n- Emergency leave has its OWN entitlement pool, separate from annual; default 5 days. Model\n  all types uniformly: annual, sick and emergency each get their own LeavePolicy entitlement\n  and their own LeaveBalance record. No special-casing of any type in the balance logic.\n- NO documentation requirement for sick leave. Do NOT add a PENDING_DOCUMENTATION state and\n  do NOT add any document-tracking entity. The LeaveRequest lifecycle is exactly:\n  PENDING -> APPROVED | REJECTED, and PENDING | APPROVED -> CANCELLED.\n- Every operation that changes a LeaveRequest status AND balance counters AND writes an audit\n  record must run all three through the SAME transaction client, in ONE transaction, so a\n  failure rolls back the whole thing. [BINDING RULE — operator decision resolving: What is the fiscal year start month? The domain currently assumes January (calendar year). Many organisations use April or July. This affects which fiscalYear a leave request maps to for balance lookups.; Should the system prevent overlapping leave requests for the same employee? If so, what states count as \"active\" for overlap detection (APPROVED only, or SUBMITTED + APPROVED)?; Should the system support half-day leave requests? The current model uses Date for startDate/endDate, implying full-day granularity.; How should remainingDays be computed — as a derived field (totalEntitlement - usedDays) or as a stored column that is updated on every deduction/restoration?; What are the fiscal-year boundary rules for balance carry-over and accrual? Should unused days carry over to the next fiscal year, and if so, up to what cap?; How are leave days counted — calendar days or business days (Mon–Fri excluding holidays)?; apply everywhere these apply, not in one place only]"],"auth_contract":"Authenticated request shape: request.user: { id: string; role: UserRole }. UserRole enum: 'employee' | 'manager' | 'hr_admin'. Identity obtained via JWT bearer token verified by an auth middleware that populates request.user. RBAC enforced by a requireRole(...) route guard at the Fastify route level, never inline in service methods. Leave endpoints: submit/cancel → employee; approve/reject/listTeam → manager; policy CRUD → hr_admin; listOwn → employee.","transaction_contract":"Repository methods that participate in multi-step writes (e.g., create leave request, update status, upsert balance, create audit record) accept an optional `client?: PoolClient` parameter. When `client` is provided, the method uses it for all queries within that call instead of the shared pool. The calling service owns the unit of work: it acquires a client from the pool, executes `BEGIN`, passes the client to every repository call in the operation, then executes `COMMIT` or `ROLLBACK`. When `client` is omitted, the repository method uses the shared pool directly (suitable for single-step reads or single-write operations).","error_response_contract":"Standard error shape: { error: string; code: string; details?: unknown }. Validation failure → HTTP 400 (code: VALIDATION_ERROR). Authentication failure → HTTP 401 (code: UNAUTHORIZED). Authorization failure → HTTP 403 (code: FORBIDDEN). Not found → HTTP 404 (code: NOT_FOUND). Conflict (e.g. overlapping leave, insufficient balance) → HTTP 409 (code: CONFLICT)."}