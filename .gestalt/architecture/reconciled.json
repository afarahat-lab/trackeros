{"domain_entities":[{"name":"LeaveRequest","attributes":["id: string","employeeId: string","leaveTypeId: string","startDate: Date","endDate: Date","reason: string | undefined","status: LeaveRequestStatus","approvedBy: string | null","approvedAt: Date | null","rejectedBy: string | null","rejectedAt: Date | null","rejectionReason: string | null","cancelledBy: string | null","cancelledAt: Date | null","createdAt: Date","updatedAt: Date"],"purpose":"Represents an employee's leave application. Tracks the full lifecycle from draft through submission, approval/rejection, and optional cancellation. References the employee, the leave type/policy, and the approving/rejecting manager."},{"name":"LeaveType","attributes":["value: 'annual' | 'sick' | 'emergency' | 'unpaid' | 'maternity' | 'paternity'"],"purpose":"Enumeration of leave categories. The feature focuses on annual, sick, and emergency, but the domain accommodates unpaid, maternity, and paternity for extensibility."},{"name":"LeavePolicy","attributes":["id: string","policyName: string","leaveType: LeaveType","entitlementDays: number","accrualRate: number | null","maxAccumulation: number | null","minimumNoticeDays: number | null","requiresManagerApproval: boolean","isActive: boolean","createdAt: Date","updatedAt: Date"],"purpose":"Defines the rules and entitlements for a specific leave type. Each policy governs how many days an employee is entitled to, whether manager approval is required, minimum notice periods, and accrual behaviour. One policy per leave type (annual, sick, emergency, etc.)."},{"name":"LeaveBalance","attributes":["id: string","employeeId: string","leaveTypeId: string","totalEntitlement: number","usedDays: number","remainingDays: number","fiscalYear: number","status: 'ACTIVE' | 'EXHAUSTED' | 'CLOSED'","createdAt: Date","updatedAt: Date"],"purpose":"Tracks an employee's leave entitlement, usage, and remaining balance for a specific leave type within a fiscal year. Deducted when leave is approved; restored when an approved leave is cancelled."}],"modules":[{"name":"shared-types","path":"src/shared/types/","owns":["LeaveType enum (annual, sick, emergency, unpaid, maternity, paternity)","LeaveStatus enum (DRAFT, SUBMITTED, APPROVED, REJECTED, CANCELLED)"]},{"name":"audit","path":"src/modules/audit/","owns":["AuditRecord model","IAuditService interface","AuditService implementation","IAuditLogRepository interface","PgAuditLogRepository","auditRoutes"]},{"name":"leave-policy","path":"src/modules/leave-policy/","owns":["LeavePolicy model","ILeavePolicyRepository interface","PgLeavePolicyRepository","ILeaveTypeRepository interface","PgLeaveTypeRepository","ILeavePolicyService interface","LeavePolicyService implementation","leavePolicyRoutes"]},{"name":"leave-balance","path":"src/modules/leave-balance/","owns":["LeaveBalance model","ILeaveBalanceRepository interface","PgLeaveBalanceRepository","ILeaveBalanceService interface","LeaveBalanceService implementation","leaveBalanceRoutes"]},{"name":"leave-request","path":"src/modules/leave-request/","owns":["LeaveRequest model","ILeaveRequestRepository interface","PgLeaveRequestRepository","ILeaveRequestService interface","LeaveRequestService implementation","leaveRequestRoutes"]}],"dependency_map":[{"from_":"leave-request","to":"leave-balance"},{"from_":"leave-request","to":"leave-policy"},{"from_":"leave-request","to":"audit"},{"from_":"leave-request","to":"shared-types"},{"from_":"leave-request","to":"employee (external module for IEmployeeRepository)"},{"from_":"leave-balance","to":"leave-policy"},{"from_":"leave-balance","to":"audit"},{"from_":"leave-balance","to":"shared-types"},{"from_":"leave-policy","to":"audit"},{"from_":"leave-policy","to":"shared-types"},{"from_":"audit","to":"shared-types"}],"recommended_phases":[{"title":"Phase 1: Shared types + Audit foundation","rationale":"Zero-dependency foundation. LeaveType and LeaveStatus enums in src/shared/types/ are consumed by every downstream module. Audit module provides the IAuditService interface and implementation that all state-changing operations depend on (GP-002).","estimated_files":6},{"title":"Phase 2: Leave Policy module","rationale":"Depends only on shared-types and audit. Defines policy configuration (max days per leave type per fiscal year) and the leave type lookup. Must exist before balance or request modules can validate against policy limits.","estimated_files":9},{"title":"Phase 3: Leave Balance module","rationale":"Depends on leave-policy (to know max days), audit, and shared-types. Manages balance lifecycle: initialization, deduction, credit. Enforces non-negative invariant. Must exist before leave-request can deduct on approval.","estimated_files":7},{"title":"Phase 4: Leave Request module","rationale":"Depends on leave-balance, leave-policy, audit, shared-types, and the external employee module. Orchestrates the full leave workflow: submit (validate against policy, check balance), approve (deduct balance, audit), reject (audit), cancel (credit balance, audit). This is the top-level orchestrating module.","estimated_files":8}],"architecture_md_update":"## Leave Management Module\n\n### Overview\nThe leave management module enables employees to apply for annual, sick, and emergency leave, managers to approve or reject requests, and the system to track leave balances. It is built as a modular monolith using TypeScript, Fastify, React Native, and PostgreSQL, following the existing architecture and golden principles.\n\n### Domain Model\n- **LeaveRequest** — Core aggregate representing a leave application. Lifecycle: DRAFT → SUBMITTED → APPROVED/REJECTED; can be CANCELLED from any non-terminal state. Tracks approver, rejector, and canceller separately for full audit.\n- **LeaveType** — Enumeration of leave categories (annual, sick, emergency, plus extensible types).\n- **LeavePolicy** — Configuration per leave type: entitlement days, accrual rules, minimum notice, manager approval requirement. Lifecycle: ACTIVE/INACTIVE.\n- **LeaveBalance** — Per-employee, per-leave-type, per-fiscal-year balance. Lifecycle: ACTIVE → EXHAUSTED (when remaining=0) → CLOSED (fiscal year end).\n\nBusiness rules enforced:\n- Date validity (start < end)\n- No overlapping approved leaves\n- Sufficient balance at approval time\n- Balance deduction on approval, restoration on cancellation\n- Manager approval required (self-approval prohibited)\n- Minimum notice period check at submission\n\n### Data Model (Conceptual Tables)\nAll tables are PostgreSQL, managed via pg Pool (`src/shared/db/connection.ts`). Migrations are generated from these specifications.\n\n- **employees** — Existing shared table; referenced by all leave entities.\n- **leave_types** — Lookup table for leave type names (annual, sick, emergency, etc.).\n- **leave_requests** — Stores leave applications with full lifecycle fields (approved_by, rejected_by, cancelled_by, etc.).\n- **leave_balances** — Tracks entitlement, used, and remaining days per employee/type/year.\n- **leave_policies** — Defines rules per leave type (entitlement, notice, approval flag).\n- **audit_logs** — Immutable audit trail for all state-changing operations (GP-002).\n\n### Module Structure\n```\nsrc/\n├── shared/\n│   └── types/          # LeaveType, LeaveStatus enums\n├── modules/\n│   ├── audit/          # AuditRecord, IAuditService, AuditService, IAuditLogRepository, PgAuditLogRepository, routes\n│   ├── leave-policy/   # LeavePolicy, ILeavePolicyRepository, PgLeavePolicyRepository, ILeaveTypeRepository, PgLeaveTypeRepository, ILeavePolicyService, LeavePolicyService, routes\n│   ├── leave-balance/  # LeaveBalance, ILeaveBalanceRepository, PgLeaveBalanceRepository, ILeaveBalanceService, LeaveBalanceService, routes\n│   └── leave-request/  # LeaveRequest, ILeaveRequestRepository, PgLeaveRequestRepository, ILeaveRequestService, LeaveRequestService, routes\n```\n\nDependencies flow inward: routes → services → repositories → db. No circular dependencies. The leave-request module orchestrates the workflow and depends on leave-balance, leave-policy, audit, shared-types, and an external employee module (for IEmployeeRepository).\n\n### Implementation Phases\n1. **Shared types + Audit** — Foundation enums and audit infrastructure.\n2. **Leave Policy** — Policy configuration and leave type lookup.\n3. **Leave Balance** — Balance management with deduction/credit.\n4. **Leave Request** — Full leave workflow orchestration.\n\n### Compliance with Golden Principles\n- **GP-001 (Repository Pattern)**: All data access through repository interfaces with PostgreSQL implementations.\n- **GP-002 (Audit)**: Every state change writes an immutable audit record via IAuditService.\n- **GP-003 (Input Validation)**: Validation at route boundaries before service invocation.\n- **GP-005 (RBAC)**: Manager-only endpoints (approve, reject, view pending) enforced at route layer; employee endpoints for own requests.\n- **GP-006 (Transaction Semantics)**: Balance deduction/credit and status updates occur within database transactions to maintain consistency.","sql_schemas":["table employees | fields: id, first_name, last_name, email, manager_id, role, hire_date, created_at, updated_at | pk: id | fks: manager_id -> employees.id | indexes: email: unique lookup for authentication and employee search; manager_id: listing direct reports for manager approval workflows; role: RBAC filtering at query level","table leave_types | fields: id, name, description, created_at, updated_at | pk: id | indexes: name: unique lookup by leave type name (annual, sick, emergency, etc.)","table leave_requests | fields: id, employee_id, leave_type_id, start_date, end_date, total_days, reason, status, approved_by, approved_at, rejected_by, rejected_at, rejection_reason, cancelled_by, cancelled_at, created_at, updated_at | pk: id | fks: employee_id -> employees.id; leave_type_id -> leave_types.id; approved_by -> employees.id; rejected_by -> employees.id; cancelled_by -> employees.id | indexes: employee_id: listing an employee's own leave requests; status: filtering by status for manager dashboards and pending-approval queues; leave_type_id: filtering requests by leave type; start_date, end_date: date-range queries for overlapping leave detection and calendar views; approved_by: manager viewing requests they approved; rejected_by: manager viewing requests they rejected","table leave_balances | fields: id, employee_id, leave_type_id, fiscal_year, total_entitlement, used_days, remaining_days, status, created_at, updated_at | pk: id | fks: employee_id -> employees.id; leave_type_id -> leave_types.id | indexes: employee_id, leave_type_id, fiscal_year: unique composite lookup for balance queries per employee per leave type per year; employee_id: fetching all balances for an employee; leave_type_id: aggregate balance queries across employees for a given leave type","table leave_policies | fields: id, leave_type_id, policy_name, entitlement_days, accrual_rate, max_accumulation, minimum_notice_days, requires_manager_approval, is_active, created_at, updated_at | pk: id | fks: leave_type_id -> leave_types.id | indexes: leave_type_id: policy lookup by leave type; is_active: filtering active policies","table audit_logs | fields: id, entity_type, entity_id, action, changed_by, changes, created_at | pk: id | fks: changed_by -> employees.id | indexes: entity_type, entity_id: audit trail lookup for a specific entity; changed_by: audit trail lookup by user; action: filtering audit records by action type (CREATE, UPDATE, DELETE, APPROVE, REJECT); created_at: time-range audit queries"]}