{"domain_entities":[{"name":"LeaveRequest","attributes":["id: string","employeeId: string","leaveTypeId: string","startDate: Date","endDate: Date","daysRequested: number","reason: string | undefined","status: LeaveRequestStatus","approvedBy: string | null","approvedAt: Date | null","rejectedBy: string | null","rejectedAt: Date | null","rejectionReason: string | null","cancelledBy: string | null","cancelledAt: Date | null","createdAt: Date","updatedAt: Date"],"purpose":"Represents an employee's application for leave. Tracks the full lifecycle from draft through submission, approval/rejection, and cancellation. References the employee, the leave policy, and the approving/rejecting/cancelling actor."},{"name":"LeaveRequestStatus","attributes":["DRAFT","SUBMITTED","APPROVED","REJECTED","CANCELLED"],"purpose":"Enumeration of all possible lifecycle states for a LeaveRequest. Drives state-transition validation and access control decisions."},{"name":"LeaveBalance","attributes":["id: string","employeeId: string","policyId: string","totalEntitlement: number","usedDays: number","remainingDays: number","fiscalYear: number","status: 'ACTIVE' | 'EXHAUSTED' | 'FROZEN'","createdAt: Date","updatedAt: Date"],"purpose":"Tracks an employee's leave entitlement, consumption, and remaining balance for a specific leave policy within a fiscal year. Deducted on approval; restored on cancellation of an approved request."},{"name":"LeavePolicy","attributes":["id: string","policyName: string","leaveType: LeaveType","entitlementDays: number","accrualRate: number | null","maxAccumulation: number | null","minimumNoticeDays: number | null","requiresManagerApproval: boolean","maxConsecutiveDays: number | null","allowNegativeBalance: boolean","isActive: boolean","createdAt: Date","updatedAt: Date"],"purpose":"Defines the rules and entitlements for a category of leave. Each policy governs one LeaveType. Determines whether manager approval is required, how many days are entitled, accrual behavior, notice requirements, and whether negative balances are permitted."},{"name":"LeaveType","attributes":["ANNUAL","SICK","EMERGENCY","UNPAID","MATERNITY","PATERNITY"],"purpose":"Enumeration of leave categories. Each LeavePolicy maps to exactly one LeaveType. Drives policy lookup and balance segregation."},{"name":"Employee","attributes":["id: string","employeeNumber: string","firstName: string","lastName: string","email: string","role: string","managerId: string | null","department: string | null","hireDate: Date","terminationDate: Date | null","employmentStatus: 'ACTIVE' | 'INACTIVE' | 'TERMINATED'","createdAt: Date","updatedAt: Date","deletedAt: Date | null"],"purpose":"Represents an employee in the organization. The employee is the actor who applies for leave and the subject whose balances are tracked. The managerId field establishes the approval hierarchy."},{"name":"Notification","attributes":["id: string","recipientId: string","type: 'LEAVE_SUBMITTED' | 'LEAVE_APPROVED' | 'LEAVE_REJECTED' | 'LEAVE_CANCELLED' | 'BALANCE_EXHAUSTED' | 'BALANCE_LOW'","title: string","message: string","relatedEntityType: 'LeaveRequest' | 'LeaveBalance' | null","relatedEntityId: string | null","status: 'PENDING' | 'SENT' | 'READ' | 'ARCHIVED'","createdAt: Date","readAt: Date | null"],"purpose":"Represents a notification triggered by leave lifecycle events. Sent to employees (on approval/rejection/cancellation of their request), managers (on new submission from their report), and when balances reach thresholds."},{"name":"AuditRecord","attributes":["id: string","entityType: string","entityId: string","action: string","actorId: string","changes: Record<string, unknown>","timestamp: Date"],"purpose":"Immutable record of every state transition on a LeaveRequest and every mutation of a LeaveBalance. Satisfies GP-002 audit requirements."}],"modules":[{"name":"audit","path":"src/modules/audit/","owns":["AuditRecord model","AuditRepository interface","AuditService interface","AuditService implementation","index.ts (public entry point)"]},{"name":"employee","path":"src/modules/employee/","owns":["Employee model","EmployeeRepository interface","EmployeeService interface","EmployeeService implementation","index.ts (public entry point)"]},{"name":"policy","path":"src/modules/policy/","owns":["LeavePolicy model","LeaveType enum","PolicyRepository interface","PolicyService interface","PolicyService implementation","index.ts (public entry point)"]},{"name":"balance","path":"src/modules/balance/","owns":["LeaveBalance model","BalanceRepository interface","BalanceService interface","BalanceService implementation","index.ts (public entry point)"]},{"name":"notification","path":"src/modules/notification/","owns":["Notification model","NotificationRepository interface","NotificationService interface","NotificationService implementation","index.ts (public entry point)"]},{"name":"leave","path":"src/modules/leave/","owns":["LeaveRequest model","LeaveRequestStatus enum","LeaveRepository interface","LeaveService interface","LeaveService implementation","LeaveController","LeaveRoutes","index.ts (public entry point)"]}],"dependency_map":[{"from_":"leave","to":"employee"},{"from_":"leave","to":"policy"},{"from_":"leave","to":"balance"},{"from_":"leave","to":"audit"},{"from_":"leave","to":"notification"},{"from_":"balance","to":"employee"},{"from_":"balance","to":"policy"},{"from_":"balance","to":"audit"},{"from_":"employee","to":"audit"},{"from_":"policy","to":"audit"},{"from_":"notification","to":"audit"}],"recommended_phases":[{"title":"Phase 1 — Foundation: Audit + Shared Infrastructure","rationale":"Audit has zero module dependencies and is required by every other module (GP-002). Build it first along with the shared DB connection and base repository. This unlocks all downstream modules.","estimated_files":5},{"title":"Phase 2 — Employee + Policy modules","rationale":"Employee and Policy each depend only on Audit (already built). They are independent of each other and can be built in parallel. Employee provides identity/manager lookups; Policy provides leave-type rules and entitlements.","estimated_files":10},{"title":"Phase 3 — Balance module","rationale":"Balance depends on Employee, Policy, and Audit — all built in Phases 1–2. It owns leave-balance tracking, deduction, restoration, and sufficiency checks. Must exist before Leave can deduct/restore balances.","estimated_files":6},{"title":"Phase 4 — Notification module","rationale":"Notification depends only on Audit. It can be built in parallel with Phase 3. Provides the notification fanout that Leave will trigger on state transitions.","estimated_files":5},{"title":"Phase 5 — Leave module (orchestrator)","rationale":"Leave depends on all other modules: Employee (identity/manager), Policy (rule validation), Balance (deduction/restoration), Audit (GP-002), Notification (state-change alerts). It orchestrates the full leave-request lifecycle: submit → approve/reject/cancel. This is the largest module and the integration point.","estimated_files":8}],"architecture_md_update":"## Leave Management Module\n\n### Overview\nThe leave management feature enables employees to apply for annual, sick, emergency, and other leave types. Managers approve or reject requests. The system tracks leave balances per employee per policy per fiscal year, enforces business rules (eligibility, overlap, balance sufficiency, notice periods), and maintains a full audit trail.\n\n### Domain Model\n\n**Core Entities**\n- **LeaveRequest** — the central aggregate representing a leave application. Lifecycle: `DRAFT → SUBMITTED → (APPROVED | REJECTED | CANCELLED)`. DRAFT and SUBMITTED can also transition directly to CANCELLED. Terminal states: APPROVED, REJECTED, CANCELLED.\n- **LeaveBalance** — tracks entitlement, used, and remaining days for an employee per policy per fiscal year. Lifecycle: `ACTIVE → EXHAUSTED` (when remaining reaches 0), `ACTIVE → FROZEN` (administrative hold), reversible.\n- **LeavePolicy** — defines rules for a leave type (entitlement, accrual, notice, approval requirement, etc.). Lifecycle: `ACTIVE → INACTIVE → ARCHIVED`.\n- **Employee** — the actor and subject. Lifecycle: `ACTIVE → INACTIVE → TERMINATED`. Manager relationship (`managerId`) drives the approval hierarchy.\n- **Notification** — triggered on leave lifecycle events (submitted, approved, rejected, cancelled, balance thresholds). Lifecycle: `PENDING → SENT → READ → ARCHIVED`.\n- **AuditRecord** — immutable log of every state transition on LeaveRequest and every mutation on LeaveBalance (GP-002).\n\n**Enums**\n- `LeaveRequestStatus`: DRAFT, SUBMITTED, APPROVED, REJECTED, CANCELLED\n- `LeaveType`: ANNUAL, SICK, EMERGENCY, UNPAID, MATERNITY, PATERNITY\n\n**Key Business Rules**\n- Only ACTIVE employees may submit leave (BR-001).\n- Date validity: start ≤ end, no past start on submission, same fiscal year (BR-002).\n- Balance sufficiency checked on approval; negative balance allowed only if policy permits (BR-003).\n- Balance deducted on APPROVED, restored on CANCELLED (BR-004, BR-005).\n- Manager approval required unless policy says otherwise; auto-approve when `requiresManagerApproval = false` (BR-006).\n- Minimum notice enforced except for EMERGENCY type (BR-007).\n- Maximum consecutive days enforced (BR-008).\n- No overlapping SUBMITTED/APPROVED requests for same leave type (BR-009).\n- State transitions authorized by role: employee, manager, or admin (BR-010).\n- Every state change produces an AuditRecord (BR-011).\n- Cross-fiscal-year requests are rejected (BR-012).\n\n### Module Boundaries & Dependencies\n\n```\nleave ──────────────► employee\nleave ──────────────► policy\nleave ──────────────► balance\nleave ──────────────► audit\nleave ──────────────► notification\nbalance ────────────► employee\nbalance ────────────► policy\nbalance ────────────► audit\nemployee ───────────► audit\npolicy ─────────────► audit\nnotification ───────► audit\n```\n\n- **audit** — zero dependencies; provides `IAuditService` consumed by all other modules.\n- **employee** — depends on audit; provides employee identity, manager lookup, and active-status checks.\n- **policy** — depends on audit; provides leave policy rules and validation.\n- **balance** — depends on employee, policy, audit; manages leave balances, deduction, restoration, and sufficiency checks.\n- **notification** — depends on audit; creates and delivers notifications for leave events.\n- **leave** — orchestrator; depends on all others. Implements the full request lifecycle: submit, approve, reject, cancel. Enforces all business rules, writes audit records, and triggers notifications.\n\n### Persistence (Conceptual)\n\nAll tables are PostgreSQL, accessed through repository interfaces with concrete `Pg*Repository` implementations using the shared `pg Pool` at `src/shared/db/connection.ts`.\n\n- **employee** — core identity table; soft-delete via `deleted_at`.\n- **leave_policy** — one row per leave type configuration; `leave_type` is unique.\n- **leave_balance** — composite unique on `(employee_id, policy_id, fiscal_year)`; `remaining_days` is stored for performance but derived from `total_entitlement - used_days`.\n- **leave_request** — references employee (applicant, approver, rejecter, canceller) and leave_policy via `leave_type_id`; `days_requested` is stored for convenience.\n- **audit_log** — immutable; `changes` stores JSON diff of old/new values.\n- **notification** — supports inbox queries by recipient and status.\n\n### Phased Build Order\n\n1. **Audit + Shared Infrastructure** — foundation for all modules.\n2. **Employee + Policy** — independent, can be parallelized.\n3. **Balance** — depends on Employee and Policy.\n4. **Notification** — depends only on Audit; can run in parallel with Balance.\n5. **Leave** — final orchestrator; integrates all modules.\n\n### Stack Compliance\n- Language: TypeScript (Node 20)\n- Framework: Fastify (routes/controllers in leave module)\n- Database: PostgreSQL via `pg`\n- Architecture: modular monolith with strict inward dependency flow\n- Testing: Jest\n- Cross-cutting: GP-001 (repository pattern), GP-002 (audit), GP-005 (RBAC at controller layer)\n\n### Reconciliation Decisions\n- **Naming**: `leaveTypeId` in LeaveRequest is a FK to `leave_policy.id`, not the enum value. `LeavePolicy.leaveType` holds the enum. `LeaveBalance.policyId` references the policy, not the raw type.\n- **Missing notification table**: Added to persistence layer to match the Notification domain entity.\n- **AuditRecord entity**: Explicitly added to domain model to satisfy BR-011 and GP-002.\n- **Employee fields**: Added `employee_number`, `hire_date`, `termination_date`, `employment_status`, `deleted_at` to align domain and data designs.\n- **LeavePolicy fields**: Added `accrual_rate`, `max_accumulation` to match domain.\n- **LeaveRequest fields**: Added `days_requested`, separate actor columns (`approved_by`, `rejected_by`, `cancelled_by`) instead of a single `reviewer_id`.\n- **LeaveBalance fields**: Added `policy_id` FK, `remaining_days`, `status`.","sql_schemas":["table employee | fields: id, employee_number, first_name, last_name, email, role, manager_id, department, hire_date, termination_date, employment_status, created_at, updated_at, deleted_at | pk: id | fks: manager_id -> employee.id | indexes: email: unique lookup for authentication and duplicate prevention; employee_number: unique lookup for employee identification; manager_id: frequent queries for direct reports and org hierarchy; role: RBAC filtering at query level (GP-005)","table leave_policy | fields: id, policy_name, leave_type, entitlement_days, accrual_rate, max_accumulation, minimum_notice_days, requires_manager_approval, max_consecutive_days, allow_negative_balance, is_active, created_at, updated_at | pk: id | indexes: leave_type: unique lookup for policy enforcement during leave application; is_active: filtering active policies only","table leave_balance | fields: id, employee_id, policy_id, total_entitlement, used_days, remaining_days, fiscal_year, status, created_at, updated_at | pk: id | fks: employee_id -> employee.id; policy_id -> leave_policy.id | indexes: employee_id, policy_id, fiscal_year: unique composite lookup for balance queries per employee per policy per year; employee_id: all balance queries are scoped to an employee; policy_id: queries by policy for reporting","table leave_request | fields: id, employee_id, leave_type_id, start_date, end_date, days_requested, reason, status, approved_by, approved_at, rejected_by, rejected_at, rejection_reason, cancelled_by, cancelled_at, created_at, updated_at | pk: id | fks: employee_id -> employee.id; leave_type_id -> leave_policy.id; approved_by -> employee.id; rejected_by -> employee.id; cancelled_by -> employee.id | indexes: employee_id: all leave requests are queried per employee; status: filtering by pending/approved/rejected for manager dashboards; approved_by: manager workload queries — all requests awaiting a specific reviewer; rejected_by: audit queries by rejecting manager; cancelled_by: audit queries by cancelling actor; start_date, end_date: date-range queries for overlap detection and calendar views; leave_type_id: filtering and aggregation by leave category","table audit_log | fields: id, entity_type, entity_id, action, actor_id, changes, timestamp | pk: id | fks: actor_id -> employee.id | indexes: entity_type, entity_id: audit trail lookups for a specific entity; actor_id: audit queries by user; timestamp: time-range audit queries; action: filtering by action type (CREATE, UPDATE, DELETE)","table notification | fields: id, recipient_id, type, title, message, related_entity_type, related_entity_id, status, created_at, read_at | pk: id | fks: recipient_id -> employee.id | indexes: recipient_id: inbox queries for a specific user; status: filtering unread/archived notifications; type: filtering by notification type; created_at: chronological ordering"]}