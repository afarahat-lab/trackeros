{"domain_entities":[{"name":"ApprovalsQueueItem","attributes":["requestId: string","employeeId: string","employeeName: string | null (derived; may be absent from API response)","leaveTypeCode: LeaveTypeCode","startDate: Date","endDate: Date","status: LeaveStatus (always SUBMITTED while in queue)"],"purpose":"Read-model projection of a LeaveRequest awaiting the signed-in manager/admin's decision. Derived, not persisted: membership computed from LeaveRequest.status === SUBMITTED and viewer role/ownership."},{"name":"LeaveRequest","attributes":["id: string","employeeId: string","leaveTypeCode: LeaveTypeCode","startDate: Date","endDate: Date","requestedDays: number","status: LeaveStatus","approverId: string | null","decidedAt: Date | null"],"purpose":"Existing source-of-truth entity. The approvals queue is a filtered projection of SUBMITTED requests not raised by the viewer; a decision transitions status out of SUBMITTED, removing the request from the queue."},{"name":"EmployeeProfile","attributes":["id: string","role: EmployeeRole (EMPLOYEE | MANAGER | ADMIN)","firstName: string","lastName: string"],"purpose":"The signed-in viewer's identity and role, fetched via GET /employees/me (never decoded from the JWT in the browser). role is the sole driver of queue visibility and link/route gating."}],"modules":[{"name":"approvals","path":"web/src/modules/approvals/","owns":["IApprovalsService","ApprovalsService","index.ts (public entry point)"]},{"name":"presentation-guards","path":"web/src/presentation/guards/","owns":["RequireApprover (route-level role guard)"]},{"name":"presentation-pages","path":"web/src/presentation/pages/","owns":["ApprovalsPage","DashboardPage (approvals link)"]},{"name":"presentation-app","path":"web/src/presentation/App.tsx","owns":["/approvals route wiring"]}],"dependency_map":[{"from_":"approvals","to":"leave"},{"from_":"approvals","to":"employee"},{"from_":"approvals","to":"shared-types"},{"from_":"presentation-pages","to":"approvals"},{"from_":"presentation-pages","to":"leave"},{"from_":"presentation-pages","to":"employee"},{"from_":"presentation-pages","to":"shared-types"},{"from_":"presentation-pages","to":"shared-date"},{"from_":"presentation-guards","to":"auth"},{"from_":"presentation-guards","to":"shared-types"},{"from_":"presentation-app","to":"approvals"},{"from_":"presentation-app","to":"presentation-guards"},{"from_":"presentation-app","to":"presentation-pages"}],"recommended_phases":[{"title":"Phase 1 — Approvals service module","rationale":"Add web/src/modules/approvals/ with IApprovalsService + ApprovalsService (listPending filters SUBMITTED non-owner rows; approve/reject delegate to ILeaveService) and index.ts. Depends only on existing leave/employee/shared-types. No UI yet.","estimated_files":3},{"title":"Phase 2 — Approvals service unit tests","rationale":"Vitest coverage beside the service: pending filter (excludes own + non-SUBMITTED), approve/reject delegation, and rejected-decision failure path.","estimated_files":1},{"title":"Phase 3 — RequireApprover route guard","rationale":"Add presentation/guards/RequireApprover.tsx (MANAGER|ADMIN only, else redirect) + test. Depends on auth context and shared-types.","estimated_files":2},{"title":"Phase 4 — ApprovalsPage + route wiring","rationale":"Add ApprovalsPage (list, approve/reject buttons, empty state, drill-in link) and register /approvals route in App.tsx wrapped in RequireAuth + RequireApprover.","estimated_files":2},{"title":"Phase 5 — ApprovalsPage tests + dashboard link","rationale":"Vitest coverage for the page (empty queue, approve/reject update, EMPLOYEE route rejection) and add the role-gated dashboard link.","estimated_files":3}],"architecture_md_update":"## Feature: Manager approvals queue (web only)\n\n### Domain entities\n- **ApprovalsQueueItem** — derived read-model projection of a LeaveRequest awaiting decision. Attributes: requestId, employeeId, employeeName (nullable), leaveTypeCode, startDate, endDate, status (always SUBMITTED while in queue). Lifecycle: AWAITING_DECISION -> RESOLVED.\n- **LeaveRequest** — existing source-of-truth entity. Attributes: id, employeeId, leaveTypeCode, startDate, endDate, requestedDays, status, approverId, decidedAt. Lifecycle: DRAFT, SUBMITTED, APPROVED, REJECTED, CANCELLED.\n- **EmployeeProfile** — signed-in viewer identity/role from GET /employees/me. Attributes: id, role (EMPLOYEE | MANAGER | ADMIN), firstName, lastName. No lifecycle.\n\n### Business rules\n- Queue membership: status === SUBMITTED AND employeeId !== viewer.id AND viewer.role ∈ {MANAGER, ADMIN}. Backend GET /leaves already scopes by role; queue applies SUBMITTED + not-self client-side.\n- Visibility role-gated at route boundary: only MANAGER/ADMIN; EMPLOYEE neither sees dashboard link nor can reach /approvals.\n- Decision authority reuses getAvailableLeaveActions(status, role, isOwner) — no second copy.\n- Decision transitions request out of SUBMITTED, removing it from queue without manual reload.\n- Empty queue is explicit normal state, distinct from loading/error.\n- Role derived only from EmployeeProfile; JWT never decoded in browser.\n\n### Conceptual tables (pre-existing, unchanged)\n- **leave_requests**: id, employee_id, leave_type_code, start_date, end_date, requested_days, reason, status, approver_id, approval_comment, submitted_at, decided_at, cancelled_by, cancelled_at. PK id. FKs employee_id -> employees.id, leave_type_code -> leave_types.code, approver_id -> employees.id, cancelled_by -> employees.id. Indexes: status (queue filter), employee_id (direct reports), (leave_type_code, start_date) (list/date-range).\n- **employees**: id, employee_number, first_name, last_name, email, password_hash, role, manager_id, department, hire_date, termination_date, employment_status. PK id. FK manager_id -> employees.id. Indexes: manager_id (direct reports), email (unique login), employee_number (unique lookup).\n\nNo new tables, columns, or repositories.\n\n### Repository interfaces / data access\nNo new repository interfaces. Existing web data-access boundary is IApiClient/ApiClient (HTTP fetch), with LeaveService/EmployeeService delegating to it. ApprovalsService reuses getLeaves(), approveLeave(), rejectLeave(), and getAvailableLeaveActions(); no new transport methods.\n\n### Module boundaries\n- `web/src/modules/approvals/` — IApprovalsService, ApprovalsService, index.ts. Methods: listPending(profile), approve(id), reject(id).\n- `web/src/presentation/guards/` — RequireApprover (MANAGER|ADMIN only, else redirect).\n- `web/src/presentation/pages/` — ApprovalsPage (list, approve/reject, empty state, drill-in link), DashboardPage (role-gated approvals link).\n- `web/src/presentation/App.tsx` — /approvals route wrapped in RequireAuth + RequireApprover.\n\nDependencies flow inward: presentation -> modules -> infrastructure. Approvals depends on leave, employee, shared-types. No circular edges.\n\n### Cross-cutting contracts\n- **Auth**: Identity/role from EmployeeProfile (GET /employees/me) held in auth session; EmployeeRole = 'EMPLOYEE' | 'MANAGER' | 'ADMIN'. /approvals wrapped in RequireAuth + RequireApprover; non-MANAGER/ADMIN redirected.\n- **Error**: No new API endpoints; page surfaces existing ApiError failures (message + optional code) from reused api-client methods. Rejected decision rendered inline without manual reload; empty queue distinct non-error state.\n- **Transaction**: EMPTY — read-only queue plus single existing API writes; atomicity owned by backend.\n\n### Recommended phases\n1. Phase 1 — Approvals service module (3 files)\n2. Phase 2 — Approvals service unit tests (1 file)\n3. Phase 3 — RequireApprover route guard (2 files)\n4. Phase 4 — ApprovalsPage + route wiring (2 files)\n5. Phase 5 — ApprovalsPage tests + dashboard link (3 files)\n\n### Open question\n- Employee column rendering: API exposes employeeId but no employee name; web employee service only getMe(). Options: render employeeId as-is, omit column, or resolve from existing list (none exists).","sql_schemas":["table leave_requests | fields: id, employee_id, leave_type_code, start_date, end_date, requested_days, reason, status, approver_id, approval_comment, submitted_at, decided_at, cancelled_by, cancelled_at | pk: id | fks: employee_id -> employees.id; leave_type_code -> leave_types.code; approver_id -> employees.id; cancelled_by -> employees.id | indexes: status: the approvals queue filters SUBMITTED requests awaiting a decision; employee_id: the queue scopes to the manager's direct reports (employee_id = ANY(...)); (leave_type_code, start_date): existing list/date-range filtering","table employees | fields: id, employee_number, first_name, last_name, email, password_hash, role, manager_id, department, hire_date, termination_date, employment_status | pk: id | fks: manager_id -> employees.id | indexes: manager_id: resolves a manager's direct reports for queue scoping; email: unique lookup for login/profile; employee_number: unique lookup"],"open_questions":[],"business_rules":["1. Render the employeeId, truncated to its first 8 characters in a monospace style, under a column headed \"Employee\" — and do NOT attempt name resolution. There is no endpoint that returns another employee's name (only GET /employees/me), and inventing one is outside this feature's scope.\n\n   I want to be straight about what that means rather than dress it up: a manager deciding leave needs to know WHOSE leave it is, and a truncated id does not tell them. This is a real limitation, and it is caused by the web-only constraint I set, not by anything you did wrong in asking. The truncated id is the least-bad option because it is at least STABLE and DISTINGUISHING — the same person's requests group visibly, and the row links to the detail page. Omitting the column would be worse: a manager could not even tell two people's requests apart.\n\n   Add a short note in the UI near the column, something like \"employee names require an API change\", so the gap is visible to whoever uses it rather than looking like a rendering bug. Do not add a TODO comment in code in place of that note; the person who needs to know is the user, not the next developer.\n\n2. Filter defensively in ApprovalsService: SUBMITTED status AND not raised by the signed-in user. Do not rely on the backend's role scoping to answer this.\n\n   The reason is that the two are different questions. GET /leaves answers \"may this caller see this request\"; the queue needs \"is this waiting on MY decision\". They coincide today, which is exactly what makes depending on it fragile — the day the backend widens what a manager may see, the queue silently fills with rows that are not actionable, and nothing will point at the cause. Do not add a status query param either; that changes the API contract, which is out of scope.\n\n3. Replace the decided row with the LeaveRequestView the approve/reject call returns, and render it as decided (its new status visible, its approve/reject controls gone). Do not remove it, and do not re-fetch the whole list.\n\n   Removing it is the option that feels tidiest and is worst in use: the row vanishes at the moment the manager acts, so nothing confirms which request they just decided or that it succeeded. Keeping the returned view is the confirmation. It drops out naturally on the next load, because the filter in (2) excludes non-SUBMITTED rows — so no extra round trip is needed, and a full re-fetch would make it disappear immediately anyway, reintroducing the same problem.\n\nNothing else in the spec changes. [BINDING RULE — operator decision resolving: The API's LeaveRequestView exposes employeeId but no employee name, and the web employee service only exposes getMe() (no direct-report name lookup). The feature asks the queue to show \"employee\" — how should the employee column be rendered when only employeeId is available?; Should the approvals queue filter to SUBMITTED-only in the service, or rely on the backend's role-scoped GET /leaves and filter client-side?; After approve/reject, should the row be removed from the queue or re-rendered with the new status?; apply everywhere these apply, not in one place only]"],"auth_contract":"Role-gated route access only (no new API endpoints). Identity/role come from the already-fetched EmployeeProfile (GET /employees/me) held in the auth session — never decoded from the JWT in the browser. EmployeeRole = 'EMPLOYEE' | 'MANAGER' | 'ADMIN'. The /approvals route is wrapped in RequireAuth (session presence) plus a new RequireApprover guard that redirects any role other than MANAGER or ADMIN, so an EMPLOYEE visiting the route directly sees no other employee's data.","transaction_contract":"","error_response_contract":"No new API endpoints; the page surfaces existing ApiError failures (message + optional code) from the reused api-client methods. A rejected decision (approve/reject returning a non-ok response) is caught and rendered as an inline error without a manual reload; the empty queue is a distinct non-error state."}