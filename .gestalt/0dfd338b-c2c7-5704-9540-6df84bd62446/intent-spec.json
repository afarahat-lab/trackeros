{
  "scope": {
    "affectedDomains": [
      "shared/types"
    ],
    "affectedLayers": [
      "domain"
    ],
    "isBreakingChange": false,
    "estimatedComplexity": "small"
  },
  "successCriteria": [
    {
      "description": "src/shared/types/leave.enums.ts exists with enum LeaveType having exactly three members: ANNUAL, SICK, EMERGENCY",
      "testable": true,
      "layer": "unit"
    },
    {
      "description": "src/shared/types/leave.enums.ts exists with enum LeaveRequestStatus having exactly five members: DRAFT, SUBMITTED, APPROVED, REJECTED, CANCELLED",
      "testable": true,
      "layer": "unit"
    },
    {
      "description": "src/shared/types/leave.enums.ts exists with enum BalanceStatus having exactly three members: ACTIVE, EXHAUSTED, EXPIRED",
      "testable": true,
      "layer": "unit"
    },
    {
      "description": "src/shared/types/index.ts re-exports all three enums (LeaveType, LeaveRequestStatus, BalanceStatus) from ./leave.enums",
      "testable": true,
      "layer": "unit"
    },
    {
      "description": "Jest unit tests in tests/unit/shared/types/ verify that all enum members exist with correct values for LeaveType, LeaveRequestStatus, and BalanceStatus",
      "testable": true,
      "layer": "unit"
    }
  ],
  "constraints": [
    "TypeScript strict mode \u2014 no implicit any, strict null checks (from HARNESS.json stack and constraints.rules no-any)",
    "Test framework: Jest (from HARNESS.json stack.testFramework)",
    "Follow AGENTS.md conventions \u2014 no violation of golden principles (GP-001 through GP-006), though none directly constrain enum definitions"
  ],
  "outOfScope": [
    "Phase 2: LeaveRequest model and repository (src/modules/leave/leave.model.ts, leave.repository.ts)",
    "Phase 3: LeavePolicy model and repository (src/modules/policy/)",
    "Phase 4: LeaveBalance model and repository (src/modules/balance/)",
    "Phase 5: LeaveService business logic (src/modules/leave/leave.service.ts)",
    "Phase 6: LeaveController and routes (src/modules/leave/leave.controller.ts, leave.routes.ts)",
    "Phase 7-8: Notification module (model, repository, service)",
    "Phase 9: Knex database migrations",
    "Phase 10: RBAC middleware and route hardening",
    "NotificationType enum \u2014 deferred to Phase 7 per PLAN.md"
  ],
  "ambiguities": [
    {
      "description": "ARCHITECTURE.md references src/shared/types/leave.types.ts while the intent and PLAN.md specify leave.enums.ts. The intent (leave.enums.ts) is authoritative for this phase.",
      "options": [
        "Use leave.enums.ts as specified in the intent",
        "Use leave.types.ts as referenced in ARCHITECTURE.md"
      ],
      "impactIfWrong": "low"
    }
  ]
}