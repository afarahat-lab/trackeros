{
  "id": "6107d1a6-8ba6-473c-93bd-f2dafe29ecde",
  "correlationId": "65f08606-7c2d-47f4-97fd-b7851ced0789",
  "rawIntent": "Create src/modules/qux/qux.controller.ts with a Fastify route handler that responds with JSON {message: 'qux'} on GET /qux. Create src/modules/qux/qux.routes.ts in the SAME src/modules/qux/ directory to register the route. Create src/modules/qux/index.ts in the SAME src/modules/qux/ directory to export the controller. No service layer needed. All three files must reside directly in src/modules/qux/; do NOT nest them under additional src/modules/qux/ subdirectories. Imports must use './qux.controller' from that single directory.",
  "scope": {
    "affectedDomains": [
      "qux"
    ],
    "affectedLayers": [
      "api"
    ],
    "isBreakingChange": false,
    "estimatedComplexity": "small"
  },
  "successCriteria": [
    {
      "id": "sc-001",
      "description": "src/modules/qux/qux.controller.ts exists with a Fastify route handler that returns JSON {message: 'qux'} on GET /qux",
      "testable": true,
      "layer": "unit"
    },
    {
      "id": "sc-002",
      "description": "src/modules/qux/qux.routes.ts exists and registers the GET /qux route using the controller",
      "testable": true,
      "layer": "unit"
    },
    {
      "id": "sc-003",
      "description": "src/modules/qux/index.ts exists and exports the controller",
      "testable": true,
      "layer": "unit"
    },
    {
      "id": "sc-004",
      "description": "All three files are placed directly in src/modules/qux/ directory (not nested in subdirectories)",
      "testable": true,
      "layer": "integration"
    },
    {
      "id": "sc-005",
      "description": "qux.routes.ts and index.ts use relative import './qux.controller' (not absolute or nested paths)",
      "testable": true,
      "layer": "integration"
    }
  ],
  "constraints": [
    "Repository pattern: All database access goes through repository interfaces.",
    "Input validation: Validate all inputs at API boundaries before processing.",
    "RBAC enforcement: All API endpoints enforce role-based access control.",
    "Error handling: No unhandled promise rejections. All async errors are caught and handled."
  ],
  "outOfScope": [
    "Service layer creation",
    "Database access or repositories",
    "Audit records for this simple endpoint",
    "Authentication or authorization implementation",
    "Testing files",
    "Configuration files",
    "UI components",
    "Any changes outside src/modules/qux/ directory",
    "Package.json dependencies"
  ],
  "ambiguities": []
}