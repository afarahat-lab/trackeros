# PLAN.md — Build the leave management module

_Generated by Gestalt planning layer (feature 7d2acd20)._

## Feature description

Build the leave management module. Employees apply for annual, sick, and emergency leave. Managers approve or reject. System tracks leave balances.

## Modules
- **leave** (`src/modules/leave`) — owns: leave.model.ts, leave.repository.ts, leave.service.ts, leave.routes.ts, leave.test.ts
- **balance** (`src/modules/balance`) — owns: balance.model.ts, balance.repository.ts, balance.service.ts
- **policy** (`src/modules/policy`) — owns: policy.model.ts, policy.repository.ts, policy.service.ts, policy.routes.ts

## Domain entities
- **LeaveRequest** — Tracks leave requests made by employees. (attributes: id, employeeId, type, startDate, endDate, status, managerId, managerComment, createdAt)
- **LeaveBalance** — Maintains the leave balance for each employee. (attributes: employeeId, leaveType, totalDays, usedDays, year)
- **LeavePolicy** — Defines the rules and limits for different types of leave. (attributes: id, leaveType, defaultDaysPerYear, maxConsecutiveDays, requiresApproval, createdAt)

## Phases

### Phase 1: Create leave model

Create src/modules/leave/leave.model.ts. Define LeaveRequest and CreateLeaveRequestDto. Import LeaveType from src/shared/types/index.ts. Include a Jest unit test in tests/unit/leave.model.test.ts.

**Architecture:**
This phase establishes the core data structure for leave requests, which is essential for the repository and service layers.

### Phase 2: Implement leave request repository
_Depends on: src/modules/leave/leave.model.ts_

Create src/modules/leave/leave.repository.ts. Implement LeaveRequestRepository to handle database interactions for leave requests. This phase depends on src/modules/leave/leave.model.ts from Phase 1. Include a Jest unit test in tests/unit/leave.repository.test.ts.

**Architecture:**
This phase provides the data access layer for leave requests, enabling CRUD operations.

### Phase 3: Develop leave request service
_Depends on: src/modules/leave/leave.model.ts, src/modules/leave/leave.repository.ts_

Create src/modules/leave/leave.service.ts. Implement LeaveRequestService to process leave requests, including validation and business rules. This phase depends on src/modules/leave/leave.model.ts and src/modules/leave/leave.repository.ts from Phases 1 and 2. Include a Jest unit test in tests/unit/leave.service.test.ts.

**Architecture:**
This phase encapsulates the business logic for leave requests, ensuring that all rules and validations are enforced.

### Phase 4: Create leave request routes
_Depends on: src/modules/leave/leave.service.ts_

Create src/modules/leave/leave.routes.ts. Expose API endpoints for leave request operations (create, approve, reject). This phase depends on src/modules/leave/leave.service.ts from Phase 3. Include a Jest unit test in tests/unit/leave.routes.test.ts.

**Architecture:**
This phase connects the service layer to the API, allowing frontend applications to interact with leave requests.
