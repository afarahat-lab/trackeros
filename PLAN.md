# PLAN.md — Build the leave management module

_Generated by Gestalt planning layer (feature 0a9b14f6)._

## Feature description

Build the leave management module. Employees apply for annual, sick, and emergency leave. Managers approve or reject. System tracks leave balances.

## Modules
- **leave** (`src/modules/leave`) — owns: leave.model.ts, leave.repository.ts, leave.service.ts, leave.routes.ts, leave.test.ts
- **balance** (`src/modules/balance`) — owns: balance.model.ts, balance.repository.ts, balance.service.ts
- **policy** (`src/modules/policy`) — owns: policy.model.ts, policy.repository.ts, policy.service.ts, policy.routes.ts
- **employee** (`src/modules/employee`) — owns: employee.model.ts, employee.repository.ts, employee.service.ts, employee.routes.ts

## Domain entities
- **LeaveRequest** — Tracks leave requests made by employees. (attributes: id, employeeId, type, startDate, endDate, status, managerId, managerComment, createdAt)
- **LeaveBalance** — Maintains the leave balance for each employee. (attributes: employeeId, leaveType, totalDays, usedDays, year)
- **LeavePolicy** — Defines the rules and limits for different types of leave. (attributes: id, leaveType, defaultDaysPerYear, maxConsecutiveDays, requiresApproval, createdAt)

## Phases

### Phase 1: Create leave model and repository

Create src/modules/leave/leave.model.ts defining LeaveRequest and CreateLeaveRequestDto. Create src/modules/leave/leave.repository.ts implementing the repository for LeaveRequest. This phase depends on no prior files.

**Architecture:**
This phase establishes the core data structure for leave requests and the persistence layer.

### Phase 2: Implement leave service logic
_Depends on: src/modules/leave/leave.model.ts, src/modules/leave/leave.repository.ts_

Create src/modules/leave/leave.service.ts to implement business logic for handling leave requests, including validation and processing. This phase depends on src/modules/leave/leave.model.ts and src/modules/leave/leave.repository.ts from Phase 1.

**Architecture:**
This phase encapsulates the business rules and logic for processing leave requests.

### Phase 3: Create leave routes and middleware
_Depends on: src/modules/leave/leave.service.ts_

Create src/modules/leave/leave.routes.ts to expose API endpoints for leave management. This phase depends on src/modules/leave/leave.service.ts from Phase 2.

**Architecture:**
This phase connects the service layer to the API, allowing external interaction with the leave management functionality.

### Phase 4: Write unit tests for leave module
_Depends on: src/modules/leave/leave.service.ts, src/modules/leave/leave.model.ts_

Create tests/unit/leave.service.test.ts to write unit tests for the leave service logic. This phase depends on src/modules/leave/leave.service.ts from Phase 2 and src/modules/leave/leave.model.ts from Phase 1.

**Architecture:**
This phase ensures the reliability of the leave management module through comprehensive testing.
