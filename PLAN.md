# PLAN.md

## Phase 1: Phase 1: Shared enums and base types

Create src/shared/types/leave.enums.ts with three enums: LeaveType (ANNUAL, SICK, EMERGENCY), LeaveRequestStatus (DRAFT, SUBMITTED, APPROVED, REJECTED, CANCELLED), and BalanceStatus (ACTIVE, EXHAUSTED, EXPIRED). Create or update src/shared/types/index.ts to re-export all three enums. No existing files are dependencies — this is the foundational phase. Include Jest unit tests in tests/unit/shared/types/ verifying enum values.

## Phase 2: Phase 2: LeaveRequest model and repository

Create src/modules/leave/leave.model.ts with the LeaveRequest interface (id: string, employeeId: string, leaveTypeId: string, startDate: Date, endDate: Date, reason: string | undefined, status: LeaveRequestStatus, approvedBy: string | null, approvedAt: Date | null, createdAt: Date, updatedAt: Date) and CreateLeaveRequestDto (employeeId, leaveTypeId, startDate, endDate, reason?). Create src/modules/leave/leave.repository.ts with ILeaveRepository interface and LeaveRepository class using the pg pool from src/shared/db/connection.ts. Methods: findById, findByEmployeeId, create, updateStatus, findAll (with optional status filter). This phase depends on src/shared/types/leave.enums.ts from Phase 1 — read it before generating any code that references LeaveType, LeaveRequestStatus, or BalanceStatus. Also depends on src/shared/db/connection.ts (already exists). Include Jest unit tests in tests/unit/modules/leave/ for the repository using a mocked pool.

## Phase 3: Phase 3: LeavePolicy model and repository

Create src/modules/policy/policy.model.ts with the LeavePolicy interface (id: string, policyName: string, leaveType: LeaveType, entitlementDays: number, accrualRate: number | undefined, maxAccumulation: number | undefined, minimumNoticeDays: number | undefined, requiresManagerApproval: boolean, isActive: boolean, createdAt: Date, updatedAt: Date) and CreateLeavePolicyDto. Create src/modules/policy/policy.repository.ts with IPolicyRepository interface and PolicyRepository class using the pg pool from src/shared/db/connection.ts. Methods: findById, findByLeaveType, findAllActive, create, update, deactivate. This phase depends on src/shared/types/leave.enums.ts from Phase 1 for LeaveType. Also depends on src/shared/db/connection.ts (already exists). Include Jest unit tests in tests/unit/modules/policy/.

## Phase 4: Phase 4: LeaveBalance model and repository

Create src/modules/balance/balance.model.ts with the LeaveBalance interface (id: string, employeeId: string, policyId: string, totalEntitlement: number, usedDays: number, remainingDays: number, fiscalYear: number, status: BalanceStatus, createdAt: Date, updatedAt: Date) and CreateLeaveBalanceDto. Create src/modules/balance/balance.repository.ts with IBalanceRepository interface and BalanceRepository class using the pg pool from src/shared/db/connection.ts. Methods: findByEmployeeId, findByEmployeeAndPolicy, create, deductDays, restoreDays, updateStatus. This phase depends on src/shared/types/leave.enums.ts from Phase 1 for BalanceStatus. Also depends on src/shared/db/connection.ts (already exists). Include Jest unit tests in tests/unit/modules/balance/.

## Phase 5: Phase 5: LeaveService — business logic

Create src/modules/leave/leave.service.interface.ts with ILeaveService interface declaring: submitLeaveRequest(dto: CreateLeaveRequestDto): Promise<LeaveRequest>, approveLeaveRequest(requestId: string, approverId: string): Promise<LeaveRequest>, rejectLeaveRequest(requestId: string, approverId: string): Promise<LeaveRequest>, cancelLeaveRequest(requestId: string, employeeId: string): Promise<LeaveRequest>, getLeaveRequests(filters: { employeeId?: string; status?: LeaveRequestStatus }): Promise<LeaveRequest[]>, getLeaveRequestById(id: string): Promise<LeaveRequest | null>. Create src/modules/leave/leave.service.ts with LeaveService class implementing ILeaveService. The service constructor takes ILeaveRepository, IPolicyRepository, and IBalanceRepository. submitLeaveRequest must validate against the active LeavePolicy (minimumNoticeDays, requiresManagerApproval), set initial status to SUBMITTED, and create the request. approveLeaveRequest must verify the request is SUBMITTED, set status to APPROVED, deduct days from LeaveBalance. rejectLeaveRequest must set status to REJECTED. cancelLeaveRequest must restore days to LeaveBalance if previously APPROVED. This phase depends on src/modules/leave/leave.model.ts and src/modules/leave/leave.repository.ts from Phase 2, src/modules/policy/policy.model.ts and src/modules/policy/policy.repository.ts from Phase 3, src/modules/balance/balance.model.ts and src/modules/balance/balance.repository.ts from Phase 4, and src/shared/types/leave.enums.ts from Phase 1. Read all of these before generating. Include Jest unit tests in tests/unit/modules/leave/leave.service.test.ts mocking all three repositories.

## Phase 6: Phase 6: LeaveController and routes

Create src/modules/leave/leave.controller.ts with LeaveController class that wraps ILeaveService and exposes methods: submit, approve, reject, cancel, getById, list. Each method extracts params/body from the Fastify request, calls the service, and returns the appropriate HTTP response. Create src/modules/leave/leave.routes.ts registering Fastify routes: POST /leave/requests, GET /leave/requests, GET /leave/requests/:id, PATCH /leave/requests/:id/approve, PATCH /leave/requests/:id/reject, PATCH /leave/requests/:id/cancel. Use Zod schemas for input validation on every route (GP-003). Create src/modules/leave/index.ts re-exporting the public surface: LeaveRequest, CreateLeaveRequestDto, ILeaveService, LeaveService, leaveRoutes. This phase depends on src/modules/leave/leave.service.interface.ts and src/modules/leave/leave.service.ts from Phase 5, src/modules/leave/leave.model.ts from Phase 2, and src/shared/types/leave.enums.ts from Phase 1. Read all of these before generating. Also depends on src/app.ts (already exists) — register leaveRoutes there. Include Jest integration tests in tests/integration/modules/leave/leave.routes.test.ts using Fastify's inject method.

## Phase 7: Phase 7: LeaveNotification model and repository

Create src/modules/notification/notification.model.ts with the LeaveNotification interface (id: string, recipientId: string, leaveRequestId: string, notificationType: NotificationType enum — SUBMITTED, APPROVED, REJECTED, CANCELLED, message: string, isRead: boolean, createdAt: Date) and CreateNotificationDto. Create src/modules/notification/notification.repository.ts with INotificationRepository interface and NotificationRepository class using the pg pool from src/shared/db/connection.ts. Methods: create, findByRecipientId, markAsRead, findUnreadByRecipientId. This phase depends on src/shared/types/leave.enums.ts from Phase 1 (add NotificationType enum there if not already present — extend the file). Also depends on src/shared/db/connection.ts (already exists). Include Jest unit tests in tests/unit/modules/notification/.

## Phase 8: Phase 8: Notification service and audit integration

Create src/modules/notification/notification.service.interface.ts with INotificationService interface declaring: notifyOnStatusChange(leaveRequest: LeaveRequest, previousStatus: LeaveRequestStatus): Promise<void>, getNotificationsForUser(userId: string): Promise<LeaveNotification[]>, markAsRead(notificationId: string): Promise<void>. Create src/modules/notification/notification.service.ts with NotificationService class implementing INotificationService. The service takes INotificationRepository. notifyOnStatusChange creates a notification for the employee and (if approved/rejected) for the approver. Create src/modules/notification/index.ts re-exporting the public surface. Update src/modules/leave/leave.service.ts from Phase 5 to inject INotificationService and call notifyOnStatusChange after every status transition (GP-002 audit trail via notifications). This phase depends on src/modules/leave/leave.model.ts from Phase 2, src/modules/leave/leave.service.ts from Phase 5, src/modules/notification/notification.model.ts and src/modules/notification/notification.repository.ts from Phase 7, and src/shared/types/leave.enums.ts from Phase 1. Read all of these before generating. Include Jest unit tests in tests/unit/modules/notification/notification.service.test.ts.

## Phase 9: Phase 9: Database migrations (Knex)

Create Knex migration files under src/shared/db/migrations/. Create migration 001_create_leave_policies.ts with columns: id (uuid PK), policy_name (varchar), leave_type (varchar), entitlement_days (integer), accrual_rate (decimal nullable), max_accumulation (decimal nullable), minimum_notice_days (integer nullable), requires_manager_approval (boolean), is_active (boolean), created_at (timestamptz), updated_at (timestamptz). Create migration 002_create_leave_balances.ts with columns: id (uuid PK), employee_id (uuid FK), policy_id (uuid FK references leave_policies), total_entitlement (decimal), used_days (decimal), remaining_days (decimal), fiscal_year (integer), status (varchar), created_at, updated_at. Create migration 003_create_leave_requests.ts with columns: id (uuid PK), employee_id (uuid), leave_type_id (uuid FK references leave_policies), start_date (date), end_date (date), reason (text nullable), status (varchar), approved_by (uuid nullable), approved_at (timestamptz nullable), created_at, updated_at. Create migration 004_create_leave_notifications.ts with columns: id (uuid PK), recipient_id (uuid), leave_request_id (uuid FK references leave_requests), notification_type (varchar), message (text), is_read (boolean default false), created_at. Create or update src/shared/db/knexfile.ts with Knex configuration reading DATABASE_URL from env. This phase depends on src/shared/types/leave.enums.ts from Phase 1 for enum values used in check constraints. Also depends on src/shared/db/connection.ts (already exists). No unit tests needed for migrations — rely on CI typecheck.

## Phase 10: Phase 10: RBAC middleware and route hardening

Create src/shared/middleware/rbac.middleware.ts with an RBAC middleware factory: requireRole(...roles: string[]) returning a Fastify preHandler hook that extracts the user's role from request.user (set by an auth plugin), and returns 403 if the role is not in the allowed list. Create src/shared/middleware/index.ts re-exporting it. Update src/modules/leave/leave.routes.ts from Phase 6 to apply requireRole('employee') on POST /leave/requests and GET /leave/requests (employee self-service), and requireRole('manager', 'admin') on PATCH approve/reject routes. Also add requireRole('employee', 'manager', 'admin') on GET /leave/requests/:id and PATCH cancel. Create src/shared/errors/error.types.ts with typed error classes: NotFoundError, ValidationError, ForbiddenError, ConflictError extending a base AppError class. Update src/modules/leave/leave.controller.ts from Phase 6 to throw these typed errors instead of generic ones. This phase depends on src/modules/leave/leave.routes.ts and src/modules/leave/leave.controller.ts from Phase 6. Read both before generating. Include Jest unit tests in tests/unit/shared/middleware/rbac.middleware.test.ts.
