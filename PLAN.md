# PLAN.md — Leave management module

_Generated by Gestalt planning layer (feature eed75889)._

## Feature description

Build the leave management module. Employees can apply for annual, sick, and emergency leave. Managers approve or reject requests. The system tracks leave balances and prevents overlapping requests.

## Modules
- **leave** (`src/modules/leave`) — owns: leave.model.ts, leave.repository.ts, leave.service.ts, leave.routes.ts, leave.test.ts
- **balance** (`src/modules/balance`) — owns: balance.model.ts, balance.repository.ts, balance.service.ts
- **policy** (`src/modules/policy`) — owns: policy.model.ts, policy.repository.ts, policy.service.ts, policy.routes.ts
- **employee** (`src/modules/employee`) — owns: employee.model.ts, employee.repository.ts, employee.service.ts, employee.routes.ts

## Domain entities
- **LeaveRequest** — Tracks leave requests made by employees and their approval status. (attributes: id, employeeId, type, startDate, endDate, status, managerId, managerComment, createdAt)
- **LeaveBalance** — Manages the leave balances for each employee by leave type. (attributes: employeeId, leaveType, totalDays, usedDays, year)
- **LeavePolicy** — Defines the rules and limits for different types of leave. (attributes: id, leaveType, defaultDaysPerYear, maxConsecutiveDays, requiresApproval, createdAt)

## Phases

### Phase 1: Define Leave Request Model and Repository

Create src/modules/leave/leave.model.ts defining LeaveRequest and CreateLeaveRequestDto. Create src/modules/leave/leave.repository.ts for database interactions. Import LeaveType from src/shared/types/index.ts.

**Architecture:**
This phase establishes the core data structure for leave requests and the repository for data access.

### Phase 2: Implement Leave Service Logic
_Depends on: src/modules/leave/leave.model.ts_

Create src/modules/leave/leave.service.ts to handle business logic for leave requests, including validation and processing. Use LeaveRequest and CreateLeaveRequestDto from src/modules/leave/leave.model.ts.

**Architecture:**
This phase implements the service layer that processes leave requests and ensures business rules are followed.

### Phase 3: Create Leave Routes
_Depends on: src/modules/leave/leave.service.ts_

Create src/modules/leave/leave.routes.ts to expose API endpoints for employees to apply for leave and for managers to approve or reject requests. Utilize leave.service.ts for handling requests.

**Architecture:**
This phase sets up the API layer for the leave management module, allowing interaction with the service logic.

### Phase 4: Develop Unit Tests
_Depends on: src/modules/leave/leave.service.ts_

Create tests/unit/leave.service.test.ts to ensure the leave service logic is thoroughly tested for reliability and correctness. Mock dependencies as necessary.

**Architecture:**
This phase ensures the leave service is tested, validating its functionality and adherence to business rules.
