# PLAN.md — Add /uptime endpoint

_Generated by Gestalt planning layer (feature 37799ea9)._

## Feature description

Add a /uptime endpoint to the Express app that returns the process uptime in seconds as JSON: {uptime: seconds}.

## Modules
- **uptime** (`src/modules/uptime/`) — owns: uptime.routes.ts

## Phases

### Phase 1: Create Uptime Module

Create src/modules/uptime/uptime.ts exporting a function getUptime(): { uptime: number }. This function should return the process uptime in seconds. Include a Jest unit test in tests/unit/uptime.test.ts.

**Architecture:**
This phase encapsulates the uptime functionality within its own module, adhering to the existing architecture.
