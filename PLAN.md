# PLAN.md — Leave management module

_Generated by Gestalt planning layer (feature 6eee3bff)._

## Feature description

Build the leave management module. Employees can apply for annual, sick, and emergency leave. Managers approve or reject requests. The system tracks leave balances and prevents overlapping requests.

## Modules
- **leave** (`src/modules/leave`) — owns: leave.model.ts, leave.repository.ts, leave.service.ts, leave.routes.ts, leave.test.ts
- **balance** (`src/modules/balance`) — owns: balance.model.ts, balance.repository.ts, balance.service.ts
- **policy** (`src/modules/policy`) — owns: policy.model.ts, policy.repository.ts, policy.service.ts, policy.routes.ts
- **employee** (`src/modules/employee`) — owns: employee.model.ts, employee.repository.ts, employee.service.ts, employee.routes.ts

## Domain entities
- **LeaveRequest** — Tracks leave requests made by employees and their statuses. (attributes: id, employeeId, type, startDate, endDate, status, managerId, managerComment, createdAt)
- **LeaveBalance** — Manages the leave balances for each employee. (attributes: employeeId, leaveType, totalDays, usedDays, year)
- **LeavePolicy** — Defines the policies governing different types of leave. (attributes: id, leaveType, defaultDaysPerYear, maxConsecutiveDays, requiresApproval, createdAt)

## Phases

### Phase 1: Create leave model

Create src/modules/leave/leave.model.ts. Define LeaveRequest, CreateLeaveRequestDto. Import LeaveType from src/shared/types/index.ts.

**Architecture:**
This phase establishes the foundational data structures for leave requests.

### Phase 2: Implement leave request management
_Depends on: src/modules/leave/leave.model.ts_

Create src/modules/leave/leave.service.ts to handle leave request submissions and approvals. Create src/modules/leave/leave.controller.ts to manage API endpoints for leave requests. Include a Jest unit test in tests/unit/leave.service.test.ts.

**Architecture:**
This phase implements the core functionality for employees to submit leave requests and for managers to approve or reject them.

### Phase 3: Create leave balance model

Create src/modules/balance/leave.balance.model.ts. Define LeaveBalance and LeaveBalanceDto. Import LeaveRequest from src/modules/leave/leave.model.ts.

**Architecture:**
This phase defines the data structures for tracking leave balances.

### Phase 4: Integrate leave balance management
_Depends on: src/modules/balance/leave.balance.model.ts, src/modules/leave/leave.model.ts_

Create src/modules/balance/leave.balance.service.ts to check leave balances before approving requests. Update src/modules/leave/leave.service.ts to include balance checks. Include a Jest unit test in tests/unit/leave.balance.service.test.ts.

**Architecture:**
This phase ensures that leave requests are only approved if sufficient leave balance is available.

### Phase 5: Create leave policy model

Create src/modules/policy/leave.policy.model.ts. Define LeavePolicy and LeavePolicyDto. Import LeaveRequest from src/modules/leave/leave.model.ts.

**Architecture:**
This phase establishes the data structures for defining leave policies.

### Phase 6: Implement leave policy configuration
_Depends on: src/modules/policy/leave.policy.model.ts_

Create src/modules/policy/leave.policy.service.ts to manage leave policy configurations. Create src/modules/policy/leave.policy.controller.ts to manage API endpoints for policies. Include a Jest unit test in tests/unit/leave.policy.service.test.ts.

**Architecture:**
This phase allows HR to configure leave policies that dictate the rules for leave requests.
