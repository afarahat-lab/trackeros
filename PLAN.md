# PLAN.md — Build the leave management module

_Generated by Gestalt planning layer (feature 25651054)._

## Feature description

Build the leave management module. Employees apply for annual, sick, and emergency leave. Managers approve or reject. System tracks leave balances.

## Modules
- **Leave** (`src/modules/leave`) — owns: leave.model.ts, leave.repository.ts, leave.service.ts, leave.controller.ts, leave.routes.ts
- **Employee** (`src/modules/employee`) — owns: employee.model.ts, employee.repository.ts, employee.service.ts, employee.controller.ts, employee.routes.ts
- **Balance** (`src/modules/balance`) — owns: balance.model.ts, balance.repository.ts, balance.service.ts, balance.controller.ts, balance.routes.ts

## Domain entities
- **LeaveRequest** — Represents a leave request made by an employee. (attributes: id, employeeId, leaveType, startDate, endDate, status)
- **LeaveBalance** — Tracks the leave balances for each employee. (attributes: employeeId, annualLeave, sickLeave, emergencyLeave)
- **Employee** — Represents an employee in the system. (attributes: id, name, position)

## Phases

### Phase 1: Create leave model

Create src/modules/leave/leave.model.ts. Define LeaveRequest, CreateLeaveRequestDto. Import LeaveType from src/shared/types/index.ts.

**Architecture:**
This phase establishes the core data structure for leave requests.

### Phase 2: Create leave repository
_Depends on: src/modules/leave/leave.model.ts_

Create src/modules/leave/leave.repository.ts. Implement ILeaveRepository interface referencing LeaveRequest and CreateLeaveRequestDto from src/modules/leave/leave.model.ts. Include Jest unit tests in tests/unit/modules/leave/leave.repository.test.ts.

**Architecture:**
This phase sets up the persistence layer for leave requests.

### Phase 3: Implement leave service
_Depends on: src/modules/leave/leave.repository.ts_

Create src/modules/leave/leave.service.ts. Implement LeaveService class to handle business logic for leave requests. Include Jest unit tests in tests/unit/modules/leave/leave.service.test.ts.

**Architecture:**
This phase encapsulates the business logic for managing leave requests.

### Phase 4: Create leave controller
_Depends on: src/modules/leave/leave.service.ts_

Create src/modules/leave/leave.controller.ts. Implement LeaveController class with API endpoints for managing leave requests. Include Jest unit tests in tests/unit/modules/leave/leave.controller.test.ts.

**Architecture:**
This phase exposes the leave management functionality through API endpoints.

### Phase 5: Develop leave balance model

Create src/modules/balance/leave.balance.model.ts. Define LeaveBalance interface. Include Jest unit tests in tests/unit/modules/balance/leave.balance.model.test.ts.

**Architecture:**
This phase defines the data structure for tracking leave balances.

### Phase 6: Create leave balance repository
_Depends on: src/modules/balance/leave.balance.model.ts_

Create src/modules/balance/leave.balance.repository.ts. Implement ILeaveBalanceRepository interface referencing LeaveBalance from src/modules/balance/leave.balance.model.ts. Include Jest unit tests in tests/unit/modules/balance/leave.balance.repository.test.ts.

**Architecture:**
This phase sets up the persistence layer for leave balances.

### Phase 7: Implement leave balance service
_Depends on: src/modules/balance/leave.balance.repository.ts_

Create src/modules/balance/leave.balance.service.ts. Implement LeaveBalanceService class to manage leave balance logic. Include Jest unit tests in tests/unit/modules/balance/leave.balance.service.test.ts.

**Architecture:**
This phase encapsulates the business logic for managing leave balances.

### Phase 8: Integrate leave balance with leave requests
_Depends on: src/modules/leave/leave.service.ts, src/modules/balance/leave.balance.service.ts_

Update src/modules/leave/leave.service.ts to integrate leave balance management. Modify methods to check and update leave balances when processing leave requests. Include Jest unit tests in tests/unit/modules/leave/leave.service.test.ts.

**Architecture:**
This phase ensures that leave requests and balances are managed together.
