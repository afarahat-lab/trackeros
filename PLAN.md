# PLAN.md

## Phase 1: Phase 1: Shared enums and base types

Create src/shared/types/leave.types.ts with the following TypeScript enums: LeaveType (ANNUAL, SICK, MATERNITY, PATERNITY, UNPAID, OTHER), LeaveStatus (PENDING, APPROVED, REJECTED, CANCELLED), NotificationType (LEAVE_REQUEST_CREATED, LEAVE_REQUEST_APPROVED, LEAVE_REQUEST_REJECTED, LEAVE_REQUEST_CANCELLED, LEAVE_BALANCE_LOW, LEAVE_BALANCE_EXPIRING), AuditAction (CREATE, UPDATE, DELETE), EntityType (LEAVE_REQUEST, LEAVE_BALANCE, LEAVE_POLICY, EMPLOYEE, NOTIFICATION). No dependencies on any other project files.

## Phase 2: Phase 2: Employee model and repository

Create src/modules/employee/employee.model.ts with TypeScript interfaces: Employee (id: string, employeeNumber: string, firstName: string, lastName: string, email: string, managerId: string | null, department: string | null, hireDate: Date, terminationDate: Date | null, employmentStatus: EmploymentStatus, createdAt: Date, updatedAt: Date, deletedAt: Date | null), CreateEmployeeDto, UpdateEmployeeDto, and the EmploymentStatus enum (ACTIVE, INACTIVE, TERMINATED, ON_LEAVE). Also create src/modules/employee/employee.repository.ts with IEmployeeRepository interface and EmployeeRepository class. The repository uses the pg Pool from src/shared/db/connection.ts (already exists). Include Jest unit tests in tests/unit/modules/employee/employee.repository.test.ts. This phase depends on src/shared/db/connection.ts (already exists) — read it before generating any code.

## Phase 3: Phase 3: LeaveType and LeavePolicy models and repository

Create src/modules/policy/policy.model.ts with TypeScript interfaces: LeaveType (id: string, code: LeaveTypeCode, name: string, description: string | null, isPaid: boolean, requiresDocumentation: boolean, isActive: boolean, createdAt: Date, updatedAt: Date), LeavePolicy (id: string, policyName: string, leaveTypeId: string, leaveTypeCode: LeaveTypeCode, entitlementDays: number, accrualRate: number | null, maxAccumulation: number | null, minimumNoticeDays: number | null, requiresManagerApproval: boolean, isActive: boolean, effectiveFrom: Date, effectiveTo: Date | null, createdAt: Date, updatedAt: Date), LeavePolicyQueryParams, and the LeaveTypeCode enum. Import LeaveTypeCode from src/shared/types/leave.types.ts (Phase 1). Also create src/modules/policy/policy.repository.ts with ILeavePolicyRepository interface and PolicyRepository class using the pg Pool from src/shared/db/connection.ts. Include Jest unit tests in tests/unit/modules/policy/policy.repository.test.ts. This phase depends on src/shared/types/leave.types.ts from Phase 1 and src/shared/db/connection.ts (already exists) — read both before generating any code.

## Phase 4: Phase 4: LeaveRequest model and repository

Create src/modules/leave/leave.model.ts with TypeScript interfaces: LeaveRequest (id: string, employeeId: string, leaveTypeId: string, leaveTypeCode: LeaveTypeCode, status: LeaveStatus, startDate: Date, endDate: Date, totalDays: number, reason: string | null, managerId: string | null, approvedBy: string | null, approvedAt: Date | null, rejectedBy: string | null, rejectedAt: Date | null, rejectionReason: string | null, cancelledAt: Date | null, createdAt: Date, updatedAt: Date), CreateLeaveRequestDto, UpdateLeaveRequestDto, LeaveRequestQueryParams. Import LeaveTypeCode, LeaveStatus from src/shared/types/leave.types.ts (Phase 1). Also create src/modules/leave/leave.repository.ts with ILeaveRepository interface and LeaveRepository class using the pg Pool from src/shared/db/connection.ts. Include Jest unit tests in tests/unit/modules/leave/leave.repository.test.ts. This phase depends on src/shared/types/leave.types.ts from Phase 1, src/modules/employee/employee.model.ts from Phase 2, and src/shared/db/connection.ts (already exists) — read all three before generating any code.

## Phase 5: Phase 5: LeaveBalance model and repository

Create src/modules/balance/balance.model.ts with TypeScript interfaces: LeaveBalance (id: string, employeeId: string, leaveTypeId: string, leaveTypeCode: LeaveTypeCode, entitlementDays: number, usedDays: number, pendingDays: number, remainingDays: number, accruedDays: number, carriedForwardDays: number, expiresAt: Date | null, year: number, createdAt: Date, updatedAt: Date), LeaveBalanceQueryParams. Import LeaveTypeCode from src/shared/types/leave.types.ts (Phase 1). Also create src/modules/balance/balance.repository.ts with ILeaveBalanceRepository interface and BalanceRepository class using the pg Pool from src/shared/db/connection.ts. Include Jest unit tests in tests/unit/modules/balance/balance.repository.test.ts. This phase depends on src/shared/types/leave.types.ts from Phase 1, src/modules/employee/employee.model.ts from Phase 2, and src/shared/db/connection.ts (already exists) — read all three before generating any code.

## Phase 6: Phase 6: Employee service

Create src/modules/employee/employee.service.ts with IEmployeeService interface and EmployeeService class. The service wraps IEmployeeRepository from src/modules/employee/employee.repository.ts (Phase 2) and provides methods: findById, findByEmployeeNumber, findByEmail, findByManagerId, findByDepartment, create, update, terminate. Import Employee, CreateEmployeeDto, UpdateEmployeeDto from src/modules/employee/employee.model.ts (Phase 2). Include Jest unit tests in tests/unit/modules/employee/employee.service.test.ts. This phase depends on src/modules/employee/employee.model.ts and src/modules/employee/employee.repository.ts from Phase 2 — read both before generating any code.

## Phase 7: Phase 7: Policy service

Create src/modules/policy/policy.service.ts with IPolicyService interface and PolicyService class. The service wraps ILeavePolicyRepository from src/modules/policy/policy.repository.ts (Phase 3) and provides methods: findActivePolicies, findPolicyByLeaveTypeCode, findPolicyById, createPolicy, updatePolicy, deactivatePolicy, validateLeaveRequest (checks minimum notice, entitlement, manager approval requirements). Import LeavePolicy, LeavePolicyQueryParams from src/modules/policy/policy.model.ts (Phase 3) and LeaveTypeCode from src/shared/types/leave.types.ts (Phase 1). Include Jest unit tests in tests/unit/modules/policy/policy.service.test.ts. This phase depends on src/modules/policy/policy.model.ts and src/modules/policy/policy.repository.ts from Phase 3 — read both before generating any code.

## Phase 8: Phase 8: Balance service

Create src/modules/balance/balance.service.ts with IBalanceService interface and BalanceService class. The service wraps ILeaveBalanceRepository from src/modules/balance/balance.repository.ts (Phase 5) and provides methods: getBalance, getBalancesByEmployee, initializeBalance, recalculateBalance, deductDays, restoreDays, accrueDays, checkSufficientBalance. Import LeaveBalance, LeaveBalanceQueryParams from src/modules/balance/balance.model.ts (Phase 5) and LeaveTypeCode from src/shared/types/leave.types.ts (Phase 1). Include Jest unit tests in tests/unit/modules/balance/balance.service.test.ts. This phase depends on src/modules/balance/balance.model.ts and src/modules/balance/balance.repository.ts from Phase 5 — read both before generating any code.

## Phase 9: Phase 9: Notification and AuditLog internal services

Create src/modules/notification/notification.service.ts with INotificationService interface and NotificationService class. Define the Notification interface inline (id, recipientEmployeeId, notificationType: NotificationType, title, body, isRead, createdAt). The service provides methods: send, markAsRead, getUnreadByEmployee. Import NotificationType from src/shared/types/leave.types.ts (Phase 1). Also create src/modules/audit/audit.service.ts with IAuditService interface and AuditService class. Define the AuditRecord interface inline (id, entityType: EntityType, entityId, action: AuditAction, performedBy, changes, timestamp). The service provides methods: log, getByEntity, getByUser. Import EntityType, AuditAction from src/shared/types/leave.types.ts (Phase 1). Both services use the pg Pool from src/shared/db/connection.ts (already exists). Include Jest unit tests in tests/unit/modules/notification/notification.service.test.ts and tests/unit/modules/audit/audit.service.test.ts. This phase depends on src/shared/types/leave.types.ts from Phase 1 and src/shared/db/connection.ts (already exists) — read both before generating any code.

## Phase 10: Phase 10: Leave orchestration service, validation, controller, and routes

Create the leave module's orchestration layer in approximately 5 files. First, create src/modules/leave/leave.validation.ts with Zod schemas for createLeaveRequest, approveLeaveRequest, rejectLeaveRequest, cancelLeaveRequest, getLeaveRequests, and getLeaveBalance. Second, create src/modules/leave/leave.service.ts with ILeaveService interface and LeaveService class. The service orchestrates the full leave request lifecycle (apply, approve, reject, cancel) by injecting and coordinating: ILeaveRepository (Phase 4), IEmployeeService (Phase 6), IPolicyService (Phase 7), IBalanceService (Phase 8), INotificationService (Phase 9), IAuditService (Phase 9). The apply method must: validate the policy via IPolicyService.validateLeaveRequest, check sufficient balance via IBalanceService.checkSufficientBalance, create the request via ILeaveRepository, deduct pending days via IBalanceService.deductDays, send notification via INotificationService, and log via IAuditService. Approve/reject/cancel follow similar orchestration patterns with state transitions enforced by LeaveStatus enum. Third, create src/modules/leave/leave.controller.ts with LeaveController class that delegates to ILeaveService. Fourth, create src/modules/leave/leave.routes.ts that registers Fastify routes (POST /leave/requests, GET /leave/requests, GET /leave/requests/:id, POST /leave/requests/:id/approve, POST /leave/requests/:id/reject, POST /leave/requests/:id/cancel, GET /leave/balances) applying Zod validation from leave.validation.ts. Include Jest unit tests in tests/unit/modules/leave/leave.service.test.ts. This phase depends on: src/modules/leave/leave.model.ts and src/modules/leave/leave.repository.ts from Phase 4, src/modules/employee/employee.service.ts from Phase 6, src/modules/policy/policy.service.ts from Phase 7, src/modules/balance/balance.service.ts from Phase 8, src/modules/notification/notification.service.ts and src/modules/audit/audit.service.ts from Phase 9, and src/shared/types/leave.types.ts from Phase 1 — read all of these before generating any code.
