# PLAN.md — Build the leave management module

_Generated by Gestalt planning layer (feature 1f24e41f)._

## Feature description

Build the leave management module. Employees apply for annual, sick, and emergency leave. Managers approve or reject. System tracks leave balances.

## Modules
- **leave** (`src/modules/leave`) — owns: LeaveRequest lifecycle, approval workflows, leave request validation
- **balance** (`src/modules/balance`) — owns: LeaveBalance storage, balance adjustments, balance queries
- **employee** (`src/modules/employee`) — owns: Employee data, reporting hierarchy, employee queries
- **policy** (`src/modules/policy`) — owns: LeavePolicy rules, entitlement configurations, policy queries
- **notification** (`src/modules/notification`) — owns: Notification creation, notification delivery, notification status

## Domain entities
- **LeaveRequest** — Represents a leave application submitted by an employee, processed by managers (attributes: id, employeeId, leaveType, startDate, endDate, status, reason, managerId, createdAt, updatedAt)
- **LeaveBalance** — Tracks remaining leave entitlement by employee and leave type (attributes: id, employeeId, leaveType, balance, fiscalYear)
- **Employee** — Employee identity and reporting hierarchy (attributes: id, name, email, managerId, department)
- **LeavePolicy** — Defines entitlement rules and leave-type configurations (attributes: id, leaveType, entitlementDays, carryOverLimit, requiresApproval)
- **Notification** — Leave workflow notifications for employees and managers (attributes: id, userId, type, title, message, read, createdAt)

## Phases

### Phase 1: Core leave request data model and repository

Create src/modules/leave/leave.model.ts with TypeScript interfaces for LeaveRequest and CreateLeaveRequestDto. Import LeaveType and LeaveStatus from src/shared/types/index.ts which already exists. Create src/modules/leave/leave.repository.ts implementing ILeaveRepository with methods for CRUD operations using the LeaveRequest interface. Include PostgreSQL schema definitions in the repository. Create src/modules/leave/index.ts exporting the repository and types. This phase establishes the foundational data layer.

**Architecture:**
Establish LeaveRequest entity with PostgreSQL schema and repository layer before adding business logic. All database operations use repository pattern (GP-001).

### Phase 2: Leave request creation service with validation
_Depends on: src/modules/leave/leave.model.ts, src/modules/leave/leave.repository.ts_

Create src/modules/leave/leave.service.ts implementing leave request creation with input validation (GP-003) and audit logging (GP-002). Create src/modules/leave/leave.validator.ts with validation rules for leave requests. Create src/modules/leave/dto/create-leave-request.dto.ts with validation decorators. Create tests/unit/modules/leave/leave.service.spec.ts with Jest unit tests. This phase depends on src/modules/leave/leave.model.ts and src/modules/leave/leave.repository.ts from Phase 1 — read them before generating code that references their types. Operations execute atomically in a single transaction.

**Architecture:**
Implement leave request creation with input validation (GP-003) and audit logging (GP-002). Operations execute atomically in a single transaction to ensure data consistency.

### Phase 3: Leave request approval workflow
_Depends on: src/modules/leave/leave.model.ts, src/modules/leave/leave.repository.ts, src/modules/leave/leave.service.ts_

Create src/modules/leave/leave.approval.service.ts implementing manager approval/rejection with RBAC enforcement (GP-005), status transitions, and balance updates. Create src/modules/leave/dto/approve-leave-request.dto.ts. Create src/modules/leave/guards/manager.guard.ts for RBAC. Create tests/unit/modules/leave/leave.approval.service.spec.ts with Jest tests. This phase depends on src/modules/leave/leave.model.ts, src/modules/leave/leave.repository.ts, and src/modules/leave/leave.service.ts from prior phases. Approval updates leave balance atomically with status change.

**Architecture:**
Implement manager approval/rejection with RBAC enforcement (GP-005), status transitions, and balance updates. Approval updates leave balance atomically with the status change.

### Phase 4: Leave balance tracking and queries

Create src/modules/balance/balance.model.ts with LeaveBalance interface. Create src/modules/balance/balance.repository.ts implementing ILeaveBalanceRepository. Create src/modules/balance/balance.service.ts for tracking remaining entitlements with proper error handling (GP-006). Create tests/unit/modules/balance/balance.service.spec.ts with Jest tests. This phase depends on existing leave module files but creates new balance module. Ensure no sensitive data in logs (GP-004).

**Architecture:**
Implement leave balance repository and service for tracking remaining entitlements, with proper error handling (GP-006) and no sensitive data in logs (GP-004).

### Phase 5: Notification integration
_Depends on: src/modules/leave/leave.model.ts, src/modules/leave/leave.service.ts_

Create src/modules/notification/notification.service.ts integrating notifications for leave request submissions and approvals. Create src/modules/notification/notification.model.ts with Notification interface. Create tests/unit/modules/notification/notification.service.spec.ts with Jest tests. This phase depends on src/modules/leave/leave.model.ts and src/modules/leave/leave.service.ts from prior phases. Ensure all async operations have proper error handling (GP-006).

**Architecture:**
Integrate notifications for leave request submissions and approvals, ensuring all async operations have proper error handling (GP-006).
