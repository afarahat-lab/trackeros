# PLAN.md — Build the leave management module

_Generated by Gestalt planning layer (feature 595033ff)._

## Feature description

Build the leave management module. Employees apply for annual, sick, and emergency leave. Managers approve or reject. System tracks leave balances.

## Modules
- **leave** (`src/modules/leave`) — owns: LeaveRequest, leave application workflow, leave request submission, approval and rejection state transitions
- **balance** (`src/modules/balance`) — owns: LeaveBalance, leave balance initialization, leave balance adjustments, leave balance queries
- **employee** (`src/modules/employee`) — owns: Employee, manager relationships
- **policy** (`src/modules/policy`) — owns: LeavePolicy, leave entitlement rules
- **notification** (`src/modules/notification`) — owns: Notification, leave workflow notifications, notification delivery status updates

## Domain entities
- **LeaveRequest** — Represents a leave application and its approval lifecycle. (attributes: id, employeeId, leaveType, startDate, endDate, status)
- **LeaveBalance** — Tracks remaining leave entitlement for an employee. (attributes: employeeId, leaveType, availableDays)
- **Employee** — Represents employee identity and reporting relationships. (attributes: id, managerId, status)
- **LeavePolicy** — Defines leave eligibility and entitlement rules. (attributes: id, leaveType, annualEntitlement)
- **Notification** — Represents workflow notifications generated by leave events. (attributes: id, recipientEmployeeId, type, status)

## Phases

### Phase 1: Create Employee model and repository

Create src/modules/employee/employee.model.ts defining the Employee entity with fields id, managerId, status. Create src/modules/employee/employee.repository.ts implementing persistence/query operations for Employee and importing types only from src/modules/employee/employee.model.ts. Include Jest unit tests in tests/unit/modules/employee/employee.repository.spec.ts covering manager relationship lookups.

**Architecture:**
Establishes the employee domain foundation used by leave approval routing.

### Phase 2: Create LeavePolicy model and repository

Create src/modules/policy/policy.model.ts defining the LeavePolicy entity with fields id, leaveType, annualEntitlement. Create src/modules/policy/policy.repository.ts implementing persistence/query operations for LeavePolicy and importing types only from src/modules/policy/policy.model.ts. Include Jest unit tests in tests/unit/modules/policy/policy.repository.spec.ts.

**Architecture:**
Provides policy ownership and entitlement rule storage.

### Phase 3: Create LeaveBalance model and repository

Create src/modules/balance/balance.model.ts defining the LeaveBalance entity with fields employeeId, leaveType, availableDays. Create src/modules/balance/balance.repository.ts implementing persistence/query operations for LeaveBalance and importing types only from src/modules/balance/balance.model.ts. Include Jest unit tests in tests/unit/modules/balance/balance.repository.spec.ts.

**Architecture:**
Creates the balance aggregate and persistence layer required by leave workflows.

### Phase 4: Implement leave balance initialization and adjustment service
_Depends on: src/modules/balance/balance.model.ts, src/modules/balance/balance.repository.ts_

This phase depends on src/modules/balance/balance.model.ts and src/modules/balance/balance.repository.ts from Phase 3 — read them before generating code. Create src/modules/balance/balance.service.ts implementing leave balance initialization, leave balance adjustments, and leave balance queries using LeaveBalance. Include Jest unit tests in tests/unit/modules/balance/balance.service.spec.ts.

**Architecture:**
Encapsulates balance business logic behind a service layer.

### Phase 5: Create LeaveRequest model and repository

Create src/modules/leave/leave.model.ts defining the LeaveRequest entity with fields id, employeeId, leaveType, startDate, endDate, status and any request DTOs needed by the repository. Create src/modules/leave/leave.repository.ts implementing persistence/query operations for LeaveRequest and importing types only from src/modules/leave/leave.model.ts. Include Jest unit tests in tests/unit/modules/leave/leave.repository.spec.ts.

**Architecture:**
Introduces leave request storage independently from workflow logic.

### Phase 6: Implement leave request submission workflow
_Depends on: src/modules/leave/leave.model.ts, src/modules/leave/leave.repository.ts, src/modules/employee/employee.repository.ts, src/modules/policy/policy.repository.ts, src/modules/balance/balance.service.ts_

This phase depends on src/modules/leave/leave.model.ts, src/modules/leave/leave.repository.ts, src/modules/employee/employee.repository.ts, src/modules/policy/policy.repository.ts, and src/modules/balance/balance.service.ts from prior phases — read them before generating code. Create src/modules/leave/leave.service.ts implementing leave request submission that creates LeaveRequest records in PENDING status after validating Employee eligibility, LeavePolicy rules, and LeaveBalance availability. Include Jest unit tests in tests/unit/modules/leave/leave.service.submit.spec.ts.

**Architecture:**
Adds the initial leave application workflow without approval processing.

### Phase 7: Implement leave approval and rejection processing
_Depends on: src/modules/leave/leave.model.ts, src/modules/leave/leave.repository.ts, src/modules/leave/leave.service.ts, src/modules/employee/employee.repository.ts, src/modules/balance/balance.service.ts_

This phase depends on src/modules/leave/leave.model.ts, src/modules/leave/leave.repository.ts, src/modules/leave/leave.service.ts, src/modules/employee/employee.repository.ts, and src/modules/balance/balance.service.ts from prior phases — read them before generating code. Update or extend src/modules/leave/leave.service.ts to implement approveLeaveRequest() and rejectLeaveRequest() transitions from PENDING to APPROVED or REJECTED and apply corresponding LeaveBalance mutations. Include Jest unit tests in tests/unit/modules/leave/leave.service.approval.spec.ts.

**Architecture:**
Completes the leave request lifecycle and balance effects.

### Phase 8: Create notification model, repository, and processing service

Create src/modules/notification/notification.model.ts defining the Notification entity with fields id, recipientEmployeeId, type, status. Create src/modules/notification/notification.repository.ts implementing persistence/query operations for Notification and importing types only from src/modules/notification/notification.model.ts. Create src/modules/notification/notification.service.ts implementing createNotification() for leave submission and decision events and markNotificationDelivered() lifecycle transitions. Include Jest unit tests in tests/unit/modules/notification/notification.service.spec.ts.

**Architecture:**
Provides notification generation and delivery state management for leave events.
