# PLAN.md — Build the leave management module

_Generated by Gestalt planning layer (feature e9240cb6)._

## Feature description

Build the leave management module. Employees can apply for annual, sick, and emergency leave. Managers approve or reject requests. The system tracks leave balances and prevents overlapping requests.

## Modules
- **leave** (`src/modules/leave`) — owns: leave.model.ts, leave.repository.ts, leave.service.ts, leave.routes.ts, leave.test.ts
- **balance** (`src/modules/balance`) — owns: balance.model.ts, balance.repository.ts, balance.service.ts
- **policy** (`src/modules/policy`) — owns: policy.model.ts, policy.repository.ts, policy.service.ts, policy.routes.ts
- **employee** (`src/modules/employee`) — owns: employee.model.ts, employee.repository.ts, employee.service.ts, employee.routes.ts

## Domain entities
- **LeaveRequest** — Tracks leave requests made by employees and their statuses. (attributes: id, employeeId, type, startDate, endDate, status, managerId, managerComment, createdAt)
- **LeaveBalance** — Maintains the leave balance for each employee. (attributes: employeeId, leaveType, totalDays, usedDays, year)
- **LeavePolicy** — Defines the rules and limits for different types of leave. (attributes: id, leaveType, defaultDaysPerYear, maxConsecutiveDays, requiresApproval, createdAt)

## Phases

### Phase 1: Create leave model

Create src/modules/leave/leave.model.ts. Define LeaveRequest, CreateLeaveRequestDto, and LeaveBalance. Import LeaveType from src/shared/types/index.ts.

**Architecture:**
This phase establishes the core data structures necessary for the leave management module.

### Phase 2: Create leave repository
_Depends on: src/modules/leave/leave.model.ts_

Create src/modules/leave/leave.repository.ts. Implement functions to interact with the leave data, using LeaveRequest and LeaveBalance. Include a Jest unit test in tests/unit/leave.repository.test.ts.

**Architecture:**
This phase provides the data access layer for leave requests and balances.

### Phase 3: Implement leave service logic
_Depends on: src/modules/leave/leave.repository.ts_

Create src/modules/leave/leave.service.ts. Develop business logic for applying, approving, and rejecting leave requests. Include a Jest unit test in tests/unit/leave.service.test.ts.

**Architecture:**
This phase encapsulates the core business rules for managing leave requests.

### Phase 4: Create leave routes
_Depends on: src/modules/leave/leave.service.ts_

Create src/modules/leave/leave.routes.ts. Expose API endpoints for employees to apply for leave and for managers to approve or reject requests. Include a Jest unit test in tests/unit/leave.routes.test.ts.

**Architecture:**
This phase sets up the API layer for the leave management functionality.
