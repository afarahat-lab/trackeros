{
  "name": "trackeros",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.5.1",
  "scripts": {
    "test": "echo 'no tests yet' && exit 0",
    "build": "echo 'no build yet' && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^1.0.0",
    "tsx": "^1.0.0"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
