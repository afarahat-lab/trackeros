{
  "name": "trackeros",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "test": "echo 'no tests yet' && exit 0",
    "build": "echo 'no build yet' && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^0.0.0",
    "tsx": "^0.0.0"
  }
}